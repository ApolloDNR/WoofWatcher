# WoofWatcher Finish Recovery Plan

**Recovery base:** `fddcfcb77217c500c26669ddb1c680f7890b0ecb`

**Working branch:** `codex/woofwatcher-finish`

**Primary specifications:**

- `docs/superpowers/plans/2026-07-26-woofwatcher-premium-navigation-ux.md`
- `docs/superpowers/plans/2026-07-23-woofwatcher-premium-ios-renovation.md`
- `docs/superpowers/plans/2026-07-25-account-deletion-recovery.md`

## Recovery boundary

Scratch maintenance removed the unpublished continuation after the navigation
contract commit. Historical controller evidence proves that the lost work had
advanced through topology, shell, lifecycle coordinator, runtime smoke, and a
two-case Task 2b harness repair, but none of those later commit objects reached
GitHub. Reconstruct behavior from the committed specifications and the
controller ledger, not from remembered source bytes.

Every accepted task must be committed and pushed immediately. Historical test
counts are diagnostic context only; all acceptance claims require fresh output
from the reconstructed commit.

## Global constraints

- Node `24.14.x` and pnpm `10.24.0`.
- `America/Los_Angeles` for care-domain baseline fixtures.
- One production writer at a time.
- RED before a production behavior change.
- Frozen commit before independent review.
- No navigation task may modify account-deletion/security behavior.
- No account-deletion task may invent a fingerprint for the lost unpublished
  `0011` migration.
- Browser runtime is blocked, not passed, when no nonempty Chromium executable
  is available.
- Provider, native-device, store, payment, AI, push, and public-launch gates
  stay explicitly external.

## Task 0: Restore and prove the published base

- Restore the isolated worktree at `fddcfcb`.
- Restore the exact frozen dependency graph.
- Run the complete focused repository suite and workspace TypeScript under
  `America/Los_Angeles`.
- Record exact commands and counts.

## Task 1: Reconstruct pathless owner stacks and the shared care shell

- Recreate the four pathless owner stacks and lift `/log` to the root.
- Preserve all public paths and route-manifest parity.
- Derive root and owner presentation options exhaustively from the route
  contract.
- Render `CareShellHost` above the root Stack and use route-contract visibility.
- Preserve four 48-point destinations plus the Quick Log action at 320px and
  accessibility scale.
- Restore root and owner `unstable_settings`, canonical fallback paths, and
  exact Adventure/Calendar presentation behavior.
- Update every executable source-path consumer after route moves.
- Require full repository tests, workspace TypeScript, fresh export, beta
  doctor, frozen commit, and independent review.

## Task 2: Reconstruct typed navigation lifecycle and runtime proof

- Add the explicit typed owner registry and coordinator.
- Migrate owner switching, back/fallback behavior, and every direct Fast Log
  launch to typed intents.
- Make Fast Log dismissal origin-aware and make `/log` focus handoffs
  generation-safe and one-use.
- Fail closed when physical owner reset cannot be verified.
- Keep fire-and-forget UI callers free of unhandled rejections and drain
  canceled generations.
- Expand runtime proof to the complete public-route matrix and the five
  interaction scenarios.
- Drive browser Back from the CDP host across destroyed document contexts,
  traverse stale history to a sentinel, and bind tests to the real dispatcher.
- Bound a stalled `Page.navigateToHistoryEntry` command and its waiter as one
  operation with cleanup.
- Require full repository tests, workspace TypeScript, fresh export, frozen
  commit, independent review, and honest browser availability reporting.

## Task 3: Finish Today and customer-facing navigation polish

- Pin and fix the positive-offset Records due-date formatting defect exposed by
  the timezone baseline.
- Recheck Today accessibility, reading order, dominant action, shell clearance,
  Reduce Motion, and large-text states against the premium navigation plan.
- Preserve owner-entered evidence and non-diagnostic health language.

## Task 4: Finish More, Plus, and WoofGuide truth surfaces

- Keep More a customer-facing directory, not an internal launch cockpit.
- Keep Plus value truthful while checkout, products, receipts, and entitlement
  providers remain unproved.
- Keep WoofGuide useful through deterministic owner-reviewed actions while
  live provider AI remains gated.
- Remove internal/operator language from ordinary customer paths without
  deleting the internal QA and release-proof routes.

## Task 5: Complete account-deletion recovery Task 2

- Follow `2026-07-25-account-deletion-recovery.md` exactly.
- Support pristine install and corrected rerun of the unpublished `0011`
  design.
- Reject unknown or partial legacy catalogs before mutation.
- If a real environment proves the lost draft was applied, require its exact
  old bytes or an additive `0012`; never fabricate a legacy hash.
- Complete API, database, mobile, audit, recovery, receipt, and poison-catalog
  tests before independent security review.

## Task 6: Whole-branch integration and release-candidate proof

- Run the full focused suite, full workspace TypeScript, CI build/export,
  source scanners, doctors, and diff checks from one frozen head.
- Run browser proof only with a real browser binary; otherwise retain the
  explicit blocked row.
- Perform independent whole-branch review and one bounded fix wave.
- Push the exact reviewed head and report remaining external native/provider/
  store gates without claiming public release.
