# Autonomous Build Queue

## Operating Rule

Every autonomous run should improve at least one of: care workflow, household usefulness, premium feel, production safety, monetization path, or App Store readiness.

## Highest Priority Queue

1. DONE 2026-06-08: Routines/logs relationship: logs satisfy/update matching routines.
2. DONE 2026-06-08: Meal logging: expected portion, served amount, eaten amount, skipped/partial completion, notes, and household visibility.
3. DONE 2026-06-08: Subscription path: pricing screen, premium preview, and plan packaging without live payments.
4. DONE 2026-06-08: Today Command uses routine-board truth for partial meals, overdue assigned routines, routes, urgency, and explanation text.
5. DONE 2026-06-08: Home Quick Log now creates routine-aware meal and walk logs with meal portion, completion, eaten amount, and household visibility detail.
6. DONE 2026-06-08: Health Watch now produces non-diagnostic pattern cards with evidence, owner next steps, and visible vet boundary language.
7. DONE 2026-06-08: Care Pass handoffs now include audience-specific checklists and Health Pattern Review next steps for sitter, caregiver, trainer, and vet exports.
8. DONE 2026-06-08: Records now derives and displays expired, due-soon, and missing-critical record reminders; real document storage remains blocked on provider rules.
9. DONE 2026-06-08: WoofGuide now creates owner-reviewed drafts for missing meal logs, record reminders, vet notes, and Care Pass review.
10. DONE 2026-06-08: Privacy/account safety first pass now includes owner data export, deletion request preparation, AI disclosure, and document/payment safety gates.
11. IN PROGRESS, CI Expo web export smoke and critical accessibility label smoke shipped 2026-06-08: Mobile QA now checks route registration, tabs, router links, launch-blocking safety copy, an Expo web export bundle, and screen-reader labels for critical Privacy, Premium, WoofGuide, and More actions. Next: simulator/device smoke, full accessibility pass, visual regression, App Store prep.
12. DONE 2026-06-08: Premium entitlement policy now defines Free, Plus, and Family feature gates in shared domain logic, keeps checkout disabled, and shows the policy on the mobile Plus screen before payments are enabled.
13. DONE 2026-06-08: Expo app identity now uses release-grade WoofWatcher slug/scheme plus Pegasus Dreamscapes iOS bundle and Android package ids instead of Replit placeholders.
14. IN PROGRESS, avatar motion foundation shipped 2026-06-08: Home now derives a deterministic avatar motion state from health signals, recent care logs, routine due/overdue state, quiet hours, and energy. Next: Rive/Lottie/Reanimated asset pipeline, Figma/code design system, full screen-by-screen visual polish, and visual regression.
15. Premium design system and motion pass: component tokens, high-end screen polish, avatar state assets, transition rules, and accessibility review.
16. DONE 2026-06-09: Care Pass report artifacts now include escaped print-ready HTML and stable file names for future PDF generation, native sharing, and server-backed storage.
17. DONE 2026-06-09: Records Report History now shows print-ready/restored Care Pass metadata, exposes separate resend and printable-source share actions, and keeps older saved report artifacts printable through escaped fallback HTML.
18. DONE 2026-06-09: Dog ID credentials now have escaped print-ready HTML, stable file names, and separate Records actions for sharing the normal ID card text or printable source.
19. DONE 2026-06-10: Medication routines now derive taken, due, missed, and upcoming adherence status from household-visible medication logs, including dose, owner, logged-by context, next action, and a mobile Records Medication Plan surface.
20. DONE 2026-06-10: Medication logging now has Home quick-log access, routine-aware dose/outcome/visibility defaults, a Log composer Medication routine panel, and skipped-medication handling that does not count as taken.
21. DONE 2026-06-11: Medication follow-ups now derive missed, due-now, and refill due-soon/overdue actions from routines, logs, and medication records; Records shows Medication Follow-ups and Care Pass report language includes adherence plus refill next steps.
22. DONE 2026-06-11: Medication history now derives recent household-visible medication logs with dose, taken/skipped/missed outcome, caregiver, routine id, note, and Records Medication History UI.
23. DONE 2026-06-11: Water care now has Home quick-log access, household-visible refill defaults, shared daily hydration summary logic, Records Hydration UI, and Care Pass report language.
24. DONE 2026-06-11: Walk activity now derives daily duration, places/routes, dog interactions, social outcome notes, caregiver participation, Records Walk Activity UI, and Care Pass report language from household-visible walk logs.
25. DONE 2026-06-11: Potty Health now derives daily pee/poop counts, stool review signals, conditions, latest detail, caregiver participation, Records Potty Health UI, and Care Pass report language from household-visible potty logs.
26. DONE 2026-06-11: Potty logging now captures stool color and routine/accident/urgent/straining context, and Potty Health carries those details into Records and Care Pass reports.
27. DONE 2026-06-11: Durable sync outbox now derives retryable create/update work from local, pending, and failed care entries, exposes it through CareContext, and shows a Log screen recovery banner with retry counts and a retry action.
28. DONE 2026-06-11: Household Sync Health now derives healthy/loading/syncing/attention dashboard state from the durable outbox, household size, and care history, and More shows the household sync status, retry/refresh action, care-log count, team count, outbox count, and next-step guidance.
29. DONE 2026-06-11: Care document refresh is now conflict-safer: mobile reconciles server care-state refreshes against the local doc timestamp, keeps newer offline/local profile, routine, record, and report changes instead of clobbering them with stale server data, and pushes the newer doc back to the household.
30. DONE 2026-06-11: Saved walk route templates now derive from household-visible route/place logs, ignoring private and stale walks, grouping repeat routes by name, and carrying visits, average duration, distance, dog interactions, caregivers, social outcomes, and suggested use into Records and Care Pass reports.
31. DONE 2026-06-11: Weekly Care Trends now derive from household-visible logs, compare the current 7-day window against the previous window, and carry meal completion, walk minutes, water refills, potty/medication/health watch signals, top caregivers, Records UI, and Care Pass report language.
32. DONE 2026-06-11: Training Progress now captures structured training outcomes, skill/cue, duration, next-practice notes, and household visibility in the Log composer, derives visible 30-day training progress in shared domain logic, and carries sessions, wins, skills, latest context, Records UI, and Care Pass report language into trainer-ready workflows.
33. DONE 2026-06-11: Household Responsibility Center now derives care-team routine ownership, open/overdue/unassigned counts, visible today log activity, and the next household action from routine-board truth, then surfaces it in Calendar and More.
34. DONE 2026-06-11: Care Log Audit Trail now records create/edit/sticky-note/delete evidence in shared care-domain logic, shows audit history in Log detail sheets, includes audit summaries in entry handoffs, and creates a non-health audit note only after deletes succeed.
35. DONE 2026-06-11: Alone Time now captures departure duration, return state, trigger/context, calming support, recovery minutes, sticky notes, and household visibility in the Log composer, derives 30-day separation/anxiety patterns in shared care-domain logic, and carries status, triggers, supports, latest context, Records UI, and Care Pass handoff language into household workflows.
36. DONE 2026-06-11: Weight Trend now derives visible recent weigh-ins, current weight, goal distance, previous-change, caregiver participation, Records chart inputs, and Care Pass report language from shared care-domain logic while preserving owner-reported/vet-review boundaries.
37. DONE 2026-06-11: Household Access now derives synced account members, local-only caregivers, routine-only owners, invite readiness, permission labels, and next-step guidance in shared care-domain logic, then surfaces that access plan in More without pretending provider-backed role enforcement is complete.
38. DONE 2026-06-11: Grooming Care now captures grooming duration, type, coat/skin notes, products/groomer context, next due date, sticky notes, and household visibility in the Log composer, derives shared grooming summaries in care-domain logic, and carries status, products, latest context, Records UI, and Care Pass language into sitter/vet handoffs.
39. DONE 2026-06-11: Reminder Center now derives one owner-visible action list from routine-board status, medication follow-ups, record reminders, and grooming due dates, then surfaces urgent/watch/total counts and notification-readiness copy in Calendar before provider-backed push delivery exists.
40. DONE 2026-06-11: Reminder Center rows now route to concrete workflows: routine reminders open the routine editor, medication routine reminders open the Medication log composer, record reminders open Records, and grooming reminders open the Grooming log composer.
41. DONE 2026-06-11: Full Log search now uses shared care-domain logic to search titles, notes, caregivers, event details, routes, medication fields, and sticky notes while combining text query with type filters, summary copy, and active-filter empty states.
42. DONE 2026-06-11: Medication History search and outcome filters now use shared care-domain logic to filter recent medication evidence by medicine, dose, caregiver, note, routine id, and taken/skipped/missed/needs-review outcome, then surface summary and empty-state copy in Records.
43. DONE 2026-06-12: v1.5 Premium Neo-Retro Pixel Care direction is mirrored into durable docs, CI was repaired, mobile nav is aligned, and the PWA v1.5 shell foundation now has grouped desktop navigation, five-tab mobile navigation, top-bar search/reminder/date/profile/theme controls, persisted light/dark theme, grouped Quick Log v2, Potty parent quick action, and meal/potty lifecycle field preservation.
44. DONE 2026-06-12: Phoenix Home v1.5 now includes a Phoenix Status card for where Phoenix is, whether she is alone, how she feels, what is next, and whether a served meal has an outcome pending. Open meal outcomes can be updated from Home with one tap.
45. DONE 2026-06-12: PWA Quick Log v2 now routes Meal and Potty quick actions into dedicated detail flows. Meal supports serving a pending meal with food, portion, caregiver, note, and later outcome update; Potty supports parent/action logging with location, pee/poop/both/tried/accident outcome, caregiver, and notes. Other quick actions remain one-tap.
46. DONE 2026-06-12: PWA Household Pulse is now a first-class route with manual Leaving Home and I'm Home flows. Leaving Home starts a household-visible open alone timer; I'm Home closes it with return outcome, duration, caregiver, recovery, and notes. More now includes a tool directory entry point back to Household Pulse.
47. DONE 2026-06-13: Superseded by item 55; Phoenix Home v1.5 polish now includes the household pulse mini-card, stronger Health/Bile snapshot, and richer pixel-room state copy.
48. DONE 2026-06-12: PWA Care Pass now exposes scoped Vet, Sitter, Trainer, and Emergency export cards, each backed by `buildScopedCarePass`, with copyable text and downloadable JSON source for Fable/Replit to style into premium report/PDF flows.
49. DONE 2026-06-12: Diet & Treats is now a first-class PWA route with daily meal target/progress, meals today, treats today, hydration context, avoid list, profile editing, and direct Log Meal/Log Treat/Edit Diet Profile actions.
50. DONE 2026-06-12: WoofGuide is now a first-class PWA route with owner-reviewed action cards that route to Meal Log, Care Pass review, Records review, and a bounded vet-note draft without pretending live AI is configured.
51. DONE 2026-06-12: PWA Timeline, Records, Reports, and Care Pass are now directly routable so desktop secondary navigation no longer collapses important tools into More.
52. DONE 2026-06-12: Avatar Studio is now a first-class PWA prototype route with local reference photo upload memory, selectable Phoenix template state, and the required Happy/Calm/Excited/Sleepy/Anxious/Bored/Hungry/Proud/Home Alone/Not Feeling Well state inventory.
53. DONE 2026-06-13: Achievements is now a first-class PWA route backed by `getAchievementReview`, deriving meaningful care milestones from household-visible logs, records, training consistency, calm alone-time outcomes, Bile Watch bedtime snack proof, and record readiness instead of fake coins.
54. DONE 2026-06-13: Settings is now a first-class PWA route that consolidates full backup, import, care-room transfer, reset, local-only provider readiness, AI boundary, health boundary, reminder delivery truth, and sync resource blockers in one system surface.
55. DONE 2026-06-13: Phoenix Home v1.5 polish now adds generated pixel-room state copy, a first-screen "Where Phoenix is" household pulse card, and a combined Health/Bile snapshot with food-gap, bedtime snack, vomiting, energy, and weight context.
56. NEXT: Runtime/browser visual QA once local dependencies or a package manager are available in the checkout.
57. DONE 2026-06-13: Fable/Replit handoff notes are now captured in `docs/design/FABLE_HANDOFF_2026-06-13.md`, including route inventory, preserved workflows, visual priorities, motion direction, and runtime QA limits.
58. NEXT: Provider-backed account/cloud/payment decision pack after Apollo chooses the release stack.
59. DONE 2026-06-13: Mobile-first release readiness now has committed Expo/EAS iOS and Android build profiles in `artifacts/woofwatcher-mobile/eas.json`, a mobile release runbook in `docs/release/MOBILE_RELEASE_RUNBOOK.md`, and static readiness tests that protect both.
60. NEXT: Native iOS/Android runtime QA with real Expo/EAS account access, simulator/device screenshots, safe-area checks, and Fable-polished assets.
61. DONE 2026-06-14: Apollo's four pixel UI reference boards are mirrored in `docs/design/reference/` and locked in `docs/superpowers/specs/2026-06-14-woofwatcher-pixel-ui-lock-design.md` as the visual source of truth for the next UI implementation pass.
62. DONE 2026-06-14: The Expo mobile app now has a board-accurate pixel UI foundation: locked palette tokens, reusable board primitives, navy bottom navigation, and a rebuilt Phoenix Home using pixel room, speech bubble, presence, segmented meters, quick actions, today summary, recent activity, and health watch cards.
63. DONE 2026-06-14: The Expo mobile board primitive system now reaches the core v1.5 routes beyond Home: Log, Plans, Health/Bile, More, Records, WoofGuide, and Avatar Studio all use shared board route chrome or board sections, and Health/Bile Watch uses shared board cards, pills, metric tiles, section headers, and care rows.
64. DONE 2026-06-14: GitHub Actions `WoofWatcher Verify` now uses Node 24-compatible action majors for checkout, setup-node, and pnpm setup while keeping the project runtime on Node 24.
65. DONE 2026-06-14: Quick Log, Plans, and Records now share stronger board card anatomy: the Log composer, Plans upcoming-events section, and Records Dog ID card use `BoardCard`, with a readiness test protecting the pattern.
66. DONE 2026-06-14: Records secondary report surfaces now use shared board anatomy: Care Pass audience preview rows, Report History resend/print rows, and Progress Report period/share controls are inside `BoardCard` sections with a readiness test protecting the structure.
67. DONE 2026-06-14: Remaining mobile Records lower sections now share board anatomy: Record Vault, Diet on File, and Records Cabinet are `BoardCard` sections with working Add/Edit/delete controls and internal vault tiles instead of old floating card islands.
68. DONE 2026-06-14: Mobile Records trend cards now share board anatomy for Weight Trend, Mood Trend, and Hydration, preserving the chart, mood bars, hydration meter, latest log context, and report-safe copy.
69. DONE 2026-06-14: Mobile Records activity and potty sections now share board anatomy for Walk Activity, Training Progress, and Potty Health while preserving saved routes, training focus/latest practice, stool color/context, and report-safe next steps.
70. DONE 2026-06-14: Mobile Records is now fully moved off its old one-off `padCard`/`sectionHeader` anatomy. Care Trends, Dog ID heading, Alone Time, Grooming Care, Incident Watch, and Medication Plan now use shared board primitives while preserving share/print, medication routines, filters, history, and care-safe copy.
71. DONE 2026-06-14: Mobile More now uses shared board anatomy for Care Team, Household Access, Responsibility Center, Sync Health, Tools & Sharing, and Diet Profile while preserving invite, rename, routine-board, sync refresh, tool routing, and diet edit/detail actions.
72. DONE 2026-06-14: Mobile Plans now uses shared board anatomy for Reminder Center and Daily Routine while preserving reminder action routing, routine add/edit/delete, routine done logging, owner load chips, household responsibility metrics, and empty routine setup.
73. DONE 2026-06-14: Mobile Quick Log now uses shared board anatomy for Today at a Glance, Find Care Logs, empty timeline state, and grouped timeline days while preserving composer boundaries, search, filters, sticky notes, edit/detail/delete actions, and sync status copy.
74. DONE 2026-06-14: Mobile WoofGuide now uses shared board anatomy for the owner-reviewed intro, Quick Questions, and Suggested Actions surfaces while preserving assistant prompts, generated action routing, owner-review drafts, and veterinary-boundary copy.
75. DONE 2026-06-14: Mobile Premium now uses shared board anatomy for Why Upgrade, gated plan cards, and Launch Entitlements while preserving the launch checklist, Free/Plus/Family policy truth, and disabled-payment safety boundary.
76. DONE 2026-06-14: Mobile Privacy & Safety now uses shared board anatomy for Export Summary, Launch Safety Gates, and Before Public Launch blockers while preserving owner data export, deletion-request sharing, AI/document/payment safety copy, and provider-gated truth.
77. DONE 2026-06-14: Mobile Avatar Studio now uses shared board anatomy for the working scan canvas, live/generated preview, mood-state set, and photo guidance while preserving library/camera generation, saved avatar state, revert-to-default, and animated scan behavior.
78. DONE 2026-06-14: Mobile Setup now uses shared board anatomy for the care-foundation header, setup-progress meter, and Dog Profile/Diet/Starter Routine/Household Caregiver sections while preserving draft application, save foundation, and finish-later behavior.
79. DONE 2026-06-16: Mobile Phoenix Home now has a Reanimated living care-twin runtime: `avatarLifeEngine` maps care state into room zones and motion modes, `LivingPhoenixRoom` uses a single animated board-accurate room stage with breathing, cue motion, care aura, sleep/hearts/reaction feedback, speech/HUD overlays, and the web preview no longer blanks on placeholder Clerk keys.
80. DONE 2026-06-16: The Care Twin Engine now exposes explicit scene phases, priority needs, HUD tones, tap verbs, recommended actions, sprite actions, and a tested `CARE_TWIN_SPRITE_MANIFEST` for transparent 256px bottom-center Phoenix strips covering idle, tail wag, ear perk, walk, eat, drink, sleep, comfort, celebrate, and health watch.
81. DONE 2026-06-16: Mobile now has the layered sprite runtime seam: `SpriteSheetPlayer` animates registered strips, `careTwinAssets.ts` gates rendering until both dogless room and transparent Phoenix strips exist, Home switches to layered rendering only when safe, and `docs/design/CARE_TWIN_ASSET_PIPELINE.md` tells Apollo/Fable/Replit exactly how to produce and register assets.
82. DONE 2026-06-17: Mobile Avatar Studio Lite now has a real template-based care-twin backbone: `PetAvatarConfig`, 12 dog templates, accessory slots, emote state inventory, mock scan suggestion, local config persistence, a Scan/Template/Customize/Emotes Studio flow, and Home/More identity wiring without claiming live AI scan.
83. IN PROGRESS 2026-06-18: Continue deep route-by-route visual polish and runtime screenshot QA for the mobile app; PixelLab v2 now has a full registered sprite manifest, first-pass dogless room variants, a standing-source walk loop, Avatar Studio previews the live layered care-twin room instead of the old generic hero art, the template picker uses a 12-item PixelLab preview catalog, selected templates can render production-scale base stills when available, the Phoenix/Shepherd, Retriever, Husky/Spitz, and Bully 10-state emote packs are wired, and the Studio presentation pass removed Home-HUD overlap from `/portrait`. Next: native safe-area/device QA, remaining Avatar Studio template packs/accessories, and screen-by-screen polish.
84. DONE 2026-06-18: PixelLab Phoenix Asset Production Phase 1/2 is no longer blocked by MCP access or credits. The approved v2 Phoenix seed/state pack, idle/tail/sleep strips, additional action strips, day dogless room, and first-pass room variants are generated, logged, and wired into the layered care-twin runtime.
85. DONE 2026-06-18: `Premium Revenue Product Builder` automation was corrected from the stale Highest Self OS prompt/workspace to the WoofWatcher repo, 3-hour cadence, worktree execution, and dog-care OS build loop.
86. NEXT: Replace first-pass locally derived room variants with final PixelLab/Figma-quality night, bedtime, health-watch, and home-alone illustrations once the core runtime is verified on device.
87. NEXT: Native iOS/Android runtime QA with real Expo/EAS account access, simulator/device screenshots, safe-area checks, frame-rate review, and App Store/Play Store readiness.
88. DONE 2026-06-18: Avatar Studio template catalog pass generated, promoted, downloaded, registered, and tested 12 PixelLab preview thumbnails for Shepherd, Retriever, Husky, Bully, Doodle, Terrier, Hound, Dachshund, Spaniel, Toy, Slender, and Mixed Breed.
89. DONE 2026-06-18: Generated, promoted, downloaded, registered, and tested the full 12-template Avatar Studio production base still pack for Shepherd, Retriever, Husky, Bully, Doodle, Terrier, Hound, Dachshund, Spaniel, Toy, Slender, and Mixed Breed.
90. DONE 2026-06-18: Generated, downloaded, registered, and tested the first Phoenix/Shepherd Avatar Studio emote still pack for Happy, Calm, Excited, Bored, Hungry, Anxious, Sleepy, Proud, Home Alone, and Not Feeling Well, and wired `/portrait` Mood set taps into real PixelLab mood art instead of repeated head-crop placeholders.
91. DONE 2026-06-18: Avatar Studio now has a PixelLab-generated 10-item transparent accessory inventory pack for forest bandana, navy collar, copper collar, heart tag, trail bandana, birthday hat, sleepy mask, training vest, cozy bed, and heart sparkles. The mobile `/portrait` route renders real accessory thumbnails, slot toggles, and readiness checks that prevent regression back to color-dot placeholders.
92. NEXT: Continue Avatar Studio production packs by generating remaining unfinished template/body-class emote packs, body-class sprite strips, and true overlay-aligned accessory layer PNGs that fit the same bottom-center avatar anchor.
93. DONE 2026-06-18: Pixel rendering and Studio presentation pass completed after Apollo's PixelLab subscription became active. The app now verifies two subscription seed strips, applies crisp pixel rendering to web image paths, uses `LivingPhoenixRoom presentation="studio"` on `/portrait`, fixes the premium revenue builder Metro/Expo export path, and has Chrome visual-smoke evidence for `/portrait` and Home.
94. NEXT: Run native iOS/Android device or simulator QA for safe areas, frame timing, touch targets, and final phone-size avatar proportions.
95. NEXT: Replace first-pass derived room variants with final illustrated PixelLab/Figma-quality scenes, then continue remaining unfinished emote/sprite packs and overlay-aligned accessory layers.
96. DONE 2026-06-18: Generated, downloaded, registered, and tested the first non-Phoenix Retriever 10-state Avatar Studio emote pack. Retriever templates now recommend `retriever-starter`, `/portrait` routes Mood set art through the selected template/emote pack, and unfinished templates fall back to their own base stills instead of borrowing Phoenix mood art.
97. DONE 2026-06-18: Generated, downloaded, registered, and tested the Husky/Spitz 10-state Avatar Studio emote pack. Husky templates now recommend `husky-starter`, selected-template routing covers the first visually distinct spitz/working-body pack, and PixelLab asset verification checked 81 local production assets at that pass.
98. DONE 2026-06-18: Generated, downloaded, visually checked, registered, and tested the Bully 10-state Avatar Studio emote pack. Bully templates now recommend `bully-starter`, compact-body selected-template routing is live, and PixelLab asset verification now checks 91 local production assets with 0 missing and 0 invalid.
99. DONE 2026-06-18: Merged main's Shepherd/Phoenix overlay and live preview work into the premium builder path. Avatar Studio now prefers file-backed Shepherd accessory overlays and Shepherd emote stills, uses the live Phoenix sprite rig for Shepherd mood states, and keeps unfinished templates on truthful still previews.
100. NEXT: Continue Avatar Studio production packs by generating template-matched sprite strips and overlay/emote packs for Retriever, Husky, Bully, Doodle, and the remaining launch templates so their previews can graduate from still-only to live animated production packs.
101. DONE 2026-06-18: Mobile Phoenix Home now has a tighter Option B-style live care twin console: the room card carries a dedicated PHOENIX HOME header, in-scene status HUD, mood/energy/hunger/hydration/bond readouts, and repositioned speech bubble so the dog remains the main character instead of competing with a second avatar. Avatar Studio now forces template, emote, room, accessory, card, and mood thumbnail images through crisp pixel rendering on web.
102. NEXT: Generate and wire true template-matched sprite strips for Retriever, Husky, Bully, Doodle, and the remaining launch templates so every prebuilt avatar can breathe, idle, walk, sleep, and emote as live game sprites instead of relying on still previews.
103. DONE 2026-06-18: Generated a subscription-backed PixelLab south-facing bark strip from the approved Phoenix character rig, registered it as `bark-loop`, and wired Phoenix Home room taps to momentarily swap the main care twin sprite into a bark reaction instead of spawning a second avatar.
104. DONE 2026-06-18: Generated, normalized, registered, and tested the first non-Phoenix live Avatar Studio sprite pack for Retriever, including idle-tail-wag and walk-loop strips. Avatar Studio now routes selected Retriever happy/calm/proud and excited/bored previews through template-specific sprite assets instead of Phoenix-only sprite tracks or still-only fallback.
105. DONE 2026-06-18: Superseded by item 108 after Husky/Spitz graduated from still-only to live sprite preview coverage; Bully, Doodle, remaining launch templates, and gait refinements continue as the next animation work.
106. DONE 2026-06-18: Polished the mobile Phoenix Home first screen toward the locked Option B board by renaming the hero to Phoenix Room, folding the Avatar Studio/Care Twin CTA into the hero console header, removing the extra first-screen care-twin identity bar, and trimming the in-room live HUD to the most important four vitals so the main dog scene has more visual room.
107. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Husky/Spitz live Avatar Studio sprite pack. The accepted walk loop now comes from a standing-source PixelLab state after rejecting a seated walk attempt, and Avatar Studio suppresses the still-template ghost while a live sprite is active so the preview reads as one care twin.
108. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Bully live Avatar Studio sprite pack. Bully now has idle/tail-wag and standing-source walk loops, expanding live prebuilt avatars to Retriever, Husky/Spitz, and compact-body families.
109. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Doodle live Avatar Studio sprite pack. Doodle now has idle/tail-wag and standing-source walk loops, with the strongest non-Phoenix walk read so far.
110. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Terrier live Avatar Studio sprite pack. Terrier now has idle/tail-wag and standing-source walk loops, and Avatar Studio now shows live/still template readiness badges plus a live-template count so unfinished templates feel intentionally in production instead of broken.
111. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Hound live Avatar Studio sprite pack. Hound now has idle/tail-wag and standing-source side-view walk loops, and the Avatar Studio live-template routing/pixel-stage polish now treats all completed template packs as animated care twins instead of still previews.
112. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Dachshund live Avatar Studio sprite pack. Dachshund now has idle/tail-wag and long-body side-view walk loops, expanding live prebuilt avatars to seven launch templates.
113. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Spaniel live Avatar Studio sprite pack. Spaniel now has idle/tail-wag and floppy-ear side-view walk loops produced from a standing-source state instead of the front-facing base still, expanding live prebuilt avatars to eight launch templates.
114. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Toy Breed, Slender, and Mixed Breed live Avatar Studio sprite packs. Every non-Phoenix launch template now has idle/tail-wag and walk-loop strips produced from side-view standing sources, so the prebuilt avatar catalog can breathe and move like a game sprite instead of relying on still previews.
115. NEXT: Run native iOS/Android device or simulator QA for Avatar Studio and Phoenix Home animation timing, touch targets, safe areas, phone-size sprite proportions, and gait quality; then refine any weak Toy/Slender/Mixed or earlier template walk loops and continue remaining template emote packs plus overlay-aligned accessory layers.
116. DONE 2026-06-19: Option B hard-pixel Phoenix runtime pack expansion. PixelLab selected and downloaded `option-b-seated.png`, `option-b-standing.png`, and `option-b-sleep-source.png`; generated/normalized Option B idle/tail-wag, walk, ear-perk, eat, drink, corrected curled sleep, comfort/home-alone, health-watch, celebrate, and dedicated bark/tap reaction strips; and wired the live Phoenix runtime plus sprite manifest to the Option B family so common Home states no longer fall back to the older v2 action art or the older south-facing bark fallback.
117. NEXT: Native iOS/Android device or simulator QA for the Option B Phoenix family, especially phone-size proportions, loop timing, comfort/health distinctness, walk gait, and tap reaction. Then replace first-pass room variants with final illustrated PixelLab/Figma-quality rooms, refine any weak Option B loops, and continue overlay-aligned accessories/emote polish.
118. DONE 2026-06-19: PixelLab final-room pass replaced the first-pass derived night, health-watch, and home-alone room runtime layers with visually accepted dogless 800x600 final candidates and preserved the derived bedtime room after rejecting PixelLab attempts that drifted isometric, baked in dogs, or added text/watermark marks.
119. DONE 2026-06-19: Generated, downloaded, visually accepted, normalized, and wired a clean PixelLab bedtime room final candidate, completing replacement of the first-pass derived room variants for night, bedtime, health-watch, and home-alone.
120. NEXT: Run native iOS/Android QA across Phoenix Home and Avatar Studio to judge room/sprite proportions, stage cropping, loop timing, and touch response on real phone-sized screens.
121. DONE 2026-06-19: Added a tested care-twin native QA matrix covering all 12 avatar motion states, expected sprite action, room variant, zone, scene phase, priority need, layered readiness, and matching manual iOS/Android phone-size prompts in `docs/release/CARE_TWIN_NATIVE_QA_MATRIX.md`.
122. NEXT: Use the care-twin native QA matrix during device/simulator review, then refine any clipped stage, weak loop, wrong room/sprite proportion, or touch-response issue before promoting Option B candidate assets to final approved paths.
123. DONE 2026-06-19: Polished the Option B living care-twin UI after Apollo's subscription-backed PixelLab review. Home quick logs now trigger action-specific Phoenix sprite reactions, `LivingPhoenixRoom` adds ambient micro-behaviors, scanline/frame polish, tighter stage zones, and rest-state safeguards, `PixelIcon` renders crisply on web, and Avatar Studio no longer paints oversized still/accessory layers over live sprite previews.
124. DONE 2026-06-19: Reviewed new PixelLab Phoenix candidate outputs. Candidate `6ad8a54d-3b19-41d8-9525-4eeca0d76529` is archived as a clean still reference, while candidates `96200e12-1297-4970-9342-beee2f18c5a9` and `93160aef-fe35-46bf-8b40-859687053072` are rejected due duplicate/cropped anatomy and identity drift. The current hard-pixel Option B runtime family remains the app source of truth.
125. NEXT: Execute the native iOS/Android QA matrix, capture phone-size evidence for Home and Avatar Studio, then refine any weak gait, stage crop, or room/sprite proportion before another PixelLab generation spend.
126. DONE 2026-06-19: Added an in-app Care Twin QA route at `/care-twin-qa` for development/internal builds. The route renders all 12 tested care-twin scenarios through the production `LivingPhoenixRoom`, exposes expected sprite/room/zone/need metadata, shows native QA prompts, and is reachable from More only under `__DEV__` so device testers can review every state without mutating care history.
127. NEXT: Open `/care-twin-qa` on iOS and Android, capture the required evidence from `docs/release/CARE_TWIN_NATIVE_QA_MATRIX.md`, then tune any stage crop, route frame height, weak loop, or room/sprite scale issue found on real device screens.
128. DONE 2026-06-19: Upgraded `/care-twin-qa` into a device evidence-capture surface. Each care-twin scenario can now be marked Pass or Needs tune, carries a per-state device note, updates summary counts, and shares a plain-text QA report through the native share sheet while still saying screenshots are required before launch approval.
129. NEXT: Run the upgraded QA route on iOS and Android, share/export the per-device QA report, attach the required screenshots, then fix the first visually obvious issue in stage crop, sprite scale, loop timing, or gait quality before generating more PixelLab art.
130. DONE 2026-06-19: Subscription-backed Option B redesign pass wired a clean dogless PixelLab day room into Home and Avatar Studio, registered Shepherd/Phoenix as a live Avatar Studio sprite pack using the approved Option B idle and walk strips, and moved sprite-action room staging into tested `careTwinStage.ts` logic so meals, water, walks, health/rest, and comfort route Phoenix to the correct game-stage zones.
131. NEXT: Review PixelLab pro character `be24cc90-7a69-4859-b9b2-42e73a2124cd` when it completes, but only promote it if it clearly beats the current hard-pixel Option B Phoenix runtime family and can produce matching transparent animation strips.
132. DONE 2026-06-19: Locked the Quick Log care-event doctrine into the mobile runtime. Tap now quick-logs safe defaults, long press opens the detailed composer path, medication/vomit route to detail-first safety flows, Meal starts a served/outcome-pending lifecycle with later outcome updates, Potty is the parent event with outcomes underneath, quick logs carry trust/confirmation metadata, Home uses the same builder as Log, and tests guard the doctrine.
133. PARTIAL 2026-06-19: Build the full structured edit/detail sheet stack for every care log. The Alone Time timer/check-in portion is complete in item 134; photo proof/confirmation is complete in items 135-137, potty details are complete in item 138, walk start/finish sessions are complete in item 139, and correction-history UI is complete in item 140. Provider-backed photo/document storage remains gated by Apollo's storage rules.
134. DONE 2026-06-19: Built the first-class Alone Time lifecycle. The mobile app now creates an active home-alone session when the household taps Alone Time/Leaving Home, Home reads that open session as `home-alone` instead of showing a stale with-human presence, Log shows an active session return check-in, I'm Home outcomes close the same log with duration, return state, recovery/note details, audit trail, and household visibility, and tests protect the start/return lifecycle.
135. DONE 2026-06-19: Added role-based care-log trust review for non-alone logs. The mobile app now has a tested `careLogTrust.ts` engine for Confirm, Reject, Request photo, and Mark corrected actions; Log detail sheets render a compact Trust review panel; adult owner/primary caregiver roles can review logs; kid/sitter/trainer/vet-viewer roles are blocked; review actions update trust metadata, proof-request status, severity when rejected, and the audit trail without deleting the original record.
136. DONE 2026-06-19: Detailed Log composer now applies the same tested trust defaults as quick logs. Medication detail logs start pending confirmation with proof-needed metadata, safety-critical health logs start pending review, kid/helper detail logs remain owner-reviewable, and timeline rows now show attention chips for needs-review, proof-needed, photo-requested, outcome-pending, rejected, corrected, and estimated states before opening the detail sheet.
137. DONE 2026-06-19: Added the first medication proof attachment seam. Care-log trust now has a tested local photo-proof attachment patch that records attachment URI/name/source, local-only storage status, attached-by/at metadata, proof-attached timeline chips, and an audit event without auto-confirming medication. Log detail Trust review can launch the Expo image picker, attach proof to a pending medication log, and truthfully labels the proof as local-only until provider storage is enabled.
138. DONE 2026-06-19: Added the potty detail correction seam for parent potty logs. The mobile Log detail sheet now has a Clarify potty log panel for outcome, location, pee detail, stool consistency/color, and context; the tested helper rewrites stale pee/stool fields when outcomes change, sets watch/alert severity only when warranted, preserves household/routine context, and appends audit history.
139. DONE 2026-06-19: Added the first-class Walk session lifecycle. Home and Log now start a household-visible active walk instead of silently treating every walk tap as a past log; Home shows Walk active in Phoenix's room, presence, and Next Up; Log shows a WALK ACTIVE panel with timer plus finish fields for route/place, distance, dog interactions, social outcome, and notes; finishing the same log records duration, audit history, and downstream Walk Activity/Saved Route evidence.
140. DONE 2026-06-19: Added the richer correction-history detail UI. Log details now show a Correction history card above the full Audit trail with the latest update, traceability state, correction count, and changed-field chips, so owners can understand what was corrected without reading raw audit rows first.
141. NEXT: Continue the structured edit/detail sheet stack with provider-backed photo/document upload storage once Apollo approves storage rules.
142. DONE 2026-06-19: Added the first local CareTwin roster readiness slice. `CareContext` now persists `activePetId` and planned `pets`, `careTwinRoster.ts` derives the active live care twin versus provider-gated future pet slots, More exposes a polished CareTwin Roster card plus Add future dog sheet, and Privacy export/deletion scope includes staged pet roster data. The UI explicitly blocks fake switching until provider-backed multi-dog care documents can separate logs, routines, records, reports, and sync.
143. NEXT: Build provider-backed multi-dog care documents and scoped dog switching after the storage/database/account rules are approved, including per-dog logs, routines, records, reports, avatars, privacy export, and household permissions.
144. DONE 2026-06-19: Added the local Access Pass and My Care Today operations slice. `access-pass.ts` now derives temporary sitter/trainer/vet/emergency helper permissions, blocked actions, active/upcoming/draft status, local-draft provider boundaries, and owner-specific assigned care for today. Mobile More stores `accessPasses`, shows Access Passes and My Care Today cards, creates local Access Pass drafts, shares a truthful draft summary, and privacy export/deletion scope includes Access Pass drafts.
145. NEXT: Build provider-backed Access Pass enforcement after account/storage rules are approved, including real invite approval, role-specific API authorization, pass revocation, helper audit trails, and notification delivery.
146. DONE 2026-06-19: Added the local Adventure Mode and memory foundation. `adventure.ts` derives private real-care quests, daily XP, levels, completed proof, next-step guidance, and local memory drafts from household-visible walks, training, play, and alone-time evidence. Mobile now has `/adventure`, More links to it, CareContext persists `adventureMemories`, and Privacy export/deletion scope includes Adventure memories without claiming cloud media, maps, or public sharing.
147. NEXT: Build provider-backed Adventure media and location features only after Apollo approves storage, maps/location retention, sharing permissions, and community safety rules. Until then Adventure stays local, private, and owner-controlled.
148. DONE 2026-06-19: Surfaced Adventure Mode on Phoenix Home inside the existing Care Quest card. Home now derives the same Adventure Mode model, shows the next quest plus level/XP/memory count, and routes directly to `/adventure` so the real-care RPG loop is visible from the first screen without adding a dead decorative section.
149. NEXT: Polish Adventure Mode visually after native phone QA, then decide whether Adventure deserves a primary tab once Home, Log, Plans, Health, and More launch navigation is stable.
150. DONE 2026-06-20: Added first-class Incident Watch for altercations, bites, rough greetings, escape/injury, and behavior-safety events. `incident` is now a canonical care event with aliases, shared `deriveIncidentWatch` logic, detail-first mobile Log composer fields for trigger/exposure/injury/action/follow-up, safety-critical trust defaults, Records Incident Watch summary, Care Pass report language, and tests covering domain derivation, Quick Log policy, mobile wiring, and Care Pass handoff output.
151. DONE 2026-06-20: Polished Incident Watch from a descriptive report into an operational follow-up workflow. `deriveIncidentWatch` now returns 7/30/lookback trend windows, rising/improving/steady/clear labels, owner follow-up tasks, and trainer goal suggestions from household-visible incident evidence. Mobile Records shows Trend signal, Follow-up plan, and Trainer goals cards, follow-up rows route to the Incident composer or trainer Care Pass preview, Care Pass reports include trend/follow-up/goal lines, and tests cover the new shared model plus mobile wiring.
152. PARTIAL 2026-06-20: Native iOS/Android QA for Incident Watch, Phoenix Home, Avatar Studio, and `/care-twin-qa` remains blocked on real device/simulator access, but the internal QA path is stronger. `/care-twin-qa` now includes a Mobile Release QA cockpit covering Phoenix Home, Care Twin State Lab, Avatar Studio, Incident Composer, Records Incident Watch, and Trainer Care Pass with Pass/Needs tune controls, per-surface notes, required screenshot evidence, launch-risk prompts, route-open buttons, and a combined native share report.
153. DONE 2026-06-20: Made the Mobile Release QA cockpit durable for real phone testing. `/care-twin-qa` now autosaves and restores release-surface status, care-twin state status, and device notes in local AsyncStorage via a tested `mobileQaSession.ts` snapshot/parser, so testers can open Home, Log, Records, Care Pass, or Avatar Studio and return without losing the QA session.
154. DONE 2026-06-20: Added a tested care-twin choreography model for game-like avatar behavior. `careTwinChoreography.ts` now derives the primary sprite loop, ambient micro-loops, state-aware tap reaction, reaction duration, and QA summary from the live `AvatarLifePlan`; Home no longer hard-codes every room tap to `bark-loop`, rest states use a soft ear-perk check-in, Health Watch uses a calm comfort response, `LivingPhoenixRoom` uses the choreography for ambient/reaction timing, and `/care-twin-qa` shows the motion recipe for each state.
155. DONE 2026-06-20: Added local screenshot evidence capture to the internal Mobile Release QA cockpit. `/care-twin-qa` can now attach iOS/Android screenshots from the device photo library to each release surface and each care-twin state, persist those attachments in the local QA session, count attached versus missing screenshots in summaries, and include screenshot filenames in the native share report. This is local evidence capture only, not provider-backed QA storage or launch approval.
156. PARTIAL 2026-06-20: Real iOS/Android Mobile Release QA still needs device/simulator execution, but the QA evidence path is more trustworthy. Screenshot attachments are now platform-aware: iOS captures count toward iOS slots, Android captures count toward Android slots, web/unknown attachments stay visible without falsely satisfying native evidence. The cockpit header and share reports now show iOS/Android coverage separately.
157. PARTIAL 2026-06-20: Real iOS/Android Mobile Release QA is still blocked on device/simulator access, but the internal cockpit is now harder to misread. Mobile release QA summary helpers now distinguish complete platform proof, flexible screenshot-slot satisfaction, and exact missing iOS/Android/flexible evidence. `/care-twin-qa` shows Native proof open/ready, platform proof copy, and the remaining evidence gap instead of coloring a generic screenshot pile as complete.
158. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach the required screenshots through the platform-aware evidence controls, share/export the combined QA report, then fix the first visible issue in stage crop, safe area, touch response, route frame, Incident Watch field fit, or weak animation. Provider-backed behavior goal persistence and provider-backed QA storage remain gated by account/storage/provider rules.
159. DONE 2026-06-20: Centralized mobile floating-paw tab safe-area spacing. `mobileLayout.ts` now owns tab bar, center paw, tabbed route, and standalone route bottom-clearance metrics; Home, Log, Plans, Health, More, Records, and the tab shell use the shared helper instead of route-local fixed padding; mobile readiness tests guard against regressing to hard-coded tab clearance. Local verification passed 298 behavior/readiness tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, and `git diff --check`.
160. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach the required screenshots through the platform-aware evidence controls, share/export the combined QA report, then fix the first visible issue in stage crop, safe area, touch response, route frame, Incident Watch field fit, bottom-nav clearance, or weak animation. Provider-backed behavior goal persistence and provider-backed QA storage remain gated by account/storage/provider rules.
161. DONE 2026-06-20: Extended the shared mobile safe-area contract beyond tabbed screens. `mobileLayout.ts` now also owns docked composer bottom spacing, and Adventure, Avatar Studio, Care Twin QA, Premium, Privacy, Setup, WoofGuide, and the auth shell use the shared standalone/docked helpers instead of route-local magic numbers. Mobile readiness tests now reject the old standalone padding constants and the WoofGuide local composer inset. Local verification passed 300 behavior/readiness tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `tmp/woofwatcher-standalone-layout-export`, and `git diff --check`.
162. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach required screenshots for tabbed routes, standalone routes, auth/setup, WoofGuide composer, Phoenix Home, Avatar Studio, and Care Twin QA, share/export the combined QA report, then fix the first visible safe-area, keyboard/composer, stage crop, touch response, or weak animation issue. Provider-backed behavior goal persistence and provider-backed QA storage remain gated by account/storage/provider rules.
163. DONE 2026-06-20: Added the first auth-aware household setup path to first-run onboarding. Setup now captures Create household, Join by invite, or Local preview intent; persists `householdSetup` in the local care document and privacy export; blocks join saves until an invite code is present; shows post-save confirmation copy based on Clerk/provider readiness and sign-in state; and protects the flow with setup, privacy, and mobile readiness tests. Apollo's latest light/dark WoofWatcher reference boards were also mirrored and locked as duplicate-confirmed visual north-star assets. Local verification passed 302 behavior/readiness tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `tmp/woofwatcher-household-setup-export`, and `git diff --check`.
164. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach required screenshots for tabbed routes, standalone routes, auth/setup household setup, WoofGuide composer, Phoenix Home, Avatar Studio, and Care Twin QA, share/export the combined QA report, then fix the first visible safe-area, keyboard/composer, stage crop, touch response, onboarding copy fit, or weak animation issue. Provider-backed invite acceptance, household creation, behavior goal persistence, and QA storage remain gated by account/storage/provider rules.
165. DONE 2026-06-21: Ported the latest mobile interaction contracts into the advanced premium builder branch without merging away the richer product/runtime work. `mobileLayout.ts` now owns top safe-area padding for tabbed, standalone, setup, and auth surfaces; modal sheet bottom padding; centered modal backdrop padding; floating feedback/debug offsets; keyboard avoiding offsets; 48px minimum touch targets; and shared inline hit slop. Home, Log, Plans, Health, More, Records, Adventure, Avatar Studio, Care Twin QA, Premium, Privacy, Setup, AuthShell, WoofGuide, ErrorFallback, and board primitives now use those shared contracts. Static readiness tests reject route-local top padding, unsafe modal bottom padding, and literal tiny hit slop. Local verification passed 306 behavior/readiness tests, focused mobile layout/readiness tests at 76 tests, mobile TypeScript, PixelLab asset verification at 149 files, and Expo web export to `tmp/woofwatcher-mobile-interaction-contract-export`.
166. NEXT: Execute real iOS and Android QA for the unified interaction contract: inspect route top spacing, bottom-nav clearance, keyboard/composer behavior, modal sheet reach, owner-review sheets, setup/auth forms, Records/Care Pass sheets, Log quick-note modals, and all tap targets. Attach platform screenshots in `/care-twin-qa`, share/export the combined report, then fix the first visible safe-area, keyboard, touch, crop, or animation issue before adding more product surface area.
167. DONE 2026-06-21: Replaced More's static launch-readiness card with a tested shared launch truth model. `launchReadiness.ts` now derives internal-preview, native-QA-open, provider-gated, approval-open, and store-ready states from native screenshot evidence, local release foundations, provider setup, sync health, account deletion, payments, AI, storage, push, legal, support, and store-account gates. More now shows six actionable launch tiles instead of claiming Expo/EAS readiness as a launch proxy, and readiness tests guard the model plus screen wiring. Local verification passed focused launch/mobile readiness tests at 73 tests, the full 310-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-launch-readiness-export`.
168. NEXT: Run real iOS and Android QA and close provider approvals before calling WoofWatcher launch-ready. The app can now show exactly why launch is blocked, but real store readiness still requires native screenshots, production auth/database/storage/AI/payments/push configuration, privacy/legal approval, support/deletion process approval, and Apollo's app-store submission decision.
169. DONE 2026-06-21: Added a shared attachment/storage manifest backbone for launch readiness. `attachmentManifest.ts` now derives one local media/report queue across medication proof photos, record receipts/documents, Adventure memory photos, Care Pass print artifacts, and QA screenshots; marks each item local-only, upload-ready, or provider-saved based on storage provider readiness; and formats owner-readable storage summaries. `launchReadiness.ts` now consumes that queue, so More's Records Storage tile can say exactly how many local files are gated instead of showing vague storage copy. Verification passed focused attachment/launch/mobile readiness at 76 tests, full behavior/readiness at 313 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-attachment-manifest-export`.
170. NEXT: Implement provider-backed upload/storage only after Apollo approves the provider rules: storage bucket/table design, signed upload/download access, household scoping, retention/export/deletion policy, migration of the local attachment queue, and native QA that confirms uploaded proof/photos/reports survive across devices.
171. DONE 2026-06-21: Extended Privacy & Safety to use the shared attachment manifest. Owner export bundles now include a storage queue summary/count for local proof photos, record documents, Adventure memory media, Care Pass print artifacts, and QA screenshots; the Privacy screen's Files stat uses the full local attachment queue; launch safety document-storage copy names the local queue; and deletion requests explicitly call out attachment-queue review before destructive deletion. Verification passed focused privacy/manifest/mobile readiness at 76 tests, full behavior/readiness at 313 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-privacy-attachment-export`.
172. NEXT: After storage provider approval, connect Privacy & Safety to real uploaded-document deletion/export guarantees, including provider object ids, signed download review, object-retention policy, and destructive deletion audit receipts.
173. DONE 2026-06-21: Added an owner-facing Privacy & Safety attachment queue review. `attachmentManifest.ts` now builds grouped review rows for care proof photos, record documents, Adventure memories, Care Pass reports, and QA screenshots with storage status, safe action copy, and sample filenames; privacy export bundles include those rows; and the mobile Privacy screen renders an Attachment queue board so local files are visible before provider-backed storage exists. Verification passed focused attachment/privacy/mobile readiness at 78 tests, full behavior/readiness at 315 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-attachment-review-export`.
174. NEXT: After provider storage approval, wire the attachment queue review to real provider object ids, signed download review, retention/export/deletion receipts, and native iOS/Android proof that uploaded files survive cross-device.
175. DONE 2026-06-21: Added a tested owner/operator release packet. `releasePacket.ts` now converts the shared launch-readiness plan into a release score, verdict, gate rows, owner approval checklist, blockers, next actions, and safe handoff notes; More shows the packet score/verdict and can share a native Launch Packet without claiming App Store/Play Store readiness. Verification passed focused release/mobile readiness at 72 tests, full behavior/readiness at 318 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-release-packet-export`.
176. PARTIAL 2026-06-21: Remote verification for the release packet commit was triggered as GitHub Actions run `27900061970`, but it failed before job execution with job `82558663293`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82558663293`, matching the active GitHub billing/spending-limit blocker.
177. DONE 2026-06-21: Added a tested support/legal runbook readiness packet. `supportRunbook.ts` now derives support inbox, refund/subscription policy, veterinary/emergency boundary, privacy/terms links, deletion escalation, incident response, launch blockers, and share text without approving public launch prematurely. Privacy & Safety now shows a Support runbook launch-gate card with explicit blockers and a native share action. Verification passed focused support/mobile readiness at 72 tests, full behavior/readiness at 321 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-support-runbook-export`.
178. PARTIAL 2026-06-21: Remote verification for the support runbook commit was triggered as GitHub Actions run `27900748947`, but it failed before job execution with job `82560508917`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82560508917`, matching the active GitHub billing/spending-limit blocker.
179. DONE 2026-06-21: Made support/legal launch settings a persisted local-first care-document object instead of static placeholders. `CareContext` now stores `launchSupportProfile` with support inbox, privacy/terms URLs, refund/subscription approval, veterinary-boundary approval, deletion escalation, incident response, owner-review timestamp, and provider status; Privacy export bundles include that profile; and Privacy & Safety now derives the Support runbook from state and exposes an editable launch-profile sheet with truthful local owner-review copy. Verification passed focused privacy/mobile readiness at 73 tests, full behavior/readiness at 321 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-launch-support-profile-export`.
180. PARTIAL 2026-06-21: Remote verification for the launch support profile commit was triggered as GitHub Actions run `27901369925`, but it failed before job execution with job `82562168168`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82562168168`, matching the active GitHub billing/spending-limit blocker.
181. DONE 2026-06-21: Connected the persisted launch support profile back into More's Launch Readiness cockpit without overstating approval. `launchReadiness.ts` now distinguishes fully approved store gates from locally owner-reviewed support/privacy packets; More derives the staged-owner-review flags from `state.launchSupportProfile` and `supportRunbook.ts`; and readiness tests protect the "Owner packet staged" state plus specific final legal/support approval blockers. Verification passed focused launch/mobile readiness at 74 tests, full behavior/readiness at 322 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-owner-staged-launch-export`.
182. PARTIAL 2026-06-21: Remote verification for the owner-staged launch cockpit commit was triggered as GitHub Actions run `27901968840`, but it failed before job execution with job `82563764563`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82563764563`, matching the active GitHub billing/spending-limit blocker.
183. DONE 2026-06-21: Added a truthful App Store/Play Store prep packet without claiming submission approval. `storeSubmissionPacket.ts` now derives store metadata, short/full description, keyword draft, screenshot checklist, review notes, privacy disclosures, and blocked-until gates from the existing release packet. More renders a Store Submission panel inside Launch Readiness and can share the store packet separately from the operator release packet. Verification passed focused store-submission/mobile readiness at 72 tests, full behavior/readiness at 325 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-store-submission-export`.
184. PARTIAL 2026-06-21: Remote verification for the store submission packet commit was triggered as GitHub Actions run `27902485718`, but it failed before job execution with job `82565178527`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82565178527`, matching the active GitHub billing/spending-limit blocker.
185. DONE 2026-06-21: Connected store-submission screenshot prep to the internal native QA cockpit. `mobileReleaseQa.ts` now converts `storeSubmissionPacket.ts` screenshot checklist items into device-review surfaces with explicit iOS and Android evidence slots, store-safe prompts, route targets, and launch-risk copy. `/care-twin-qa` now renders a Store Screenshot QA section, shares the store packet from the QA cockpit, includes store screenshot surfaces in the platform evidence counts, and keeps store-prep claims truthful instead of treating screenshots as approval.
186. PARTIAL 2026-06-21: Remote verification for the store screenshot QA cockpit commit was triggered as GitHub Actions run `27903415928`, but it failed before job execution with job `82567714718`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82567714718`, matching the active GitHub billing/spending-limit blocker.
187. DONE 2026-06-21: Connected saved Mobile Release QA proof back into More's Launch Readiness cockpit. `mobileLaunchQaEvidence.ts` now builds the combined launch/store QA surface set, converts saved `/care-twin-qa` session status/evidence into the `LaunchReadinessNativeQaSummary`, and returns `null` for empty sessions so the app does not invent native proof. More reloads the saved QA session from AsyncStorage on focus, feeds the derived native QA summary into `deriveLaunchReadiness`, and still routes open native-QA work back to `/care-twin-qa`. Verification passed focused launch-QA/mobile readiness at 73 tests, full behavior/readiness at 330 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-saved-qa-launch-export`.
188. PARTIAL 2026-06-21: Remote verification for the saved-QA launch-readiness commit `286fa5e` was triggered as GitHub Actions run `27904305112`, but it failed before job execution with job `82570057038`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82570057038`, matching the active GitHub billing/spending-limit blocker.
189. DONE 2026-06-21: Added a tested Native QA next-captures plan so Launch Readiness can tell testers exactly what to capture next. `mobileLaunchQaEvidence.ts` now derives prioritized capture targets from the saved `/care-twin-qa` session, including missing iOS, Android, and flexible screenshot evidence per surface, with launch-critical surfaces sorted first. More renders a `Native QA Next Captures` panel inside Launch Readiness with open/complete counts, the next target routes, and direct route jumps.
190. PARTIAL 2026-06-21: Remote verification for the Native QA next-captures commit `bea5793` was triggered as GitHub Actions run `27905215517`, but it failed before job execution with job `82572570665`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82572570665`, matching the active GitHub billing/spending-limit blocker. Local verification passed focused capture-plan/mobile readiness at 75 tests, full behavior/readiness at 332 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-native-qa-captures-export`.
191. NEXT: Execute real iOS and Android QA with `/care-twin-qa`: capture/attach platform screenshots for Mobile Release QA, Store Screenshot QA, and the 12-state care-twin matrix; confirm More's Launch Readiness updates from the saved proof and next-captures panel; share/export the combined QA report; then fix the first visible crop, safe-area, touch, keyboard, route, or animation issue.
192. DONE 2026-06-21: Added a tested shareable Native QA capture plan so Apollo can send the next device-testing script to himself, Fable/Replit, or a helper without rebuilding the checklist manually. `buildMobileLaunchQaCaptureShareText` formats progress, ordered next routes, missing evidence, attached counts, and the done condition from the same capture plan that More renders. More now includes a `Share QA Plan` action inside `Native QA Next Captures`.
193. PARTIAL 2026-06-21: Remote verification for the shareable Native QA capture plan commit `3544c11` was triggered as GitHub Actions run `27906100583`, but it failed before job execution with job `82574915830`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82574915830`, matching the active GitHub billing/spending-limit blocker. Local verification passed focused capture-plan/mobile readiness at 76 tests, full behavior/readiness at 333 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-native-qa-share-plan-export`.
194. DONE 2026-06-21: Added a persisted Provider Launch Setup cockpit so the final production gates are editable, shareable, and launch-readiness-aware instead of hard-coded false states. `launchProviderSetup.ts` derives an eight-gate provider plan for production auth, household database sync, storage, WoofGuide AI, payments, push, store accounts, and self-serve deletion; clamps partial "provider approved" claims back to owner-reviewed; formats a safe share packet; and More renders the plan inside Launch Readiness with an edit sheet and Share Provider Plan action. `CareContext` now persists `launchProviderProfile`, Privacy export includes it, and `deriveLaunchReadiness` consumes the saved provider gates while still requiring native QA, legal/support/store approval, and Apollo sign-off.
195. PARTIAL 2026-06-21: Remote verification for Provider Launch Setup commit `743f3dd` was triggered as GitHub Actions run `27907578184`, but it failed before job execution with job `82578864889`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82578864889`, matching the active GitHub billing/spending-limit blocker. Local verification passed the full 337-test suite, mobile TypeScript, PixelLab asset verification, `git diff --check`, and Expo web export.
196. NEXT: Execute real iOS/Android QA and production-provider setup: fill the Provider Launch Setup sheet only when Clerk/Supabase/storage/AI/payments/push/store/deletion gates are actually configured, attach native screenshots through `/care-twin-qa`, share/export the QA and launch packets, then fix the first visible or provider-backed issue. Public launch remains blocked until those external approvals are complete.
197. DONE 2026-06-22: Added a tested Home care-RPG mission deck so Phoenix Home reads closer to the locked mockups and routes like an intentional command center instead of a stack of decorative cards. `homeMissionDeck.ts` derives My Care Today, Adventure, Health Watch, and Care Pass missions from real local-first state: pending meal/open walk/alone-time loops route to Log, normal care routes to Plans, Adventure routes to `/adventure`, Health routes to `/health`, and Care Pass routes to Records. Home renders the deck as a dark navy pixel-console section directly under the Phoenix status tiles, with compact touch rows, status labels, CTAs, pixel icons, and no fake cloud/provider readiness claims. Verification passed the new mission-deck tests, mobile readiness contract, full 355-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, and direct Expo web export to `.expo-smoke`.
198. DONE 2026-06-22: Added a tested small-phone Home mission-deck layout contract as the strongest unblocked pre-device QA step. `homeMissionLayout.ts` now derives compact versus regular mission density from viewport width, estimates deck height, hides the secondary badge on small iPhone-class screens, tightens row/icon/CTA sizing without dropping below mobile touch targets, and exposes a QA label for the mission rows. Home now uses that contract through `useWindowDimensions`, so the care-RPG mission console stays more reachable before real iOS/Android screenshots. Verification passed focused mission/readiness tests at 76 tests, the full 358-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, and direct Expo web export to `.expo-smoke`.
199. DONE 2026-06-23: Added the Home mission deck as an explicit launch-critical Mobile Release QA surface instead of leaving it as a loose manual note. `/care-twin-qa`, Launch Readiness, and the shareable Native QA capture plan now include Home Mission Deck evidence slots requiring iOS and Android compact-deck screenshots, floating paw-nav visibility, no-overflow review, and route confirmation for pending meal, active walk/alone sessions, Adventure, Health, and Care Pass. Focused release-QA/readiness verification passed 84 tests.
200. DONE 2026-06-23: Tuned the Native QA capture plan to preserve release-surface order within each priority group instead of alphabetizing launch-critical targets. The shareable QA script now naturally leads with Phoenix Home, then Home Mission Deck, then Avatar Studio and the rest of the launch-critical review path, matching the flagship first-screen product risk. Focused release-QA/readiness verification passed 85 tests.
201. DONE 2026-06-23: Added explicit device verification steps to every Mobile Release QA surface and generated Store Screenshot QA surface. `/care-twin-qa` now renders numbered Device steps/Store steps before evidence capture, the shareable Mobile Release QA report includes the same steps, the Native QA Next Captures model carries those steps into More, and the More launch panel shows the first route-check step for each next target. Focused release-QA/readiness verification passed 85 tests.
202. DONE 2026-06-23: Added reproducible setup/precondition steps to every Mobile Release QA surface and generated Store Screenshot QA surface. QA packets now explain how to prepare demo-safe state before capture, including Home Mission Deck pending meal setup, active walk/Alone Time setup, Incident Watch sample-data setup, PixelLab pack readiness, and blocked store-screenshot truth boundaries. `/care-twin-qa` renders Setup first/Store prep ahead of Device steps, More's Native QA Next Captures rows show the first prep step, and share text includes setup plus verification instructions. Focused release-QA verification passed 14 tests.
203. DONE 2026-06-23: Added explicit pass criteria and Needs tune failure escalation to every Mobile Release QA and generated Store Screenshot QA surface. `/care-twin-qa` now renders Pass criteria before evidence capture, More's Native QA Next Captures rows show the first pass criterion, and both Mobile Release QA and Native QA capture share text carry pass/failure criteria so device testers know what counts as launch-quality proof. Focused release-QA verification passed 14 tests.
204. DONE 2026-06-23: Added API readiness coverage for mounted-but-previously-unmodeled WoofGuide event and Avatar Studio routes. `apiReadiness.test.ts` now guards OpenAPI, the generated React client, Zod operation validators, Zod type exports, and root focused-test wiring for `/woofguide-events`, `/avatar-stylize`, and `/avatar-emotions`; `openapi.yaml`, React schemas/hooks, and Zod exports now cover those routes without claiming provider-backed event curation or avatar generation is live when keys are missing. Local verification passed the API readiness test, the 362-test focused behavior/readiness suite, PixelLab asset verification at 149 files, `git diff --check`, and direct Expo web export through the package-local CLI. Direct workspace typecheck/API build remain limited in this Windows runtime by the missing pnpm workspace command/symlink layer, while remote GitHub Actions remains blocked by billing/spending-limit state.
205. DONE 2026-06-23: Closed the care-entries list query contract drift. The server already supported `/care-entries?limit=...`, and API readiness now proves the route, OpenAPI, React generated params, Zod validator, and Zod generated param type all expose `limit`; OpenAPI documents the 1-500 range and default. Local verification passed the API readiness test and the 363-test focused behavior/readiness suite.
206. NEXT: Run real iOS/Android QA on the updated Home first screen and Home Mission Deck QA surface using the in-app setup, numbered device steps, pass criteria, and Needs tune escalation: create/confirm pending meal and active walk/alone-time state, capture/attach compact-deck screenshots through `/care-twin-qa`, confirm rows stay reachable around the floating paw nav, confirm text does not overflow, confirm mission routes land correctly, share/export the QA report, then fix the first visible phone-size issue before provider-backed setup. Continue provider-backed setup only when Apollo confirms Clerk/Supabase/storage/AI/payments/push/store/deletion gates are actually configured.
207. DONE 2026-06-23: Hardened the care-state write error contract while native QA remains device-blocked. The `PUT /care-state` route already returns validation errors, missing-document errors, and optimistic conflict envelopes; API readiness now proves OpenAPI documents `400`, `404`, and `409`, and the generated React mutation exposes `ApiError | CareStateEnvelope` so clients can handle validation/not-found errors separately from stale-write refetch envelopes. Local verification passed the API readiness test, the full focused behavior/readiness suite, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28063020164` failed before job start with the standing GitHub billing/spending-limit annotation.
208. DONE 2026-06-23: Followed the unavailable-device fallback by hardening care-entry write error contracts for the core logs that feed routines, reports, and household trust. API readiness now proves create/update/delete care-entry routes validate bodies or ids, OpenAPI documents `400` validation errors plus existing `404` not-found responses, and the generated React create mutation exposes `ApiError` instead of `unknown` for invalid log writes. Local verification passed the API readiness test, the 365-test focused behavior/readiness suite, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28064200143` failed before job start with the standing GitHub billing/spending-limit annotation.
209. DONE 2026-06-23: Continued the unavailable-device fallback by hardening household provisioning and auth-error contracts. API readiness now proves `/me`, `/me` updates, household rename, and household join stay authenticated; profile, household, and invite payloads stay validated; first-login provisioning still documents default household, membership, and care-state bootstrap; OpenAPI documents household `401`, `400`, and missing-invite `404` responses; and generated React household hooks expose `ApiError` instead of `unknown` for auth/validation failures. Local verification passed the API readiness test, the 366-test focused behavior/readiness suite, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28065179874` failed before job start with the standing GitHub billing/spending-limit annotation.
210. DONE 2026-06-23: Continued the unavailable-device fallback by hardening provider-gated WoofGuide action contracts. API readiness now proves authenticated care-helper questions, provider-only care-helper rate limits, truthful local care-helper fallback copy, authenticated WoofGuide events status/creation, WoofGuide event creation rate limits, and local curated event fallback when provider keys are unavailable. OpenAPI now documents care-helper `401`/`429`/provider `502` responses without falsely treating local fallback as a provider-missing error, WoofGuide events status/creation now document `401`, and generated React hooks expose `ApiError | CareHelperError` or `ApiError` instead of `unknown` for these launch-critical paths. Local verification passed the RED/GREEN API readiness test, the 367-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28066357245` failed before job start with the standing GitHub billing/spending-limit annotation.
211. DONE 2026-06-23: Continued API behavior contract coverage for authenticated household scoping across care-state and care-entry read/write paths. API readiness now proves care-state reads/writes require auth, resolve the active household from the authenticated user, and query/update only that household document; care-entry list/create/update/delete routes require auth, scope queries/mutations by active household, and stamp new logs with the authenticated household and caregiver id. OpenAPI now documents care-state read `401`/`404`, care-state write `401`, and care-entry list/create/update/delete `401`; generated React care-state and care-entry list queries expose `ApiError` instead of `unknown`. Local verification passed the RED/GREEN API readiness test, the 368-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28067734120` failed before job start with the standing GitHub billing/spending-limit annotation.
212. DONE 2026-06-23: Continued API/provider hardening with role-aware care-entry write readiness beyond basic household membership. `care-entry-authorization.ts` now defines read-only role boundaries, kid/helper pending-confirmation defaults, safety-critical review defaults for medication/vomit/symptom/incident logs, and medication proof metadata. Care-entry create/update/delete routes now resolve the authenticated household member role before writes, return ApiError-shaped `403` responses for read-only or non-adult delete attempts, preserve household scoping, and keep serious/restricted logs from being silently confirmed. OpenAPI now documents care-entry create/update/delete `403` responses. Local verification passed the RED/GREEN API readiness test, the 369-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, generated-client and API-route syntax checks, `git diff --check`, and Expo web export.
213. PARTIAL 2026-06-23: Remote verification for the care-entry role policy commit `fa764e2` was triggered as GitHub Actions run `28069107846`, but it failed before job execution with job `83099726112`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83099726112`. The run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
214. DONE 2026-06-23: Continued provider-backed authorization readiness with owner/admin household member mutation and helper revocation contracts. `household-authorization.ts` now defines canonical household roles, owner/admin-only role changes, self-change/self-revocation blocking, protected-owner safeguards, Access Pass-compatible helper scopes, and helper revocation policy language. `PATCH /household/members/{id}` and `DELETE /household/members/{id}` are authenticated, scoped to the active household, validate ids/payloads, reject empty patches, reject unknown role strings, return typed `403` errors for forbidden mutations, update canonical roles/display names, and revoke helpers without crossing households. OpenAPI, Zod validators, and the generated React client now expose the update/revoke operations, canonical role enum, and `ApiError` mutation errors. Local verification passed the RED/GREEN API readiness test, the 370-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, generated-client and API-route syntax checks, `git diff --check`, and package-local Expo web export.
215. PARTIAL 2026-06-23: Remote verification for the household member role mutation commit `6e25c2f` was triggered as GitHub Actions run `28072320208`, but it failed before job execution with job `83109274416`, `steps: []`, `runner_id: 0`, and `gh run view --log-failed` returned `log not found: 83109274416`. The check-run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
216. DONE 2026-06-23: Continued provider-backed household sharing readiness with typed invitation and Access Pass audit contracts. Invite acceptance now stores canonical adult caregiver roles instead of legacy member strings and returns a typed `HouseholdAuditEvent`. Owner/admin Access Pass activation and revocation routes now validate payloads, stay scoped to the authenticated active household, enforce sitter/trainer/walker/vet-viewer helper roles, reject self/owner/core-member misuse, update or revoke helper membership, and return helper audit metadata. OpenAPI, Zod validators, and the generated React client now expose `HouseholdJoinResponse`, `HouseholdAuditEvent`, `HouseholdAccessPassMutationResponse`, `activateHouseholdAccessPass`, and `revokeHouseholdAccessPass`. Local verification passed the RED/GREEN API readiness test, the 371-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, generated-client/API-route syntax checks, `git diff --check`, and package-local Expo web export. Direct API build remains environment-limited by the missing pnpm workspace symlink layer in this Windows shell.
217. PARTIAL 2026-06-23: Remote verification for the household Access Pass audit contract commit `fa26845` was triggered as GitHub Actions run `28074177667`, but it failed before job execution with job `83114878625`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83114878625`. The run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
218. DONE 2026-06-24: Continued provider readiness by moving household invitation, role mutation, and Access Pass audit metadata from response-only to a provider-durable contract. `household_audit_events` is now in the Drizzle schema with lifecycle state, expiry metadata, and provider/export metadata; join/update/revoke/Access Pass routes persist audit rows through `buildHouseholdAuditInsert`; Access Pass activation rejects invalid or past `expiresAt` windows before changing helper roles; and OpenAPI, Zod validators, React schemas, API readiness, and behavior tests now expose typed audit lifecycle states including `access-pass-expired`. Local verification passed RED/GREEN API readiness and Access Pass behavior tests, the 374-test focused behavior/readiness suite, mobile TypeScript, DB/API/generated syntax checks, PixelLab asset verification at 149 files, `git diff --check`, and package-local Expo web export. Direct `tsc -p lib/db` remains environment-limited by missing local `@types/node`, and direct API build remains environment-limited by unresolved `esbuild` without the pnpm workspace runner.
219. PARTIAL 2026-06-24: Remote verification for the durable household audit readiness commit `c67364e` was triggered as GitHub Actions run `28075849741`, but it failed before job execution with job `83119832168`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83119832168`. The run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
220. DONE 2026-06-24: Continued provider readiness with owner/admin household audit review APIs while real device QA remains unavailable. `GET /household/audit-events` is now authenticated, active-household scoped, owner/admin-only, sorted newest first, accepts safe `limit`/`action`/`lifecycleState` filters through `normalizeHouseholdAuditListQuery`, and returns typed provider-durable audit rows. OpenAPI, Zod validators, React schemas/hooks, generated type files, API readiness tests, and behavior tests now cover the audit-list/admin review contract without claiming provider migration, RLS, retention, export, deletion, or expiry cleanup is complete. Local verification passed RED/GREEN audit tests, the 376-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export.
221. PARTIAL 2026-06-24: Remote verification for the household audit review API commit `eb50f5c` was triggered as GitHub Actions run `28078084503`, but it failed before job execution with job `83126533628`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83126533628`. This matches the standing GitHub billing/spending-limit blocker; local verification remains the current evidence for the audit-review slice.
222. DONE 2026-06-24: Continued provider readiness with request-time Access Pass expiry enforcement. Household member rows now carry `accessPassExpiresAt`, Access Pass activation persists valid future expiry windows, helper roles with expired passes are downgraded to `expired access pass` during authorization, care-entry writes treat that state as read-only, `/me` exposes `accessPassExpiresAt` and `accessPassExpired` for UI truth, and OpenAPI/Zod/React generated member schemas expose the same fields. Local verification passed RED/GREEN Access Pass/API readiness tests, the 378-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export.
223. DONE 2026-06-24: Continued provider readiness with durable household invitation lifecycle storage and typed owner/admin invite APIs. `household_invitations` is now in the Drizzle schema with unique invite codes, invited email/user metadata, canonical helper/adult roles, pending/approved/accepted/revoked/expired/rejected lifecycle states, expiry windows, actor timestamps, notes, and provider metadata. Owner/admin `GET /household/invitations`, `POST /household/invitations`, and `POST /household/invitations/{id}/revoke` routes are authenticated, active-household scoped, generated-code typed, and audit-backed; `/household/join` now prefers durable invitation rows, blocks pending/revoked/expired/rejected/accepted invites, applies invite roles, marks accepted invites, and falls back to legacy household invite codes only when no durable row exists. Local verification passed RED/GREEN invitation/API readiness tests, the 381-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export.
224. DONE 2026-06-24: Continued provider readiness with owner/admin household sharing cleanup review. `GET /household/sharing-cleanup` is authenticated, active-household scoped, owner/admin-only, read-only, and returns typed stale sharing candidates for runtime-expired invitation rows plus expired Access Pass helper memberships without deleting, revoking, or mutating access. `household-sharing-cleanup.ts` owns query normalization, non-destructive candidate derivation, review-only storage copy, and counts for expired invites versus expired helper passes. OpenAPI, Zod validators/types, React schemas/hooks, API readiness tests, and behavior tests now cover the cleanup-review contract while preserving the launch boundary that applying cleanup, Supabase migration/RLS, retention/export/deletion policy, notification delivery, and legal/privacy approval remain separate gates. Local verification passed RED/GREEN cleanup/API readiness tests, the 384-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, and package-local Expo web export.
225. DONE 2026-06-25: Pivoted the launch path for Apollo's two-day deadline from vague public-launch readiness to a concrete internal beta / owner-preview ship target. `releasePacket.ts` now separates `betaShipStatus`, `betaVerdictLabel`, `betaSummary`, and `betaNextActions` from public `storeLaunchReady`, so the app can say "Beta candidate - capture device proof" or "Ready for internal beta" without claiming App Store readiness. More's Launch Readiness board now shows a dedicated 48-hour beta card. `releasePacket.test.ts` covers beta candidate, internal beta ready, blocked local beta gates, and store-ready cases; `mobileReadiness.test.ts` protects the More UI wiring. Added `docs/release/TWO_DAY_BETA_SHIP_PLAN.md` to define what can ship in two days and what remains blocked for public launch. Local verification passed 81 targeted release/readiness tests, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, release-packet syntax check, and package-local Expo web export with HTML/JS output verified and cleaned.
226. DONE 2026-06-25: Turned the two-day beta card into an actionable in-app mission path instead of a passive readiness summary. More's Launch Readiness board now renders the first three `betaNextActions` from the release packet, keeps the 48-hour beta verdict visible, and exposes a route-aware button: "Open QA Cockpit" when device proof is the next blocker, or "Share Beta Packet" when the beta is ready to circulate internally. Static mobile readiness now protects the visible beta action rows and accessible QA/share CTAs. Local verification passed the targeted 81-test release/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
227. DONE 2026-06-25: Made `/care-twin-qa` start with a focused 48-hour beta run card instead of dropping testers directly into the full checklist. The QA cockpit now derives the same next-capture plan from saved local QA evidence, shows the next surface, missing proof, complete/open count, and provides a one-tap `Open Next Surface` action plus `Share QA`. Static mobile readiness now protects `buildMobileLaunchQaCapturePlan`, `48-hour beta run`, `nextBetaTarget`, `Open Next Surface`, and the accessible route CTA. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
228. DONE 2026-06-25: Reduced two-day beta QA friction by adding explicit screenshot platform tagging to `/care-twin-qa`. The 48-hour beta run card now lets testers tag new evidence as iOS, Android, or Web before attaching screenshots from Photos, uses that selected platform when building saved QA screenshot evidence, and shows each attached file's counted platform so native proof gaps can close truthfully even when screenshots are attached from an Expo/PWA preview. Static mobile readiness now protects `selectedEvidencePlatform`, `Tag screenshot evidence`, accessible `Tag QA screenshots as ...` controls, `targetPlatform: selectedEvidencePlatform`, and visible attachment platform labels. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
229. DONE 2026-06-25: Reduced device-test navigation friction by adding a QA return loop from `/care-twin-qa` into every shared board-header route opened for capture. QA target launches now append `qaReturn=care-twin-qa`, `qaSurface`, and `qaTitle` query context, while the shared `BoardRouteHeader` renders a temporary `Return to QA Cockpit` banner only for those capture sessions. Testers can open Home, Log, Health, Records, Avatar Studio, store screenshot targets, and other board-header routes, capture the screen, then jump back to `/care-twin-qa` to attach proof and mark Pass/Needs tune without losing the QA workflow. Static mobile readiness now protects `buildQaReturnRoute`, query context, `useLocalSearchParams`, the return banner, and the cockpit return action. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
230. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the tester briefing in `/care-twin-qa`. The 48-hour beta run card now shows a `Next device mission` panel before route launch with target route, priority, current review status, evidence count, setup steps, pass criteria, and explicit `Needs tune if` escalation copy. Static mobile readiness protects the mission briefing fields so the beta capture path stays understandable under deadline pressure. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
231. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by turning the 48-hour beta run card into a single mission-control surface. `/care-twin-qa` now resolves `nextBetaSurface` from the active capture target and exposes top-card controls to `Attach proof`, mark the current mission `Pass`, or mark it `Needs tune`, all using the selected iOS/Android/Web evidence tag and shared 48px mobile touch target contract. Static mobile readiness protects the mission action rail, accessible labels, selected-platform copy, proof attachment call, and pass/needs-tune state writes. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
232. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by adding a launch-critical `Owner Preview Core Loop` QA surface. The mobile release QA model now requires the real beta path to be proven in order: Home, Log, Plans, Health, More, Records, Avatar Studio, and Care Pass, including a quick safe care log, Launch Readiness review, non-diagnostic Health Watch/Bile Watch copy, and explicit iOS/Android evidence requirements. Local verification passed the targeted 86-test QA/readiness suite, the 387-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
233. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by adding an explicit `Owner route loop` panel to the `/care-twin-qa` 48-hour beta run card. The owner-preview capture plan now carries a route-by-route checklist for Home, Log, Plans, Health, More, Records, Avatar Studio, and Care Pass, including expected outcome and proof notes. The shareable QA capture script includes the same ordered route loop, and static readiness protects the in-app panel so a device tester can run the actual owner path without cross-checking separate docs. Local verification passed the targeted 87-test QA/readiness suite, the 388-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
234. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by tightening the owner-preview proof algorithm and mission card. The native QA capture plan now treats required `Note ...` evidence as a real missing item, so the Owner Preview Core Loop cannot be considered complete with screenshots and Pass alone. `/care-twin-qa` now exposes a `Mission note` field directly in the 48-hour beta run card, marks it Required when the active target needs QA-note proof, and saves it through the existing local QA session state. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
235. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by wiring the live 48-hour native capture plan into `/care-twin-qa`'s own `Share QA` packet. The cockpit share action now includes `buildMobileLaunchQaCaptureShareText(betaCapturePlan, reviewedAtIso)` before the full Mobile Release QA, store submission packet, and 12-state care-twin report, so Apollo or a device helper can share the exact next target, missing evidence, Owner route loop, Mission note requirement, and done condition from the same screen where proof is attached. Static mobile readiness protects the route import and share packet wiring. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
236. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by preventing false Pass confidence in the `/care-twin-qa` 48-hour beta run card. The cockpit now derives `nextBetaTargetMissingEvidence` once, shows a visible `Pass pending proof` gate when a mission is marked Pass but still lacks required screenshots or Mission note proof, and lists the first missing proof items directly in the mission card. Static readiness protects the proof-gate state, copy, and missing-evidence bullets. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
237. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by carrying the `Pass pending proof` truth state into More's Launch Readiness flow. The Native QA Next Captures panel now shows a visible `Proof status` row per target, uses `Pass pending proof` when a target is marked Pass but still lacks required evidence, includes that status in the accessibility label, and the shareable QA capture script now sends the same owner-readable status instead of raw internal status strings. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
238. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by making More's Native QA Next Captures panel a clearer route back to proof completion. The panel now has a mobile-sized action rail with `Share QA Plan` plus `Finish Proof` when any target is `Pass pending proof`, otherwise `Open QA Cockpit`; the proof action routes directly to `/care-twin-qa` with accessible copy so testers can resolve missing screenshots or Mission notes without hunting through the app. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
239. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening `/care-twin-qa` itself for the two-day beta run. The QA cockpit now uses the shared `MIN_MOBILE_TOUCH_TARGET` contract for the screenshot platform picker, Open Next Surface / Share QA actions, Share QA Summary buttons, evidence attach/clear controls, per-surface Open Surface CTAs, and Pass / Needs tune review buttons. Static readiness now parses named style blocks and fails if any of those critical QA controls regress to route-local undersized heights. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
240. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the core Log route touch targets for the owner-preview loop. Quick Log now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for outbox retry, launcher tabs, quick-feedback Undo/Add details, alone-time return outcomes, active-walk finish, trust proof attachment, trust review actions, meal outcome updates, potty outcome options, and potty detail save. Static readiness now protects those named style blocks so the under-five-second logging flow cannot regress to cramped 36-44px controls. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
241. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the Health Watch route for the owner-preview loop. Health now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for the Health/Bile segmented tabs plus the `Log health note` and `Records` hero actions, while leaving decorative pixel meters and cards alone. Static readiness now protects those named Health style blocks so the non-diagnostic Health Watch/Bile Watch screen cannot regress to cramped controls during beta review. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
242. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the Plans route for the owner-preview loop. Plans now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for Add plan, Find event, suggestion add, schedule tabs, schedule completion, routine add, event remove, routine done, modal type chips, owner chips, save, and delete controls. Static readiness now protects those named Plans style blocks so schedule/routine work cannot regress to cramped 28-42px controls during beta review. Local verification passed the red/green mobile readiness test, the targeted 89-test QA/readiness suite, the 390-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
243. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the More route's launch and household gateway actions. More now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for profile edit, Care Intelligence action, provider setup actions, native QA share/cockpit actions, beta next action, Launch/Store packet share actions, Access Pass share and role chips, household invite, prompt modal actions, provider status chips, weight-unit chips, and profile/diet/provider save buttons. Static readiness now protects those named More style blocks so the Launch Readiness and household handoff gateway cannot regress to cramped 32-46px controls during beta review. Local verification passed the red/green mobile readiness test, the targeted 90-test QA/readiness suite, the 391-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
244. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the Records and Care Pass route for the owner-preview loop. Records now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for Dog ID share/print actions, medication search clear and filter chips, Care Pass preview rows, saved report artifact resend/print actions, progress report period tabs, record delete, empty add, record type chips, attachment, and sheet cancel/save controls. Static readiness now protects those named Records style blocks so report, ID, and vault handoff actions cannot regress to cramped 26-48px controls during beta review. Local verification passed the red/green mobile readiness test after first failing on `shareInline`, the targeted 91-test QA/readiness suite, the 392-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
245. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening Avatar Studio for the owner-preview loop. Avatar Studio now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for creator tabs, gallery/take-photo/reset/save buttons, coat swatches, face-marking options, mood preview chips, and shared-constant-backed large template/accessory tiles. Static readiness now protects those named Avatar Studio style blocks so the scan-assisted creator cannot regress to cramped 36-42px controls during beta review while preserving the larger mockup-like art tiles. Local verification passed the red/green mobile readiness test after first failing on the 40px `tab`, the targeted 92-test QA/readiness suite, the 393-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
246. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening Adventure Mode for the owner-preview game loop. Adventure now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for the `Save Memory` primary action and `Share Adventure` secondary action. Static readiness protects `primaryBtn` and `secondaryBtn` so the care-RPG memory layer cannot regress to local 47px controls during beta review. Local verification passed the red/green mobile readiness test after first failing on `primaryBtn`, the targeted 93-test QA/readiness suite, the 394-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript is currently dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found). Expo web export smoke did not complete in this Windows sandbox: the first attempt lacked `pnpm`, and the network-enabled bundled-pnpm attempt reached the registry but failed before export because the root preinstall script calls `sh`, which is unavailable in this shell.
247. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening Phoenix Home's owner-preview first-screen actions. Home now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for the header button, Avatar Studio hero action, presence panel, and Adventure inline action. Static readiness protects `headerButton`, `heroStudioButton`, `presencePanel`, and `adventureInline` so the beta's first impression cannot regress to cramped route-local controls. Local verification passed the red/green mobile readiness test after first failing on the 42px `headerButton`, the targeted 94-test QA/readiness suite, the 395-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found), and Expo web export still needs a shell-compatible environment with `sh` or a preinstalled dependency layer.
248. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening WoofGuide's prompt, send, and owner-review actions. WoofGuide now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for quick question chips, suggested action rows, the composer send button, and the owner-review Cancel/Apply draft controls. Static readiness protects `quickChip`, `actionRow`, `sendBtn`, `reviewCancel`, and `reviewApply` so the care assistant cannot regress to cramped prompt or review actions during beta review. Local verification passed the red/green mobile readiness test after first failing on `quickChip`, the targeted 95-test QA/readiness suite, the 396-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found), and Expo web export still needs a shell-compatible environment with `sh` or a preinstalled dependency layer.
249. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by adding a one-tap 48-hour beta handoff packet. `betaHandoffPacket.ts` now combines the truthful release packet with the live native QA capture plan, including beta verdict, public-launch verdict, QA progress, next device mission, missing proof, setup/device steps, pass criteria, Needs tune copy, Owner route loop run order, Pass pending proof instruction, and public/provider/AI truth boundaries. More's Launch Readiness beta card now exposes a phone-sized `Share Beta Handoff` action backed by that packet, while preserving `Open QA Cockpit` as the primary action when device proof is still required. Local verification passed the red/green helper/readiness tests, the targeted 97-test beta QA/readiness suite, the 398-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found), and Expo web export still needs a shell-compatible environment with `sh` or a preinstalled dependency layer.
250. PARTIAL 2026-06-25: Real iOS/Android capture remains the next external gate, so the unavailable-device fallback continued by adding a focused Needs Tune fix brief to the native QA flow. `mobileLaunchQaEvidence.ts` now carries `firstNeedsTuneTarget` even when that target is outside the visible next-four capture list and can build a `WoofWatcher Needs Tune Fix Brief` with route, QA note, proof gaps, setup/repro steps, route loop, done condition, and return-to-`/care-twin-qa` instructions. More's Native QA Next Captures panel now shows a phone-sized `Share Fix Brief` action only when a Needs tune target exists, and static readiness protects the import, accessibility label, button copy, and shared 48px touch-target style. Local verification passed the red/green helper/readiness tests after first failing on the missing helper and More wiring, then passed the 100-test targeted beta QA/readiness suite, the 401-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell by missing Expo/mobile dependencies and `expo/tsconfig.base`.
251. CARRIED 2026-06-25: The actual owner-preview native capture run from `/care-twin-qa` remains the next external gate. Use the 48-hour beta run card, read the `Owner route loop`, complete Home, Log, Plans, Health, More, Records, Avatar Studio, Care Pass, and Adventure in order, choose the correct iOS or Android evidence tag, attach an iOS Quick Log/Log screenshot and an Android Launch Readiness screenshot through the top-card `Attach proof` control, write the required `Mission note`, confirm `Pass pending proof` clears in both `/care-twin-qa` and More's Native QA Next Captures only after required proof is attached, mark Pass or Needs tune, share the QA summary, tap More's `Share Beta Handoff` action, and if any route is marked Needs tune tap More's new `Share Fix Brief` action before fixing the first Needs tune route. On Home, specifically confirm the menu/header action, Avatar Studio hero entry, presence panel, and Adventure inline action feel phone-sized and lead somewhere useful. On WoofGuide, confirm quick questions, suggested actions, send, and owner-review draft controls are reachable and phone-sized.
252. DONE 2026-06-25: Removed a real two-day beta install/export friction point by replacing the root Unix-shell `preinstall` guard with `scripts/enforce-pnpm-install.mjs`. The guard still removes forbidden `package-lock.json` and `yarn.lock` files and still rejects npm/yarn installs, but it now runs through Node instead of `sh -c`, so Windows, Replit, Git Bash, WSL, CI, and bundled Node environments are no longer blocked by a missing Unix shell before Expo export can start. Static mobile readiness now protects the root `preinstall` command, the Node guard source, pnpm user-agent enforcement, and lockfile cleanup. Red/green verification passed after first failing on the missing guard script; direct guard verification passed with a `pnpm/9.0.0` user agent and failed as expected with `npm/10.0.0`. Follow-up verification passed the 101-test targeted beta QA/readiness suite, the 402-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Local `pnpm`, Expo deps, and native device tools remain unavailable in this shell.
253. DONE 2026-06-25: Tightened the Expo mobile app config for the two-day beta export path. `artifacts/woofwatcher-mobile/app.json` now declares the intended `ios`, `android`, and `web` platforms and sets `expo.web.bundler` to `metro`, matching the existing smoke export script's Metro-based Expo web export expectation. Static mobile readiness now protects both settings inside the CI export smoke test so the app cannot regress to an Expo config that hides the web export surface. Red/green verification first failed on the missing app config assertions, then passed after the config was wired. A direct package-local Expo CLI export attempt advanced past the previous `No platforms are configured to use the Metro bundler` error and now stops at the truthful dependency-layer blocker: `Cannot determine the project's Expo SDK version because the module 'expo' is not installed`.
254. DONE 2026-06-25: Added a root `doctor:mobile-beta` command for the two-day ship handoff. `scripts/mobile-beta-doctor.mjs` checks pnpm availability, the Windows-friendly install guard, the mobile `smoke:web` command, Expo iOS/Android/web platform config, Metro web export config, mobile Expo dependency resolution, PixelLab verifier presence, and the required `/care-twin-qa` owner-preview proof steps. The doctor exits blocked in this cleaned Windows shell with exactly the current actionable issues: missing `pnpm` and missing mobile `expo` dependency resolution. Static mobile readiness protects the command and the owner-proof language so Replit, a device helper, or Apollo can run one command before export instead of reading the whole release plan.
255. DONE 2026-06-25: Pinned the root package manager to the same pnpm version used by GitHub Actions. `package.json` now declares `packageManager: pnpm@10.24.0`, the mobile beta doctor checks that pin against `.github/workflows/verify.yml`, and static readiness protects the pin, workflow version, and doctor check. Red/green verification first failed on the missing package manager pin, then passed after the root package and doctor were aligned. The doctor now passes the package-manager gate while still truthfully blocking this cleaned Windows shell on missing local `pnpm` and missing mobile `expo` dependency resolution.
256. CARRIED 2026-06-25: Under the two-day deadline, restore or run a dependency-complete mobile environment now that the root preinstall guard, package manager pin, Expo platform config, and doctor handoff are fixed, then execute real owner-preview iOS/Android capture from `/care-twin-qa`. If local Windows still lacks pnpm or Expo dependencies, use Replit, Git Bash/WSL with pnpm 10.24.0 installed or Corepack-enabled, CI after billing is fixed, or a device/helper environment. Do not claim internal beta proof until focused behavior/readiness tests, PixelLab verification, `git diff --check`, dependency/export proof, and at least the required iOS/Android owner-preview screenshots plus Mission note are current.
257. DONE 2026-06-25: Made the mobile beta doctor more actionable for the dependency/export gate. `scripts/mobile-beta-doctor.mjs` now checks Corepack availability as a warning-level bootstrap helper, tells helpers to run `corepack prepare pnpm@10.24.0 --activate` when pnpm is missing, and keeps the missing pnpm plus missing mobile `expo` dependency as the two true export blockers. Static mobile readiness now protects the Corepack and exact pnpm activation guidance. Red/green verification first failed on missing Corepack guidance, then passed after the doctor was updated; the direct doctor run now reports one Corepack warning plus the same two actionable blockers.
258. DONE 2026-06-25: Hardened the native beta environment doctor so helpers cannot accidentally export from the wrong runtime or incomplete EAS setup. `scripts/mobile-beta-doctor.mjs` now blocks below Node 24 and verifies `artifacts/woofwatcher-mobile/eas.json` has both iOS and Android build profiles for preview and production. Static mobile readiness first failed on the missing `Node 24 runtime` and `EAS build profiles include iOS and Android` doctor contracts, then passed after the checks were added. Direct doctor output now passes Node 24, package-manager alignment, Expo platform config, Metro web config, and EAS native profile coverage while still truthfully blocking this cleaned Windows shell on missing `pnpm` and missing mobile `expo` dependency resolution. Follow-up verification passed the 102-test targeted beta QA/readiness suite, the 403-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
259. DONE 2026-06-25: Closed another handoff trap by making the mobile beta doctor enforce the actual pnpm CLI version, not just the root package-manager pin. `scripts/mobile-beta-doctor.mjs` now defines `expectedPnpmVersion: 10.24.0`, derives `expectedPackageManager` from it, and when a `pnpm` command is present blocks if `pnpm --version` does not equal `10.24.0`. Static readiness first failed on the missing version enforcement, then passed after the doctor compared `pnpm.stdout.trim() === expectedPnpmVersion`. This matters because the Codex bundled runtime contains pnpm 11.7.0 as a library, which should not be treated as the beta export CLI. In this shell, no `pnpm` command is on PATH, so the doctor still reports the two true blockers: missing pnpm and missing mobile `expo` dependency resolution.
260. DONE 2026-06-26: Added machine-readable mobile beta doctor output so Replit, native helpers, or automation can parse the same export readiness signal without screen-scraping terminal text. `package.json` now exposes `doctor:mobile-beta:json`, `scripts/mobile-beta-doctor.mjs --json` emits `name`, `purpose`, `result`, `checks`, `issues`, `warnings`, and `nextActions`, and static mobile readiness executes the JSON command, parses the payload, and protects the current blocked state plus Node 24/EAS pass signals. Red/green readiness first failed on the missing root script, then passed with 81 tests after the script and JSON mode were wired. In this shell the JSON doctor correctly exits blocked on the two true export blockers: missing pnpm and missing mobile `expo` dependency resolution.
261. DONE 2026-06-26: Added the dependency/export proof runbook directly to the in-app 48-hour beta handoff packet. `betaHandoffPacket.ts` now includes `corepack prepare pnpm@10.24.0 --activate`, `pnpm install`, `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, and `pnpm --filter @workspace/woofwatcher-mobile run smoke:web`, plus truthful copy that dependency proof only counts when both doctor commands report no blockers. Red/green helper verification first failed on the missing `Dependency proof commands` section, then passed after the packet was updated.
262. DONE 2026-06-26: Made the mobile beta doctor JSON contract carry the exact dependency proof command sequence as structured `proofCommands`. `scripts/mobile-beta-doctor.mjs --json` now emits `proofCommands` beside `nextActions` for both `BLOCKED` and `READY_FOR_EXPORT` results, and the human doctor output prints the same dependency proof commands before the required device proof list. Red/green readiness first failed on missing `proofCommands`, then passed after the doctor emitted `corepack prepare pnpm@10.24.0 --activate`, `pnpm install`, `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, and `pnpm --filter @workspace/woofwatcher-mobile run smoke:web`.
263. DONE 2026-06-26: Hardened Provider Launch Setup with explicit proof-needed prompts so provider rows are actionable without claiming approval. `launchProviderSetup.ts` now gives every production gate a `proofRequired` checklist covering Clerk, Supabase/RLS, storage signed uploads, WoofGuide AI policy, Plus payments, push, Apple/Google store accounts, and account deletion evidence. The share packet now includes a `Proof Needed` section, and More renders `Proof needed:` under each provider row. Red/green verification first failed on missing proof requirements/share copy, then passed after the provider model, More UI, and static readiness contract were updated.
264. CARRIED 2026-06-26: Run the dependency-complete mobile beta proof path from Replit, Git Bash/WSL, or another environment with Node 24, pnpm 10.24.0 exactly, and Expo dependencies. First run `corepack prepare pnpm@10.24.0 --activate` if Corepack is available and pnpm is missing, then `pnpm install`, `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, `pnpm --filter @workspace/woofwatcher-mobile run smoke:web`, focused behavior/readiness tests, PixelLab verification, and the `/care-twin-qa` owner-preview iOS/Android screenshot plus Mission note loop. GitHub Actions remains infrastructure-blocked until billing/spending-limit is fixed.
265. DONE 2026-06-26: Merged provider proof into the one-tap 48-hour beta handoff so Apollo, Replit, Fable, or a native helper do not need to stitch together separate packets under the deadline. `buildBetaHandoffPacketShareText` now accepts a provider setup plan, preserves the old timestamp call form, and adds a `Provider proof needed` section with each production gate's evidence checklist plus explicit copy that proof collection does not approve store, payment, AI, storage, or database readiness. More now passes `launchProviderSetupPlan` into `Share Beta Handoff`, and static readiness protects that wiring. Red/green verification first failed because the options object was treated as `[object Object]`, then passed after the beta handoff options contract and More wiring were added.
266. DONE 2026-06-26: Made the mobile beta doctor expose the expected 48-hour handoff proof sections as a structured machine-readable contract. `scripts/mobile-beta-doctor.mjs --json` now emits `handoffProofSections` with `Dependency proof commands`, `Required beta proof after export`, `Provider proof needed`, and `Truth boundaries`, and the human doctor output prints the same checklist so Apollo, Replit, Fable, or a native helper can confirm the one-tap beta packet is complete before claiming proof. Static readiness now protects the JSON contract. Direct bundled-Node verification confirmed the doctor still truthfully reports `BLOCKED` on the two real local issues: missing pnpm and missing mobile `expo` dependency resolution.
267. DONE 2026-06-26: Hardened the mobile beta doctor from an expected-section checklist into a source-backed readiness check. `buildBetaHandoffPacketShareText` now includes an explicit `Required beta proof after export` section with `/care-twin-qa`, iOS/Android screenshot, Mission note, and Pass pending proof requirements. `scripts/mobile-beta-doctor.mjs` now reads the actual handoff packet source plus More route source and passes `beta handoff source includes proof sections` only when dependency proof, required beta proof, provider proof, truth boundaries, `Share Beta Handoff`, and `providerSetupPlan: launchProviderSetupPlan` wiring are all present. Red/green verification first failed on the missing doctor check, then passed the targeted 81-test mobile readiness suite, 2 beta handoff packet tests, and the 365-test zero-dependency behavior/readiness suite.
268. DONE 2026-06-26: Hardened the mobile beta doctor against the Codex bundled package-manager trap. `scripts/mobile-beta-doctor.mjs --json` now inspects the bundled runtime pnpm package metadata and emits a warning-level `unsupported bundled pnpm candidate` check when that candidate is not the required `pnpm 10.24.0`. Static mobile readiness now protects the warning so Replit, native helpers, or Apollo do not mistake bundled `pnpm 11.7.0` for valid beta export proof. Direct doctor output remains truthfully `BLOCKED` on the two real local issues: missing PATH pnpm and missing mobile `expo` dependency resolution.
269. DONE 2026-06-26: Added an owner-preview beta proof verdict to the native QA handoff model so the two-day owner loop cannot disappear behind the top-four next captures. `buildMobileLaunchQaCapturePlan` now always returns `ownerPreviewProofStatus` for `Owner Preview Core Loop`, including status label, completion, missing proof, evidence count, and note. The shareable Native QA capture plan and the 48-hour Beta Handoff packet now print `Owner preview proof` and `Owner preview missing`, and More's Native QA panel shows an `Owner preview proof` row plus `Finish Proof` when the saved owner loop is Pass but still missing the Mission note. Red/green verification first failed on the missing shared status and handoff/More wiring, then passed targeted QA evidence, beta handoff, and mobile readiness tests plus the broad zero-dependency suite.
270. DONE 2026-06-26: Made the mobile beta doctor source-validate the owner-preview proof chain. `scripts/mobile-beta-doctor.mjs --json` now emits `owner preview proof wiring is source-backed` only when `mobileLaunchQaEvidence.ts` still derives `ownerPreviewProofStatus` from `OWNER_PREVIEW_CORE_LOOP_ID`, `betaHandoffPacket.ts` still prints `Owner preview proof` and `Owner preview missing`, and More still surfaces `Owner preview proof` with the `Finish Proof` recovery CTA. Red/green verification first failed on the missing doctor check, then passed after the doctor read the actual QA plan, beta handoff packet, and More route source. This keeps Replit, Fable, Apollo, or a native helper from losing the highest-priority owner-preview beta proof row while the dependency/export gate is still external.
271. DONE 2026-06-26: Made the mobile beta doctor source-validate the `/care-twin-qa` proof flow itself. `scripts/mobile-beta-doctor.mjs --json` now emits `care-twin QA route proof flow is source-backed` only when the actual route still carries Mission note, Pass pending proof, attach-proof, stage test IDs, and QA-return wiring, and when the release QA matrix still declares `Owner Preview Core Loop`, the iOS Quick Log/Log screenshot proof, the Android Launch Readiness screenshot proof, and `/care-twin-qa` as the route. Red/green verification first failed on the missing doctor check, then passed after the doctor read the actual route and QA matrix sources. This protects the real device mission Apollo or a helper will run under the two-day deadline.
272. DONE 2026-06-26: Made the mobile beta doctor source-validate the Native QA Needs tune recovery loop. `scripts/mobile-beta-doctor.mjs --json` now emits `native QA Needs tune fix brief is source-backed` only when `mobileLaunchQaEvidence.ts` still builds a `WoofWatcher Needs Tune Fix Brief`, tracks `firstNeedsTuneTarget`, includes the truthful no-needs-tune fallback, and tells the helper to return to `/care-twin-qa` after the fix, while More still imports the builder, detects `nativeQaCaptureNeedsTuneTarget`, and exposes the `Share Fix Brief` action with an accessibility label. Red/green verification first failed on the missing doctor check, then passed after the doctor read the actual evidence builder and More route source. This keeps a concrete repair handoff ready if the iOS/Android owner-preview run finds the first below-beta route.
273. DONE 2026-06-26: Promoted the Native QA Needs tune recovery loop into the required 48-hour beta handoff contract. `buildBetaHandoffPacketShareText` now prints a dedicated `Native QA Needs tune fix brief` section that tells helpers to use More's `Share Fix Brief` before claiming beta proof when any route is marked Needs tune, then fix the first below-beta route, return to `/care-twin-qa`, attach confirmation proof, and update the Mission note. `scripts/mobile-beta-doctor.mjs --json` now lists that same section in `handoffProofSections` and source-validates it with the other required handoff sections. Red/green verification first failed on the missing handoff section and JSON section, then passed after the packet and doctor were aligned.
274. NEXT: Run the dependency-complete mobile beta proof path from Replit, Git Bash/WSL, or another environment with Node 24, pnpm 10.24.0 exactly, and Expo dependencies, then complete the `/care-twin-qa` owner-preview iOS/Android screenshot plus Mission note loop. If that remains unavailable, continue only with launch-risk-reducing fallback work that improves the same handoff, native QA, provider-proof, or runtime-readiness surfaces without claiming beta proof.
275. DONE 2026-06-26: Fixed a core routines/logs meal-lifecycle formula so a meal that was only served no longer gets treated as fully resolved across shared care outputs. `deriveCareDayStatus` now reports meal `pending` outcomes separately from resolved meal count for `served`, `outcome-pending`, and `grazing` states; skipped/refused/resolved outcomes still close the meal record. Today Command now prioritizes an `update-meal-outcome` action for the oldest household-visible pending meal before moving on to walks or other routines, and caregiver handoff now says meals are resolved versus outcome pending instead of generic "logged" copy. Red/green verification first failed on the old done-count, Home command, and handoff copy, then passed the targeted care-domain/handoff/Today Command tests, the full zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check`.
276. DONE 2026-06-26: Aligned Routine Board with the pending-meal lifecycle so Plans/assigned routines no longer mark served-only meals as completed. `deriveRoutineBoard` now supports a `pending` routine status and `pending` completion label of `Outcome pending`; pending meal routines remain open, count against `openCount`, preserve the caregiver/timestamp that served the meal, and become the board's next item until someone records ate all/most/some, refused, skipped, or another resolved outcome. Red/green verification first failed because the routine board counted a served breakfast and walk as 2 done routines, then passed the routine-board test plus dependent Today Command, Care Intelligence, and Access Pass tests.
277. DONE 2026-06-26: Made pending meal outcome edits first-class from Log detail/Timeline/Recent Activity instead of leaving the completion formula buried in the screen. `mealOutcomeUpdate.ts` now owns the shared update contract for `Ate all`, `Ate most`, `Ate some`, `Refused`, and `Still grazing`, preserving household/routine context, updating eaten amount and unit, keeping grazing meals open, setting refused/partial/grazing watch severity, confirming the trust state, and appending correction audit history. Log's detail sheet now uses that helper and inherits the complete outcome set, so a served dinner can be resolved accurately without forcing the owner into the wrong bucket. Red/green verification first failed on the missing helper and missing `Ate some` contract, then passed the focused meal-outcome tests, Log-adjacent trust/potty tests, the 81-test mobile readiness suite, the 375-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only the expected Windows line-ending warning.
278. DONE 2026-06-26: Hardened downstream diet/report math so served-only meals no longer inflate eaten-food progress. `deriveDietProgress` now treats `served`, `grazing`, and `outcome-pending` meals as pending instead of eaten, counts pending outcomes separately, estimates partial intake only when no exact eaten amount exists, labels estimated partial amounts, and keeps skipped/refused meals at zero intake. Care Pass Diet sections now include the same Daily food summary plus a Meal amount note when outcomes are pending or estimated, so sitter/vet exports do not present a bowl on the floor as food eaten. Red/green verification first failed on the missing pending/estimated diet contract and missing Care Pass Diet lines, then passed the focused diet/Care Pass tests, 111 targeted mobile/domain tests, the 377-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
279. DONE 2026-06-26: Hardened WoofGuide and Home command copy around pending meals so assistant help cannot invent a completed meal. WoofGuide's owner-reviewed missing-meal draft now creates a `served` meal with `mealLifecycle: outcome-pending` and `requiresOutcomeUpdate: true` instead of marking the meal complete. Today Command's latest handoff now says a pending meal was served with outcome pending rather than generically logged. Red/green verification first failed on the old completed/logged wording, then passed the targeted WoofGuide/Today Command tests.
280. DONE 2026-06-26: Added shareable meal follow-up rows to Care Pass reports so sitter/vet handoffs call out unresolved, estimated, and corrected meal evidence directly. Care Pass now emits a `Meal Follow-ups` section for household-visible pending meal outcomes, partial meals with estimated eaten amounts, and corrected meal logs with audit history, so exported reports show what still needs owner review instead of hiding it in totals. Red/green verification first failed on the missing section, then passed the Care Pass report tests.
281. DONE 2026-06-26: Hardened Weekly Care Trends and Records/Care Pass trend copy around open meal loops. `deriveCareTrends` now separates pending meal outcomes from complete, partial, and skipped meals for `served`, `outcome-pending`, and `grazing` states; trend summaries, highlights, and Meal Watch signals now say how many outcomes are pending and tell the household to update served meal outcomes before treating the week as resolved. Care Pass trend sections include pending meal outcome counts, and Records' Care Trends card now shows a `Meal open` metric backed by `careTrends.current.meals.pending`. Red/green verification first failed on missing pending trend/report UI contracts, then passed the focused Care Trends/Care Pass tests and the mobile readiness suite.
282. PARTIAL 2026-06-26: Remote verification for pending-meal care-trend commit `dd64c6d` was triggered as GitHub Actions run `28234625027`, but it failed before job execution with job `83646365355`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83646365355`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed focused Care Trends/Care Pass tests, mobile readiness, the 418-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
283. DONE 2026-06-26: Polished Records report history so saved Care Pass artifacts show truthful local/provider storage state instead of implying cloud-backed PDF readiness. `CarePassArtifact` now defaults to `storageStatus: "local-only"`, `describeCarePassArtifactStorage` gives owner-readable labels/details for local-only, upload-ready, uploaded, and failed states, and Records renders the storage label plus `Cloud storage pending` detail beside each saved report's resend/print-source actions. Red/green verification first failed on missing domain helper/UI wiring, then passed focused Care Pass tests, mobile readiness, the 419-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
284. PARTIAL 2026-06-26: Remote verification for Care Pass report storage truth commit `86a5545` was triggered as GitHub Actions run `28235628241`, but it failed before job execution with job `83649703875`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83649703875`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed focused Care Pass tests, mobile readiness, the 419-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
285. DONE 2026-06-26: Connected Care Pass report-history storage truth to Provider Launch Setup readiness without claiming cloud upload. `describeCarePassArtifactStorage` now accepts a provider setup option and turns local Care Pass print sources into `upload-ready` only when storage provider rules are configured, while keeping `providerBacked` false until a real provider upload exists. Records now passes `state.launchProviderProfile.storageProviderConfigured` into the shared helper so Report History can show `Ready to upload` when provider storage is prepared and `Saved on this device` otherwise. Red/green verification first failed on the missing domain option and missing Records wiring, then passed focused Care Pass tests, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
286. PARTIAL 2026-06-26: Remote verification for provider-aware Care Pass storage commit `2636b04` was triggered as GitHub Actions run `28236929754`, but it failed before job execution with job `83653949111`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83653949111`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed focused Care Pass tests, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
287. DONE 2026-06-26: Hardened the mobile beta doctor so provider-aware Care Pass report storage cannot regress silently during Replit/Fable/native handoff. `scripts/mobile-beta-doctor.mjs --json` now emits `provider-aware Care Pass storage is source-backed` only when the shared care-domain helper still supports storage-provider setup, keeps provider-backed false for local print sources, and Records still passes `state.launchProviderProfile.storageProviderConfigured` into `describeCarePassArtifactStorage`. Red/green verification first failed on the missing doctor label, then passed mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
288. PARTIAL 2026-06-26: Remote verification for beta doctor provider-storage guard commit `64c4968` was triggered as GitHub Actions run `28237677413`, but it failed before job execution with job `83656402075`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83656402075`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
289. DONE 2026-06-26: Tightened the owner-preview native QA route loop so Care Pass report storage truth is visually checked during the two-day beta pass. The `Owner Preview Core Loop` surface now tells testers to confirm Records/Care Pass Report History shows `Saved on this device` or `Ready to upload` without implying provider-backed upload, adds that boundary to pass criteria, and requires a QA note confirming the storage status stayed truthful instead of adding another screenshot slot. Red/green verification first failed on the missing Report History storage-status wording, then passed release QA tests, native capture-plan tests, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
290. PARTIAL 2026-06-26: Remote verification for owner-preview Care Pass storage-proof commit `35b4735` was triggered as GitHub Actions run `28238656796`, but it failed before job execution with job `83659654553`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83659654553`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed release QA tests, native capture-plan tests, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
291. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so local fallback work continued only where it improved device QA, provider proof, runtime readiness, launch handoff truth, or Care Pass route polish without claiming beta proof.
292. DONE 2026-06-26: Made the mobile beta doctor source-validate the Owner Preview Care Pass storage proof chain so the new two-day route-loop requirement cannot disappear during Replit/Fable/native handoff. `scripts/mobile-beta-doctor.mjs --json` now emits `owner-preview Care Pass storage proof is source-backed` only when the release QA matrix still includes the Care Pass Report History storage-status proof, the native QA capture share text still carries route-check proof lines, and `/care-twin-qa` still renders `Proof: {routeCheck.proof}` in the Owner route loop. Red/green verification first failed on the missing doctor check, then passed the focused mobile readiness test, release QA/native capture readiness tests, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings. Direct JSON doctor output remains truthfully `BLOCKED` on missing pnpm and missing mobile Expo dependency resolution.
293. PARTIAL 2026-06-26: Remote verification for owner-preview storage-proof doctor guard commit `2f23753` was triggered as GitHub Actions run `28240015482`, but it failed before job execution with job `83664251661`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83664251661`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, release QA/native capture readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check`.
294. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so the next fallback hardened the one-tap beta handoff packet around Owner Preview Care Pass storage proof without claiming beta proof.
295. DONE 2026-06-26: Made the one-tap 48-hour Beta Handoff packet explicitly call out Care Pass Report History storage-status proof in the `Required beta proof after export` section. `buildBetaHandoffPacketShareText` now tells helpers to confirm Report History says `Saved on this device` or `Ready to upload`, and the mobile beta doctor source-backed Owner Preview storage-proof guard now checks the beta handoff packet for the same line before reporting `owner-preview Care Pass storage proof is source-backed`. Red/green verification first failed on the missing handoff line, then passed the beta handoff packet test, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and the JSON doctor remained truthfully `BLOCKED` on missing pnpm and missing mobile Expo dependency resolution.
296. PARTIAL 2026-06-26: Remote verification for beta handoff Care Pass storage-proof commit `941d4f0` was triggered as GitHub Actions run `28256289432`, but it failed before job execution with job `83720072278`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83720072278`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed the beta handoff packet test, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
297. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so the next fallback hardened the 48-hour handoff packet against the bundled-pnpm false-positive that the doctor already detects.
298. DONE 2026-06-26: Made the 48-hour Beta Handoff packet explicitly warn helpers that dependency proof requires a real PATH `pnpm` at `10.24.0` and must not use a bundled `pnpm 11.x` candidate. The mobile beta doctor source-backed handoff check now requires that same line before `beta handoff source includes proof sections` can pass, keeping the human packet aligned with the machine-readable unsupported-bundled-pnpm warning. Red/green verification first failed on the missing handoff warning, then passed the beta handoff packet test, mobile readiness, JSON doctor source check, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
299. PARTIAL 2026-06-26: Remote verification for bundled-pnpm handoff guard commit `9e74abc` was triggered as GitHub Actions run `28257100651`, but it failed before job execution with job `83722840126`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83722840126`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed the beta handoff packet test, mobile readiness, direct JSON doctor source check, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
300. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate. Because this shell still lacks exact `pnpm@10.24.0` on PATH and mobile Expo dependency resolution, fallback work continued only where it strengthened final Records/Care Pass route polish and export truth without claiming beta proof.
301. DONE 2026-06-26: Added a Care Pass export manifest for saved report artifacts so Records can tell owners exactly what exists now and what does not. `describeCarePassArtifactExport` now returns the printable HTML filename, MIME type, format label, byte size, source status, storage view, provider-backed truth flag, and explicit `PDF not generated` boundary. Records Report History now renders `Printable HTML`, calculated KB, `PDF pending`, the provider/local storage pill, and the PDF/native-provider detail copy while still using the existing resend and printable-source actions. The mobile beta doctor now source-validates the new export-helper route instead of the older direct storage-helper call. Red/green verification first failed on the missing domain export helper and missing Records export manifest wiring, then passed Care Pass tests, mobile readiness, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, direct JSON doctor source checks, and `git diff --check` with expected Windows line-ending warnings only. The JSON doctor still truthfully exits `BLOCKED` on missing local pnpm and missing mobile Expo dependency resolution.
302. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
303. PARTIAL 2026-06-26: Remote verification for Care Pass export manifest commit `82850d8` was triggered as GitHub Actions run `28258738261`, but it failed before job execution with job `83728433081`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83728433081`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed Care Pass tests, mobile readiness, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, direct JSON doctor source checks, and `git diff --check` with expected Windows line-ending warnings only.
304. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate. Because the local environment still lacks exact `pnpm@10.24.0` and mobile Expo dependency resolution, fallback work continued only where it strengthened one-tap launch handoff truth for the same Records/Care Pass export proof.
305. DONE 2026-06-26: Added the Care Pass export manifest proof to the one-tap 48-hour Beta Handoff packet. `buildBetaHandoffPacketShareText` now tells helpers to confirm Report History shows `Printable HTML`, file size, and `PDF pending` before anyone claims PDF readiness, and the mobile beta doctor source-backed handoff/storage guards require that same line before reporting the packet/source chain as protected. Red/green verification first failed on the missing handoff proof line, then passed the beta handoff packet test, mobile readiness, direct JSON doctor source checks, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. The JSON doctor still truthfully exits `BLOCKED` on missing local pnpm and missing mobile Expo dependency resolution.
306. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
307. PARTIAL 2026-06-26: Remote verification for beta handoff export-manifest proof commit `79f9489` was triggered as GitHub Actions run `28259505376`, but it failed before job execution with job `83731021379`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83731021379`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed the beta handoff packet test, mobile readiness, direct JSON doctor source checks, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
308. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so fallback work continued only where it made the machine-readable beta doctor stricter about the same Records/Care Pass export truth.
309. DONE 2026-06-26: Added the Care Pass export-manifest proof to the mobile beta doctor's machine-readable `nextActions`. `scripts/mobile-beta-doctor.mjs --json` now tells Replit, Fable, Apollo, or a device helper to verify Records/Care Pass Report History shows `Printable HTML`, file size, and `PDF pending` before claiming PDF readiness. Static readiness now protects that `nextActions` contract so the human Beta Handoff packet and JSON doctor cannot drift apart. Red/green verification first failed on the missing JSON next action, then passed after the doctor line was added. Follow-up verification passed 81-test mobile readiness, the direct JSON doctor source check while still truthfully `BLOCKED` on missing pnpm and missing Expo dependency resolution, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
310. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
311. PARTIAL 2026-06-26: Remote verification for beta doctor export-manifest next-action commit `5c1f9fd` was triggered as GitHub Actions run `28260679123`, but it failed before job execution with job `83735044753`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83735044753`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, the direct JSON doctor source check, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
312. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so fallback work continued by making the machine-readable beta doctor harder to misread as public launch or provider approval.
313. DONE 2026-06-26: Added explicit `truthBoundaries` to `scripts/mobile-beta-doctor.mjs --json` and the human doctor output. The doctor now says `READY_FOR_EXPORT` only covers dependency install and web export gates, does not approve App Store, Play Store, native device QA, provider sync, storage, AI, payments, legal/privacy/support, or Apollo launch sign-off, and that `BLOCKED` means nobody should claim beta export readiness until listed issues and proof commands pass. Static readiness protects those boundaries in the JSON payload. Red/green verification first failed on the missing `truthBoundaries` payload, then passed after the doctor output was wired. Follow-up verification passed 81-test mobile readiness, direct JSON doctor output, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
314. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
315. PARTIAL 2026-06-26: Remote verification for beta doctor truth-boundary commit `9078035` was triggered as GitHub Actions run `28261499997`, but it failed before job execution with job `83737811594`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83737811594`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, direct JSON doctor output, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
316. DONE 2026-06-26: Made the mobile beta doctor source-validate that the one-tap Beta Handoff packet still carries launch truth boundaries before helpers treat the doctor as proof. `scripts/mobile-beta-doctor.mjs --json` now emits `beta handoff truth boundaries are source-backed` only when `betaHandoffPacket.ts` still says no App Store/Play Store submission is approved by the packet, provider-backed auth/database/storage/AI/push/payments remain gated, WoofGuide stays non-diagnostic and owner-reviewed, public launch remains separate from local beta evidence, and the JSON `truthBoundaries` still distinguish `READY_FOR_EXPORT`, public-launch approval, and `BLOCKED`. Static readiness protects the new check. Local verification passed 81-test mobile readiness, direct JSON doctor output, the 382-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. The doctor still truthfully reports `BLOCKED` on missing pnpm and missing mobile Expo dependency resolution.
317. PARTIAL 2026-06-26: Remote verification for beta handoff truth-boundary source guard commit `93dd154` was triggered as GitHub Actions run `28263774240`, but it failed before job execution with job `83745430354` and GitHub's annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 83745430354`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed 81-test mobile readiness, direct JSON doctor output, the 382-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
318. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.

319. DONE 2026-06-26: Unblocked the local Expo web preview for Apollo review. `metro.config.js` now aliases React, Expo, Expo Router, React Navigation, safe-area, gesture-handler, and screens package roots and subpaths through single runtime instances, fixing the Expo Router `No filename found` crash caused by duplicate module roots in the junctioned Windows worktree. Root route registrations are restored in the mobile `_layout.tsx`, public error fallbacks no longer leak stack details, and the in-app browser renders the real exported app at `http://127.0.0.1:4194/` instead of the stale fallback shell. Local verification passed web export smoke, mobile readiness, the 382-test behavior/readiness suite, PixelLab verification at 149 files, JSON beta doctor `READY_FOR_EXPORT`, browser DOM smoke, and `git diff --check` with expected Windows line-ending warnings only.
320. NEXT: Execute real native iOS and Android device/simulator QA from `/care-twin-qa` and the owner route loop. Capture Phoenix Home, Quick Log, Plans, Health/Bile, Household Pulse, Care Pass/Records, Avatar Studio, Adventure, and More/Launch Readiness screenshots with pass/needs-tune notes before claiming native beta proof.
321. NEXT: Continue final mockup-accuracy polish only where it raises launch confidence: hard-pixel Phoenix stage, sprite-state motion, tap/long-press logging sheets, open-loop Home cards, readable desktop/sidebar grouping, mobile bottom-nav polish, and truthful provider/storage/AI/payment pending states.
322. DONE 2026-06-26: Tightened Quick Log launcher UX so casual care actions and detail-first safety actions no longer look identical. `describeQuickLogLauncherAction` now derives owner-readable launcher copy from the shared quick-log policy, safe actions render a `Tap log` chip, medication/vomit/incident-style detail-required actions render a `Details` chip, and screen-reader labels/hints now match the actual tap behavior. This keeps tap logging fast while making health, medication, and incident context visibly intentional before saving. Verification passed quick-log policy tests, 81-test mobile readiness, 383-test behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
323. NEXT: Keep native device proof as the external gate. If real iOS/Android simulator/device capture is still unavailable, continue fallback work only on launch-risk-reducing polish that improves the actual mobile routes: Quick Log bottom-sheet depth, Phoenix sprite/game feel, Care Twin QA proof clarity, provider proof truth, or mockup-accurate navigation without claiming native QA is complete.
324. DONE 2026-06-26: Improved Phoenix Home's game-feel reaction layer without adding a duplicate avatar. `careTwinReactionPolicy.ts` now maps real care logs to the main Phoenix sprite action, owner-readable reaction copy, and tone role: meal served keeps the outcome open, potty remains a parent bathroom attempt, water drives the drink loop, walks drive the walk loop, care wins can celebrate, and health/safety logs stay calm and review-oriented. Home quick logs and walk starts now call this shared policy instead of carrying per-tile sprite guesses, so the living room reaction is tied to care meaning and remains testable. Verification passed the new care-twin reaction policy tests, 81-test mobile readiness, the 386-test behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
325. NEXT: Keep native device proof as the external gate. If real iOS/Android simulator/device capture is still unavailable, continue fallback work only where it improves the actual owner-preview product: Quick Log bottom-sheet detail depth, Phoenix stage crop/motion polish, Care Twin QA proof clarity, mockup-accurate Home/Log/Health layouts, or provider-proof truth without claiming native QA is complete.
326. PARTIAL 2026-06-26: Remote verification for Phoenix Home reaction-policy commit `834d8df` was manually dispatched as GitHub Actions run `28272011652`, but it failed before job execution with job `83771107991`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83771107991`. This matches the standing GitHub billing/spending-limit pre-job blocker. Local verification passed the care-twin reaction policy tests, mobile readiness, the 386-test behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check`.
327. DONE 2026-06-27: Tightened Quick Log v2 detail-depth so launcher tiles now support the locked tap/long-press doctrine in the product vision. Long press opens a compact launcher detail sheet instead of jumping straight to the full composer; Meal explains usual-meal quick logging plus served -> outcome follow-up, Potty explains the bathroom-attempt parent/outcome model, Medication and health/safety logs route to full details first, and every sheet keeps the accessible 48px mobile touch-target contract. Local verification passed `quickLogEntry.test.ts` 10/10, `mobileReadiness.test.ts` 81/81, the 387-test behavior/readiness suite, PixelLab verification at 149 files, Expo web export to `.expo-smoke`, and browser DOM smoke at `http://127.0.0.1:4194/`.
328. CARRIED 2026-06-27: Native iOS/Android proof remains the external launch gate. Fallback work continued on the highest visible owner-preview launch risk: Phoenix Home first-screen framing and bottom-nav/mission clearance.
329. DONE 2026-06-27: Added a responsive Phoenix Home first-screen layout contract so the flagship Home screen can stay closer to Apollo's mockups across iPhone-class preview sizes. `homeFirstScreenLayout.ts` now owns Home density, hero aspect ratio, Care Twin button sizing, presence-card overlap, status tile sizing, and the estimated mission-deck peek above the floating paw nav. Home now consumes that layout instead of a hardcoded hero ratio, and `mobileReadiness.test.ts` protects the wiring so future design polish cannot accidentally drift away from the mockup-accurate first screen. Local verification passed focused Home/readiness tests, the 390-test behavior/readiness suite, TypeScript, PixelLab verification at 149 files, and Expo web export to `.expo-smoke`. The refreshed app is visible at `http://127.0.0.1:4194/`.
330. PARTIAL 2026-06-27: Remote verification for Phoenix Home first-screen layout commit `6f6b682` was manually dispatched as GitHub Actions run `28274880697`, but it failed before job execution with job `83779509676`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83779509676`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local test regression. Local verification passed focused Home/readiness tests, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, and `git diff --check`.
331. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the highest visible launch-risk surfaces: mockup-accurate Home/Log/Health spacing, Phoenix room HUD/sprite crop polish after real device review, detail sheet visual polish, Avatar Studio template scan truth, Care Pass/Records export clarity, and `/care-twin-qa` device proof collection. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion until those proofs exist.
332. DONE 2026-06-27: Polished Health Watch into a more reference-board-accurate Care Status screen instead of a generic health metrics stack. The Health tab now opens with a cream board-card console, pixel medallion, score track, segmented StatusMeter rows for appetite/hydration/energy/vomiting, a 7-day rhythm bar strip derived from recent care logs versus watch signals, and explicit `Health observations, not diagnosis` boundary copy. Mobile readiness now protects the Care Status console, StatusMeter usage, score track, rhythm bars, and non-diagnostic copy so future design passes cannot flatten the Health screen. Local verification passed the red/green Health readiness guard, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, local preview server `HEAD 200`, and browser console smoke on `http://127.0.0.1:4194/health` with no console errors.
333. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the next highest visible launch-risk surface: Quick Log bottom-sheet visual polish, Log/Timeline edit-detail scanability, Avatar Studio template scan truth, Phoenix room HUD/sprite crop polish after real device review, Care Pass/Records export clarity, or `/care-twin-qa` device proof collection. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion until those proofs exist.
334. PARTIAL 2026-06-27: Remote verification for Health Watch Care Status commit `0791747` was manually dispatched as GitHub Actions run `28282997631`, but it failed before job execution with job `83802116271`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83802116271`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local test regression. Local verification passed the red/green Health readiness guard, 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, browser console smoke, and `git diff --check`.
335. DONE 2026-06-27: Polished Quick Log's workflow literacy so the owner can immediately understand the locked interaction model without reading docs. The Log screen now shows a compact `Tap / Hold / Edit later` doctrine rail above the launcher grid, and each launcher detail sheet now renders the shared presentation model's interaction rail plus editable-Timeline reminder. The shared `describeQuickLogDetailSheet` model owns the rail and edit-later copy so meal, potty, medication, and health/safety logs stay consistent: safe routines can quick-log, safety-critical logs open details first, and every saved log remains updateable/correctable in Timeline. Red/green verification first failed on the missing rail/model fields, then passed focused Quick Log/mobile readiness, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, `/log` browser console smoke with no errors, and `git diff --check` with expected Windows CRLF warnings only.
336. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the next highest visible launch-risk surface: Log/Timeline edit-detail scanability, Avatar Studio template scan truth, Phoenix room HUD/sprite crop polish after real device review, Care Pass/Records export clarity, `/care-twin-qa` device proof collection, or provider-proof truth. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion until those proofs exist.

## Cadence

Recommended automation cadence from Apollo's shared thread: every 3 hours, using a dedicated Git worktree where the automation system supports it.

Operational manifest: `docs/operations/PREMIUM_REVENUE_PRODUCT_BUILDER.md`.

## Stop Conditions

Stop only for missing secrets, destructive data risk, app-store approval, legal/veterinary safety boundaries, production approval, or source-of-truth contradictions.
