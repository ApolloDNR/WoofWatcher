# Autonomous Build Queue

## Operating Rule

Every autonomous run should improve at least one of: care workflow, household usefulness, premium feel, production safety, monetization path, or App Store readiness.

## Highest Priority Queue

1. DONE 2026-06-08: Routines/logs relationship: logs satisfy/update matching routines.
2. DONE 2026-06-08: Meal logging: expected portion, served amount, eaten amount, skipped/partial completion, notes, and household visibility.
3. DONE 2026-06-08: Subscription path: pricing screen, premium preview, and plan packaging without live payments.
4. DONE 2026-06-08: Today Command uses routine-board truth for partial meals, overdue assigned routines, routes, urgency, and explanation text.
5. DONE 2026-06-08: Home Quick Log now creates routine-aware meal and walk logs with meal portion, completion, eaten amount, and household visibility detail.
6. DONE 2026-06-08: Health Watch now produces non-diagnostic pattern cards with evidence, owner next steps, and visible vet boundary language.
7. DONE 2026-06-08: Care Pass handoffs now include audience-specific checklists and Health Pattern Review next steps for sitter, caregiver, trainer, and vet exports.
8. DONE 2026-06-08: Records now derives and displays expired, due-soon, and missing-critical record reminders; real document storage remains blocked on provider rules.
9. DONE 2026-06-08: WoofGuide now creates owner-reviewed drafts for missing meal logs, record reminders, vet notes, and Care Pass review.
10. DONE 2026-06-08: Privacy/account safety first pass now includes owner data export, deletion request preparation, AI disclosure, and document/payment safety gates.
11. IN PROGRESS, CI Expo web export smoke and critical accessibility label smoke shipped 2026-06-08: Mobile QA now checks route registration, tabs, router links, launch-blocking safety copy, an Expo web export bundle, and screen-reader labels for critical Privacy, Premium, WoofGuide, and More actions. Next: simulator/device smoke, full accessibility pass, visual regression, App Store prep.
12. DONE 2026-06-08: Premium entitlement policy now defines Free, Plus, and Family feature gates in shared domain logic, keeps checkout disabled, and shows the policy on the mobile Plus screen before payments are enabled.
13. DONE 2026-06-08: Expo app identity now uses release-grade WoofWatcher slug/scheme plus Pegasus Dreamscapes iOS bundle and Android package ids instead of Replit placeholders.
14. IN PROGRESS, avatar motion foundation shipped 2026-06-08: Home now derives a deterministic avatar motion state from health signals, recent care logs, routine due/overdue state, quiet hours, and energy. Next: Rive/Lottie/Reanimated asset pipeline, Figma/code design system, full screen-by-screen visual polish, and visual regression.
15. Premium design system and motion pass: component tokens, high-end screen polish, avatar state assets, transition rules, and accessibility review.
16. DONE 2026-06-09: Care Pass report artifacts now include escaped print-ready HTML and stable file names for future PDF generation, native sharing, and server-backed storage.
17. DONE 2026-06-09: Records Report History now shows print-ready/restored Care Pass metadata, exposes separate resend and printable-source share actions, and keeps older saved report artifacts printable through escaped fallback HTML.
18. DONE 2026-06-09: Dog ID credentials now have escaped print-ready HTML, stable file names, and separate Records actions for sharing the normal ID card text or printable source.
19. DONE 2026-06-10: Medication routines now derive taken, due, missed, and upcoming adherence status from household-visible medication logs, including dose, owner, logged-by context, next action, and a mobile Records Medication Plan surface.
20. DONE 2026-06-10: Medication logging now has Home quick-log access, routine-aware dose/outcome/visibility defaults, a Log composer Medication routine panel, and skipped-medication handling that does not count as taken.
21. DONE 2026-06-11: Medication follow-ups now derive missed, due-now, and refill due-soon/overdue actions from routines, logs, and medication records; Records shows Medication Follow-ups and Care Pass report language includes adherence plus refill next steps.
22. DONE 2026-06-11: Medication history now derives recent household-visible medication logs with dose, taken/skipped/missed outcome, caregiver, routine id, note, and Records Medication History UI.
23. DONE 2026-06-11: Water care now has Home quick-log access, household-visible refill defaults, shared daily hydration summary logic, Records Hydration UI, and Care Pass report language.
24. DONE 2026-06-11: Walk activity now derives daily duration, places/routes, dog interactions, social outcome notes, caregiver participation, Records Walk Activity UI, and Care Pass report language from household-visible walk logs.
25. DONE 2026-06-11: Potty Health now derives daily pee/poop counts, stool review signals, conditions, latest detail, caregiver participation, Records Potty Health UI, and Care Pass report language from household-visible potty logs.
26. DONE 2026-06-11: Potty logging now captures stool color and routine/accident/urgent/straining context, and Potty Health carries those details into Records and Care Pass reports.
27. DONE 2026-06-11: Durable sync outbox now derives retryable create/update work from local, pending, and failed care entries, exposes it through CareContext, and shows a Log screen recovery banner with retry counts and a retry action.
28. DONE 2026-06-11: Household Sync Health now derives healthy/loading/syncing/attention dashboard state from the durable outbox, household size, and care history, and More shows the household sync status, retry/refresh action, care-log count, team count, outbox count, and next-step guidance.
29. DONE 2026-06-11: Care document refresh is now conflict-safer: mobile reconciles server care-state refreshes against the local doc timestamp, keeps newer offline/local profile, routine, record, and report changes instead of clobbering them with stale server data, and pushes the newer doc back to the household.
30. DONE 2026-06-11: Saved walk route templates now derive from household-visible route/place logs, ignoring private and stale walks, grouping repeat routes by name, and carrying visits, average duration, distance, dog interactions, caregivers, social outcomes, and suggested use into Records and Care Pass reports.
31. DONE 2026-06-11: Weekly Care Trends now derive from household-visible logs, compare the current 7-day window against the previous window, and carry meal completion, walk minutes, water refills, potty/medication/health watch signals, top caregivers, Records UI, and Care Pass report language.
32. DONE 2026-06-11: Training Progress now captures structured training outcomes, skill/cue, duration, next-practice notes, and household visibility in the Log composer, derives visible 30-day training progress in shared domain logic, and carries sessions, wins, skills, latest context, Records UI, and Care Pass report language into trainer-ready workflows.
33. DONE 2026-06-11: Household Responsibility Center now derives care-team routine ownership, open/overdue/unassigned counts, visible today log activity, and the next household action from routine-board truth, then surfaces it in Calendar and More.
34. DONE 2026-06-11: Care Log Audit Trail now records create/edit/sticky-note/delete evidence in shared care-domain logic, shows audit history in Log detail sheets, includes audit summaries in entry handoffs, and creates a non-health audit note only after deletes succeed.
35. DONE 2026-06-11: Alone Time now captures departure duration, return state, trigger/context, calming support, recovery minutes, sticky notes, and household visibility in the Log composer, derives 30-day separation/anxiety patterns in shared care-domain logic, and carries status, triggers, supports, latest context, Records UI, and Care Pass handoff language into household workflows.
36. DONE 2026-06-11: Weight Trend now derives visible recent weigh-ins, current weight, goal distance, previous-change, caregiver participation, Records chart inputs, and Care Pass report language from shared care-domain logic while preserving owner-reported/vet-review boundaries.
37. DONE 2026-06-11: Household Access now derives synced account members, local-only caregivers, routine-only owners, invite readiness, permission labels, and next-step guidance in shared care-domain logic, then surfaces that access plan in More without pretending provider-backed role enforcement is complete.
38. DONE 2026-06-11: Grooming Care now captures grooming duration, type, coat/skin notes, products/groomer context, next due date, sticky notes, and household visibility in the Log composer, derives shared grooming summaries in care-domain logic, and carries status, products, latest context, Records UI, and Care Pass language into sitter/vet handoffs.
39. DONE 2026-06-11: Reminder Center now derives one owner-visible action list from routine-board status, medication follow-ups, record reminders, and grooming due dates, then surfaces urgent/watch/total counts and notification-readiness copy in Calendar before provider-backed push delivery exists.
40. DONE 2026-06-11: Reminder Center rows now route to concrete workflows: routine reminders open the routine editor, medication routine reminders open the Medication log composer, record reminders open Records, and grooming reminders open the Grooming log composer.
41. DONE 2026-06-11: Full Log search now uses shared care-domain logic to search titles, notes, caregivers, event details, routes, medication fields, and sticky notes while combining text query with type filters, summary copy, and active-filter empty states.
42. DONE 2026-06-11: Medication History search and outcome filters now use shared care-domain logic to filter recent medication evidence by medicine, dose, caregiver, note, routine id, and taken/skipped/missed/needs-review outcome, then surface summary and empty-state copy in Records.
43. DONE 2026-06-12: v1.5 Premium Neo-Retro Pixel Care direction is mirrored into durable docs, CI was repaired, mobile nav is aligned, and the PWA v1.5 shell foundation now has grouped desktop navigation, five-tab mobile navigation, top-bar search/reminder/date/profile/theme controls, persisted light/dark theme, grouped Quick Log v2, Potty parent quick action, and meal/potty lifecycle field preservation.
44. DONE 2026-06-12: Phoenix Home v1.5 now includes a Phoenix Status card for where Phoenix is, whether she is alone, how she feels, what is next, and whether a served meal has an outcome pending. Open meal outcomes can be updated from Home with one tap.
45. DONE 2026-06-12: PWA Quick Log v2 now routes Meal and Potty quick actions into dedicated detail flows. Meal supports serving a pending meal with food, portion, caregiver, note, and later outcome update; Potty supports parent/action logging with location, pee/poop/both/tried/accident outcome, caregiver, and notes. Other quick actions remain one-tap.
46. DONE 2026-06-12: PWA Household Pulse is now a first-class route with manual Leaving Home and I'm Home flows. Leaving Home starts a household-visible open alone timer; I'm Home closes it with return outcome, duration, caregiver, recovery, and notes. More now includes a tool directory entry point back to Household Pulse.
47. DONE 2026-06-13: Superseded by item 55; Phoenix Home v1.5 polish now includes the household pulse mini-card, stronger Health/Bile snapshot, and richer pixel-room state copy.
48. DONE 2026-06-12: PWA Care Pass now exposes scoped Vet, Sitter, Trainer, and Emergency export cards, each backed by `buildScopedCarePass`, with copyable text and downloadable JSON source for Fable/Replit to style into premium report/PDF flows.
49. DONE 2026-06-12: Diet & Treats is now a first-class PWA route with daily meal target/progress, meals today, treats today, hydration context, avoid list, profile editing, and direct Log Meal/Log Treat/Edit Diet Profile actions.
50. DONE 2026-06-12: WoofGuide is now a first-class PWA route with owner-reviewed action cards that route to Meal Log, Care Pass review, Records review, and a bounded vet-note draft without pretending live AI is configured.
51. DONE 2026-06-12: PWA Timeline, Records, Reports, and Care Pass are now directly routable so desktop secondary navigation no longer collapses important tools into More.
52. DONE 2026-06-12: Avatar Studio is now a first-class PWA prototype route with local reference photo upload memory, selectable Phoenix template state, and the required Happy/Calm/Excited/Sleepy/Anxious/Bored/Hungry/Proud/Home Alone/Not Feeling Well state inventory.
53. DONE 2026-06-13: Achievements is now a first-class PWA route backed by `getAchievementReview`, deriving meaningful care milestones from household-visible logs, records, training consistency, calm alone-time outcomes, Bile Watch bedtime snack proof, and record readiness instead of fake coins.
54. DONE 2026-06-13: Settings is now a first-class PWA route that consolidates full backup, import, care-room transfer, reset, local-only provider readiness, AI boundary, health boundary, reminder delivery truth, and sync resource blockers in one system surface.
55. DONE 2026-06-13: Phoenix Home v1.5 polish now adds generated pixel-room state copy, a first-screen "Where Phoenix is" household pulse card, and a combined Health/Bile snapshot with food-gap, bedtime snack, vomiting, energy, and weight context.
56. NEXT: Runtime/browser visual QA once local dependencies or a package manager are available in the checkout.
57. DONE 2026-06-13: Fable/Replit handoff notes are now captured in `docs/design/FABLE_HANDOFF_2026-06-13.md`, including route inventory, preserved workflows, visual priorities, motion direction, and runtime QA limits.
58. NEXT: Provider-backed account/cloud/payment decision pack after Apollo chooses the release stack.
59. DONE 2026-06-13: Mobile-first release readiness now has committed Expo/EAS iOS and Android build profiles in `artifacts/woofwatcher-mobile/eas.json`, a mobile release runbook in `docs/release/MOBILE_RELEASE_RUNBOOK.md`, and static readiness tests that protect both.
60. NEXT: Native iOS/Android runtime QA with real Expo/EAS account access, simulator/device screenshots, safe-area checks, and Fable-polished assets.
61. DONE 2026-06-14: Apollo's four pixel UI reference boards are mirrored in `docs/design/reference/` and locked in `docs/superpowers/specs/2026-06-14-woofwatcher-pixel-ui-lock-design.md` as the visual source of truth for the next UI implementation pass.
62. DONE 2026-06-14: The Expo mobile app now has a board-accurate pixel UI foundation: locked palette tokens, reusable board primitives, navy bottom navigation, and a rebuilt Phoenix Home using pixel room, speech bubble, presence, segmented meters, quick actions, today summary, recent activity, and health watch cards.
63. DONE 2026-06-14: The Expo mobile board primitive system now reaches the core v1.5 routes beyond Home: Log, Plans, Health/Bile, More, Records, WoofGuide, and Avatar Studio all use shared board route chrome or board sections, and Health/Bile Watch uses shared board cards, pills, metric tiles, section headers, and care rows.
64. DONE 2026-06-14: GitHub Actions `WoofWatcher Verify` now uses Node 24-compatible action majors for checkout, setup-node, and pnpm setup while keeping the project runtime on Node 24.
65. DONE 2026-06-14: Quick Log, Plans, and Records now share stronger board card anatomy: the Log composer, Plans upcoming-events section, and Records Dog ID card use `BoardCard`, with a readiness test protecting the pattern.
66. DONE 2026-06-14: Records secondary report surfaces now use shared board anatomy: Care Pass audience preview rows, Report History resend/print rows, and Progress Report period/share controls are inside `BoardCard` sections with a readiness test protecting the structure.
67. DONE 2026-06-14: Remaining mobile Records lower sections now share board anatomy: Record Vault, Diet on File, and Records Cabinet are `BoardCard` sections with working Add/Edit/delete controls and internal vault tiles instead of old floating card islands.
68. DONE 2026-06-14: Mobile Records trend cards now share board anatomy for Weight Trend, Mood Trend, and Hydration, preserving the chart, mood bars, hydration meter, latest log context, and report-safe copy.
69. DONE 2026-06-14: Mobile Records activity and potty sections now share board anatomy for Walk Activity, Training Progress, and Potty Health while preserving saved routes, training focus/latest practice, stool color/context, and report-safe next steps.
70. DONE 2026-06-14: Mobile Records is now fully moved off its old one-off `padCard`/`sectionHeader` anatomy. Care Trends, Dog ID heading, Alone Time, Grooming Care, Incident Watch, and Medication Plan now use shared board primitives while preserving share/print, medication routines, filters, history, and care-safe copy.
71. DONE 2026-06-14: Mobile More now uses shared board anatomy for Care Team, Household Access, Responsibility Center, Sync Health, Tools & Sharing, and Diet Profile while preserving invite, rename, routine-board, sync refresh, tool routing, and diet edit/detail actions.
72. DONE 2026-06-14: Mobile Plans now uses shared board anatomy for Reminder Center and Daily Routine while preserving reminder action routing, routine add/edit/delete, routine done logging, owner load chips, household responsibility metrics, and empty routine setup.
73. DONE 2026-06-14: Mobile Quick Log now uses shared board anatomy for Today at a Glance, Find Care Logs, empty timeline state, and grouped timeline days while preserving composer boundaries, search, filters, sticky notes, edit/detail/delete actions, and sync status copy.
74. DONE 2026-06-14: Mobile WoofGuide now uses shared board anatomy for the owner-reviewed intro, Quick Questions, and Suggested Actions surfaces while preserving assistant prompts, generated action routing, owner-review drafts, and veterinary-boundary copy.
75. DONE 2026-06-14: Mobile Premium now uses shared board anatomy for Why Upgrade, gated plan cards, and Launch Entitlements while preserving the launch checklist, Free/Plus/Family policy truth, and disabled-payment safety boundary.
76. DONE 2026-06-14: Mobile Privacy & Safety now uses shared board anatomy for Export Summary, Launch Safety Gates, and Before Public Launch blockers while preserving owner data export, deletion-request sharing, AI/document/payment safety copy, and provider-gated truth.
77. DONE 2026-06-14: Mobile Avatar Studio now uses shared board anatomy for the working scan canvas, live/generated preview, mood-state set, and photo guidance while preserving library/camera generation, saved avatar state, revert-to-default, and animated scan behavior.
78. DONE 2026-06-14: Mobile Setup now uses shared board anatomy for the care-foundation header, setup-progress meter, and Dog Profile/Diet/Starter Routine/Household Caregiver sections while preserving draft application, save foundation, and finish-later behavior.
79. DONE 2026-06-16: Mobile Phoenix Home now has a Reanimated living care-twin runtime: `avatarLifeEngine` maps care state into room zones and motion modes, `LivingPhoenixRoom` uses a single animated board-accurate room stage with breathing, cue motion, care aura, sleep/hearts/reaction feedback, speech/HUD overlays, and the web preview no longer blanks on placeholder Clerk keys.
80. DONE 2026-06-16: The Care Twin Engine now exposes explicit scene phases, priority needs, HUD tones, tap verbs, recommended actions, sprite actions, and a tested `CARE_TWIN_SPRITE_MANIFEST` for transparent 256px bottom-center Phoenix strips covering idle, tail wag, ear perk, walk, eat, drink, sleep, comfort, celebrate, and health watch.
81. DONE 2026-06-16: Mobile now has the layered sprite runtime seam: `SpriteSheetPlayer` animates registered strips, `careTwinAssets.ts` gates rendering until both dogless room and transparent Phoenix strips exist, Home switches to layered rendering only when safe, and `docs/design/CARE_TWIN_ASSET_PIPELINE.md` tells Apollo/Fable/Replit exactly how to produce and register assets.
82. DONE 2026-06-17: Mobile Avatar Studio Lite now has a real template-based care-twin backbone: `PetAvatarConfig`, 12 dog templates, accessory slots, emote state inventory, mock scan suggestion, local config persistence, a Scan/Template/Customize/Emotes Studio flow, and Home/More identity wiring without claiming live AI scan.
83. IN PROGRESS 2026-06-18: Continue deep route-by-route visual polish and runtime screenshot QA for the mobile app; PixelLab v2 now has a full registered sprite manifest, first-pass dogless room variants, a standing-source walk loop, Avatar Studio previews the live layered care-twin room instead of the old generic hero art, the template picker uses a 12-item PixelLab preview catalog, selected templates can render production-scale base stills when available, the Phoenix/Shepherd, Retriever, Husky/Spitz, and Bully 10-state emote packs are wired, and the Studio presentation pass removed Home-HUD overlap from `/portrait`. Next: native safe-area/device QA, remaining Avatar Studio template packs/accessories, and screen-by-screen polish.
83a. DONE 2026-06-30: Health Watch second design-system polish now uses the normal route header, a compact Appetite/Potty/Vomit/Water health command deck, a shorter pixel stage, safer two-line Health Snapshot row anatomy, and tighter Review Packet spacing while preserving Health Watch/Bile Watch formulas, non-diagnostic copy, and Log/Records routing.
83b. DONE 2026-07-01: Compact route visual QA polish tightened Quick Log, Health Watch, and Avatar Studio against the active neo-retro pixel mockups. Quick Log now reveals the action grid sooner, Health prioritizes the first status panel before the pixel room, and Avatar Studio follows a one-dog/one-room/one-truth-label hero rule with fresh exported screenshot proof.
84. DONE 2026-06-18: PixelLab Phoenix Asset Production Phase 1/2 is no longer blocked by MCP access or credits. The approved v2 Phoenix seed/state pack, idle/tail/sleep strips, additional action strips, day dogless room, and first-pass room variants are generated, logged, and wired into the layered care-twin runtime.
85. DONE 2026-06-18: `Premium Revenue Product Builder` automation was corrected from the stale Highest Self OS prompt/workspace to the WoofWatcher repo, 3-hour cadence, worktree execution, and dog-care OS build loop.
86. NEXT: Replace first-pass locally derived room variants with final PixelLab/Figma-quality night, bedtime, health-watch, and home-alone illustrations once the core runtime is verified on device.
87. NEXT: Native iOS/Android runtime QA with real Expo/EAS account access, simulator/device screenshots, safe-area checks, frame-rate review, and App Store/Play Store readiness.
88. DONE 2026-06-18: Avatar Studio template catalog pass generated, promoted, downloaded, registered, and tested 12 PixelLab preview thumbnails for Shepherd, Retriever, Husky, Bully, Doodle, Terrier, Hound, Dachshund, Spaniel, Toy, Slender, and Mixed Breed.
89. DONE 2026-06-18: Generated, promoted, downloaded, registered, and tested the full 12-template Avatar Studio production base still pack for Shepherd, Retriever, Husky, Bully, Doodle, Terrier, Hound, Dachshund, Spaniel, Toy, Slender, and Mixed Breed.
90. DONE 2026-06-18: Generated, downloaded, registered, and tested the first Phoenix/Shepherd Avatar Studio emote still pack for Happy, Calm, Excited, Bored, Hungry, Anxious, Sleepy, Proud, Home Alone, and Not Feeling Well, and wired `/portrait` Mood set taps into real PixelLab mood art instead of repeated head-crop placeholders.
91. DONE 2026-06-18: Avatar Studio now has a PixelLab-generated 10-item transparent accessory inventory pack for forest bandana, navy collar, copper collar, heart tag, trail bandana, birthday hat, sleepy mask, training vest, cozy bed, and heart sparkles. The mobile `/portrait` route renders real accessory thumbnails, slot toggles, and readiness checks that prevent regression back to color-dot placeholders.
92. NEXT: Continue Avatar Studio production packs by generating remaining unfinished template/body-class emote packs, body-class sprite strips, and true overlay-aligned accessory layer PNGs that fit the same bottom-center avatar anchor.
93. DONE 2026-06-18: Pixel rendering and Studio presentation pass completed after Apollo's PixelLab subscription became active. The app now verifies two subscription seed strips, applies crisp pixel rendering to web image paths, uses `LivingPhoenixRoom presentation="studio"` on `/portrait`, fixes the premium revenue builder Metro/Expo export path, and has Chrome visual-smoke evidence for `/portrait` and Home.
94. NEXT: Run native iOS/Android device or simulator QA for safe areas, frame timing, touch targets, and final phone-size avatar proportions.
95. NEXT: Replace first-pass derived room variants with final illustrated PixelLab/Figma-quality scenes, then continue remaining unfinished emote/sprite packs and overlay-aligned accessory layers.
96. DONE 2026-06-18: Generated, downloaded, registered, and tested the first non-Phoenix Retriever 10-state Avatar Studio emote pack. Retriever templates now recommend `retriever-starter`, `/portrait` routes Mood set art through the selected template/emote pack, and unfinished templates fall back to their own base stills instead of borrowing Phoenix mood art.
97. DONE 2026-06-18: Generated, downloaded, registered, and tested the Husky/Spitz 10-state Avatar Studio emote pack. Husky templates now recommend `husky-starter`, selected-template routing covers the first visually distinct spitz/working-body pack, and PixelLab asset verification checked 81 local production assets at that pass.
98. DONE 2026-06-18: Generated, downloaded, visually checked, registered, and tested the Bully 10-state Avatar Studio emote pack. Bully templates now recommend `bully-starter`, compact-body selected-template routing is live, and PixelLab asset verification now checks 91 local production assets with 0 missing and 0 invalid.
99. DONE 2026-06-18: Merged main's Shepherd/Phoenix overlay and live preview work into the premium builder path. Avatar Studio now prefers file-backed Shepherd accessory overlays and Shepherd emote stills, uses the live Phoenix sprite rig for Shepherd mood states, and keeps unfinished templates on truthful still previews.
100. NEXT: Continue Avatar Studio production packs by generating template-matched sprite strips and overlay/emote packs for Retriever, Husky, Bully, Doodle, and the remaining launch templates so their previews can graduate from still-only to live animated production packs.
101. DONE 2026-06-18: Mobile Phoenix Home now has a tighter Option B-style live care twin console: the room card carries a dedicated PHOENIX HOME header, in-scene status HUD, mood/energy/hunger/hydration/bond readouts, and repositioned speech bubble so the dog remains the main character instead of competing with a second avatar. Avatar Studio now forces template, emote, room, accessory, card, and mood thumbnail images through crisp pixel rendering on web.
102. NEXT: Generate and wire true template-matched sprite strips for Retriever, Husky, Bully, Doodle, and the remaining launch templates so every prebuilt avatar can breathe, idle, walk, sleep, and emote as live game sprites instead of relying on still previews.
103. DONE 2026-06-18: Generated a subscription-backed PixelLab south-facing bark strip from the approved Phoenix character rig, registered it as `bark-loop`, and wired Phoenix Home room taps to momentarily swap the main care twin sprite into a bark reaction instead of spawning a second avatar.
104. DONE 2026-06-18: Generated, normalized, registered, and tested the first non-Phoenix live Avatar Studio sprite pack for Retriever, including idle-tail-wag and walk-loop strips. Avatar Studio now routes selected Retriever happy/calm/proud and excited/bored previews through template-specific sprite assets instead of Phoenix-only sprite tracks or still-only fallback.
105. DONE 2026-06-18: Superseded by item 108 after Husky/Spitz graduated from still-only to live sprite preview coverage; Bully, Doodle, remaining launch templates, and gait refinements continue as the next animation work.
106. DONE 2026-06-18: Polished the mobile Phoenix Home first screen toward the locked Option B board by renaming the hero to Phoenix Room, folding the Avatar Studio/Care Twin CTA into the hero console header, removing the extra first-screen care-twin identity bar, and trimming the in-room live HUD to the most important four vitals so the main dog scene has more visual room.
107. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Husky/Spitz live Avatar Studio sprite pack. The accepted walk loop now comes from a standing-source PixelLab state after rejecting a seated walk attempt, and Avatar Studio suppresses the still-template ghost while a live sprite is active so the preview reads as one care twin.
108. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Bully live Avatar Studio sprite pack. Bully now has idle/tail-wag and standing-source walk loops, expanding live prebuilt avatars to Retriever, Husky/Spitz, and compact-body families.
109. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Doodle live Avatar Studio sprite pack. Doodle now has idle/tail-wag and standing-source walk loops, with the strongest non-Phoenix walk read so far.
110. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Terrier live Avatar Studio sprite pack. Terrier now has idle/tail-wag and standing-source walk loops, and Avatar Studio now shows live/still template readiness badges plus a live-template count so unfinished templates feel intentionally in production instead of broken.
111. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Hound live Avatar Studio sprite pack. Hound now has idle/tail-wag and standing-source side-view walk loops, and the Avatar Studio live-template routing/pixel-stage polish now treats all completed template packs as animated care twins instead of still previews.
112. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Dachshund live Avatar Studio sprite pack. Dachshund now has idle/tail-wag and long-body side-view walk loops, expanding live prebuilt avatars to seven launch templates.
113. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Spaniel live Avatar Studio sprite pack. Spaniel now has idle/tail-wag and floppy-ear side-view walk loops produced from a standing-source state instead of the front-facing base still, expanding live prebuilt avatars to eight launch templates.
114. DONE 2026-06-18: Generated, normalized, visually checked, registered, and tested the Toy Breed, Slender, and Mixed Breed live Avatar Studio sprite packs. Every non-Phoenix launch template now has idle/tail-wag and walk-loop strips produced from side-view standing sources, so the prebuilt avatar catalog can breathe and move like a game sprite instead of relying on still previews.
115. NEXT: Run native iOS/Android device or simulator QA for Avatar Studio and Phoenix Home animation timing, touch targets, safe areas, phone-size sprite proportions, and gait quality; then refine any weak Toy/Slender/Mixed or earlier template walk loops and continue remaining template emote packs plus overlay-aligned accessory layers.
116. DONE 2026-06-19: Option B hard-pixel Phoenix runtime pack expansion. PixelLab selected and downloaded `option-b-seated.png`, `option-b-standing.png`, and `option-b-sleep-source.png`; generated/normalized Option B idle/tail-wag, walk, ear-perk, eat, drink, corrected curled sleep, comfort/home-alone, health-watch, celebrate, and dedicated bark/tap reaction strips; and wired the live Phoenix runtime plus sprite manifest to the Option B family so common Home states no longer fall back to the older v2 action art or the older south-facing bark fallback.
117. NEXT: Native iOS/Android device or simulator QA for the Option B Phoenix family, especially phone-size proportions, loop timing, comfort/health distinctness, walk gait, and tap reaction. Then replace first-pass room variants with final illustrated PixelLab/Figma-quality rooms, refine any weak Option B loops, and continue overlay-aligned accessories/emote polish.
118. DONE 2026-06-19: PixelLab final-room pass replaced the first-pass derived night, health-watch, and home-alone room runtime layers with visually accepted dogless 800x600 final candidates and preserved the derived bedtime room after rejecting PixelLab attempts that drifted isometric, baked in dogs, or added text/watermark marks.
119. DONE 2026-06-19: Generated, downloaded, visually accepted, normalized, and wired a clean PixelLab bedtime room final candidate, completing replacement of the first-pass derived room variants for night, bedtime, health-watch, and home-alone.
120. NEXT: Run native iOS/Android QA across Phoenix Home and Avatar Studio to judge room/sprite proportions, stage cropping, loop timing, and touch response on real phone-sized screens.
121. DONE 2026-06-19: Added a tested care-twin native QA matrix covering all 12 avatar motion states, expected sprite action, room variant, zone, scene phase, priority need, layered readiness, and matching manual iOS/Android phone-size prompts in `docs/release/CARE_TWIN_NATIVE_QA_MATRIX.md`.
122. NEXT: Use the care-twin native QA matrix during device/simulator review, then refine any clipped stage, weak loop, wrong room/sprite proportion, or touch-response issue before promoting Option B candidate assets to final approved paths.
123. DONE 2026-06-19: Polished the Option B living care-twin UI after Apollo's subscription-backed PixelLab review. Home quick logs now trigger action-specific Phoenix sprite reactions, `LivingPhoenixRoom` adds ambient micro-behaviors, scanline/frame polish, tighter stage zones, and rest-state safeguards, `PixelIcon` renders crisply on web, and Avatar Studio no longer paints oversized still/accessory layers over live sprite previews.
124. DONE 2026-06-19: Reviewed new PixelLab Phoenix candidate outputs. Candidate `6ad8a54d-3b19-41d8-9525-4eeca0d76529` is archived as a clean still reference, while candidates `96200e12-1297-4970-9342-beee2f18c5a9` and `93160aef-fe35-46bf-8b40-859687053072` are rejected due duplicate/cropped anatomy and identity drift. The current hard-pixel Option B runtime family remains the app source of truth.
125. NEXT: Execute the native iOS/Android QA matrix, capture phone-size evidence for Home and Avatar Studio, then refine any weak gait, stage crop, or room/sprite proportion before another PixelLab generation spend.
126. DONE 2026-06-19: Added an in-app Care Twin QA route at `/care-twin-qa` for development/internal builds. The route renders all 12 tested care-twin scenarios through the production `LivingPhoenixRoom`, exposes expected sprite/room/zone/need metadata, shows native QA prompts, and is reachable from More only under `__DEV__` so device testers can review every state without mutating care history.
127. NEXT: Open `/care-twin-qa` on iOS and Android, capture the required evidence from `docs/release/CARE_TWIN_NATIVE_QA_MATRIX.md`, then tune any stage crop, route frame height, weak loop, or room/sprite scale issue found on real device screens.
128. DONE 2026-06-19: Upgraded `/care-twin-qa` into a device evidence-capture surface. Each care-twin scenario can now be marked Pass or Needs tune, carries a per-state device note, updates summary counts, and shares a plain-text QA report through the native share sheet while still saying screenshots are required before launch approval.
129. NEXT: Run the upgraded QA route on iOS and Android, share/export the per-device QA report, attach the required screenshots, then fix the first visually obvious issue in stage crop, sprite scale, loop timing, or gait quality before generating more PixelLab art.
130. DONE 2026-06-19: Subscription-backed Option B redesign pass wired a clean dogless PixelLab day room into Home and Avatar Studio, registered Shepherd/Phoenix as a live Avatar Studio sprite pack using the approved Option B idle and walk strips, and moved sprite-action room staging into tested `careTwinStage.ts` logic so meals, water, walks, health/rest, and comfort route Phoenix to the correct game-stage zones.
131. NEXT: Review PixelLab pro character `be24cc90-7a69-4859-b9b2-42e73a2124cd` when it completes, but only promote it if it clearly beats the current hard-pixel Option B Phoenix runtime family and can produce matching transparent animation strips.
132. DONE 2026-06-19: Locked the Quick Log care-event doctrine into the mobile runtime. Tap now quick-logs safe defaults, long press opens the detailed composer path, medication/vomit route to detail-first safety flows, Meal starts a served/outcome-pending lifecycle with later outcome updates, Potty is the parent event with outcomes underneath, quick logs carry trust/confirmation metadata, Home uses the same builder as Log, and tests guard the doctrine.
133. PARTIAL 2026-06-19: Build the full structured edit/detail sheet stack for every care log. The Alone Time timer/check-in portion is complete in item 134; photo proof/confirmation is complete in items 135-137, potty details are complete in item 138, walk start/finish sessions are complete in item 139, and correction-history UI is complete in item 140. Provider-backed photo/document storage remains gated by Apollo's storage rules.
134. DONE 2026-06-19: Built the first-class Alone Time lifecycle. The mobile app now creates an active home-alone session when the household taps Alone Time/Leaving Home, Home reads that open session as `home-alone` instead of showing a stale with-human presence, Log shows an active session return check-in, I'm Home outcomes close the same log with duration, return state, recovery/note details, audit trail, and household visibility, and tests protect the start/return lifecycle.
135. DONE 2026-06-19: Added role-based care-log trust review for non-alone logs. The mobile app now has a tested `careLogTrust.ts` engine for Confirm, Reject, Request photo, and Mark corrected actions; Log detail sheets render a compact Trust review panel; adult owner/primary caregiver roles can review logs; kid/sitter/trainer/vet-viewer roles are blocked; review actions update trust metadata, proof-request status, severity when rejected, and the audit trail without deleting the original record.
136. DONE 2026-06-19: Detailed Log composer now applies the same tested trust defaults as quick logs. Medication detail logs start pending confirmation with proof-needed metadata, safety-critical health logs start pending review, kid/helper detail logs remain owner-reviewable, and timeline rows now show attention chips for needs-review, proof-needed, photo-requested, outcome-pending, rejected, corrected, and estimated states before opening the detail sheet.
137. DONE 2026-06-19: Added the first medication proof attachment seam. Care-log trust now has a tested local photo-proof attachment patch that records attachment URI/name/source, local-only storage status, attached-by/at metadata, proof-attached timeline chips, and an audit event without auto-confirming medication. Log detail Trust review can launch the Expo image picker, attach proof to a pending medication log, and truthfully labels the proof as local-only until provider storage is enabled.
138. DONE 2026-06-19: Added the potty detail correction seam for parent potty logs. The mobile Log detail sheet now has a Clarify potty log panel for outcome, location, pee detail, stool consistency/color, and context; the tested helper rewrites stale pee/stool fields when outcomes change, sets watch/alert severity only when warranted, preserves household/routine context, and appends audit history.
139. DONE 2026-06-19: Added the first-class Walk session lifecycle. Home and Log now start a household-visible active walk instead of silently treating every walk tap as a past log; Home shows Walk active in Phoenix's room, presence, and Next Up; Log shows a WALK ACTIVE panel with timer plus finish fields for route/place, distance, dog interactions, social outcome, and notes; finishing the same log records duration, audit history, and downstream Walk Activity/Saved Route evidence.
140. DONE 2026-06-19: Added the richer correction-history detail UI. Log details now show a Correction history card above the full Audit trail with the latest update, traceability state, correction count, and changed-field chips, so owners can understand what was corrected without reading raw audit rows first.
141. NEXT: Continue the structured edit/detail sheet stack with provider-backed photo/document upload storage once Apollo approves storage rules.
142. DONE 2026-06-19: Added the first local CareTwin roster readiness slice. `CareContext` now persists `activePetId` and planned `pets`, `careTwinRoster.ts` derives the active live care twin versus provider-gated future pet slots, More exposes a polished CareTwin Roster card plus Add future dog sheet, and Privacy export/deletion scope includes staged pet roster data. The UI explicitly blocks fake switching until provider-backed multi-dog care documents can separate logs, routines, records, reports, and sync.
143. NEXT: Build provider-backed multi-dog care documents and scoped dog switching after the storage/database/account rules are approved, including per-dog logs, routines, records, reports, avatars, privacy export, and household permissions.
144. DONE 2026-06-19: Added the local Access Pass and My Care Today operations slice. `access-pass.ts` now derives temporary sitter/trainer/vet/emergency helper permissions, blocked actions, active/upcoming/draft status, local-draft provider boundaries, and owner-specific assigned care for today. Mobile More stores `accessPasses`, shows Access Passes and My Care Today cards, creates local Access Pass drafts, shares a truthful draft summary, and privacy export/deletion scope includes Access Pass drafts.
145. NEXT: Build provider-backed Access Pass enforcement after account/storage rules are approved, including real invite approval, role-specific API authorization, pass revocation, helper audit trails, and notification delivery.
146. DONE 2026-06-19: Added the local Adventure Mode and memory foundation. `adventure.ts` derives private real-care quests, daily XP, levels, completed proof, next-step guidance, and local memory drafts from household-visible walks, training, play, and alone-time evidence. Mobile now has `/adventure`, More links to it, CareContext persists `adventureMemories`, and Privacy export/deletion scope includes Adventure memories without claiming cloud media, maps, or public sharing.
147. NEXT: Build provider-backed Adventure media and location features only after Apollo approves storage, maps/location retention, sharing permissions, and community safety rules. Until then Adventure stays local, private, and owner-controlled.
148. DONE 2026-06-19: Surfaced Adventure Mode on Phoenix Home inside the existing Care Quest card. Home now derives the same Adventure Mode model, shows the next quest plus level/XP/memory count, and routes directly to `/adventure` so the real-care RPG loop is visible from the first screen without adding a dead decorative section.
149. NEXT: Polish Adventure Mode visually after native phone QA, then decide whether Adventure deserves a primary tab once Home, Log, Plans, Health, and More launch navigation is stable.
150. DONE 2026-06-20: Added first-class Incident Watch for altercations, bites, rough greetings, escape/injury, and behavior-safety events. `incident` is now a canonical care event with aliases, shared `deriveIncidentWatch` logic, detail-first mobile Log composer fields for trigger/exposure/injury/action/follow-up, safety-critical trust defaults, Records Incident Watch summary, Care Pass report language, and tests covering domain derivation, Quick Log policy, mobile wiring, and Care Pass handoff output.
151. DONE 2026-06-20: Polished Incident Watch from a descriptive report into an operational follow-up workflow. `deriveIncidentWatch` now returns 7/30/lookback trend windows, rising/improving/steady/clear labels, owner follow-up tasks, and trainer goal suggestions from household-visible incident evidence. Mobile Records shows Trend signal, Follow-up plan, and Trainer goals cards, follow-up rows route to the Incident composer or trainer Care Pass preview, Care Pass reports include trend/follow-up/goal lines, and tests cover the new shared model plus mobile wiring.
152. PARTIAL 2026-06-20: Native iOS/Android QA for Incident Watch, Phoenix Home, Avatar Studio, and `/care-twin-qa` remains blocked on real device/simulator access, but the internal QA path is stronger. `/care-twin-qa` now includes a Mobile Release QA cockpit covering Phoenix Home, Care Twin State Lab, Avatar Studio, Incident Composer, Records Incident Watch, and Trainer Care Pass with Pass/Needs tune controls, per-surface notes, required screenshot evidence, launch-risk prompts, route-open buttons, and a combined native share report.
153. DONE 2026-06-20: Made the Mobile Release QA cockpit durable for real phone testing. `/care-twin-qa` now autosaves and restores release-surface status, care-twin state status, and device notes in local AsyncStorage via a tested `mobileQaSession.ts` snapshot/parser, so testers can open Home, Log, Records, Care Pass, or Avatar Studio and return without losing the QA session.
154. DONE 2026-06-20: Added a tested care-twin choreography model for game-like avatar behavior. `careTwinChoreography.ts` now derives the primary sprite loop, ambient micro-loops, state-aware tap reaction, reaction duration, and QA summary from the live `AvatarLifePlan`; Home no longer hard-codes every room tap to `bark-loop`, rest states use a soft ear-perk check-in, Health Watch uses a calm comfort response, `LivingPhoenixRoom` uses the choreography for ambient/reaction timing, and `/care-twin-qa` shows the motion recipe for each state.
155. DONE 2026-06-20: Added local screenshot evidence capture to the internal Mobile Release QA cockpit. `/care-twin-qa` can now attach iOS/Android screenshots from the device photo library to each release surface and each care-twin state, persist those attachments in the local QA session, count attached versus missing screenshots in summaries, and include screenshot filenames in the native share report. This is local evidence capture only, not provider-backed QA storage or launch approval.
156. PARTIAL 2026-06-20: Real iOS/Android Mobile Release QA still needs device/simulator execution, but the QA evidence path is more trustworthy. Screenshot attachments are now platform-aware: iOS captures count toward iOS slots, Android captures count toward Android slots, web/unknown attachments stay visible without falsely satisfying native evidence. The cockpit header and share reports now show iOS/Android coverage separately.
157. PARTIAL 2026-06-20: Real iOS/Android Mobile Release QA is still blocked on device/simulator access, but the internal cockpit is now harder to misread. Mobile release QA summary helpers now distinguish complete platform proof, flexible screenshot-slot satisfaction, and exact missing iOS/Android/flexible evidence. `/care-twin-qa` shows Native proof open/ready, platform proof copy, and the remaining evidence gap instead of coloring a generic screenshot pile as complete.
158. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach the required screenshots through the platform-aware evidence controls, share/export the combined QA report, then fix the first visible issue in stage crop, safe area, touch response, route frame, Incident Watch field fit, or weak animation. Provider-backed behavior goal persistence and provider-backed QA storage remain gated by account/storage/provider rules.
159. DONE 2026-06-20: Centralized mobile floating-paw tab safe-area spacing. `mobileLayout.ts` now owns tab bar, center paw, tabbed route, and standalone route bottom-clearance metrics; Home, Log, Plans, Health, More, Records, and the tab shell use the shared helper instead of route-local fixed padding; mobile readiness tests guard against regressing to hard-coded tab clearance. Local verification passed 298 behavior/readiness tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, and `git diff --check`.
160. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach the required screenshots through the platform-aware evidence controls, share/export the combined QA report, then fix the first visible issue in stage crop, safe area, touch response, route frame, Incident Watch field fit, bottom-nav clearance, or weak animation. Provider-backed behavior goal persistence and provider-backed QA storage remain gated by account/storage/provider rules.
161. DONE 2026-06-20: Extended the shared mobile safe-area contract beyond tabbed screens. `mobileLayout.ts` now also owns docked composer bottom spacing, and Adventure, Avatar Studio, Care Twin QA, Premium, Privacy, Setup, WoofGuide, and the auth shell use the shared standalone/docked helpers instead of route-local magic numbers. Mobile readiness tests now reject the old standalone padding constants and the WoofGuide local composer inset. Local verification passed 300 behavior/readiness tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `tmp/woofwatcher-standalone-layout-export`, and `git diff --check`.
162. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach required screenshots for tabbed routes, standalone routes, auth/setup, WoofGuide composer, Phoenix Home, Avatar Studio, and Care Twin QA, share/export the combined QA report, then fix the first visible safe-area, keyboard/composer, stage crop, touch response, or weak animation issue. Provider-backed behavior goal persistence and provider-backed QA storage remain gated by account/storage/provider rules.
163. DONE 2026-06-20: Added the first auth-aware household setup path to first-run onboarding. Setup now captures Create household, Join by invite, or Local preview intent; persists `householdSetup` in the local care document and privacy export; blocks join saves until an invite code is present; shows post-save confirmation copy based on Clerk/provider readiness and sign-in state; and protects the flow with setup, privacy, and mobile readiness tests. Apollo's latest light/dark WoofWatcher reference boards were also mirrored and locked as duplicate-confirmed visual north-star assets. Local verification passed 302 behavior/readiness tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `tmp/woofwatcher-household-setup-export`, and `git diff --check`.
164. NEXT: Run the Mobile Release QA cockpit plus the 12-state care-twin matrix on real iOS and Android, attach required screenshots for tabbed routes, standalone routes, auth/setup household setup, WoofGuide composer, Phoenix Home, Avatar Studio, and Care Twin QA, share/export the combined QA report, then fix the first visible safe-area, keyboard/composer, stage crop, touch response, onboarding copy fit, or weak animation issue. Provider-backed invite acceptance, household creation, behavior goal persistence, and QA storage remain gated by account/storage/provider rules.
165. DONE 2026-06-21: Ported the latest mobile interaction contracts into the advanced premium builder branch without merging away the richer product/runtime work. `mobileLayout.ts` now owns top safe-area padding for tabbed, standalone, setup, and auth surfaces; modal sheet bottom padding; centered modal backdrop padding; floating feedback/debug offsets; keyboard avoiding offsets; 48px minimum touch targets; and shared inline hit slop. Home, Log, Plans, Health, More, Records, Adventure, Avatar Studio, Care Twin QA, Premium, Privacy, Setup, AuthShell, WoofGuide, ErrorFallback, and board primitives now use those shared contracts. Static readiness tests reject route-local top padding, unsafe modal bottom padding, and literal tiny hit slop. Local verification passed 306 behavior/readiness tests, focused mobile layout/readiness tests at 76 tests, mobile TypeScript, PixelLab asset verification at 149 files, and Expo web export to `tmp/woofwatcher-mobile-interaction-contract-export`.
166. NEXT: Execute real iOS and Android QA for the unified interaction contract: inspect route top spacing, bottom-nav clearance, keyboard/composer behavior, modal sheet reach, owner-review sheets, setup/auth forms, Records/Care Pass sheets, Log quick-note modals, and all tap targets. Attach platform screenshots in `/care-twin-qa`, share/export the combined report, then fix the first visible safe-area, keyboard, touch, crop, or animation issue before adding more product surface area.
167. DONE 2026-06-21: Replaced More's static launch-readiness card with a tested shared launch truth model. `launchReadiness.ts` now derives internal-preview, native-QA-open, provider-gated, approval-open, and store-ready states from native screenshot evidence, local release foundations, provider setup, sync health, account deletion, payments, AI, storage, push, legal, support, and store-account gates. More now shows six actionable launch tiles instead of claiming Expo/EAS readiness as a launch proxy, and readiness tests guard the model plus screen wiring. Local verification passed focused launch/mobile readiness tests at 73 tests, the full 310-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-launch-readiness-export`.
168. NEXT: Run real iOS and Android QA and close provider approvals before calling WoofWatcher launch-ready. The app can now show exactly why launch is blocked, but real store readiness still requires native screenshots, production auth/database/storage/AI/payments/push configuration, privacy/legal approval, support/deletion process approval, and Apollo's app-store submission decision.
169. DONE 2026-06-21: Added a shared attachment/storage manifest backbone for launch readiness. `attachmentManifest.ts` now derives one local media/report queue across medication proof photos, record receipts/documents, Adventure memory photos, Care Pass print artifacts, and QA screenshots; marks each item local-only, upload-ready, or provider-saved based on storage provider readiness; and formats owner-readable storage summaries. `launchReadiness.ts` now consumes that queue, so More's Records Storage tile can say exactly how many local files are gated instead of showing vague storage copy. Verification passed focused attachment/launch/mobile readiness at 76 tests, full behavior/readiness at 313 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-attachment-manifest-export`.
170. NEXT: Implement provider-backed upload/storage only after Apollo approves the provider rules: storage bucket/table design, signed upload/download access, household scoping, retention/export/deletion policy, migration of the local attachment queue, and native QA that confirms uploaded proof/photos/reports survive across devices.
171. DONE 2026-06-21: Extended Privacy & Safety to use the shared attachment manifest. Owner export bundles now include a storage queue summary/count for local proof photos, record documents, Adventure memory media, Care Pass print artifacts, and QA screenshots; the Privacy screen's Files stat uses the full local attachment queue; launch safety document-storage copy names the local queue; and deletion requests explicitly call out attachment-queue review before destructive deletion. Verification passed focused privacy/manifest/mobile readiness at 76 tests, full behavior/readiness at 313 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-privacy-attachment-export`.
172. NEXT: After storage provider approval, connect Privacy & Safety to real uploaded-document deletion/export guarantees, including provider object ids, signed download review, object-retention policy, and destructive deletion audit receipts.
173. DONE 2026-06-21: Added an owner-facing Privacy & Safety attachment queue review. `attachmentManifest.ts` now builds grouped review rows for care proof photos, record documents, Adventure memories, Care Pass reports, and QA screenshots with storage status, safe action copy, and sample filenames; privacy export bundles include those rows; and the mobile Privacy screen renders an Attachment queue board so local files are visible before provider-backed storage exists. Verification passed focused attachment/privacy/mobile readiness at 78 tests, full behavior/readiness at 315 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-attachment-review-export`.
174. NEXT: After provider storage approval, wire the attachment queue review to real provider object ids, signed download review, retention/export/deletion receipts, and native iOS/Android proof that uploaded files survive cross-device.
175. DONE 2026-06-21: Added a tested owner/operator release packet. `releasePacket.ts` now converts the shared launch-readiness plan into a release score, verdict, gate rows, owner approval checklist, blockers, next actions, and safe handoff notes; More shows the packet score/verdict and can share a native Launch Packet without claiming App Store/Play Store readiness. Verification passed focused release/mobile readiness at 72 tests, full behavior/readiness at 318 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-release-packet-export`.
176. PARTIAL 2026-06-21: Remote verification for the release packet commit was triggered as GitHub Actions run `27900061970`, but it failed before job execution with job `82558663293`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82558663293`, matching the active GitHub billing/spending-limit blocker.
177. DONE 2026-06-21: Added a tested support/legal runbook readiness packet. `supportRunbook.ts` now derives support inbox, refund/subscription policy, veterinary/emergency boundary, privacy/terms links, deletion escalation, incident response, launch blockers, and share text without approving public launch prematurely. Privacy & Safety now shows a Support runbook launch-gate card with explicit blockers and a native share action. Verification passed focused support/mobile readiness at 72 tests, full behavior/readiness at 321 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-support-runbook-export`.
178. PARTIAL 2026-06-21: Remote verification for the support runbook commit was triggered as GitHub Actions run `27900748947`, but it failed before job execution with job `82560508917`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82560508917`, matching the active GitHub billing/spending-limit blocker.
179. DONE 2026-06-21: Made support/legal launch settings a persisted local-first care-document object instead of static placeholders. `CareContext` now stores `launchSupportProfile` with support inbox, privacy/terms URLs, refund/subscription approval, veterinary-boundary approval, deletion escalation, incident response, owner-review timestamp, and provider status; Privacy export bundles include that profile; and Privacy & Safety now derives the Support runbook from state and exposes an editable launch-profile sheet with truthful local owner-review copy. Verification passed focused privacy/mobile readiness at 73 tests, full behavior/readiness at 321 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-launch-support-profile-export`.
180. PARTIAL 2026-06-21: Remote verification for the launch support profile commit was triggered as GitHub Actions run `27901369925`, but it failed before job execution with job `82562168168`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82562168168`, matching the active GitHub billing/spending-limit blocker.
181. DONE 2026-06-21: Connected the persisted launch support profile back into More's Launch Readiness cockpit without overstating approval. `launchReadiness.ts` now distinguishes fully approved store gates from locally owner-reviewed support/privacy packets; More derives the staged-owner-review flags from `state.launchSupportProfile` and `supportRunbook.ts`; and readiness tests protect the "Owner packet staged" state plus specific final legal/support approval blockers. Verification passed focused launch/mobile readiness at 74 tests, full behavior/readiness at 322 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-owner-staged-launch-export`.
182. PARTIAL 2026-06-21: Remote verification for the owner-staged launch cockpit commit was triggered as GitHub Actions run `27901968840`, but it failed before job execution with job `82563764563`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82563764563`, matching the active GitHub billing/spending-limit blocker.
183. DONE 2026-06-21: Added a truthful App Store/Play Store prep packet without claiming submission approval. `storeSubmissionPacket.ts` now derives store metadata, short/full description, keyword draft, screenshot checklist, review notes, privacy disclosures, and blocked-until gates from the existing release packet. More renders a Store Submission panel inside Launch Readiness and can share the store packet separately from the operator release packet. Verification passed focused store-submission/mobile readiness at 72 tests, full behavior/readiness at 325 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-store-submission-export`.
184. PARTIAL 2026-06-21: Remote verification for the store submission packet commit was triggered as GitHub Actions run `27902485718`, but it failed before job execution with job `82565178527`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82565178527`, matching the active GitHub billing/spending-limit blocker.
185. DONE 2026-06-21: Connected store-submission screenshot prep to the internal native QA cockpit. `mobileReleaseQa.ts` now converts `storeSubmissionPacket.ts` screenshot checklist items into device-review surfaces with explicit iOS and Android evidence slots, store-safe prompts, route targets, and launch-risk copy. `/care-twin-qa` now renders a Store Screenshot QA section, shares the store packet from the QA cockpit, includes store screenshot surfaces in the platform evidence counts, and keeps store-prep claims truthful instead of treating screenshots as approval.
186. PARTIAL 2026-06-21: Remote verification for the store screenshot QA cockpit commit was triggered as GitHub Actions run `27903415928`, but it failed before job execution with job `82567714718`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82567714718`, matching the active GitHub billing/spending-limit blocker.
187. DONE 2026-06-21: Connected saved Mobile Release QA proof back into More's Launch Readiness cockpit. `mobileLaunchQaEvidence.ts` now builds the combined launch/store QA surface set, converts saved `/care-twin-qa` session status/evidence into the `LaunchReadinessNativeQaSummary`, and returns `null` for empty sessions so the app does not invent native proof. More reloads the saved QA session from AsyncStorage on focus, feeds the derived native QA summary into `deriveLaunchReadiness`, and still routes open native-QA work back to `/care-twin-qa`. Verification passed focused launch-QA/mobile readiness at 73 tests, full behavior/readiness at 330 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-saved-qa-launch-export`.
188. PARTIAL 2026-06-21: Remote verification for the saved-QA launch-readiness commit `286fa5e` was triggered as GitHub Actions run `27904305112`, but it failed before job execution with job `82570057038`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82570057038`, matching the active GitHub billing/spending-limit blocker.
189. DONE 2026-06-21: Added a tested Native QA next-captures plan so Launch Readiness can tell testers exactly what to capture next. `mobileLaunchQaEvidence.ts` now derives prioritized capture targets from the saved `/care-twin-qa` session, including missing iOS, Android, and flexible screenshot evidence per surface, with launch-critical surfaces sorted first. More renders a `Native QA Next Captures` panel inside Launch Readiness with open/complete counts, the next target routes, and direct route jumps.
190. PARTIAL 2026-06-21: Remote verification for the Native QA next-captures commit `bea5793` was triggered as GitHub Actions run `27905215517`, but it failed before job execution with job `82572570665`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82572570665`, matching the active GitHub billing/spending-limit blocker. Local verification passed focused capture-plan/mobile readiness at 75 tests, full behavior/readiness at 332 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-native-qa-captures-export`.
191. NEXT: Execute real iOS and Android QA with `/care-twin-qa`: capture/attach platform screenshots for Mobile Release QA, Store Screenshot QA, and the 12-state care-twin matrix; confirm More's Launch Readiness updates from the saved proof and next-captures panel; share/export the combined QA report; then fix the first visible crop, safe-area, touch, keyboard, route, or animation issue.
192. DONE 2026-06-21: Added a tested shareable Native QA capture plan so Apollo can send the next device-testing script to himself, Fable/Replit, or a helper without rebuilding the checklist manually. `buildMobileLaunchQaCaptureShareText` formats progress, ordered next routes, missing evidence, attached counts, and the done condition from the same capture plan that More renders. More now includes a `Share QA Plan` action inside `Native QA Next Captures`.
193. PARTIAL 2026-06-21: Remote verification for the shareable Native QA capture plan commit `3544c11` was triggered as GitHub Actions run `27906100583`, but it failed before job execution with job `82574915830`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82574915830`, matching the active GitHub billing/spending-limit blocker. Local verification passed focused capture-plan/mobile readiness at 76 tests, full behavior/readiness at 333 tests, mobile TypeScript, PixelLab asset verification at 149 files, `git diff --check`, and Expo web export to `tmp/woofwatcher-native-qa-share-plan-export`.
194. DONE 2026-06-21: Added a persisted Provider Launch Setup cockpit so the final production gates are editable, shareable, and launch-readiness-aware instead of hard-coded false states. `launchProviderSetup.ts` derives an eight-gate provider plan for production auth, household database sync, storage, WoofGuide AI, payments, push, store accounts, and self-serve deletion; clamps partial "provider approved" claims back to owner-reviewed; formats a safe share packet; and More renders the plan inside Launch Readiness with an edit sheet and Share Provider Plan action. `CareContext` now persists `launchProviderProfile`, Privacy export includes it, and `deriveLaunchReadiness` consumes the saved provider gates while still requiring native QA, legal/support/store approval, and Apollo sign-off.
195. PARTIAL 2026-06-21: Remote verification for Provider Launch Setup commit `743f3dd` was triggered as GitHub Actions run `27907578184`, but it failed before job execution with job `82578864889`, `steps: []`, and `gh run view --log-failed` returned `log not found: 82578864889`, matching the active GitHub billing/spending-limit blocker. Local verification passed the full 337-test suite, mobile TypeScript, PixelLab asset verification, `git diff --check`, and Expo web export.
196. NEXT: Execute real iOS/Android QA and production-provider setup: fill the Provider Launch Setup sheet only when Clerk/Supabase/storage/AI/payments/push/store/deletion gates are actually configured, attach native screenshots through `/care-twin-qa`, share/export the QA and launch packets, then fix the first visible or provider-backed issue. Public launch remains blocked until those external approvals are complete.
197. DONE 2026-06-22: Added a tested Home care-RPG mission deck so Phoenix Home reads closer to the locked mockups and routes like an intentional command center instead of a stack of decorative cards. `homeMissionDeck.ts` derives My Care Today, Adventure, Health Watch, and Care Pass missions from real local-first state: pending meal/open walk/alone-time loops route to Log, normal care routes to Plans, Adventure routes to `/adventure`, Health routes to `/health`, and Care Pass routes to Records. Home renders the deck as a dark navy pixel-console section directly under the Phoenix status tiles, with compact touch rows, status labels, CTAs, pixel icons, and no fake cloud/provider readiness claims. Verification passed the new mission-deck tests, mobile readiness contract, full 355-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, and direct Expo web export to `.expo-smoke`.
198. DONE 2026-06-22: Added a tested small-phone Home mission-deck layout contract as the strongest unblocked pre-device QA step. `homeMissionLayout.ts` now derives compact versus regular mission density from viewport width, estimates deck height, hides the secondary badge on small iPhone-class screens, tightens row/icon/CTA sizing without dropping below mobile touch targets, and exposes a QA label for the mission rows. Home now uses that contract through `useWindowDimensions`, so the care-RPG mission console stays more reachable before real iOS/Android screenshots. Verification passed focused mission/readiness tests at 76 tests, the full 358-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, and direct Expo web export to `.expo-smoke`.
199. DONE 2026-06-23: Added the Home mission deck as an explicit launch-critical Mobile Release QA surface instead of leaving it as a loose manual note. `/care-twin-qa`, Launch Readiness, and the shareable Native QA capture plan now include Home Mission Deck evidence slots requiring iOS and Android compact-deck screenshots, floating paw-nav visibility, no-overflow review, and route confirmation for pending meal, active walk/alone sessions, Adventure, Health, and Care Pass. Focused release-QA/readiness verification passed 84 tests.
200. DONE 2026-06-23: Tuned the Native QA capture plan to preserve release-surface order within each priority group instead of alphabetizing launch-critical targets. The shareable QA script now naturally leads with Phoenix Home, then Home Mission Deck, then Avatar Studio and the rest of the launch-critical review path, matching the flagship first-screen product risk. Focused release-QA/readiness verification passed 85 tests.
201. DONE 2026-06-23: Added explicit device verification steps to every Mobile Release QA surface and generated Store Screenshot QA surface. `/care-twin-qa` now renders numbered Device steps/Store steps before evidence capture, the shareable Mobile Release QA report includes the same steps, the Native QA Next Captures model carries those steps into More, and the More launch panel shows the first route-check step for each next target. Focused release-QA/readiness verification passed 85 tests.
202. DONE 2026-06-23: Added reproducible setup/precondition steps to every Mobile Release QA surface and generated Store Screenshot QA surface. QA packets now explain how to prepare demo-safe state before capture, including Home Mission Deck pending meal setup, active walk/Alone Time setup, Incident Watch sample-data setup, PixelLab pack readiness, and blocked store-screenshot truth boundaries. `/care-twin-qa` renders Setup first/Store prep ahead of Device steps, More's Native QA Next Captures rows show the first prep step, and share text includes setup plus verification instructions. Focused release-QA verification passed 14 tests.
203. DONE 2026-06-23: Added explicit pass criteria and Needs tune failure escalation to every Mobile Release QA and generated Store Screenshot QA surface. `/care-twin-qa` now renders Pass criteria before evidence capture, More's Native QA Next Captures rows show the first pass criterion, and both Mobile Release QA and Native QA capture share text carry pass/failure criteria so device testers know what counts as launch-quality proof. Focused release-QA verification passed 14 tests.
204. DONE 2026-06-23: Added API readiness coverage for mounted-but-previously-unmodeled WoofGuide event and Avatar Studio routes. `apiReadiness.test.ts` now guards OpenAPI, the generated React client, Zod operation validators, Zod type exports, and root focused-test wiring for `/woofguide-events`, `/avatar-stylize`, and `/avatar-emotions`; `openapi.yaml`, React schemas/hooks, and Zod exports now cover those routes without claiming provider-backed event curation or avatar generation is live when keys are missing. Local verification passed the API readiness test, the 362-test focused behavior/readiness suite, PixelLab asset verification at 149 files, `git diff --check`, and direct Expo web export through the package-local CLI. Direct workspace typecheck/API build remain limited in this Windows runtime by the missing pnpm workspace command/symlink layer, while remote GitHub Actions remains blocked by billing/spending-limit state.
205. DONE 2026-06-23: Closed the care-entries list query contract drift. The server already supported `/care-entries?limit=...`, and API readiness now proves the route, OpenAPI, React generated params, Zod validator, and Zod generated param type all expose `limit`; OpenAPI documents the 1-500 range and default. Local verification passed the API readiness test and the 363-test focused behavior/readiness suite.
206. NEXT: Run real iOS/Android QA on the updated Home first screen and Home Mission Deck QA surface using the in-app setup, numbered device steps, pass criteria, and Needs tune escalation: create/confirm pending meal and active walk/alone-time state, capture/attach compact-deck screenshots through `/care-twin-qa`, confirm rows stay reachable around the floating paw nav, confirm text does not overflow, confirm mission routes land correctly, share/export the QA report, then fix the first visible phone-size issue before provider-backed setup. Continue provider-backed setup only when Apollo confirms Clerk/Supabase/storage/AI/payments/push/store/deletion gates are actually configured.
207. DONE 2026-06-23: Hardened the care-state write error contract while native QA remains device-blocked. The `PUT /care-state` route already returns validation errors, missing-document errors, and optimistic conflict envelopes; API readiness now proves OpenAPI documents `400`, `404`, and `409`, and the generated React mutation exposes `ApiError | CareStateEnvelope` so clients can handle validation/not-found errors separately from stale-write refetch envelopes. Local verification passed the API readiness test, the full focused behavior/readiness suite, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28063020164` failed before job start with the standing GitHub billing/spending-limit annotation.
208. DONE 2026-06-23: Followed the unavailable-device fallback by hardening care-entry write error contracts for the core logs that feed routines, reports, and household trust. API readiness now proves create/update/delete care-entry routes validate bodies or ids, OpenAPI documents `400` validation errors plus existing `404` not-found responses, and the generated React create mutation exposes `ApiError` instead of `unknown` for invalid log writes. Local verification passed the API readiness test, the 365-test focused behavior/readiness suite, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28064200143` failed before job start with the standing GitHub billing/spending-limit annotation.
209. DONE 2026-06-23: Continued the unavailable-device fallback by hardening household provisioning and auth-error contracts. API readiness now proves `/me`, `/me` updates, household rename, and household join stay authenticated; profile, household, and invite payloads stay validated; first-login provisioning still documents default household, membership, and care-state bootstrap; OpenAPI documents household `401`, `400`, and missing-invite `404` responses; and generated React household hooks expose `ApiError` instead of `unknown` for auth/validation failures. Local verification passed the API readiness test, the 366-test focused behavior/readiness suite, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28065179874` failed before job start with the standing GitHub billing/spending-limit annotation.
210. DONE 2026-06-23: Continued the unavailable-device fallback by hardening provider-gated WoofGuide action contracts. API readiness now proves authenticated care-helper questions, provider-only care-helper rate limits, truthful local care-helper fallback copy, authenticated WoofGuide events status/creation, WoofGuide event creation rate limits, and local curated event fallback when provider keys are unavailable. OpenAPI now documents care-helper `401`/`429`/provider `502` responses without falsely treating local fallback as a provider-missing error, WoofGuide events status/creation now document `401`, and generated React hooks expose `ApiError | CareHelperError` or `ApiError` instead of `unknown` for these launch-critical paths. Local verification passed the RED/GREEN API readiness test, the 367-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28066357245` failed before job start with the standing GitHub billing/spending-limit annotation.
211. DONE 2026-06-23: Continued API behavior contract coverage for authenticated household scoping across care-state and care-entry read/write paths. API readiness now proves care-state reads/writes require auth, resolve the active household from the authenticated user, and query/update only that household document; care-entry list/create/update/delete routes require auth, scope queries/mutations by active household, and stamp new logs with the authenticated household and caregiver id. OpenAPI now documents care-state read `401`/`404`, care-state write `401`, and care-entry list/create/update/delete `401`; generated React care-state and care-entry list queries expose `ApiError` instead of `unknown`. Local verification passed the RED/GREEN API readiness test, the 368-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification, generated-client syntax checks, `git diff --check`, and Expo web export. Remote verify run `28067734120` failed before job start with the standing GitHub billing/spending-limit annotation.
212. DONE 2026-06-23: Continued API/provider hardening with role-aware care-entry write readiness beyond basic household membership. `care-entry-authorization.ts` now defines read-only role boundaries, kid/helper pending-confirmation defaults, safety-critical review defaults for medication/vomit/symptom/incident logs, and medication proof metadata. Care-entry create/update/delete routes now resolve the authenticated household member role before writes, return ApiError-shaped `403` responses for read-only or non-adult delete attempts, preserve household scoping, and keep serious/restricted logs from being silently confirmed. OpenAPI now documents care-entry create/update/delete `403` responses. Local verification passed the RED/GREEN API readiness test, the 369-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, generated-client and API-route syntax checks, `git diff --check`, and Expo web export.
213. PARTIAL 2026-06-23: Remote verification for the care-entry role policy commit `fa764e2` was triggered as GitHub Actions run `28069107846`, but it failed before job execution with job `83099726112`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83099726112`. The run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
214. DONE 2026-06-23: Continued provider-backed authorization readiness with owner/admin household member mutation and helper revocation contracts. `household-authorization.ts` now defines canonical household roles, owner/admin-only role changes, self-change/self-revocation blocking, protected-owner safeguards, Access Pass-compatible helper scopes, and helper revocation policy language. `PATCH /household/members/{id}` and `DELETE /household/members/{id}` are authenticated, scoped to the active household, validate ids/payloads, reject empty patches, reject unknown role strings, return typed `403` errors for forbidden mutations, update canonical roles/display names, and revoke helpers without crossing households. OpenAPI, Zod validators, and the generated React client now expose the update/revoke operations, canonical role enum, and `ApiError` mutation errors. Local verification passed the RED/GREEN API readiness test, the 370-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, generated-client and API-route syntax checks, `git diff --check`, and package-local Expo web export.
215. PARTIAL 2026-06-23: Remote verification for the household member role mutation commit `6e25c2f` was triggered as GitHub Actions run `28072320208`, but it failed before job execution with job `83109274416`, `steps: []`, `runner_id: 0`, and `gh run view --log-failed` returned `log not found: 83109274416`. The check-run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
216. DONE 2026-06-23: Continued provider-backed household sharing readiness with typed invitation and Access Pass audit contracts. Invite acceptance now stores canonical adult caregiver roles instead of legacy member strings and returns a typed `HouseholdAuditEvent`. Owner/admin Access Pass activation and revocation routes now validate payloads, stay scoped to the authenticated active household, enforce sitter/trainer/walker/vet-viewer helper roles, reject self/owner/core-member misuse, update or revoke helper membership, and return helper audit metadata. OpenAPI, Zod validators, and the generated React client now expose `HouseholdJoinResponse`, `HouseholdAuditEvent`, `HouseholdAccessPassMutationResponse`, `activateHouseholdAccessPass`, and `revokeHouseholdAccessPass`. Local verification passed the RED/GREEN API readiness test, the 371-test focused behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, generated-client/API-route syntax checks, `git diff --check`, and package-local Expo web export. Direct API build remains environment-limited by the missing pnpm workspace symlink layer in this Windows shell.
217. PARTIAL 2026-06-23: Remote verification for the household Access Pass audit contract commit `fa26845` was triggered as GitHub Actions run `28074177667`, but it failed before job execution with job `83114878625`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83114878625`. The run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
218. DONE 2026-06-24: Continued provider readiness by moving household invitation, role mutation, and Access Pass audit metadata from response-only to a provider-durable contract. `household_audit_events` is now in the Drizzle schema with lifecycle state, expiry metadata, and provider/export metadata; join/update/revoke/Access Pass routes persist audit rows through `buildHouseholdAuditInsert`; Access Pass activation rejects invalid or past `expiresAt` windows before changing helper roles; and OpenAPI, Zod validators, React schemas, API readiness, and behavior tests now expose typed audit lifecycle states including `access-pass-expired`. Local verification passed RED/GREEN API readiness and Access Pass behavior tests, the 374-test focused behavior/readiness suite, mobile TypeScript, DB/API/generated syntax checks, PixelLab asset verification at 149 files, `git diff --check`, and package-local Expo web export. Direct `tsc -p lib/db` remains environment-limited by missing local `@types/node`, and direct API build remains environment-limited by unresolved `esbuild` without the pnpm workspace runner.
219. PARTIAL 2026-06-24: Remote verification for the durable household audit readiness commit `c67364e` was triggered as GitHub Actions run `28075849741`, but it failed before job execution with job `83119832168`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83119832168`. The run annotation says the job was not started because recent account payments failed or the spending limit needs to be increased, matching the standing GitHub billing/spending-limit blocker.
220. DONE 2026-06-24: Continued provider readiness with owner/admin household audit review APIs while real device QA remains unavailable. `GET /household/audit-events` is now authenticated, active-household scoped, owner/admin-only, sorted newest first, accepts safe `limit`/`action`/`lifecycleState` filters through `normalizeHouseholdAuditListQuery`, and returns typed provider-durable audit rows. OpenAPI, Zod validators, React schemas/hooks, generated type files, API readiness tests, and behavior tests now cover the audit-list/admin review contract without claiming provider migration, RLS, retention, export, deletion, or expiry cleanup is complete. Local verification passed RED/GREEN audit tests, the 376-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export.
221. PARTIAL 2026-06-24: Remote verification for the household audit review API commit `eb50f5c` was triggered as GitHub Actions run `28078084503`, but it failed before job execution with job `83126533628`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83126533628`. This matches the standing GitHub billing/spending-limit blocker; local verification remains the current evidence for the audit-review slice.
222. DONE 2026-06-24: Continued provider readiness with request-time Access Pass expiry enforcement. Household member rows now carry `accessPassExpiresAt`, Access Pass activation persists valid future expiry windows, helper roles with expired passes are downgraded to `expired access pass` during authorization, care-entry writes treat that state as read-only, `/me` exposes `accessPassExpiresAt` and `accessPassExpired` for UI truth, and OpenAPI/Zod/React generated member schemas expose the same fields. Local verification passed RED/GREEN Access Pass/API readiness tests, the 378-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export.
223. DONE 2026-06-24: Continued provider readiness with durable household invitation lifecycle storage and typed owner/admin invite APIs. `household_invitations` is now in the Drizzle schema with unique invite codes, invited email/user metadata, canonical helper/adult roles, pending/approved/accepted/revoked/expired/rejected lifecycle states, expiry windows, actor timestamps, notes, and provider metadata. Owner/admin `GET /household/invitations`, `POST /household/invitations`, and `POST /household/invitations/{id}/revoke` routes are authenticated, active-household scoped, generated-code typed, and audit-backed; `/household/join` now prefers durable invitation rows, blocks pending/revoked/expired/rejected/accepted invites, applies invite roles, marks accepted invites, and falls back to legacy household invite codes only when no durable row exists. Local verification passed RED/GREEN invitation/API readiness tests, the 381-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export.
224. DONE 2026-06-24: Continued provider readiness with owner/admin household sharing cleanup review. `GET /household/sharing-cleanup` is authenticated, active-household scoped, owner/admin-only, read-only, and returns typed stale sharing candidates for runtime-expired invitation rows plus expired Access Pass helper memberships without deleting, revoking, or mutating access. `household-sharing-cleanup.ts` owns query normalization, non-destructive candidate derivation, review-only storage copy, and counts for expired invites versus expired helper passes. OpenAPI, Zod validators/types, React schemas/hooks, API readiness tests, and behavior tests now cover the cleanup-review contract while preserving the launch boundary that applying cleanup, Supabase migration/RLS, retention/export/deletion policy, notification delivery, and legal/privacy approval remain separate gates. Local verification passed RED/GREEN cleanup/API readiness tests, the 384-test focused behavior/readiness suite, mobile TypeScript, syntax checks, PixelLab verification at 149 files, and package-local Expo web export.
225. DONE 2026-06-25: Pivoted the launch path for Apollo's two-day deadline from vague public-launch readiness to a concrete internal beta / owner-preview ship target. `releasePacket.ts` now separates `betaShipStatus`, `betaVerdictLabel`, `betaSummary`, and `betaNextActions` from public `storeLaunchReady`, so the app can say "Beta candidate - capture device proof" or "Ready for internal beta" without claiming App Store readiness. More's Launch Readiness board now shows a dedicated 48-hour beta card. `releasePacket.test.ts` covers beta candidate, internal beta ready, blocked local beta gates, and store-ready cases; `mobileReadiness.test.ts` protects the More UI wiring. Added `docs/release/TWO_DAY_BETA_SHIP_PLAN.md` to define what can ship in two days and what remains blocked for public launch. Local verification passed 81 targeted release/readiness tests, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, release-packet syntax check, and package-local Expo web export with HTML/JS output verified and cleaned.
226. DONE 2026-06-25: Turned the two-day beta card into an actionable in-app mission path instead of a passive readiness summary. More's Launch Readiness board now renders the first three `betaNextActions` from the release packet, keeps the 48-hour beta verdict visible, and exposes a route-aware button: "Open QA Cockpit" when device proof is the next blocker, or "Share Beta Packet" when the beta is ready to circulate internally. Static mobile readiness now protects the visible beta action rows and accessible QA/share CTAs. Local verification passed the targeted 81-test release/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
227. DONE 2026-06-25: Made `/care-twin-qa` start with a focused 48-hour beta run card instead of dropping testers directly into the full checklist. The QA cockpit now derives the same next-capture plan from saved local QA evidence, shows the next surface, missing proof, complete/open count, and provides a one-tap `Open Next Surface` action plus `Share QA`. Static mobile readiness now protects `buildMobileLaunchQaCapturePlan`, `48-hour beta run`, `nextBetaTarget`, `Open Next Surface`, and the accessible route CTA. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
228. DONE 2026-06-25: Reduced two-day beta QA friction by adding explicit screenshot platform tagging to `/care-twin-qa`. The 48-hour beta run card now lets testers tag new evidence as iOS, Android, or Web before attaching screenshots from Photos, uses that selected platform when building saved QA screenshot evidence, and shows each attached file's counted platform so native proof gaps can close truthfully even when screenshots are attached from an Expo/PWA preview. Static mobile readiness now protects `selectedEvidencePlatform`, `Tag screenshot evidence`, accessible `Tag QA screenshots as ...` controls, `targetPlatform: selectedEvidencePlatform`, and visible attachment platform labels. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
229. DONE 2026-06-25: Reduced device-test navigation friction by adding a QA return loop from `/care-twin-qa` into every shared board-header route opened for capture. QA target launches now append `qaReturn=care-twin-qa`, `qaSurface`, and `qaTitle` query context, while the shared `BoardRouteHeader` renders a temporary `Return to QA Cockpit` banner only for those capture sessions. Testers can open Home, Log, Health, Records, Avatar Studio, store screenshot targets, and other board-header routes, capture the screen, then jump back to `/care-twin-qa` to attach proof and mark Pass/Needs tune without losing the QA workflow. Static mobile readiness now protects `buildQaReturnRoute`, query context, `useLocalSearchParams`, the return banner, and the cockpit return action. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
230. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the tester briefing in `/care-twin-qa`. The 48-hour beta run card now shows a `Next device mission` panel before route launch with target route, priority, current review status, evidence count, setup steps, pass criteria, and explicit `Needs tune if` escalation copy. Static mobile readiness protects the mission briefing fields so the beta capture path stays understandable under deadline pressure. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
231. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by turning the 48-hour beta run card into a single mission-control surface. `/care-twin-qa` now resolves `nextBetaSurface` from the active capture target and exposes top-card controls to `Attach proof`, mark the current mission `Pass`, or mark it `Needs tune`, all using the selected iOS/Android/Web evidence tag and shared 48px mobile touch target contract. Static mobile readiness protects the mission action rail, accessible labels, selected-platform copy, proof attachment call, and pass/needs-tune state writes. Local verification passed the targeted 85-test QA/readiness suite, the 386-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
232. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by adding a launch-critical `Owner Preview Core Loop` QA surface. The mobile release QA model now requires the real beta path to be proven in order: Home, Log, Plans, Health, More, Records, Avatar Studio, and Care Pass, including a quick safe care log, Launch Readiness review, non-diagnostic Health Watch/Bile Watch copy, and explicit iOS/Android evidence requirements. Local verification passed the targeted 86-test QA/readiness suite, the 387-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
233. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by adding an explicit `Owner route loop` panel to the `/care-twin-qa` 48-hour beta run card. The owner-preview capture plan now carries a route-by-route checklist for Home, Log, Plans, Health, More, Records, Avatar Studio, and Care Pass, including expected outcome and proof notes. The shareable QA capture script includes the same ordered route loop, and static readiness protects the in-app panel so a device tester can run the actual owner path without cross-checking separate docs. Local verification passed the targeted 87-test QA/readiness suite, the 388-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
234. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by tightening the owner-preview proof algorithm and mission card. The native QA capture plan now treats required `Note ...` evidence as a real missing item, so the Owner Preview Core Loop cannot be considered complete with screenshots and Pass alone. `/care-twin-qa` now exposes a `Mission note` field directly in the 48-hour beta run card, marks it Required when the active target needs QA-note proof, and saves it through the existing local QA session state. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
235. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by wiring the live 48-hour native capture plan into `/care-twin-qa`'s own `Share QA` packet. The cockpit share action now includes `buildMobileLaunchQaCaptureShareText(betaCapturePlan, reviewedAtIso)` before the full Mobile Release QA, store submission packet, and 12-state care-twin report, so Apollo or a device helper can share the exact next target, missing evidence, Owner route loop, Mission note requirement, and done condition from the same screen where proof is attached. Static mobile readiness protects the route import and share packet wiring. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
236. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by preventing false Pass confidence in the `/care-twin-qa` 48-hour beta run card. The cockpit now derives `nextBetaTargetMissingEvidence` once, shows a visible `Pass pending proof` gate when a mission is marked Pass but still lacks required screenshots or Mission note proof, and lists the first missing proof items directly in the mission card. Static readiness protects the proof-gate state, copy, and missing-evidence bullets. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
237. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by carrying the `Pass pending proof` truth state into More's Launch Readiness flow. The Native QA Next Captures panel now shows a visible `Proof status` row per target, uses `Pass pending proof` when a target is marked Pass but still lacks required evidence, includes that status in the accessibility label, and the shareable QA capture script now sends the same owner-readable status instead of raw internal status strings. Local verification passed the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
238. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by making More's Native QA Next Captures panel a clearer route back to proof completion. The panel now has a mobile-sized action rail with `Share QA Plan` plus `Finish Proof` when any target is `Pass pending proof`, otherwise `Open QA Cockpit`; the proof action routes directly to `/care-twin-qa` with accessible copy so testers can resolve missing screenshots or Mission notes without hunting through the app. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
239. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening `/care-twin-qa` itself for the two-day beta run. The QA cockpit now uses the shared `MIN_MOBILE_TOUCH_TARGET` contract for the screenshot platform picker, Open Next Surface / Share QA actions, Share QA Summary buttons, evidence attach/clear controls, per-surface Open Surface CTAs, and Pass / Needs tune review buttons. Static readiness now parses named style blocks and fails if any of those critical QA controls regress to route-local undersized heights. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
240. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the core Log route touch targets for the owner-preview loop. Quick Log now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for outbox retry, launcher tabs, quick-feedback Undo/Add details, alone-time return outcomes, active-walk finish, trust proof attachment, trust review actions, meal outcome updates, potty outcome options, and potty detail save. Static readiness now protects those named style blocks so the under-five-second logging flow cannot regress to cramped 36-44px controls. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
241. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the Health Watch route for the owner-preview loop. Health now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for the Health/Bile segmented tabs plus the `Log health note` and `Records` hero actions, while leaving decorative pixel meters and cards alone. Static readiness now protects those named Health style blocks so the non-diagnostic Health Watch/Bile Watch screen cannot regress to cramped controls during beta review. Local verification passed the red/green mobile readiness test, the targeted 88-test QA/readiness suite, the 389-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
242. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the Plans route for the owner-preview loop. Plans now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for Add plan, Find event, suggestion add, schedule tabs, schedule completion, routine add, event remove, routine done, modal type chips, owner chips, save, and delete controls. Static readiness now protects those named Plans style blocks so schedule/routine work cannot regress to cramped 28-42px controls during beta review. Local verification passed the red/green mobile readiness test, the targeted 89-test QA/readiness suite, the 390-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
243. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the More route's launch and household gateway actions. More now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for profile edit, Care Intelligence action, provider setup actions, native QA share/cockpit actions, beta next action, Launch/Store packet share actions, Access Pass share and role chips, household invite, prompt modal actions, provider status chips, weight-unit chips, and profile/diet/provider save buttons. Static readiness now protects those named More style blocks so the Launch Readiness and household handoff gateway cannot regress to cramped 32-46px controls during beta review. Local verification passed the red/green mobile readiness test, the targeted 90-test QA/readiness suite, the 391-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
244. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening the Records and Care Pass route for the owner-preview loop. Records now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for Dog ID share/print actions, medication search clear and filter chips, Care Pass preview rows, saved report artifact resend/print actions, progress report period tabs, record delete, empty add, record type chips, attachment, and sheet cancel/save controls. Static readiness now protects those named Records style blocks so report, ID, and vault handoff actions cannot regress to cramped 26-48px controls during beta review. Local verification passed the red/green mobile readiness test after first failing on `shareInline`, the targeted 91-test QA/readiness suite, the 392-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
245. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening Avatar Studio for the owner-preview loop. Avatar Studio now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for creator tabs, gallery/take-photo/reset/save buttons, coat swatches, face-marking options, mood preview chips, and shared-constant-backed large template/accessory tiles. Static readiness now protects those named Avatar Studio style blocks so the scan-assisted creator cannot regress to cramped 36-42px controls during beta review while preserving the larger mockup-like art tiles. Local verification passed the red/green mobile readiness test after first failing on the 40px `tab`, the targeted 92-test QA/readiness suite, the 393-test focused behavior/readiness suite, mobile TypeScript, PixelLab verification at 149 files, `git diff --check`, and package-local Expo web export with HTML/JavaScript output verified and cleaned.
246. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening Adventure Mode for the owner-preview game loop. Adventure now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for the `Save Memory` primary action and `Share Adventure` secondary action. Static readiness protects `primaryBtn` and `secondaryBtn` so the care-RPG memory layer cannot regress to local 47px controls during beta review. Local verification passed the red/green mobile readiness test after first failing on `primaryBtn`, the targeted 93-test QA/readiness suite, the 394-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript is currently dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found). Expo web export smoke did not complete in this Windows sandbox: the first attempt lacked `pnpm`, and the network-enabled bundled-pnpm attempt reached the registry but failed before export because the root preinstall script calls `sh`, which is unavailable in this shell.
247. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening Phoenix Home's owner-preview first-screen actions. Home now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for the header button, Avatar Studio hero action, presence panel, and Adventure inline action. Static readiness protects `headerButton`, `heroStudioButton`, `presencePanel`, and `adventureInline` so the beta's first impression cannot regress to cramped route-local controls. Local verification passed the red/green mobile readiness test after first failing on the 42px `headerButton`, the targeted 94-test QA/readiness suite, the 395-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found), and Expo web export still needs a shell-compatible environment with `sh` or a preinstalled dependency layer.
248. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by hardening WoofGuide's prompt, send, and owner-review actions. WoofGuide now imports the shared `MIN_MOBILE_TOUCH_TARGET` and uses it for quick question chips, suggested action rows, the composer send button, and the owner-review Cancel/Apply draft controls. Static readiness protects `quickChip`, `actionRow`, `sendBtn`, `reviewCancel`, and `reviewApply` so the care assistant cannot regress to cramped prompt or review actions during beta review. Local verification passed the red/green mobile readiness test after first failing on `quickChip`, the targeted 95-test QA/readiness suite, the 396-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found), and Expo web export still needs a shell-compatible environment with `sh` or a preinstalled dependency layer.
249. PARTIAL 2026-06-25: Real iOS/Android capture still remains the next external gate, so the unavailable-device fallback continued by adding a one-tap 48-hour beta handoff packet. `betaHandoffPacket.ts` now combines the truthful release packet with the live native QA capture plan, including beta verdict, public-launch verdict, QA progress, next device mission, missing proof, setup/device steps, pass criteria, Needs tune copy, Owner route loop run order, Pass pending proof instruction, and public/provider/AI truth boundaries. More's Launch Readiness beta card now exposes a phone-sized `Share Beta Handoff` action backed by that packet, while preserving `Open QA Cockpit` as the primary action when device proof is still required. Local verification passed the red/green helper/readiness tests, the targeted 97-test beta QA/readiness suite, the 398-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell because the Expo/mobile dependency layer is absent (`expo/tsconfig.base` not found), and Expo web export still needs a shell-compatible environment with `sh` or a preinstalled dependency layer.
250. PARTIAL 2026-06-25: Real iOS/Android capture remains the next external gate, so the unavailable-device fallback continued by adding a focused Needs Tune fix brief to the native QA flow. `mobileLaunchQaEvidence.ts` now carries `firstNeedsTuneTarget` even when that target is outside the visible next-four capture list and can build a `WoofWatcher Needs Tune Fix Brief` with route, QA note, proof gaps, setup/repro steps, route loop, done condition, and return-to-`/care-twin-qa` instructions. More's Native QA Next Captures panel now shows a phone-sized `Share Fix Brief` action only when a Needs tune target exists, and static readiness protects the import, accessibility label, button copy, and shared 48px touch-target style. Local verification passed the red/green helper/readiness tests after first failing on the missing helper and More wiring, then passed the 100-test targeted beta QA/readiness suite, the 401-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Mobile TypeScript remains dependency-blocked in this cleaned Windows shell by missing Expo/mobile dependencies and `expo/tsconfig.base`.
251. CARRIED 2026-06-25: The actual owner-preview native capture run from `/care-twin-qa` remains the next external gate. Use the 48-hour beta run card, read the `Owner route loop`, complete Home, Log, Plans, Health, More, Records, Avatar Studio, Care Pass, and Adventure in order, choose the correct iOS or Android evidence tag, attach an iOS Quick Log/Log screenshot and an Android Launch Readiness screenshot through the top-card `Attach proof` control, write the required `Mission note`, confirm `Pass pending proof` clears in both `/care-twin-qa` and More's Native QA Next Captures only after required proof is attached, mark Pass or Needs tune, share the QA summary, tap More's `Share Beta Handoff` action, and if any route is marked Needs tune tap More's new `Share Fix Brief` action before fixing the first Needs tune route. On Home, specifically confirm the menu/header action, Avatar Studio hero entry, presence panel, and Adventure inline action feel phone-sized and lead somewhere useful. On WoofGuide, confirm quick questions, suggested actions, send, and owner-review draft controls are reachable and phone-sized.
252. DONE 2026-06-25: Removed a real two-day beta install/export friction point by replacing the root Unix-shell `preinstall` guard with `scripts/enforce-pnpm-install.mjs`. The guard still removes forbidden `package-lock.json` and `yarn.lock` files and still rejects npm/yarn installs, but it now runs through Node instead of `sh -c`, so Windows, Replit, Git Bash, WSL, CI, and bundled Node environments are no longer blocked by a missing Unix shell before Expo export can start. Static mobile readiness now protects the root `preinstall` command, the Node guard source, pnpm user-agent enforcement, and lockfile cleanup. Red/green verification passed after first failing on the missing guard script; direct guard verification passed with a `pnpm/9.0.0` user agent and failed as expected with `npm/10.0.0`. Follow-up verification passed the 101-test targeted beta QA/readiness suite, the 402-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. Local `pnpm`, Expo deps, and native device tools remain unavailable in this shell.
253. DONE 2026-06-25: Tightened the Expo mobile app config for the two-day beta export path. `artifacts/woofwatcher-mobile/app.json` now declares the intended `ios`, `android`, and `web` platforms and sets `expo.web.bundler` to `metro`, matching the existing smoke export script's Metro-based Expo web export expectation. Static mobile readiness now protects both settings inside the CI export smoke test so the app cannot regress to an Expo config that hides the web export surface. Red/green verification first failed on the missing app config assertions, then passed after the config was wired. A direct package-local Expo CLI export attempt advanced past the previous `No platforms are configured to use the Metro bundler` error and now stops at the truthful dependency-layer blocker: `Cannot determine the project's Expo SDK version because the module 'expo' is not installed`.
254. DONE 2026-06-25: Added a root `doctor:mobile-beta` command for the two-day ship handoff. `scripts/mobile-beta-doctor.mjs` checks pnpm availability, the Windows-friendly install guard, the mobile `smoke:web` command, Expo iOS/Android/web platform config, Metro web export config, mobile Expo dependency resolution, PixelLab verifier presence, and the required `/care-twin-qa` owner-preview proof steps. The doctor exits blocked in this cleaned Windows shell with exactly the current actionable issues: missing `pnpm` and missing mobile `expo` dependency resolution. Static mobile readiness protects the command and the owner-proof language so Replit, a device helper, or Apollo can run one command before export instead of reading the whole release plan.
255. DONE 2026-06-25: Pinned the root package manager to the same pnpm version used by GitHub Actions. `package.json` now declares `packageManager: pnpm@10.24.0`, the mobile beta doctor checks that pin against `.github/workflows/verify.yml`, and static readiness protects the pin, workflow version, and doctor check. Red/green verification first failed on the missing package manager pin, then passed after the root package and doctor were aligned. The doctor now passes the package-manager gate while still truthfully blocking this cleaned Windows shell on missing local `pnpm` and missing mobile `expo` dependency resolution.
256. CARRIED 2026-06-25: Under the two-day deadline, restore or run a dependency-complete mobile environment now that the root preinstall guard, package manager pin, Expo platform config, and doctor handoff are fixed, then execute real owner-preview iOS/Android capture from `/care-twin-qa`. If local Windows still lacks pnpm or Expo dependencies, use Replit, Git Bash/WSL with pnpm 10.24.0 installed or Corepack-enabled, CI after billing is fixed, or a device/helper environment. Do not claim internal beta proof until focused behavior/readiness tests, PixelLab verification, `git diff --check`, dependency/export proof, and at least the required iOS/Android owner-preview screenshots plus Mission note are current.
257. DONE 2026-06-25: Made the mobile beta doctor more actionable for the dependency/export gate. `scripts/mobile-beta-doctor.mjs` now checks Corepack availability as a warning-level bootstrap helper, tells helpers to run `corepack prepare pnpm@10.24.0 --activate` when pnpm is missing, and keeps the missing pnpm plus missing mobile `expo` dependency as the two true export blockers. Static mobile readiness now protects the Corepack and exact pnpm activation guidance. Red/green verification first failed on missing Corepack guidance, then passed after the doctor was updated; the direct doctor run now reports one Corepack warning plus the same two actionable blockers.
258. DONE 2026-06-25: Hardened the native beta environment doctor so helpers cannot accidentally export from the wrong runtime or incomplete EAS setup. `scripts/mobile-beta-doctor.mjs` now blocks below Node 24 and verifies `artifacts/woofwatcher-mobile/eas.json` has both iOS and Android build profiles for preview and production. Static mobile readiness first failed on the missing `Node 24 runtime` and `EAS build profiles include iOS and Android` doctor contracts, then passed after the checks were added. Direct doctor output now passes Node 24, package-manager alignment, Expo platform config, Metro web config, and EAS native profile coverage while still truthfully blocking this cleaned Windows shell on missing `pnpm` and missing mobile `expo` dependency resolution. Follow-up verification passed the 102-test targeted beta QA/readiness suite, the 403-test focused behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
259. DONE 2026-06-25: Closed another handoff trap by making the mobile beta doctor enforce the actual pnpm CLI version, not just the root package-manager pin. `scripts/mobile-beta-doctor.mjs` now defines `expectedPnpmVersion: 10.24.0`, derives `expectedPackageManager` from it, and when a `pnpm` command is present blocks if `pnpm --version` does not equal `10.24.0`. Static readiness first failed on the missing version enforcement, then passed after the doctor compared `pnpm.stdout.trim() === expectedPnpmVersion`. This matters because the Codex bundled runtime contains pnpm 11.7.0 as a library, which should not be treated as the beta export CLI. In this shell, no `pnpm` command is on PATH, so the doctor still reports the two true blockers: missing pnpm and missing mobile `expo` dependency resolution.
260. DONE 2026-06-26: Added machine-readable mobile beta doctor output so Replit, native helpers, or automation can parse the same export readiness signal without screen-scraping terminal text. `package.json` now exposes `doctor:mobile-beta:json`, `scripts/mobile-beta-doctor.mjs --json` emits `name`, `purpose`, `result`, `checks`, `issues`, `warnings`, and `nextActions`, and static mobile readiness executes the JSON command, parses the payload, and protects the current blocked state plus Node 24/EAS pass signals. Red/green readiness first failed on the missing root script, then passed with 81 tests after the script and JSON mode were wired. In this shell the JSON doctor correctly exits blocked on the two true export blockers: missing pnpm and missing mobile `expo` dependency resolution.
261. DONE 2026-06-26: Added the dependency/export proof runbook directly to the in-app 48-hour beta handoff packet. `betaHandoffPacket.ts` now includes `corepack prepare pnpm@10.24.0 --activate`, `pnpm install`, `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, and `pnpm --filter @workspace/woofwatcher-mobile run smoke:web`, plus truthful copy that dependency proof only counts when both doctor commands report no blockers. Red/green helper verification first failed on the missing `Dependency proof commands` section, then passed after the packet was updated.
262. DONE 2026-06-26: Made the mobile beta doctor JSON contract carry the exact dependency proof command sequence as structured `proofCommands`. `scripts/mobile-beta-doctor.mjs --json` now emits `proofCommands` beside `nextActions` for both `BLOCKED` and `READY_FOR_EXPORT` results, and the human doctor output prints the same dependency proof commands before the required device proof list. Red/green readiness first failed on missing `proofCommands`, then passed after the doctor emitted `corepack prepare pnpm@10.24.0 --activate`, `pnpm install`, `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, and `pnpm --filter @workspace/woofwatcher-mobile run smoke:web`.
263. DONE 2026-06-26: Hardened Provider Launch Setup with explicit proof-needed prompts so provider rows are actionable without claiming approval. `launchProviderSetup.ts` now gives every production gate a `proofRequired` checklist covering Clerk, Supabase/RLS, storage signed uploads, WoofGuide AI policy, Plus payments, push, Apple/Google store accounts, and account deletion evidence. The share packet now includes a `Proof Needed` section, and More renders `Proof needed:` under each provider row. Red/green verification first failed on missing proof requirements/share copy, then passed after the provider model, More UI, and static readiness contract were updated.
264. CARRIED 2026-06-26: Run the dependency-complete mobile beta proof path from Replit, Git Bash/WSL, or another environment with Node 24, pnpm 10.24.0 exactly, and Expo dependencies. First run `corepack prepare pnpm@10.24.0 --activate` if Corepack is available and pnpm is missing, then `pnpm install`, `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, `pnpm --filter @workspace/woofwatcher-mobile run smoke:web`, focused behavior/readiness tests, PixelLab verification, and the `/care-twin-qa` owner-preview iOS/Android screenshot plus Mission note loop. GitHub Actions remains infrastructure-blocked until billing/spending-limit is fixed.
265. DONE 2026-06-26: Merged provider proof into the one-tap 48-hour beta handoff so Apollo, Replit, Fable, or a native helper do not need to stitch together separate packets under the deadline. `buildBetaHandoffPacketShareText` now accepts a provider setup plan, preserves the old timestamp call form, and adds a `Provider proof needed` section with each production gate's evidence checklist plus explicit copy that proof collection does not approve store, payment, AI, storage, or database readiness. More now passes `launchProviderSetupPlan` into `Share Beta Handoff`, and static readiness protects that wiring. Red/green verification first failed because the options object was treated as `[object Object]`, then passed after the beta handoff options contract and More wiring were added.
266. DONE 2026-06-26: Made the mobile beta doctor expose the expected 48-hour handoff proof sections as a structured machine-readable contract. `scripts/mobile-beta-doctor.mjs --json` now emits `handoffProofSections` with `Dependency proof commands`, `Required beta proof after export`, `Provider proof needed`, and `Truth boundaries`, and the human doctor output prints the same checklist so Apollo, Replit, Fable, or a native helper can confirm the one-tap beta packet is complete before claiming proof. Static readiness now protects the JSON contract. Direct bundled-Node verification confirmed the doctor still truthfully reports `BLOCKED` on the two real local issues: missing pnpm and missing mobile `expo` dependency resolution.
267. DONE 2026-06-26: Hardened the mobile beta doctor from an expected-section checklist into a source-backed readiness check. `buildBetaHandoffPacketShareText` now includes an explicit `Required beta proof after export` section with `/care-twin-qa`, iOS/Android screenshot, Mission note, and Pass pending proof requirements. `scripts/mobile-beta-doctor.mjs` now reads the actual handoff packet source plus More route source and passes `beta handoff source includes proof sections` only when dependency proof, required beta proof, provider proof, truth boundaries, `Share Beta Handoff`, and `providerSetupPlan: launchProviderSetupPlan` wiring are all present. Red/green verification first failed on the missing doctor check, then passed the targeted 81-test mobile readiness suite, 2 beta handoff packet tests, and the 365-test zero-dependency behavior/readiness suite.
268. DONE 2026-06-26: Hardened the mobile beta doctor against the Codex bundled package-manager trap. `scripts/mobile-beta-doctor.mjs --json` now inspects the bundled runtime pnpm package metadata and emits a warning-level `unsupported bundled pnpm candidate` check when that candidate is not the required `pnpm 10.24.0`. Static mobile readiness now protects the warning so Replit, native helpers, or Apollo do not mistake bundled `pnpm 11.7.0` for valid beta export proof. Direct doctor output remains truthfully `BLOCKED` on the two real local issues: missing PATH pnpm and missing mobile `expo` dependency resolution.
269. DONE 2026-06-26: Added an owner-preview beta proof verdict to the native QA handoff model so the two-day owner loop cannot disappear behind the top-four next captures. `buildMobileLaunchQaCapturePlan` now always returns `ownerPreviewProofStatus` for `Owner Preview Core Loop`, including status label, completion, missing proof, evidence count, and note. The shareable Native QA capture plan and the 48-hour Beta Handoff packet now print `Owner preview proof` and `Owner preview missing`, and More's Native QA panel shows an `Owner preview proof` row plus `Finish Proof` when the saved owner loop is Pass but still missing the Mission note. Red/green verification first failed on the missing shared status and handoff/More wiring, then passed targeted QA evidence, beta handoff, and mobile readiness tests plus the broad zero-dependency suite.
270. DONE 2026-06-26: Made the mobile beta doctor source-validate the owner-preview proof chain. `scripts/mobile-beta-doctor.mjs --json` now emits `owner preview proof wiring is source-backed` only when `mobileLaunchQaEvidence.ts` still derives `ownerPreviewProofStatus` from `OWNER_PREVIEW_CORE_LOOP_ID`, `betaHandoffPacket.ts` still prints `Owner preview proof` and `Owner preview missing`, and More still surfaces `Owner preview proof` with the `Finish Proof` recovery CTA. Red/green verification first failed on the missing doctor check, then passed after the doctor read the actual QA plan, beta handoff packet, and More route source. This keeps Replit, Fable, Apollo, or a native helper from losing the highest-priority owner-preview beta proof row while the dependency/export gate is still external.
271. DONE 2026-06-26: Made the mobile beta doctor source-validate the `/care-twin-qa` proof flow itself. `scripts/mobile-beta-doctor.mjs --json` now emits `care-twin QA route proof flow is source-backed` only when the actual route still carries Mission note, Pass pending proof, attach-proof, stage test IDs, and QA-return wiring, and when the release QA matrix still declares `Owner Preview Core Loop`, the iOS Quick Log/Log screenshot proof, the Android Launch Readiness screenshot proof, and `/care-twin-qa` as the route. Red/green verification first failed on the missing doctor check, then passed after the doctor read the actual route and QA matrix sources. This protects the real device mission Apollo or a helper will run under the two-day deadline.
272. DONE 2026-06-26: Made the mobile beta doctor source-validate the Native QA Needs tune recovery loop. `scripts/mobile-beta-doctor.mjs --json` now emits `native QA Needs tune fix brief is source-backed` only when `mobileLaunchQaEvidence.ts` still builds a `WoofWatcher Needs Tune Fix Brief`, tracks `firstNeedsTuneTarget`, includes the truthful no-needs-tune fallback, and tells the helper to return to `/care-twin-qa` after the fix, while More still imports the builder, detects `nativeQaCaptureNeedsTuneTarget`, and exposes the `Share Fix Brief` action with an accessibility label. Red/green verification first failed on the missing doctor check, then passed after the doctor read the actual evidence builder and More route source. This keeps a concrete repair handoff ready if the iOS/Android owner-preview run finds the first below-beta route.
273. DONE 2026-06-26: Promoted the Native QA Needs tune recovery loop into the required 48-hour beta handoff contract. `buildBetaHandoffPacketShareText` now prints a dedicated `Native QA Needs tune fix brief` section that tells helpers to use More's `Share Fix Brief` before claiming beta proof when any route is marked Needs tune, then fix the first below-beta route, return to `/care-twin-qa`, attach confirmation proof, and update the Mission note. `scripts/mobile-beta-doctor.mjs --json` now lists that same section in `handoffProofSections` and source-validates it with the other required handoff sections. Red/green verification first failed on the missing handoff section and JSON section, then passed after the packet and doctor were aligned.
274. NEXT: Run the dependency-complete mobile beta proof path from Replit, Git Bash/WSL, or another environment with Node 24, pnpm 10.24.0 exactly, and Expo dependencies, then complete the `/care-twin-qa` owner-preview iOS/Android screenshot plus Mission note loop. If that remains unavailable, continue only with launch-risk-reducing fallback work that improves the same handoff, native QA, provider-proof, or runtime-readiness surfaces without claiming beta proof.
275. DONE 2026-06-26: Fixed a core routines/logs meal-lifecycle formula so a meal that was only served no longer gets treated as fully resolved across shared care outputs. `deriveCareDayStatus` now reports meal `pending` outcomes separately from resolved meal count for `served`, `outcome-pending`, and `grazing` states; skipped/refused/resolved outcomes still close the meal record. Today Command now prioritizes an `update-meal-outcome` action for the oldest household-visible pending meal before moving on to walks or other routines, and caregiver handoff now says meals are resolved versus outcome pending instead of generic "logged" copy. Red/green verification first failed on the old done-count, Home command, and handoff copy, then passed the targeted care-domain/handoff/Today Command tests, the full zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check`.
276. DONE 2026-06-26: Aligned Routine Board with the pending-meal lifecycle so Plans/assigned routines no longer mark served-only meals as completed. `deriveRoutineBoard` now supports a `pending` routine status and `pending` completion label of `Outcome pending`; pending meal routines remain open, count against `openCount`, preserve the caregiver/timestamp that served the meal, and become the board's next item until someone records ate all/most/some, refused, skipped, or another resolved outcome. Red/green verification first failed because the routine board counted a served breakfast and walk as 2 done routines, then passed the routine-board test plus dependent Today Command, Care Intelligence, and Access Pass tests.
277. DONE 2026-06-26: Made pending meal outcome edits first-class from Log detail/Timeline/Recent Activity instead of leaving the completion formula buried in the screen. `mealOutcomeUpdate.ts` now owns the shared update contract for `Ate all`, `Ate most`, `Ate some`, `Refused`, and `Still grazing`, preserving household/routine context, updating eaten amount and unit, keeping grazing meals open, setting refused/partial/grazing watch severity, confirming the trust state, and appending correction audit history. Log's detail sheet now uses that helper and inherits the complete outcome set, so a served dinner can be resolved accurately without forcing the owner into the wrong bucket. Red/green verification first failed on the missing helper and missing `Ate some` contract, then passed the focused meal-outcome tests, Log-adjacent trust/potty tests, the 81-test mobile readiness suite, the 375-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only the expected Windows line-ending warning.
278. DONE 2026-06-26: Hardened downstream diet/report math so served-only meals no longer inflate eaten-food progress. `deriveDietProgress` now treats `served`, `grazing`, and `outcome-pending` meals as pending instead of eaten, counts pending outcomes separately, estimates partial intake only when no exact eaten amount exists, labels estimated partial amounts, and keeps skipped/refused meals at zero intake. Care Pass Diet sections now include the same Daily food summary plus a Meal amount note when outcomes are pending or estimated, so sitter/vet exports do not present a bowl on the floor as food eaten. Red/green verification first failed on the missing pending/estimated diet contract and missing Care Pass Diet lines, then passed the focused diet/Care Pass tests, 111 targeted mobile/domain tests, the 377-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
279. DONE 2026-06-26: Hardened WoofGuide and Home command copy around pending meals so assistant help cannot invent a completed meal. WoofGuide's owner-reviewed missing-meal draft now creates a `served` meal with `mealLifecycle: outcome-pending` and `requiresOutcomeUpdate: true` instead of marking the meal complete. Today Command's latest handoff now says a pending meal was served with outcome pending rather than generically logged. Red/green verification first failed on the old completed/logged wording, then passed the targeted WoofGuide/Today Command tests.
280. DONE 2026-06-26: Added shareable meal follow-up rows to Care Pass reports so sitter/vet handoffs call out unresolved, estimated, and corrected meal evidence directly. Care Pass now emits a `Meal Follow-ups` section for household-visible pending meal outcomes, partial meals with estimated eaten amounts, and corrected meal logs with audit history, so exported reports show what still needs owner review instead of hiding it in totals. Red/green verification first failed on the missing section, then passed the Care Pass report tests.
281. DONE 2026-06-26: Hardened Weekly Care Trends and Records/Care Pass trend copy around open meal loops. `deriveCareTrends` now separates pending meal outcomes from complete, partial, and skipped meals for `served`, `outcome-pending`, and `grazing` states; trend summaries, highlights, and Meal Watch signals now say how many outcomes are pending and tell the household to update served meal outcomes before treating the week as resolved. Care Pass trend sections include pending meal outcome counts, and Records' Care Trends card now shows a `Meal open` metric backed by `careTrends.current.meals.pending`. Red/green verification first failed on missing pending trend/report UI contracts, then passed the focused Care Trends/Care Pass tests and the mobile readiness suite.
282. PARTIAL 2026-06-26: Remote verification for pending-meal care-trend commit `dd64c6d` was triggered as GitHub Actions run `28234625027`, but it failed before job execution with job `83646365355`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83646365355`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed focused Care Trends/Care Pass tests, mobile readiness, the 418-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
283. DONE 2026-06-26: Polished Records report history so saved Care Pass artifacts show truthful local/provider storage state instead of implying cloud-backed PDF readiness. `CarePassArtifact` now defaults to `storageStatus: "local-only"`, `describeCarePassArtifactStorage` gives owner-readable labels/details for local-only, upload-ready, uploaded, and failed states, and Records renders the storage label plus `Cloud storage pending` detail beside each saved report's resend/print-source actions. Red/green verification first failed on missing domain helper/UI wiring, then passed focused Care Pass tests, mobile readiness, the 419-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
284. PARTIAL 2026-06-26: Remote verification for Care Pass report storage truth commit `86a5545` was triggered as GitHub Actions run `28235628241`, but it failed before job execution with job `83649703875`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83649703875`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed focused Care Pass tests, mobile readiness, the 419-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
285. DONE 2026-06-26: Connected Care Pass report-history storage truth to Provider Launch Setup readiness without claiming cloud upload. `describeCarePassArtifactStorage` now accepts a provider setup option and turns local Care Pass print sources into `upload-ready` only when storage provider rules are configured, while keeping `providerBacked` false until a real provider upload exists. Records now passes `state.launchProviderProfile.storageProviderConfigured` into the shared helper so Report History can show `Ready to upload` when provider storage is prepared and `Saved on this device` otherwise. Red/green verification first failed on the missing domain option and missing Records wiring, then passed focused Care Pass tests, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
286. PARTIAL 2026-06-26: Remote verification for provider-aware Care Pass storage commit `2636b04` was triggered as GitHub Actions run `28236929754`, but it failed before job execution with job `83653949111`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83653949111`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed focused Care Pass tests, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
287. DONE 2026-06-26: Hardened the mobile beta doctor so provider-aware Care Pass report storage cannot regress silently during Replit/Fable/native handoff. `scripts/mobile-beta-doctor.mjs --json` now emits `provider-aware Care Pass storage is source-backed` only when the shared care-domain helper still supports storage-provider setup, keeps provider-backed false for local print sources, and Records still passes `state.launchProviderProfile.storageProviderConfigured` into `describeCarePassArtifactStorage`. Red/green verification first failed on the missing doctor label, then passed mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
288. PARTIAL 2026-06-26: Remote verification for beta doctor provider-storage guard commit `64c4968` was triggered as GitHub Actions run `28237677413`, but it failed before job execution with job `83656402075`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83656402075`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
289. DONE 2026-06-26: Tightened the owner-preview native QA route loop so Care Pass report storage truth is visually checked during the two-day beta pass. The `Owner Preview Core Loop` surface now tells testers to confirm Records/Care Pass Report History shows `Saved on this device` or `Ready to upload` without implying provider-backed upload, adds that boundary to pass criteria, and requires a QA note confirming the storage status stayed truthful instead of adding another screenshot slot. Red/green verification first failed on the missing Report History storage-status wording, then passed release QA tests, native capture-plan tests, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
290. PARTIAL 2026-06-26: Remote verification for owner-preview Care Pass storage-proof commit `35b4735` was triggered as GitHub Actions run `28238656796`, but it failed before job execution with job `83659654553`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83659654553`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed release QA tests, native capture-plan tests, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
291. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so local fallback work continued only where it improved device QA, provider proof, runtime readiness, launch handoff truth, or Care Pass route polish without claiming beta proof.
292. DONE 2026-06-26: Made the mobile beta doctor source-validate the Owner Preview Care Pass storage proof chain so the new two-day route-loop requirement cannot disappear during Replit/Fable/native handoff. `scripts/mobile-beta-doctor.mjs --json` now emits `owner-preview Care Pass storage proof is source-backed` only when the release QA matrix still includes the Care Pass Report History storage-status proof, the native QA capture share text still carries route-check proof lines, and `/care-twin-qa` still renders `Proof: {routeCheck.proof}` in the Owner route loop. Red/green verification first failed on the missing doctor check, then passed the focused mobile readiness test, release QA/native capture readiness tests, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings. Direct JSON doctor output remains truthfully `BLOCKED` on missing pnpm and missing mobile Expo dependency resolution.
293. PARTIAL 2026-06-26: Remote verification for owner-preview storage-proof doctor guard commit `2f23753` was triggered as GitHub Actions run `28240015482`, but it failed before job execution with job `83664251661`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83664251661`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, release QA/native capture readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check`.
294. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so the next fallback hardened the one-tap beta handoff packet around Owner Preview Care Pass storage proof without claiming beta proof.
295. DONE 2026-06-26: Made the one-tap 48-hour Beta Handoff packet explicitly call out Care Pass Report History storage-status proof in the `Required beta proof after export` section. `buildBetaHandoffPacketShareText` now tells helpers to confirm Report History says `Saved on this device` or `Ready to upload`, and the mobile beta doctor source-backed Owner Preview storage-proof guard now checks the beta handoff packet for the same line before reporting `owner-preview Care Pass storage proof is source-backed`. Red/green verification first failed on the missing handoff line, then passed the beta handoff packet test, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and the JSON doctor remained truthfully `BLOCKED` on missing pnpm and missing mobile Expo dependency resolution.
296. PARTIAL 2026-06-26: Remote verification for beta handoff Care Pass storage-proof commit `941d4f0` was triggered as GitHub Actions run `28256289432`, but it failed before job execution with job `83720072278`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83720072278`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed the beta handoff packet test, mobile readiness, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
297. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so the next fallback hardened the 48-hour handoff packet against the bundled-pnpm false-positive that the doctor already detects.
298. DONE 2026-06-26: Made the 48-hour Beta Handoff packet explicitly warn helpers that dependency proof requires a real PATH `pnpm` at `10.24.0` and must not use a bundled `pnpm 11.x` candidate. The mobile beta doctor source-backed handoff check now requires that same line before `beta handoff source includes proof sections` can pass, keeping the human packet aligned with the machine-readable unsupported-bundled-pnpm warning. Red/green verification first failed on the missing handoff warning, then passed the beta handoff packet test, mobile readiness, JSON doctor source check, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
299. PARTIAL 2026-06-26: Remote verification for bundled-pnpm handoff guard commit `9e74abc` was triggered as GitHub Actions run `28257100651`, but it failed before job execution with job `83722840126`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83722840126`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed the beta handoff packet test, mobile readiness, direct JSON doctor source check, the 420-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with only expected Windows line-ending warnings.
300. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate. Because this shell still lacks exact `pnpm@10.24.0` on PATH and mobile Expo dependency resolution, fallback work continued only where it strengthened final Records/Care Pass route polish and export truth without claiming beta proof.
301. DONE 2026-06-26: Added a Care Pass export manifest for saved report artifacts so Records can tell owners exactly what exists now and what does not. `describeCarePassArtifactExport` now returns the printable HTML filename, MIME type, format label, byte size, source status, storage view, provider-backed truth flag, and explicit `PDF not generated` boundary. Records Report History now renders `Printable HTML`, calculated KB, `PDF pending`, the provider/local storage pill, and the PDF/native-provider detail copy while still using the existing resend and printable-source actions. The mobile beta doctor now source-validates the new export-helper route instead of the older direct storage-helper call. Red/green verification first failed on the missing domain export helper and missing Records export manifest wiring, then passed Care Pass tests, mobile readiness, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, direct JSON doctor source checks, and `git diff --check` with expected Windows line-ending warnings only. The JSON doctor still truthfully exits `BLOCKED` on missing local pnpm and missing mobile Expo dependency resolution.
302. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
303. PARTIAL 2026-06-26: Remote verification for Care Pass export manifest commit `82850d8` was triggered as GitHub Actions run `28258738261`, but it failed before job execution with job `83728433081`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83728433081`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed Care Pass tests, mobile readiness, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, direct JSON doctor source checks, and `git diff --check` with expected Windows line-ending warnings only.
304. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate. Because the local environment still lacks exact `pnpm@10.24.0` and mobile Expo dependency resolution, fallback work continued only where it strengthened one-tap launch handoff truth for the same Records/Care Pass export proof.
305. DONE 2026-06-26: Added the Care Pass export manifest proof to the one-tap 48-hour Beta Handoff packet. `buildBetaHandoffPacketShareText` now tells helpers to confirm Report History shows `Printable HTML`, file size, and `PDF pending` before anyone claims PDF readiness, and the mobile beta doctor source-backed handoff/storage guards require that same line before reporting the packet/source chain as protected. Red/green verification first failed on the missing handoff proof line, then passed the beta handoff packet test, mobile readiness, direct JSON doctor source checks, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. The JSON doctor still truthfully exits `BLOCKED` on missing local pnpm and missing mobile Expo dependency resolution.
306. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
307. PARTIAL 2026-06-26: Remote verification for beta handoff export-manifest proof commit `79f9489` was triggered as GitHub Actions run `28259505376`, but it failed before job execution with job `83731021379`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83731021379`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed the beta handoff packet test, mobile readiness, direct JSON doctor source checks, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
308. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so fallback work continued only where it made the machine-readable beta doctor stricter about the same Records/Care Pass export truth.
309. DONE 2026-06-26: Added the Care Pass export-manifest proof to the mobile beta doctor's machine-readable `nextActions`. `scripts/mobile-beta-doctor.mjs --json` now tells Replit, Fable, Apollo, or a device helper to verify Records/Care Pass Report History shows `Printable HTML`, file size, and `PDF pending` before claiming PDF readiness. Static readiness now protects that `nextActions` contract so the human Beta Handoff packet and JSON doctor cannot drift apart. Red/green verification first failed on the missing JSON next action, then passed after the doctor line was added. Follow-up verification passed 81-test mobile readiness, the direct JSON doctor source check while still truthfully `BLOCKED` on missing pnpm and missing Expo dependency resolution, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
310. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
311. PARTIAL 2026-06-26: Remote verification for beta doctor export-manifest next-action commit `5c1f9fd` was triggered as GitHub Actions run `28260679123`, but it failed before job execution with job `83735044753`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83735044753`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, the direct JSON doctor source check, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
312. CARRIED 2026-06-26: Dependency-complete native beta proof remained the top external gate, so fallback work continued by making the machine-readable beta doctor harder to misread as public launch or provider approval.
313. DONE 2026-06-26: Added explicit `truthBoundaries` to `scripts/mobile-beta-doctor.mjs --json` and the human doctor output. The doctor now says `READY_FOR_EXPORT` only covers dependency install and web export gates, does not approve App Store, Play Store, native device QA, provider sync, storage, AI, payments, legal/privacy/support, or Apollo launch sign-off, and that `BLOCKED` means nobody should claim beta export readiness until listed issues and proof commands pass. Static readiness protects those boundaries in the JSON payload. Red/green verification first failed on the missing `truthBoundaries` payload, then passed after the doctor output was wired. Follow-up verification passed 81-test mobile readiness, direct JSON doctor output, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
314. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.
315. PARTIAL 2026-06-26: Remote verification for beta doctor truth-boundary commit `9078035` was triggered as GitHub Actions run `28261499997`, but it failed before job execution with job `83737811594`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83737811594`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed mobile readiness, direct JSON doctor output, the 421-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
316. DONE 2026-06-26: Made the mobile beta doctor source-validate that the one-tap Beta Handoff packet still carries launch truth boundaries before helpers treat the doctor as proof. `scripts/mobile-beta-doctor.mjs --json` now emits `beta handoff truth boundaries are source-backed` only when `betaHandoffPacket.ts` still says no App Store/Play Store submission is approved by the packet, provider-backed auth/database/storage/AI/push/payments remain gated, WoofGuide stays non-diagnostic and owner-reviewed, public launch remains separate from local beta evidence, and the JSON `truthBoundaries` still distinguish `READY_FOR_EXPORT`, public-launch approval, and `BLOCKED`. Static readiness protects the new check. Local verification passed 81-test mobile readiness, direct JSON doctor output, the 382-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only. The doctor still truthfully reports `BLOCKED` on missing pnpm and missing mobile Expo dependency resolution.
317. PARTIAL 2026-06-26: Remote verification for beta handoff truth-boundary source guard commit `93dd154` was triggered as GitHub Actions run `28263774240`, but it failed before job execution with job `83745430354` and GitHub's annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 83745430354`. This matches the standing GitHub billing/spending-limit blocker. Local verification passed 81-test mobile readiness, direct JSON doctor output, the 382-test zero-dependency behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
318. NEXT: Keep dependency-complete native beta proof as the top external gate. If this shell still lacks exact `pnpm@10.24.0` and mobile Expo dependencies, continue fallback work only where it strengthens device QA, provider-proof, launch handoff truth, open-loop report correctness, runtime readiness, final Records/Care Pass route polish, or native QA readiness without claiming beta proof.

319. DONE 2026-06-26: Unblocked the local Expo web preview for Apollo review. `metro.config.js` now aliases React, Expo, Expo Router, React Navigation, safe-area, gesture-handler, and screens package roots and subpaths through single runtime instances, fixing the Expo Router `No filename found` crash caused by duplicate module roots in the junctioned Windows worktree. Root route registrations are restored in the mobile `_layout.tsx`, public error fallbacks no longer leak stack details, and the in-app browser renders the real exported app at `http://127.0.0.1:4194/` instead of the stale fallback shell. Local verification passed web export smoke, mobile readiness, the 382-test behavior/readiness suite, PixelLab verification at 149 files, JSON beta doctor `READY_FOR_EXPORT`, browser DOM smoke, and `git diff --check` with expected Windows line-ending warnings only.
320. NEXT: Execute real native iOS and Android device/simulator QA from `/care-twin-qa` and the owner route loop. Capture Phoenix Home, Quick Log, Plans, Health/Bile, Household Pulse, Care Pass/Records, Avatar Studio, Adventure, and More/Launch Readiness screenshots with pass/needs-tune notes before claiming native beta proof.
321. NEXT: Continue final mockup-accuracy polish only where it raises launch confidence: hard-pixel Phoenix stage, sprite-state motion, tap/long-press logging sheets, open-loop Home cards, readable desktop/sidebar grouping, mobile bottom-nav polish, and truthful provider/storage/AI/payment pending states.
322. DONE 2026-06-26: Tightened Quick Log launcher UX so casual care actions and detail-first safety actions no longer look identical. `describeQuickLogLauncherAction` now derives owner-readable launcher copy from the shared quick-log policy, safe actions render a `Tap log` chip, medication/vomit/incident-style detail-required actions render a `Details` chip, and screen-reader labels/hints now match the actual tap behavior. This keeps tap logging fast while making health, medication, and incident context visibly intentional before saving. Verification passed quick-log policy tests, 81-test mobile readiness, 383-test behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
323. NEXT: Keep native device proof as the external gate. If real iOS/Android simulator/device capture is still unavailable, continue fallback work only on launch-risk-reducing polish that improves the actual mobile routes: Quick Log bottom-sheet depth, Phoenix sprite/game feel, Care Twin QA proof clarity, provider proof truth, or mockup-accurate navigation without claiming native QA is complete.
324. DONE 2026-06-26: Improved Phoenix Home's game-feel reaction layer without adding a duplicate avatar. `careTwinReactionPolicy.ts` now maps real care logs to the main Phoenix sprite action, owner-readable reaction copy, and tone role: meal served keeps the outcome open, potty remains a parent bathroom attempt, water drives the drink loop, walks drive the walk loop, care wins can celebrate, and health/safety logs stay calm and review-oriented. Home quick logs and walk starts now call this shared policy instead of carrying per-tile sprite guesses, so the living room reaction is tied to care meaning and remains testable. Verification passed the new care-twin reaction policy tests, 81-test mobile readiness, the 386-test behavior/readiness suite, PixelLab verification at 149 files, and `git diff --check` with expected Windows line-ending warnings only.
325. NEXT: Keep native device proof as the external gate. If real iOS/Android simulator/device capture is still unavailable, continue fallback work only where it improves the actual owner-preview product: Quick Log bottom-sheet detail depth, Phoenix stage crop/motion polish, Care Twin QA proof clarity, mockup-accurate Home/Log/Health layouts, or provider-proof truth without claiming native QA is complete.
326. PARTIAL 2026-06-26: Remote verification for Phoenix Home reaction-policy commit `834d8df` was manually dispatched as GitHub Actions run `28272011652`, but it failed before job execution with job `83771107991`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83771107991`. This matches the standing GitHub billing/spending-limit pre-job blocker. Local verification passed the care-twin reaction policy tests, mobile readiness, the 386-test behavior/readiness suite, PixelLab asset verification at 149 files, and `git diff --check`.
327. DONE 2026-06-27: Tightened Quick Log v2 detail-depth so launcher tiles now support the locked tap/long-press doctrine in the product vision. Long press opens a compact launcher detail sheet instead of jumping straight to the full composer; Meal explains usual-meal quick logging plus served -> outcome follow-up, Potty explains the bathroom-attempt parent/outcome model, Medication and health/safety logs route to full details first, and every sheet keeps the accessible 48px mobile touch-target contract. Local verification passed `quickLogEntry.test.ts` 10/10, `mobileReadiness.test.ts` 81/81, the 387-test behavior/readiness suite, PixelLab verification at 149 files, Expo web export to `.expo-smoke`, and browser DOM smoke at `http://127.0.0.1:4194/`.
328. CARRIED 2026-06-27: Native iOS/Android proof remains the external launch gate. Fallback work continued on the highest visible owner-preview launch risk: Phoenix Home first-screen framing and bottom-nav/mission clearance.
329. DONE 2026-06-27: Added a responsive Phoenix Home first-screen layout contract so the flagship Home screen can stay closer to Apollo's mockups across iPhone-class preview sizes. `homeFirstScreenLayout.ts` now owns Home density, hero aspect ratio, Care Twin button sizing, presence-card overlap, status tile sizing, and the estimated mission-deck peek above the floating paw nav. Home now consumes that layout instead of a hardcoded hero ratio, and `mobileReadiness.test.ts` protects the wiring so future design polish cannot accidentally drift away from the mockup-accurate first screen. Local verification passed focused Home/readiness tests, the 390-test behavior/readiness suite, TypeScript, PixelLab verification at 149 files, and Expo web export to `.expo-smoke`. The refreshed app is visible at `http://127.0.0.1:4194/`.
330. PARTIAL 2026-06-27: Remote verification for Phoenix Home first-screen layout commit `6f6b682` was manually dispatched as GitHub Actions run `28274880697`, but it failed before job execution with job `83779509676`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83779509676`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local test regression. Local verification passed focused Home/readiness tests, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, and `git diff --check`.
331. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the highest visible launch-risk surfaces: mockup-accurate Home/Log/Health spacing, Phoenix room HUD/sprite crop polish after real device review, detail sheet visual polish, Avatar Studio template scan truth, Care Pass/Records export clarity, and `/care-twin-qa` device proof collection. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion until those proofs exist.
332. DONE 2026-06-27: Polished Health Watch into a more reference-board-accurate Care Status screen instead of a generic health metrics stack. The Health tab now opens with a cream board-card console, pixel medallion, score track, segmented StatusMeter rows for appetite/hydration/energy/vomiting, a 7-day rhythm bar strip derived from recent care logs versus watch signals, and explicit `Health observations, not diagnosis` boundary copy. Mobile readiness now protects the Care Status console, StatusMeter usage, score track, rhythm bars, and non-diagnostic copy so future design passes cannot flatten the Health screen. Local verification passed the red/green Health readiness guard, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, local preview server `HEAD 200`, and browser console smoke on `http://127.0.0.1:4194/health` with no console errors.
333. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the next highest visible launch-risk surface: Quick Log bottom-sheet visual polish, Log/Timeline edit-detail scanability, Avatar Studio template scan truth, Phoenix room HUD/sprite crop polish after real device review, Care Pass/Records export clarity, or `/care-twin-qa` device proof collection. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion until those proofs exist.
334. PARTIAL 2026-06-27: Remote verification for Health Watch Care Status commit `0791747` was manually dispatched as GitHub Actions run `28282997631`, but it failed before job execution with job `83802116271`, `steps: []`, and `gh run view --log-failed` returned `log not found: 83802116271`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local test regression. Local verification passed the red/green Health readiness guard, 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, browser console smoke, and `git diff --check`.
335. DONE 2026-06-27: Polished Quick Log's workflow literacy so the owner can immediately understand the locked interaction model without reading docs. The Log screen now shows a compact `Tap / Hold / Edit later` doctrine rail above the launcher grid, and each launcher detail sheet now renders the shared presentation model's interaction rail plus editable-Timeline reminder. The shared `describeQuickLogDetailSheet` model owns the rail and edit-later copy so meal, potty, medication, and health/safety logs stay consistent: safe routines can quick-log, safety-critical logs open details first, and every saved log remains updateable/correctable in Timeline. Red/green verification first failed on the missing rail/model fields, then passed focused Quick Log/mobile readiness, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, `/log` browser console smoke with no errors, and `git diff --check` with expected Windows CRLF warnings only.
336. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the next highest visible launch-risk surface: Log/Timeline edit-detail scanability, Avatar Studio template scan truth, Phoenix room HUD/sprite crop polish after real device review, Care Pass/Records export clarity, `/care-twin-qa` device proof collection, or provider-proof truth. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion until those proofs exist.
337. PARTIAL 2026-06-27: Remote verification for Quick Log workflow guidance commit `9a181ac` was manually dispatched as GitHub Actions run `28283693471`, but the job did not start. GitHub reported: `The job was not started because recent account payments have failed or your spending limit needs to be increased`; job `83803898312` had no executable steps, and `gh run view --log-failed` returned `log not found: 83803898312`. This is the standing account billing/spending-limit blocker, not a local regression. Local verification passed focused Quick Log/mobile readiness, 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, `/log` browser console smoke, and `git diff --check`.
338. DONE 2026-06-27: Polished Log detail and Timeline record controls so every saved care event feels more like a trustworthy product record than a dead-end modal. The detail sheet now opens with a compact `Review / Edit / Sticky / Audit` command rail, shows audit event counts when present, labels the bottom action cluster as `Record controls`, and gives share, sticky note, edit, and delete buttons explicit accessible names while preserving 48px mobile touch targets. Local verification passed focused mobile readiness, the 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, preview `HEAD 200`, `/log` browser console smoke with no errors, and `git diff --check` with expected Windows CRLF warnings only.
339. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on the highest visible launch-risk surfaces: Avatar Studio template scan truth, Phoenix room HUD/sprite crop polish after real device review, Care Pass/Records export clarity, `/care-twin-qa` device proof collection, provider-proof truth, or final mockup-accuracy passes on Home/Log/Health without claiming App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, or native beta completion.
340. PARTIAL 2026-06-27: Remote verification for Log detail record controls commit `687f322` was manually dispatched as GitHub Actions run `28284289996`, but the job failed before executing any steps. Job `83805462813` had `steps: []`, `gh run view --log-failed` returned `log not found: 83805462813`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused mobile readiness, 390-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, `/log` browser console smoke, and `git diff --check`.
341. DONE 2026-06-27: Polished Avatar Studio scan truth and pixel-care-twin onboarding so the product hook reads as a professional scan-to-template pipeline instead of a vague mock scan. `AVATAR_SCAN_WORKFLOW_STEPS` now defines the locked `Photo reference -> Template match -> Pixel twin -> Owner approval` flow, scan suggestion copy explicitly says photos guide a PixelLab template suggestion, and `/portrait` renders PixelLab-backed/not-a-photo-filter truth chips plus the four-step pipeline before Gallery/Take Photo. Local verification passed the intentional red/green Avatar Studio guard, focused Avatar Studio/mobile readiness, the 391-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, preview `HEAD 200`, `/portrait` browser console smoke with no errors, and `git diff --check` with expected Windows CRLF warnings only.
342. NEXT: Continue visible owner-preview polish where it reduces launch risk: Care Pass/Records export clarity, Phoenix room HUD/sprite crop after real device review, `/care-twin-qa` device proof collection, provider-proof truth, remaining Avatar Studio accessory alignment notes, or final mockup-accuracy passes on Home/Log/Health. Native iOS/Android proof and provider/store/payment/AI/storage/push/legal/support approval remain external gates.
343. PARTIAL 2026-06-27: Remote verification for Avatar Studio scan-truth commit `c944e61` was manually dispatched as GitHub Actions run `28284851874`, but the job failed before executing any steps. Job `83806951495` had `steps: []`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed the intentional red/green Avatar Studio guard, focused Avatar Studio/mobile readiness, 391-test behavior/readiness suite, TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, `/portrait` browser console smoke, and `git diff --check`.

344. DONE 2026-06-27: Polished Records/Care Pass Report History so saved handoff artifacts read like a professional export manifest instead of a vague file row. `describeCarePassArtifactExport` now returns manifest rows for Format, Source, PDF, and Storage with owner-readable details, and Records renders those rows in a compact two-column grid beside the existing resend/print-source actions. The UI still refuses to claim generated PDF or provider-backed upload until those systems are real. Local verification passed the red/green Care Pass/mobile readiness guard, focused Care Pass/mobile readiness, the 391-test behavior/readiness suite, PixelLab asset verification at 149 files, mobile TypeScript after regenerating ignored care-domain declarations, Expo web export to `.expo-smoke`, preview `HEAD 200`, `/records` browser console smoke with no errors, and `git diff --check` with expected Windows CRLF warnings only. A broad root `tsc --build --pretty false` still fails on pre-existing duplicate exports in `lib/api-zod/src/index.ts`, unrelated to this Records polish.
345. NEXT: Keep native iOS/Android proof as the external launch gate. Continue owner-preview polish on Phoenix room HUD/sprite crop after real device review, `/care-twin-qa` device proof collection, provider-proof truth, remaining Avatar Studio accessory alignment notes, final mockup-accuracy passes on Home/Log/Health, or the pre-existing `lib/api-zod/src/index.ts` duplicate-export typecheck cleanup. Do not claim App Store/Play Store, provider sync/storage/AI/payments/push, legal/privacy/support, generated PDF, or native beta completion until those proofs exist.
346. PARTIAL 2026-06-27: Remote verification for Care Pass export-manifest polish commit `b76121c` was manually dispatched as GitHub Actions run `28285718091`, but the job failed before executing any steps. Job `83809213051` had `steps: []`, `gh run view --log-failed` returned `log not found: 83809213051`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused Care Pass/mobile readiness, 391-test behavior/readiness suite, PixelLab asset verification at 149 files, mobile TypeScript, Expo web export, preview `HEAD 200`, `/records` browser console smoke, and `git diff --check`.
347. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue only with fallback work that improves device QA, visual polish, provider truth, Records/Care Pass handoff clarity, or the pre-existing `lib/api-zod/src/index.ts` duplicate-export typecheck cleanup without claiming generated PDF, provider upload, App Store/Play Store, or native beta readiness.
348. DONE 2026-06-27: Cleared the pre-existing root TypeScript duplicate-export blocker in `lib/api-zod/src/index.ts`. The public API-Zod barrel now keeps generated Zod schemas as runtime exports, re-exports non-colliding generated model types explicitly, and aliases the eight colliding household invitation/cleanup model types with `Type` suffixes so schema names stay stable. `apiReadiness.test.ts` now protects the barrel from regressing back to ambiguous star exports. Local verification passed API readiness, root focused tests with 431 tests, and `tsc --build --pretty false`.
349. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue fallback work only where it improves visible product quality, device QA, provider truth, Records/Care Pass handoff clarity, Phoenix room sprite/crop proof, or App Store/Play Store preparation docs without claiming generated PDF, provider upload, store approval, or native beta readiness.
350. PARTIAL 2026-06-27: Remote verification for API Zod barrel typecheck commit `e1ce860` was manually dispatched as GitHub Actions run `28286143588`, but the job failed before executing any steps. Job `83810329982` had `steps: []`, `gh run view --log-failed` returned `log not found: 83810329982`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed API readiness, root focused tests with 431 tests, and `tsc --build --pretty false`.
351. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue fallback work only where it improves visible product quality, device QA, provider truth, Records/Care Pass handoff clarity, Phoenix room sprite/crop proof, or App Store/Play Store preparation docs without claiming generated PDF, provider upload, store approval, or native beta readiness.
352. DONE 2026-06-27: Added state-specific living motion recipes to the main care-twin sprite rig so Phoenix feels more like a video-game character without adding a second avatar. `motionRecipeForSpriteAction` now defines bob, sway, tilt, scale pulse, shadow pulse, and QA hints for every sprite action; `LivingPhoenixRoom` applies the recipe to the single layered sprite and animated ground shadow. Local verification passed focused care-twin choreography/mobile readiness with 85 tests, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, and `git diff --check` with expected Windows CRLF warnings only.
353. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible owner-preview polish on Home room crop/stage QA, `/care-twin-qa` device proof collection, Avatar Studio accessory alignment, store-safe screenshot prep, or provider setup truth without claiming generated PDF, provider upload, store approval, or native beta readiness.
354. PARTIAL 2026-06-27: Remote verification for living care-twin motion recipe commit `7d04b20` was manually dispatched as GitHub Actions run `28286493067`, but the job failed before executing any steps. Job `83811229532` had `steps: []`, `gh run view --log-failed` returned `log not found: 83811229532`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused care-twin choreography/mobile readiness, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200`, and `git diff --check`.
355. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible owner-preview polish on Home room crop/stage QA, `/care-twin-qa` device proof collection, Avatar Studio accessory alignment, store-safe screenshot prep, or provider setup truth without claiming generated PDF, provider upload, store approval, or native beta readiness.
356. DONE 2026-06-27: Made `/care-twin-qa` source-backed against the new living motion recipes so device testers can verify Phoenix feels alive like one game character, not just visually present. `describeMotionRecipeForSpriteAction` now formats the exact bob, sway, tilt, scale pulse, shadow pulse, and QA hint for each sprite action; the Care Twin QA share report includes that line for every state; and each state card now renders a `Motion proof` panel with those recipe metrics plus the single-sprite QA hint. Red/green verification first failed on the missing report/route recipe proof, then passed focused Care Twin QA/mobile readiness, the 392-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, preview `HEAD 200` on `/` and `/care-twin-qa`, and `git diff --check` with expected Windows CRLF warnings only.
357. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible owner-preview polish on Home room crop/stage QA after real device review, Avatar Studio accessory alignment, store-safe screenshot prep, provider setup truth, or another `/care-twin-qa` proof-collection improvement without claiming generated PDF, provider upload, store approval, or native beta readiness.
358. PARTIAL 2026-06-27: Remote verification for care-twin motion QA proof commit `fa48670` was manually dispatched as GitHub Actions run `28287189715`, but the job failed before executing any steps. Job `83813113862` had `steps: []`, `gh run view --log-failed` returned `log not found: 83813113862`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused Care Twin QA/mobile readiness, 392-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200` on `/` and `/care-twin-qa`, and `git diff --check`.
359. DONE 2026-06-27: Made Avatar Studio accessory customization more production-truthful and store-ready. `deriveAvatarAccessoryFit` now distinguishes template-fitted overlays from inventory-ready/pending overlay packs, `deriveAvatarPreviewAccessories` carries fit labels/details into the preview model, and `/portrait` shows a compact hero fit badge plus a Customize-tab `Template overlay readiness` panel and per-accessory `Template-fitted`/`Pack pending` labels. This keeps Phoenix's Shepherd accessories honest against the current PixelLab overlay pack while making non-Shepherd templates visibly pending instead of implying finished overlay alignment. Red/green verification first failed on missing fit exports/layer fields, then passed focused Avatar Studio/mobile readiness, the 394-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, preview `HEAD 200` on `/` and `/portrait`, and `git diff --check` with expected Windows CRLF warnings only.
360. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible owner-preview polish on Home room crop/stage QA after real device review, store-safe screenshot prep, provider setup truth, `/care-twin-qa` proof collection, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
361. PARTIAL 2026-06-27: Remote verification for Avatar Studio accessory-fit readiness commit `08a3ff9` was manually dispatched as GitHub Actions run `28287872512`, but the job failed before executing any steps. Job/check-run `83814845814` had `steps: []`, `gh run view --log-failed` returned `log not found: 83814845814`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused Avatar Studio/mobile readiness, 394-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200` on `/` and `/portrait`, and `git diff --check`.
362. DONE 2026-06-27: Tightened store screenshot QA around the Avatar Studio accessory-fit truth. The Store Submission packet now requires Avatar Studio screenshots that show `Template overlay readiness`, `Template-fitted`, and `Pack pending` labels, and `buildStoreSubmissionScreenshotQaSurfaces` adds Avatar-specific setup, verification, required evidence, acceptance, and failure-escalation copy so testers do not stage misleading store screenshots. Red/green verification first failed on the old generic Avatar Studio store copy, then passed focused store/QA/mobile readiness, the 394-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200` on `/portrait` and `/care-twin-qa`, and `git diff --check`.
363. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible owner-preview polish on Home room crop/stage QA after real device review, provider setup truth, `/care-twin-qa` proof collection, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
364. PARTIAL 2026-06-27: Remote verification for Avatar Studio store screenshot QA commit `1c51ce5` was manually dispatched as GitHub Actions run `28288357429`, but the job failed before executing any steps. Job/check-run `83816058572` had `steps: []`, `gh run view --log-failed` returned `log not found: 83816058572`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused store/QA/mobile readiness, 394-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200` on `/portrait` and `/care-twin-qa`, and `git diff --check`.
365. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible owner-preview polish on Home room crop/stage QA after real device review, provider setup truth, `/care-twin-qa` proof collection, production-scale accessory/emote overlay packs, or screen-level mockup accuracy without claiming generated PDF, provider upload, store approval, or native beta readiness.
366. DONE 2026-06-27: Added source-backed Phoenix room stage-framing proof to the care-twin QA system. `getCareTwinStageFraming` now defines zone-specific crop, HUD-clearance, single-live-sprite, Option B hard-pixel mockup, and phone-screenshot rules for rug, door, bowl, bed, and window states. `evaluateCareTwinRuntimeQaScenario` carries the framing contract, `/care-twin-qa` renders a `Stage framing proof` panel for every state, and the shareable Care Twin QA report includes the same stage-framing line so native testers can review crop and game feel consistently. Red/green verification first failed on missing stage-framing contracts, then passed focused Care Twin/mobile readiness, the 395-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200` on `/` and `/care-twin-qa`, and `git diff --check`.
367. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible launch polish on Home/Log/Health mockup accuracy, provider setup truth, `/care-twin-qa` proof collection, production-scale accessory/emote overlay packs, or actual native screenshot execution without claiming generated PDF, provider upload, store approval, or native beta readiness.
368. PARTIAL 2026-06-27: Remote verification for care-twin stage-framing QA commit `0449107` was manually dispatched as GitHub Actions run `28288840784`, but the job failed before executing any steps. Job/check-run `83817272943` had `steps: []`, `gh run view --log-failed` returned `log not found: 83817272943`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused Care Twin/mobile readiness, the 395-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, preview `HEAD 200` on `/` and `/care-twin-qa`, and `git diff --check`.
369. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible launch polish on Home/Log/Health mockup accuracy, provider setup truth, `/care-twin-qa` proof collection, production-scale accessory/emote overlay packs, or actual native screenshot execution without claiming generated PDF, provider upload, store approval, or native beta readiness.
370. DONE 2026-06-27: Added a source-backed Health Review Packet to the mobile Health/Bile Watch surface so the screen now turns Health Watch signals into owner/vet-prep prompts instead of only showing status. `deriveHealthReviewPacket` returns steady/watch/review labels, approved non-diagnostic language pills, prompts, a vet-share checklist, boundary copy, and Log/WoofGuide actions. `/health` now renders the packet with `Review packet`, `Vet-share checklist`, `Log health detail`, and `Draft vet questions` actions while preserving Health Watch/Bile Watch boundaries. Red/green verification first failed on the missing helper and missing Health route wiring, then passed focused Health/mobile readiness, the 398-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, live preview `GET /health` with no browser console errors, and `git diff --check` with expected Windows CRLF warnings only.
371. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible launch polish on Home/Log/Health mockup accuracy, provider setup truth, `/care-twin-qa` proof collection, production-scale accessory/emote overlay packs, actual native screenshot execution, or store-safe screenshot prep without claiming generated PDF, provider upload, store approval, or native beta readiness.
372. PARTIAL 2026-06-27: Remote verification for Health Review Packet commit `b45c41e` was manually dispatched as GitHub Actions run `28289670042`, but the job failed before executing any steps. Job/check-run `83819441855` had `steps: []`, `gh run view --log-failed` returned `log not found: 83819441855`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused Health/mobile readiness, the 398-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, live preview `GET /health`, and `git diff --check`.
373. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue visible launch polish on Home/Log/Health mockup accuracy, provider setup truth, `/care-twin-qa` proof collection, production-scale accessory/emote overlay packs, actual native screenshot execution, or store-safe screenshot prep without claiming generated PDF, provider upload, store approval, or native beta readiness.
374. DONE 2026-06-27: Tightened App Store/Play Store screenshot QA around the new Health Review Packet so store prep cannot regress to a generic Health Watch image. The Store Submission packet now requires Health Watch screenshots to show the non-diagnostic Review packet, Vet-share checklist, and Draft vet questions action, and `buildStoreSubmissionScreenshotQaSurfaces` adds Health-specific setup, verification, evidence, acceptance, and failure-escalation copy. The Owner Preview Core Loop also requires Health Watch/Bile Watch plus the Review packet and Draft vet questions. Red/green verification first failed on the missing Health screenshot guard, then passed focused store/QA tests, the 398-test behavior/readiness suite, mobile TypeScript, and `git diff --check` with expected Windows CRLF warnings only.
375. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue store-safe screenshot prep, actual native screenshot execution, `/care-twin-qa` proof collection, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
376. PARTIAL 2026-06-27: Remote verification for Health Review Packet store QA proof commit `a7abf92` was manually dispatched as GitHub Actions run `28290226684`, but the job failed before executing any steps. Job/check-run `83820856141` had `steps: []`, `gh run view --log-failed` returned `log not found: 83820856141`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused store/QA tests, the 398-test behavior/readiness suite, mobile TypeScript, and `git diff --check`.
377. DONE 2026-06-27: Made store screenshot proof first-class in the native QA capture plan and More Launch Readiness. `buildMobileLaunchQaCapturePlan` now exposes `storeScreenshotProofStatus` with total/open/complete counts, the next store screenshot target, and missing proof; the shareable QA plan includes Store screenshot proof, Next store screenshot, and Store screenshot missing lines. More now renders a tappable Store screenshot proof row beside Owner preview proof so Apollo, Replit, Fable, or a device tester can jump into `/care-twin-qa` and capture the next store-safe screen without hunting through the full QA list. Red/green verification first failed on the missing store proof status, then passed focused mobile launch QA/mobile readiness, the 399-test behavior/readiness suite, mobile TypeScript, Expo web export to `.expo-smoke`, and `git diff --check` with expected Windows CRLF warnings only.
378. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue actual native screenshot execution, store-safe screenshot capture polish, `/care-twin-qa` proof collection, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
379. PARTIAL 2026-06-27: Remote verification for store screenshot proof visibility commit `2be41c0` was manually dispatched as GitHub Actions run `28290628303`, but the job failed before executing any steps. Job/check-run `83821873036` had `steps: []`, `gh run view --log-failed` returned `log not found: 83821873036`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused mobile launch QA/mobile readiness, the 399-test behavior/readiness suite, mobile TypeScript, Expo web export, preview `HEAD 200` on `/more`, and `git diff --check`.
380. DONE 2026-06-27: Focused the mobile release QA cockpit from More's Launch Readiness rows so the next proof target is no longer buried in the full checklist. `buildMobileLaunchQaFocusedTarget` now derives a single source-backed target from the same missing-evidence formula as the capture plan; `/care-twin-qa` reads `qaSurface`, renders a `Focused QA Target` card with route, priority, proof gaps, setup, verification, pass criteria, note, attach-proof, open-route, Pass, and Needs tune controls; and More deep-links native QA, store screenshot proof, next-capture rows, and the 48-hour beta action into `/care-twin-qa?qaSurface=...`. Red/green verification first failed on the missing helper and generic More links, then passed focused mobile launch QA/mobile readiness, the 400-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, and `git diff --check`.
381. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Use the focused `/care-twin-qa?qaSurface=...` flow from More to capture actual device screenshots and notes, then continue store-safe screenshot capture polish, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
382. PARTIAL 2026-06-27: Remote verification for focused mobile release QA commit `61f29e5` was manually dispatched as GitHub Actions run `28291975054`, but the job failed before executing any steps. Job/check-run `83825401869` had `steps: []`, `gh run view --log-failed` returned `log not found: 83825401869`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Fresh local verification passed the 440-test focused contract suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, and `git diff --check`.
383. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Use the focused `/care-twin-qa?qaSurface=...` flow from More to capture actual device screenshots and notes, then continue store-safe screenshot capture polish, visible Home/Log/Health mockup accuracy, provider setup truth, production-scale accessory/emote overlay packs, or the first focused QA Needs tune fix without claiming generated PDF, provider upload, store approval, or native beta readiness.
384. DONE 2026-06-27: Preserved focused QA context when testers leave the QA cockpit to inspect a target route and return. `BoardRouteHeader` now builds the return destination from `qaSurface`, so the route banner sends users back to `/care-twin-qa?qaSurface=...` instead of the generic QA cockpit. This keeps the exact proof mission, missing evidence, Pass/Needs tune controls, and focused notes visible after a tester captures the target screen. Red/green verification first failed on the old generic return route, then passed `mobileReadiness.test.ts` with 81/81 tests.
385. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue tightening the focused QA proof loop: real device screenshots, focused evidence attachment, store-safe screenshot capture polish, first Needs tune fix routing, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
386. PARTIAL 2026-06-27: Remote verification for focused QA return-context commit `6bc4f08` was manually dispatched as GitHub Actions run `28292452126`, but the job failed before executing any steps. Job/check-run `83826666530` had `steps: []`, `gh run view --log-failed` returned `log not found: 83826666530`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed the red/green mobile readiness check, the 440-test focused contract suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export, focused route `HEAD 200`, and `git diff --check`.
387. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue tightening the focused QA proof loop: real device screenshots, focused evidence attachment, store-safe screenshot capture polish, first Needs tune fix routing, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
388. DONE 2026-06-27: Added a direct first-issue jump for Native QA Needs tune work from More's Launch Readiness actions. When the saved QA session marks a route as Needs tune, More now shows `Open Needs Tune` beside `Share Fix Brief`; the action deep-links to `/care-twin-qa?qaSurface=...` for the failing target instead of forcing testers to read a share sheet and manually find the issue. The new button has its own shared touch-target style and keeps the fix brief as the handoff/export action. Red/green verification first failed on the missing action/style, then passed `mobileReadiness.test.ts` with 81/81 tests.
389. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue tightening the focused QA proof loop: real device screenshots, focused evidence attachment, store-safe screenshot capture polish, first Needs tune fix resolution, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
390. PARTIAL 2026-06-27: Remote verification for Needs Tune focused target commit `eb9986e` was manually dispatched as GitHub Actions run `28292935016`, but the job failed before executing any steps. Job/check-run `83827915264` had `steps: []`, `gh run view --log-failed` returned `log not found: 83827915264`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed the red/green mobile readiness check, the 440-test focused contract suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, root route `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check`. Direct `/more` `HEAD` returned `404` from the current local server, while the app root and focused QA route served successfully.
391. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue tightening the focused QA proof loop: real device screenshots, focused evidence attachment, store-safe screenshot capture polish, first Needs tune fix resolution from the focused target, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
392. DONE 2026-06-27: Made the focused `/care-twin-qa?qaSurface=...` target self-contained for Needs Tune repair handoff. When the focused target is marked `Needs tune`, the cockpit now shows `Share fix brief` directly on that focused card and shares the same source-backed `WoofWatcher Needs Tune Fix Brief` used by More, so testers do not have to leave the repair context to send the first issue packet. Red/green verification first failed on the missing import/action, then passed `mobileReadiness.test.ts` with 81/81 tests.
393. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue tightening focused QA proof: actual device screenshots, focused evidence attachment, store-safe screenshot capture polish, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
394. PARTIAL 2026-06-27: Remote verification for focused Needs Tune brief commit `eeaf315` was manually dispatched as GitHub Actions run `28293402700`, but the job failed before executing any steps. Job/check-run `83829115323` had `steps: []`, `gh run view --log-failed` returned `log not found: 83829115323`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed the red/green mobile readiness check, the 440-test focused contract suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, root route `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check`.
395. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Continue tightening focused QA proof: actual device screenshots, focused evidence attachment, store-safe screenshot capture polish, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
396. DONE 2026-06-27: Made focused QA screenshot proof visible inside `/care-twin-qa?qaSurface=...` instead of hiding it behind a count. The focused target now derives `focusedQaEvidence` from the saved surface evidence, renders a `Focused screenshot proof` panel with attached filenames/platform tags, keeps the focused attach action, and lets testers clear the exact focused proof set without losing unrelated QA evidence. Red/green verification first failed on the missing focused proof panel, then passed `mobileReadiness.test.ts` with 81/81 tests.
397. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Run the focused QA flow on real iOS and Android devices, attach screenshots through the focused panel, confirm missing evidence clears in both `/care-twin-qa` and More's Launch Readiness, then continue store-safe screenshot capture polish, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
398. PARTIAL 2026-06-27: Remote verification for focused QA screenshot proof commit `0de87ef` was manually dispatched as GitHub Actions run `28294054369`, but the job failed before executing any steps. Job/check-run `83830830154` had `steps: []`, `gh run view --log-failed` returned `log not found: 83830830154`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed the red/green mobile readiness check, the 440-test focused contract suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, root route `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check`.
399. DONE 2026-06-27: Added a source-backed `Share target checklist` action to focused `/care-twin-qa?qaSurface=...` cards so Apollo, Fable, Replit, or a device tester can share the exact focused route before capturing proof. `buildMobileLaunchQaFocusedTargetShareText` now exports a phone-ready checklist with focused cockpit URL, target route, status, goal, device prompt, missing proof, attached proof count, setup, verification, pass criteria, Needs tune rule, owner route loop when present, and the App Store/Play Store approval boundary. Red/green verification first failed on the missing export/action, then passed focused mobile launch QA/mobile readiness with 97/97 tests.
400. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Use `Share target checklist` from the focused QA card when sending a phone/tester task, then attach real iOS/Android screenshots through the focused proof panel and continue visible Home/Log/Health mockup accuracy, provider setup truth, store-safe screenshots, or production-scale accessory/emote overlay packs without claiming generated PDF, provider upload, store approval, or native beta readiness.
401. PARTIAL 2026-06-27: Remote verification for focused target checklist commit `591c203` was manually dispatched as GitHub Actions run `28294610682`, but the job failed before executing any steps. Job/check-run `83832264186` had `steps: []`, `gh run view --log-failed` returned `log not found: 83832264186`, and the check-run annotation said: `The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings`. This is the standing GitHub billing/spending-limit blocker, not a local regression. Local verification passed focused mobile launch QA/mobile readiness with 97/97 tests, the 441-test focused contract suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, root route `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check`.
402. DONE 2026-06-27: Tightened Provider Launch Setup into a single next-gate handoff instead of a row list that requires Apollo or a helper to infer the next production blocker. `deriveLaunchProviderSetup` now exposes `openCount` and `nextGate`, `buildLaunchProviderSetupShareText` includes a `Next Provider Gate` section with owner/action/proof or the all-gates-ready review state, and More's Launch Readiness provider panel now highlights the next provider gate while prioritizing open rows before ready rows. This preserves the no-fake-provider boundary while making Clerk/Supabase/storage/AI/payments/push/store/deletion setup easier to execute. Red/green verification first failed on the missing `openCount`, `nextGate`, share section, and More UI, then passed focused provider/mobile readiness, the 402-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, Expo web export to `.expo-smoke`, root route `HEAD 200`, and focused QA route `HEAD 200`.
403. NEXT: Keep native iOS/Android proof and GitHub billing/spending-limit recovery as launch gates. Use the Provider Launch Setup next-gate card to close real production provider proof only when Apollo has configured the actual service, then continue focused device screenshots, store-safe screenshot capture, visible Home/Log/Health mockup accuracy, production-scale accessory/emote packs, or provider-backed invite/sync work without claiming provider upload, AI, payments, push, store approval, or native beta completion.
404. PARTIAL 2026-06-27: Remote verification for Provider Launch Setup next-gate commit `3cc6534` was manually dispatched as GitHub Actions run `28295375561`, but the job failed before executing any steps. Job/check-run `83834270843` reported `steps: []`, and `gh run view --log-failed` returned `log not found: 83834270843`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
405. DONE 2026-06-28: Made Health Review Packet shareable directly from Health Watch without adding medical certainty. `buildHealthReviewPacketShareText` now packages the current Health Review Packet with generated time, dog name, status, safe language label, summary, suggested prompts, vet-share checklist, and boundary text. Health Watch exposes a thumb-safe `Share review` action below the packet boundary and sends the same source-backed, non-diagnostic copy through React Native Share. Red/green verification first failed on the missing packet share helper/UI contract, then passed the focused Health Review Packet/mobile readiness checks, the 403-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, preview root `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check` with expected Windows CRLF warnings only.
406. NEXT: Keep native iOS/Android proof, GitHub billing/spending-limit recovery, real provider setup, and owner visual approval as launch gates. Use Health Watch's `Share review` action during vet/caregiver handoff QA, then continue focused device screenshots, Health/Log/Home mockup accuracy, store-safe screenshot proof, production-scale accessory/emote packs, provider-backed invite/sync work, or the first focused QA Needs tune fix without claiming diagnosis, generated PDF, provider upload, store approval, or native beta completion.
407. PARTIAL 2026-06-28: Remote verification for Health Review Packet share commit `295db19` was manually dispatched as GitHub Actions run `28309546119`, but the job failed before executing any steps. Job/check-run `83871694493` reported `steps: []`, and `gh run view --log-failed` returned `log not found: 83871694493`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression. Local verification passed the 403-test behavior/readiness suite, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export, preview root `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check`.
408. DONE 2026-06-28: Ported the latest structured mood and energy care logic into the premium automation branch without a broad merge. A full `origin/main` merge was intentionally aborted after it produced conflicts across API, mobile routes, generated clients, binary avatar assets, and docs; this slice instead surgically adds shared `deriveMoodTrend` care-domain logic, Log composer mood energy/context/household visibility fields, Records Mood Trend status/energy/latest-context UI, and mobile readiness guards. Local verification passed focused mood/mobile readiness tests 88/88, the 405-test behavior/readiness suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, preview root `HEAD 200`, focused QA route `HEAD 200`, and `git diff --check` with expected Windows CRLF warnings only.
409. NEXT: Keep native iOS/Android proof, GitHub billing/spending-limit recovery, real provider setup, and owner visual approval as launch gates. Continue with the next highest launch-risk slice: real device screenshots through `/care-twin-qa?qaSurface=...`, visible Home/Log/Health mockup accuracy, Records/Care Pass report polish, provider-backed invite/sync truth, production-scale accessory/emote packs, or first Needs tune fix. Do not claim generated PDF, provider upload, AI/payment/push readiness, App Store/Play Store approval, or native beta completion until those proofs exist.
410. DONE 2026-06-28: Hardened the mobile beta doctor readiness contract after fresh verification caught that a wrong `pnpm` CLI version was not accepted as a named dependency/export gate. The JSON doctor already blocks when `pnpm 11.7.0` is detected against the pinned `pnpm 10.24.0`; the mobile readiness assertion now recognizes `pnpm CLI matches pinned version` alongside missing pnpm and missing Expo resolution, so helpers can see the exact beta-export blocker without weakening the truth boundary. Fresh local verification passed mobile readiness 82/82, the 405-test behavior/readiness suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, and `git diff --check` with expected Windows CRLF warnings only.
411. NEXT: Push the verified mood/energy and beta-doctor repair commits, dispatch remote `WoofWatcher Verify`, record the expected GitHub billing/spending-limit state if the job still fails before steps, then continue the highest launch-risk work: real iOS/Android screenshots through `/care-twin-qa?qaSurface=...`, visible Home/Log/Health mockup accuracy, provider setup truth, or production-scale accessory/emote packs without claiming native beta, provider upload, AI/payment/push readiness, generated PDF, or store approval.
412. PARTIAL 2026-06-28: Remote verification for mood/energy plus beta-doctor repair head `3aa29b7` was manually dispatched as GitHub Actions run `28311224386`, but the job failed before executing any steps. Job/check-run `83876299726` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83876299726`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression. Fresh local verification passed mobile readiness 82/82, the 405-test behavior/readiness suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, and `git diff --check`.
413. NEXT: Continue with launch-risk work that does not require GitHub runners: real iOS/Android screenshots through `/care-twin-qa?qaSurface=...`, visible Home/Log/Health mockup accuracy, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix. Keep GitHub billing/spending-limit recovery, real provider setup, native beta approval, App Store/Play Store approval, generated PDF, AI/payment/push readiness, and owner visual approval as explicit gates.
414. DONE 2026-06-28: Made the exported beta preview path explicit and test-protected for Apollo/Fable/Replit handoff. Root `preview:mobile-beta` delegates to the mobile `preview:smoke` script; the mobile package exposes `preview:smoke` and `preview:web`, both pinned to `node scripts/serve-smoke-preview.js 4194`; the foreground static server now defaults to `http://127.0.0.1:4194/`, uses a root-relative path traversal guard, and tells reviewers to keep the terminal open. The beta doctor proof sequence now includes `pnpm --filter @workspace/woofwatcher-mobile run preview:smoke` after `smoke:web`, and the JSON next actions tell helpers to serve the exact `.expo-smoke` export before visual review. Red/green verification first failed on the missing preview command contract, then passed mobile readiness 83/83, the 406-test behavior/readiness suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200`, and `git diff --check` with expected Windows CRLF warnings only.
415. NEXT: Commit/push the static beta preview handoff and record the standing GitHub billing/spending-limit blocker if remote verify still fails before steps. After that, continue launch-risk work that does not require GitHub runners: real iOS/Android screenshots, visible Home/Log/Health mockup accuracy, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta/store/provider completion.
416. PARTIAL 2026-06-28: Remote verification for static beta preview handoff commit `21ce8b3` was manually dispatched as GitHub Actions run `28312278566`, but the job failed before executing any steps. Job/check-run `83879075992` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83879075992`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression. Fresh local verification passed mobile readiness 83/83, the 406-test behavior/readiness suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200`, and `git diff --check`.
417. NEXT: Continue launch-risk work that does not require GitHub runners: real iOS/Android screenshots through `/care-twin-qa?qaSurface=...`, visible Home/Log/Health mockup accuracy, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix. Keep GitHub billing/spending-limit recovery, real provider setup, native beta approval, App Store/Play Store approval, generated PDF, AI/payment/push readiness, and owner visual approval as explicit gates.
418. DONE 2026-06-28: Tightened Phoenix Home toward the locked mockups by moving the `Next Up` and `Quick Log` split above the richer navy care-RPG mission deck. Home now keeps the living Phoenix room and status first, then immediately exposes what is next and what can be logged, with the story/progress mission layer following as premium context rather than blocking the under-five-second care loop. `mobileReadiness.test.ts` now guards this screen order. Fresh local verification passed the 407-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
419. NEXT: Let Apollo visually review the refreshed `http://127.0.0.1:4194/` export, then continue the highest launch-risk work: real iOS/Android screenshots through `/care-twin-qa?qaSurface=...`, visible Home/Log/Health mockup accuracy, focused store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix. Keep GitHub billing/spending-limit recovery, real provider setup, native beta approval, App Store/Play Store approval, generated PDF, AI/payment/push readiness, and owner visual approval as explicit gates.
420. PARTIAL 2026-06-28: Remote verification for Home immediate-action reorder commit `e83bf7c` was manually dispatched as GitHub Actions run `28312943812`, but the job failed before executing any steps. Job/check-run `83880810426` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83880810426`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression. Fresh local verification passed the 407-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check`.
421. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review while remote CI is blocked. Continue launch-risk work that does not require GitHub runners: real device screenshots, focused store-safe screenshot proof, visible Home/Log/Health mockup accuracy, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
422. DONE 2026-06-28: Removed a first-screen dead affordance by turning Home's Quick Log `Open` header label into a real thumb-safe route target. The Quick Log card now uses a `Pressable` accessory that opens `/log`, carries the `Open full Quick Log` accessibility label, uses shared inline hit slop, and keeps the 48px mobile touch-target contract through `quickHeaderAction`. Fresh local verification passed mobile readiness 85/85, the 408-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check`.
423. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue the highest launch-risk work that can be completed locally: visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
424. PARTIAL 2026-06-28: Remote verification for Home Quick Log header action commit `f6d66d1` was manually dispatched as GitHub Actions run `28313525292`, but the job failed before executing any steps. Job/check-run `83882396792` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83882396792`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression. Fresh local verification passed mobile readiness 85/85, the 408-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check`.
425. NEXT: Continue local launch polish while GitHub runners are blocked: visual owner review at `http://127.0.0.1:4194/`, real device screenshot proof through `/care-twin-qa?qaSurface=...`, visible Home/Log/Health mockup accuracy, provider setup truth, store-safe screenshot proof, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.

426. DONE 2026-06-28: Removed two more Home owner-preview dead affordances by turning `Recent activity / View all` and `Phoenix status / View full report` into real `HomeHeaderAction` route targets. Recent activity now opens `/log`, Phoenix status opens `/health`, both actions carry explicit accessibility labels, shared inline hit slop, and the 48px mobile touch-target contract through `homeHeaderAction`. Red/green verification first failed on the missing route targets, then passed mobile readiness 86/86. Fresh local verification passed the 409-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` after adding the bundled Node directory to `PATH` for the local shell, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check`.
427. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue launch-risk work that can be completed locally: visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
428. PARTIAL 2026-06-28: Remote verification for Home section-action polish commit `30ca6c5` was manually dispatched as GitHub Actions run `28314204282`, but the job failed before executing any steps. Job/check-run `83884302449` had `steps: []`, `runner_id: 0`, empty runner name, and `gh run view --log-failed` returned `log not found: 83884302449`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
429. NEXT: Keep local verification and the live exported preview as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
430. DONE 2026-06-28: Removed two Health Watch dead affordances by replacing static section action labels with a reusable `HealthHeaderAction`. `Health Snapshot / 7-day view` now returns owners to the top 7-day rhythm view, while `Pattern Board / Owner notes` opens Quick Log with the symptom/health note composer so owners can add observation evidence. Both actions use shared inline hit slop, the 48px touch-target contract, and explicit accessibility labels. Red/green verification first failed on the missing Health action contract, then passed mobile readiness 86/86. Fresh local verification passed the 409-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check`.
431. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue the highest local launch-risk work: visible Log/Health/Home mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
432. PARTIAL 2026-06-28: Remote verification for Health header action polish commit `5ed483a` was manually dispatched as GitHub Actions run `28314910163`, but the job failed before executing any steps. Job/check-run `83886240566` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83886240566`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
433. NEXT: Keep local verification and the live exported preview as the trustworthy evidence until GitHub runners can start again. Continue visible Log/Home/Health mockup accuracy, real iOS/Android screenshot proof, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
434. DONE 2026-06-28: Removed three Log dead affordances by converting passive section action labels into shared `BoardPill` accessories. `Choose care type / Fast tap`, `Today at a glance / N logged`, and `Find care logs / Filtered` now read as status/state instead of fake controls, while real navigation remains reserved for actual pressable actions. Red/green verification first failed on the old action-label contract, then passed mobile readiness 86/86. Fresh local verification passed the 409-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
435. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue the highest local launch-risk work: visible Log/Home/Health mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
436. PARTIAL 2026-06-28: Remote verification for Log status-pill polish commit `7c9d934` was manually dispatched as GitHub Actions run `28315508447`, but the job failed before executing any steps. Job/check-run `83887885078` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83887885078`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
437. NEXT: Keep local verification and the live exported preview as the trustworthy evidence until GitHub runners can start again. Continue visible Log/Home/Health mockup accuracy, real iOS/Android screenshot proof, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
438. DONE 2026-06-28: Removed another Home first-screen dead affordance by converting `Next Up / 1 of N` from a `BoardSectionHeader action` label into a shared `BoardPill` status accessory. Real Home header actions remain pressable route targets, while passive counts now read as state. Red/green verification first failed on the old action-label contract, then passed mobile readiness 86/86. Fresh local verification passed the 409-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript after rerunning with an absolute TypeScript path, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
439. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, store-safe screenshot proof, provider setup truth, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
440. PARTIAL 2026-06-28: Remote verification for Home Next Up status-pill commit `165ae80` was manually dispatched as GitHub Actions run `28315976464`, but the job failed before executing any steps. Job/check-run `83889217105` had `steps: []`, and `gh run view --log-failed` returned `log not found: 83889217105`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
441. NEXT: Keep local verification and the live exported preview as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
442. DONE 2026-06-28: Removed the remaining Records/Care Pass passive header action labels by converting Care Trends, Weight Trend, Mood Trend, Hydration, Walk Activity, Training Progress, Alone Time, Grooming Care, Potty Health, Care Pass, Report History, and Records Cabinet status text into shared `BoardPill` accessories. Real Records actions remain actual touch targets for sharing, previewing, printing, editing, and adding records. Red/green verification first failed on the old Records action-label contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke`, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
443. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
444. DONE 2026-06-28: Removed Premium route passive header action labels by converting `Why upgrade`, `Plans`, and `Launch entitlements` status text into shared `BoardPill` accessories. The monetization screen now communicates `signals`, `Checkout gated`, and `Current: Free` as state while leaving real actions for launch checklist/back navigation. Red/green verification first failed on the missing Premium pill contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` with 223 files, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
445. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records/Premium mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
446. DONE 2026-06-28: Removed Adventure Mode passive header action labels by converting `Next quest`, `Quest board`, `Care proof`, and `Memory shelf` status text into shared `BoardPill` accessories. Adventure now keeps quest readiness, quest count, proof count, and memory privacy/empty state as badges while real actions remain save memory, share summary, and back navigation. Red/green verification first failed on the missing Adventure pill contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` with 223 files, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
447. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records/Premium/Adventure mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
448. DONE 2026-06-28: Removed Avatar Studio passive header action labels by converting scan, template, customization, accessory, and emote state into shared `BoardPill` accessories. Generated mood review, Start/Configured scan state, live template count, Editable coat state, face marking, Fit map, and Phoenix pack/Starter mood state now read as badges while real actions remain gallery, camera, template selection, customization controls, mood preview, save, reset, and back navigation. Red/green verification first failed on the old Avatar Studio action-label contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` with 223 files, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
449. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records/Premium/Adventure/Avatar Studio mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
450. DONE 2026-06-28: Removed WoofGuide passive header action labels by converting `Quick questions / Tap to ask` and `Suggested actions / Owner reviewed` into shared `BoardPill` accessories. The assistant route now keeps those labels as status while real actions remain quick-question chips, suggested action rows, send controls, owner-review modal, and back navigation. Red/green verification first failed on the missing WoofGuide pill contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` with 223 files, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
451. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records/Premium/Adventure/Avatar Studio/WoofGuide mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, Setup/Privacy/Calendar passive-label cleanup, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
452. DONE 2026-06-28: Removed Privacy route passive header action labels by converting `Export summary / Local bundle`, `Attachment queue / N files`, `Support runbook / Launch gate`, and `Launch safety gates / N gates` into shared `BoardPill` accessories. The launch trust route now keeps those labels as status while real actions remain care-data export, deletion request, support profile edit, support runbook share, modal saves, and back navigation. Red/green verification first failed on the missing Privacy pill contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` with 223 files, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
453. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records/Premium/Adventure/Avatar Studio/WoofGuide/Privacy mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, Setup/Calendar/care-twin-QA passive-label cleanup, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
454. DONE 2026-06-28: Removed the remaining Setup, Plans, and Care Twin QA passive header action labels by converting setup progress, after-save review, upcoming event count, Reminder Center status, launch workflow proof state, store screenshot verdict, and device review matrix count into shared `BoardPill` accessories. The real actions remain save/finish-later, plan/routine editing and logging, reminder rows, QA proof attachment, QA/store packet sharing, review surfaces, and back navigation. Red/green verification first failed on the missing setup/plans/QA pill contracts, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, PixelLab asset verification at 149 files, package-local Expo web export to `.expo-smoke` with 223 files, foreground preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
455. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Records/Premium/Adventure/Avatar Studio/WoofGuide/Privacy/Setup/Plans/Care Twin QA mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
456. DONE 2026-06-28: Wired Home Quick Log tiles for the locked tap/long-press logging doctrine. Tapping still performs the fast default quick log, while long pressing Meal, Walk, Potty, Water, Training, Treat, or Play now routes to the typed Log detail flow through `/log?type=...`; the More tile keeps opening the full Quick Log surface. Shared `QuickActionTile` now carries `onLongPress`, `delayLongPress`, and accessibility hints so future quick-action grids can reuse the same fast/detailed split. Red/green verification first failed on the missing long-press contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, preview root `HEAD 200` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
457. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or a real in-place bottom-sheet composer pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
458. DONE 2026-06-28: Tightened Home long-press quick logging into a true compact-detail handoff. Home now sends `/log?type=...&detail=1&intent=...`; Log reads `type`, `detail`, and `intent`, selects the matching launcher action, and opens the existing bottom-sheet launcher detail flow while ignoring stale already-consumed intents. This keeps Home fast while reusing the real Log meal lifecycle, potty, medication, trust, sticky-note, edit, and audit systems. Red/green verification first failed on the missing detail-intent route contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview root `HEAD 200`, detail route `HEAD 200` at `/log?type=meal&detail=1&intent=smoke`, and `git diff --check` with expected Windows CRLF warnings only.
459. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
460. DONE 2026-06-28: Added Home quick-log recovery actions so fast taps are safer for real household use. A successful Home quick log now keeps a longer feedback toast with real `Undo` and `Add details` buttons; Undo deletes the just-created local care entry, while Add details routes to `/log?entry=...` and Log opens the saved entry detail sheet for that exact event. Log now consumes `entry` route params once and ignores stale already-consumed entry params. Red/green verification first failed on the missing Home undo/detail and Log entry-route contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview root `HEAD 200`, entry route `HEAD 200` at `/log?entry=smoke`, and `git diff --check` with expected Windows CRLF warnings only.
461. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
462. DONE 2026-06-28: Made Home pending-meal open loops route to the exact saved meal entry. `Next Up` and the care-RPG `My Care Today` mission now use `/log?entry=...` when a served/grazing meal has an unresolved outcome, while active walk/alone loops still route to Log and ordinary scheduled care routes to Plans. This keeps "Dinner served - outcome pending" attached to the same household record for outcome updates, sticky notes, trust review, and audit history instead of opening a generic Meal composer. Red/green verification first failed on the missing exact-entry route contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files after adding bundled Node to `PATH`, preview root `HEAD 200`, entry route `HEAD 200` at `/log?entry=smoke`, and `git diff --check` with expected Windows CRLF warnings only.
463. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
464. DONE 2026-06-28: Made Home Recent Activity rows open the exact saved care log instead of acting as passive summaries. The Home recent activity view model now keeps `entry.id`, uses it as the stable row key, and routes row taps to `/log?entry=...` so owners land on the real log detail sheet for sticky notes, outcome updates, corrections, trust review, and audit history. `CareRow` now accepts explicit accessibility labels for route-backed rows. Fresh local verification passed mobile readiness 87/87, the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, and package-local Expo web export to `.expo-smoke` with 223 files.
465. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
466. DONE 2026-06-28: Made Home's first-screen HUD status tiles route-backed controls instead of static display boxes. `Happiness` opens the Mood detail sheet, `Energy` opens Health Watch, `Hunger` opens More with Diet Profile expanded via `/more?section=diet`, and `Bond` opens the Play detail sheet. More now reads the `section=diet` route param and expands Diet Profile without claiming a standalone Diet route. Red verification first failed on the missing HUD route contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, `tsc --build`, mobile TypeScript, and package-local Expo web export to `.expo-smoke` with 223 files.
467. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
468. DONE 2026-06-28: Made Health Snapshot cards route-backed care actions instead of static signal tiles. Activity opens the Walk detail sheet, Appetite opens Meal detail, Stool opens Potty detail, Hydration opens Water detail, Energy opens Mood detail, and Vomiting opens the Symptom/Vomit detail path through `/log?type=...&detail=1&intent=...`. The Health hero `Log health note` and Pattern Board `Owner notes` actions now use the same detail-intent route. Red/green verification first failed on the missing Health Snapshot route contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/health`, and `/log?type=water&detail=1&intent=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
469. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
470. DONE 2026-06-28: Tightened Plans Reminder Center routing so medication and grooming reminder rows now open the typed compact Log detail flow through `/log?type=...&detail=1&intent=...` instead of landing on a generic Log screen. Routine reminders still open the routine editor, and record reminders still open Records. Red/green verification first failed on the missing Plans reminder detail-intent route contract, then passed mobile readiness 87/87. Fresh local verification passed the 410-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/calendar`, and `/log?type=grooming&detail=1&intent=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
471. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
472. DONE 2026-06-28: Made Plans routine quick logging recoverable and editable. The Daily Routine `Log done` button now captures the created care entry id, shows a bottom-safe feedback bar with real `Undo` and `Add details` actions, deletes the exact entry on Undo, and routes Add Details to `/log?entry=...` so owners edit the real source log instead of recreating care evidence. Red/green verification first failed on the missing recoverable Plans routine logging contract, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/calendar`, and `/log?entry=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
473. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans mockup accuracy, tighter first-screen app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
474. DONE 2026-06-28: Removed mock-era Avatar Studio language from the PixelLab care-twin path. The scan suggestion helper is now named `buildTemplateScanSuggestion`, the working scan badge says `PixelLab template match`, and the future-scanning note now says provider scanning can plug in later while the current build ships the reliable PixelLab template matcher, character creator, and emote-preview system first. Red/green verification first failed on the missing truthful Avatar Studio copy contract, then passed mobile readiness 88/88. Fresh local verification passed focused Avatar Studio/mobile readiness 96/96, the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/` and `/portrait`, and `git diff --check` with only expected Windows CRLF warnings.
475. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Avatar Studio mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
476. DONE 2026-06-28: Made Adventure Mode quests operational instead of passive. Shared `AdventureQuest` data now carries action contracts and labels; Adventure rows can start a walk session, log training/play proof, save a memory, or open the exact completed proof log. Newly-created quest evidence shows a bottom-safe recovery panel with real `Undo` and `Add details`, where Undo deletes the exact care entry and Add Details routes to `/log?entry=...` for notes, sticky notes, corrections, trust review, and audit history. Red/green verification first failed on missing quest action contracts, then passed focused adventure/mobile readiness 91/91. Fresh local verification passed the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/adventure`, and `/log?entry=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
477. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
478. DONE 2026-06-28: Fixed Adventure Mode's top `Next quest` CTA so it now matches and runs the actual next quest instead of always saying `Save Memory`. The primary button derives proof for the current quest, shows `Start walk`, `Log training`, `Log play`, `Save memory`, `Open proof`, or `Locked` as appropriate, and calls the same `startQuest(availableQuest, availableQuestProofEntryId)` path as Quest Board rows. Red/green verification first failed on the missing action-aware CTA contract, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/adventure`, and `/log?entry=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
479. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
480. DONE 2026-06-28: Made Adventure Mode `Care proof` rows open the exact saved care log instead of acting as passive XP summaries. Each completed proof row is now a pressable, haptic, accessible 48px mobile target with `Open Adventure proof log: ...` labeling, a chevron affordance, and `/log?entry=...` routing so owners can inspect or edit the source evidence for sticky notes, outcome updates, corrections, trust review, and audit history. Red/green verification first failed on the missing proof-route and touch-target contracts, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/adventure`, and `/log?entry=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
481. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
482. DONE 2026-06-28: Tightened Adventure Quest Board row copy so the visible action button uses the real quest action label instead of the generic `Start quest` fallback. Available rows now surface specific owner actions such as `Start walk`, `Log training`, `Log play`, or `Save memory`, while complete rows still show `Open proof` and locked rows show `Locked`. Red/green verification first failed on the old generic label contract, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/adventure`, and `/log?entry=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
483. NEXT: Keep using the live exported preview at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, or CI completion.
484. DONE 2026-06-29: Made Adventure Mode `Memory shelf` rows actionable instead of decorative. Saved memories now render as 48px pressable rows with `Share Adventure memory: ...` accessibility labels, a visible `Share` affordance, haptic feedback, and a private share sheet summary containing the memory title, note, humans, XP, local storage/media status, and truthful provider-gated photo/sync boundary. Red/green verification first failed on the missing memory-share and touch-target contracts, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, and preview route smoke for `/`, `/adventure`, and `/log?entry=smoke`.
485. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
486. DONE 2026-06-29: Polished Avatar Studio customization controls so the creator feels more like a finished mobile product. Studio tabs, coat swatches, face-marking chips, mood previews, Reset, and Save now use explicit named handlers, haptic selection feedback, shared hit slop, screen-reader labels, and action hints while preserving the PixelLab template/config data model. Red/green verification first failed on the missing creator-control contract, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, root TypeScript, mobile TypeScript, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/` and `/portrait`, and `git diff --check` with only expected Windows CRLF warnings.
487. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
488. DONE 2026-06-29: Made Care Intelligence open-loop actions source-backed instead of generic. `deriveCareIntelligence` now carries `targetEntryId` and `targetRoutineId` on open loops and next actions for failed sync, pending meals, and due/overdue routines. More's `Care Intelligence` next-action button now uses a single `openCareIntelligenceNextAction` router so pending meal outcomes open the exact `/log?entry=...` record for outcome updates, sticky notes, corrections, trust review, and audit history, while sync retry and routine actions keep their proper workflows. Red/green verification first failed on the missing target ids and exact route helper, then passed focused Care Intelligence tests 2/2 and mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, and preview route smoke for `/`, `/more`, `/log?entry=dinner-1`, and `/care-twin-qa?qaSurface=home`. The beta doctor remains blocked in this Windows shell because the available `pnpm` is `11.7.0` while the pinned launch path requires `10.24.0`. Remote verify run `28344966240` failed before job execution with GitHub's billing/spending-limit annotation and no useful app verification.
489. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, first-screen Care Intelligence/Open Loop polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
490. DONE 2026-06-29: Made Home's first-screen Care Intelligence panel actionable instead of score-only. The Home `Care quest` card now includes a `Next care move` CTA that follows the shared Care Intelligence next-action contract: failed sync retries the care document refresh, pending meal/low-confidence open loops route to the exact `/log?entry=...` record, due routines route to Plans, and generic care actions route to Log. The control uses a pixel HUD icon, concise CTA label, explicit accessibility label/hint, haptic feedback, and the shared 48px mobile touch-target contract. Red/green verification first failed on the missing Home next-action route contract, then passed mobile readiness 88/88. Fresh local verification passed the 411-test mobile/domain behavior suite, root TypeScript, mobile TypeScript via the absolute TypeScript CLI path, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/log?entry=dinner-1`, and `/care-twin-qa?qaSurface=home`, and `git diff --check` with only expected Windows CRLF warnings.
491. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
492. DONE 2026-06-29: Made Home `Today at a glance` metrics route-backed instead of decorative. Activity now opens the Walk detail flow, Meals opens Meal detail for portions/outcomes, and Potty opens the Potty parent detail flow for pee, poop, accidents, and notes through `/log?type=...&detail=1&intent=...`. The three metric cells are pressable, accessible, haptic 48px mobile targets with typed route mapping protected by readiness tests. Red/green verification first failed on the missing Today metric route/touch-target contract, then passed mobile readiness 89/89. Fresh local verification passed the 412-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, and preview route smoke for `/`, `/log?type=walk&detail=1&intent=smoke`, `/log?type=meal&detail=1&intent=smoke`, and `/log?type=potty&detail=1&intent=smoke`.
493. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, exact-log/open-loop routing from any remaining first-screen cards, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
494. DONE 2026-06-29: Made the lower Home `Phoenix status` meter stack actionable instead of a passive duplicate HUD. The shared `StatusMeter` component now supports optional pressable behavior with inline hit slop, accessibility labels/hints, and a 48px target only when a caller provides `onPress`. Home wires Energy and Bile Risk to Health Watch, Hunger to the Meal detail flow, Hydration to Water detail, and Bond to Play detail through the same detail-intent routing used by the rest of Home. Red/green verification first failed on the missing pressable meter contract, then passed mobile readiness 90/90. Fresh local verification passed the 413-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, and preview route smoke for `/`, `/health`, `/log?type=meal&detail=1&intent=smoke`, `/log?type=water&detail=1&intent=smoke`, and `/log?type=play&detail=1&intent=smoke`. Remote verify run `28347096325` failed before job execution with GitHub's billing/spending-limit annotation and no useful app verification.
495. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, exact-log/open-loop routing from any remaining visible cards, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
496. DONE 2026-06-29: Made Home's watch-card row deep-link into exact care workflows instead of broad decorative cards. `Health Watch` now opens the Health overview route, `Bile Watch` opens `/health?tab=bile` and Health honors that route param, and `Alone Time` opens the typed Alone Time detail-intent Log flow for leaving-home or return check-in behavior. The row now uses a typed `HomeWatchTarget` helper, haptics, inline hit slop, and accessibility hints for each card. Red/green verification first failed on the missing watch-card deep-link contract, then passed mobile readiness 91/91. Fresh local verification passed the 414-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/health?tab=bile`, and `/log?type=alone&detail=1&intent=smoke`, and `git diff --check` with only expected Windows CRLF warnings.
497. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, exact-log/open-loop routing from any remaining visible cards, real iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
498. DONE 2026-06-29: Made remaining Home health signals tab-specific instead of generic. Home's Health status tile and Energy meter now open `/health?tab=health`, Bile Risk opens `/health?tab=bile`, and the Home mission deck routes its Health Review mission to Bile Watch while stable health opens Health Overview. Red/green verification first failed on the old generic `/health` mission routes, then passed focused Home mission deck tests 2/2 and mobile readiness 92/92. Fresh local verification passed the 415-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, and preview route smoke for `/`, `/health?tab=health`, and `/health?tab=bile`.
499. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, exact-log/open-loop routing from any remaining visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
500. DONE 2026-06-29: Finished the remaining generic Health entry points from Home and Quick Log. Home's notification-style health shortcut and `View full report` Phoenix Status action now open `/health?tab=health`, and Quick Log's header Health action opens the same Health Overview tab. Red/green mobile readiness first failed on the broad `/health` contracts, then passed 92/92 after the route updates. Fresh local verification passed the 415-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, and preview route smoke for `/`, `/log`, `/health?tab=health`, and `/health?tab=bile`.
501. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, exact-log/open-loop routing from any remaining visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
502. DONE 2026-06-29: Made Home `Next Up` rows route to their own care workflow instead of reusing the first row's route. Active walks, home-alone sessions, and pending meals now open the exact source `/log?entry=...` when an entry id exists, with typed detail-sheet fallbacks for older imported/id-less rows. Routine rows stay in Plans, and starter fallback rows open the matching Walk, Meal, or Training detail composer through `/log?type=...&detail=1&intent=...`. Red/green mobile readiness first failed on the missing row-level route contract, then passed 92/92 after implementation; focused Home/readiness verification passed 94/94. Fresh local verification passed root TypeScript, mobile TypeScript, the 415-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, `git diff --check` with expected Windows CRLF warnings only, and preview route smoke for `/`, `/log?entry=dinner-1`, `/log?type=walk&detail=1&intent=123`, `/log?type=alone&detail=1&intent=123`, `/calendar`, `/health?tab=health`, and `/health?tab=bile`.
503. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, tighter app-store polish, exact-log/open-loop routing from any remaining visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
504. DONE 2026-06-29: Made the Home presence panel source-backed instead of a broad shortcut. When Phoenix is home alone or on an active walk, the panel now opens the exact source `/log?entry=...` record when available, with typed Alone Time or Walk detail-sheet fallback routes for older id-less imported sessions. When Phoenix is simply with a caregiver, it opens `/more?section=household`, and More now renders a `Household focus` card at the top so the route lands on care-team, household access, and Household Pulse context instead of a generic tools page. Red/green mobile readiness first failed on the missing presence-panel route contract, then passed 93/93 after implementation. Fresh local verification passed root TypeScript, mobile TypeScript, the 416-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, `git diff --check` with expected Windows CRLF warnings only, and preview route smoke for `/`, `/more?section=household`, `/log?entry=dinner-1`, `/log?type=walk&detail=1&intent=123`, `/log?type=alone&detail=1&intent=123`, `/health?tab=health`, and `/health?tab=bile`.
505. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from any visible cards, first native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
506. DONE 2026-06-29: Made Home's active-walk quick tap source-backed. If a walk is already running and the owner taps Walk from the Home Quick Log grid, Home now opens the exact active walk `/log?entry=...` record when the entry id exists, with a typed Walk detail-sheet fallback for older id-less/imported active sessions. This keeps the fast tap doctrine intact while preserving the real walk session for finishing, notes, route details, trust review, and audit history instead of dumping owners into a broad walk composer. Red/green mobile readiness first failed on the old `Walk already active` generic route, then passed 93/93 after implementation. Fresh local verification passed root TypeScript, mobile TypeScript, the 416-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, `git diff --check` with expected Windows CRLF warnings only, and preview route smoke for `/`, `/log?entry=dinner-1`, `/log?type=walk&detail=1&intent=123`, and `/more?section=household`.
507. NEXT: Keep the live exported preview running at `http://127.0.0.1:4194/` for Apollo visual review. Continue highest local launch-risk work: visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from any visible cards, first native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
508. PARTIAL 2026-06-29: Remote verification for active-walk quick-log commit `0e368fc` was manually dispatched as GitHub Actions run `28382937262` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84090077600` after the run completed in about six seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
509. NEXT: Keep local verification and the live exported preview as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
510. DONE 2026-06-29: Removed the legacy bare Meal route from the Home mission deck contract. `HomeMissionRoute` no longer permits `/log?type=meal`; mission rows must use exact log entries, typed detail-intent routes, Plans, Health tabs, Adventure, Records, or the full Log route. The mission deck unit test now uses `/log?type=meal&detail=1&intent=123`, and mobile readiness guards against reintroducing the old broad meal mission route. Red/green mobile readiness first failed on the old route allowance, then passed 93/93 after implementation. Fresh local verification passed Home mission deck tests 2/2, root TypeScript, mobile TypeScript, the 416-test mobile/domain behavior suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, `git diff --check` with expected Windows CRLF warnings only, and preview route smoke for `/`, `/log?type=meal&detail=1&intent=123`, `/adventure`, and `/health?tab=health`.
511. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
512. PARTIAL 2026-06-29: Remote verification for Home mission meal-route commit `ea969da` was manually dispatched as GitHub Actions run `28384327830` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84094921945` after the run completed in about seven seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
513. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
514. DONE 2026-06-29: Tightened the Native QA Incident Composer target so testers open the real detail-first incident flow instead of the old broad `/log?type=incident` route. The `/care-twin-qa?qaSurface=incident-composer` Open Route action now lands on `/log?type=incident&detail=1&intent=incident-composer`, which triggers Log's compact detail sheet for factual behavior-safety fields, trust/visibility, notes, and household review. Red/green verification first failed on the old shallow route, then passed `mobileReleaseQa.test.ts` 9/9 and mobile readiness 93/93. Fresh local verification passed the 417-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, `git diff --check` with expected Windows CRLF warnings only, and preview route smoke for `/`, `/care-twin-qa?qaSurface=incident-composer`, and `/log?type=incident&detail=1&intent=incident-composer`.
515. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
516. PARTIAL 2026-06-29: Remote verification for Incident Composer route commit `dd8db31` was manually dispatched as GitHub Actions run `28386277013` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84101633637` after the run completed in about five seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
517. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
518. DONE 2026-06-29: Made Records Incident Watch follow-up rows route to source-backed workflows instead of reopening a broad incident log. `Review latest` now opens `/log?entry=...` for the latest saved incident when available, `Log incident` and id-less fallbacks open `/log?type=incident&detail=1&intent=...`, and the trainer handoff still opens the Trainer Care Pass preview. Red/green mobile readiness first failed on the old `params: { type: "incident" }` route contract, then passed 93/93 after implementation. Fresh local verification passed the 417-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/records`, `/log?entry=dog-gate`, and `/log?type=incident&detail=1&intent=123`, and `git diff --check` with expected Windows CRLF warnings only.
519. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
520. PARTIAL 2026-06-29: Remote verification for Records Incident Watch follow-up commit `3fef8c1` was manually dispatched as GitHub Actions run `28387648585` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84106367436` after the run completed in about six seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
521. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
522. DONE 2026-06-29: Tightened Home safety-critical quick taps so detail-required care types open the structured detail composer instead of a broad `/log?type=...` route. Home now routes detail-required policies through `homeLogDetailRoute(policy.type, Date.now())`, preserving normalized safety types like medication, vomit, symptom, and incident so the real confirmation/detail sheet handles context, notes, proof, and household review. Red/green mobile readiness first failed on the old bare route branch, then passed 93/93 after implementation. Fresh local verification passed the 417-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/`, `/log?type=medication&detail=1&intent=123`, `/log?type=vomit&detail=1&intent=123`, and `/log?type=incident&detail=1&intent=123`, and `git diff --check` with expected Windows CRLF warnings only.
523. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
524. PARTIAL 2026-06-29: Remote verification for Home safety-critical quick-tap commit `c48c163` was manually dispatched as GitHub Actions run `28389127987` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84111370284` after the run completed in about ten seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
525. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
526. DONE 2026-06-29: Tightened the Health Review Packet primary action so `Log health detail` opens the detail-first symptom composer directly instead of carrying an old `/log` plus `params: { type: "symptom" }` action shape. The packet action route is now `/log?type=symptom&detail=1&intent=health-review`, and Health Watch routes `/log?...` packet actions directly while keeping WoofGuide prompt params for `Draft vet questions`. Red/green tests first failed on the old packet action and handler, then passed `healthReviewPacket.test.ts` 4/4 and mobile readiness 93/93 after implementation. Fresh local verification passed the 417-test mobile/domain behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, preview route smoke for `/health`, `/log?type=symptom&detail=1&intent=health-review`, and `/woofguide?prompt=health-review`, and `git diff --check` with expected Windows CRLF warnings only.
527. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
528. PARTIAL 2026-06-29: Remote verification for Health Review Packet detail-route commit `ddb6987` was manually dispatched as GitHub Actions run `28390628880` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84116536531` after the run completed in about five seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
529. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
530. DONE 2026-06-29: Made Today Command visible and source-backed on Phoenix Home. The tested `deriveTodayCommand` model now returns exact source-log routes for pending meal outcomes and handoff review, detail-first routes for missed meal, walk, and potty commands, and Home renders a compact `Today Command` control under the status tiles with a pixel icon, urgency color, concise CTA, haptics-compatible route target, and shared 48px mobile touch contract. Red/green verification first failed on the old broad `/log` Today Command routes and missing Home render, then passed `todayCommand.test.ts` 8/8 and mobile readiness 94/94 after implementation. Fresh local verification passed the 459-test mobile/domain/API/PWA suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets, preview route smoke for `/`, `/log?type=meal&detail=1&intent=today-command-meal`, `/log?type=walk&detail=1&intent=today-command-walk`, `/log?type=potty&detail=1&intent=today-command-potty`, and `/log?entry=training_1`, plus `git diff --check` with expected Windows CRLF warnings only.
531. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
532. PARTIAL 2026-06-29: Remote verification for Home Today Command commit `c735e2e` was manually dispatched as GitHub Actions run `28392240344` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. `gh run view --log-failed` returned `log not found: 84121968977` after the run completed in about six seconds. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
533. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, remaining exact-log/open-loop routing from visible cards, native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, provider setup truth, store-safe screenshots, production-scale accessory/emote packs, first Needs tune fix, or native QA proof capture without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
534. DONE 2026-06-29: Made Avatar Studio sprite-first so the care twin reads like a video-game avatar instead of a softened portrait. When a PixelLab live pack exists, the hero stage now renders a dedicated `avatar-studio-pixel-sprite-viewport` with the 256px sprite strip as the visible dog layer, removes the hidden still-image ghost layer, and labels the preview `PIXELLAB SPRITE`. All launch-template mood previews now keep an animated idle or walk loop instead of falling back to still art for hungry, anxious, sleepy, home-alone, or not-feeling-well states. Red/green verification first failed on the missing sprite viewport, then passed Avatar Studio/mobile readiness 109/109 after implementation. Fresh local verification passed the 459-test mobile/domain/API/PWA suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, and preview route smoke for `/`, `/portrait`, `/log?type=meal&detail=1&intent=smoke`, `/health?tab=health`, and `/more`.
535. NEXT: Keep the live exported preview at `http://127.0.0.1:4194/` as the review surface. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, first native iOS/Android screenshot proof through `/care-twin-qa?qaSurface=...`, remaining exact-log/open-loop routing, provider setup truth, store-safe screenshots, production-scale accessory/emote overlays, gait refinement after phone-size review, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
536. PARTIAL 2026-06-29: Remote verification for Avatar Studio sprite-first commit `5f2c4be` was manually dispatched as GitHub Actions run `28393907730` on branch `automation/premium-revenue-product-builder`, but the job failed before useful logs were available. Job `84127618580` completed with zero recorded steps, and `gh run view --log-failed` returned `log not found: 84127618580`. This matches the standing GitHub billing/spending-limit pre-job blocker rather than a local product regression.
537. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, native iOS/Android screenshot proof, provider setup truth, store-safe screenshots, production-scale accessory/emote overlays, gait refinement after phone-size review, or first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
538. DONE 2026-06-29: Wired Phoenix Home's living care-twin room to the Avatar Studio runtime instead of leaving saved template/accessory choices isolated in Studio. `avatarRoomRuntime.ts` now maps the saved Avatar Studio config into Home room behavior: Shepherd/Phoenix keeps the full Option B action sprite pack and receives fitted accessory overlay/underlay layers, while non-Shepherd launch templates use their live PixelLab idle/walk sprite packs instead of defaulting to the Phoenix action family. Home and Avatar Studio both pass the saved/draft avatar config into `LivingPhoenixRoom`, and readiness tests protect the runtime modes, Home prop wiring, template sprite player, and overlay layer test IDs. Fresh local verification passed mobile readiness 94/94, the 461-test mobile/domain/API/PWA behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, route smoke for `/`, `/portrait`, `/log?type=meal&detail=1&intent=smoke`, `/health?tab=health`, and `/more`, plus `git diff --check` with expected Windows CRLF warnings only.
539. NEXT: Keep the live exported preview at `http://127.0.0.1:4194/` as the local review surface. Continue visible Home/Log/Health/Plans/Adventure/Avatar Studio mockup accuracy, native iOS/Android screenshot proof, production-scale accessory/emote overlays for all templates, gait/crop refinement after phone-size review, provider setup truth, store-safe screenshots, or the first Needs tune fix without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
540. PARTIAL 2026-06-29: Remote verification for Home care-twin avatar-runtime commit `9c29788` was manually dispatched as GitHub Actions run `28397233907` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84139040035`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 84139040035`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
541. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue native iOS/Android screenshot proof, first visible Needs tune fix, production-scale accessory/emote overlays, gait/crop refinement after phone-size review, or provider setup truth without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
542. DONE 2026-06-29: Polished the Home room live HUD so the first screen visibly confirms the connected Avatar Studio care twin instead of always saying `PHOENIX ROOM`. `LivingPhoenixRoom` now derives a compact `PHOENIX TWIN`/`STUDIO RIG` title plus a clipped detail line from the active Avatar Studio runtime, showing the selected template and fitted add-on count when present. The live chip stays within a bounded width, preserves the room status panel, and readiness tests guard the new runtime label/detail contract. Fresh local verification passed mobile readiness 94/94, the 461-test mobile/domain/API/PWA suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 223 files, and route smoke for `/`, `/portrait`, `/log?type=meal&detail=1&intent=smoke`, `/health?tab=health`, and `/more`.
543. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue first visible Needs tune fix, native iOS/Android screenshot proof, production-scale accessory/emote overlays, gait/crop refinement after phone-size review, or provider setup truth without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
544. PARTIAL 2026-06-29: Remote verification for Home live-twin HUD commit `0b440d3` was manually dispatched as GitHub Actions run `28398344784` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84142884629`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 84142884629`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
545. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue first visible Needs tune fix, native iOS/Android screenshot proof, production-scale accessory/emote overlays, gait/crop refinement after phone-size review, or provider setup truth without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
546. DONE 2026-06-29: Made the Home care-twin dog target more game-like by keeping tap-to-react feedback and adding long-press to Avatar Studio through the shared `openAvatarStudio` handler. `LivingPhoenixRoom` now accepts `onLongPress` and an explicit accessibility hint, and Home wires the main dog plus the visible Studio button to the same route-backed Studio entry. Fresh local verification passed mobile readiness 94/94, the 461-test mobile/domain/API/PWA behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, route smoke for `/`, `/portrait`, `/log?type=meal&detail=1&intent=smoke`, `/health?tab=health`, and `/more`, plus `git diff --check` with expected Windows CRLF warnings only.
547. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, or provider setup truth without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
548. PARTIAL 2026-06-29: Remote verification for Home care-twin long-press commit `dbf56ff` was manually dispatched as GitHub Actions run `28399882587` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84148107633`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 84148107633`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
549. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, or store-safe screenshot proof without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
550. DONE 2026-06-29: Added explicit native-QA coverage for the Home dog long-press Studio handoff. `MOBILE_RELEASE_QA_SURFACES` now names long-press-to-Studio in Phoenix Home's device prompt, verification steps, pass criteria, failure escalation, required evidence, and the Owner Preview Core Loop route checklist. Red/green verification first failed on the missing QA coverage, then passed `mobileReleaseQa.test.ts` 10/10. Fresh local verification passed the 462-test behavior/readiness suite, root TypeScript, mobile TypeScript, Expo web export to `.expo-smoke` with 219 assets / 223 files, and route smoke for `/care-twin-qa?qaSurface=phoenix-home`, `/`, and `/portrait`.
551. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, or store-safe screenshot proof without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
552. PARTIAL 2026-06-29: Remote verification for native-QA long-press coverage commit `227bd37` was manually dispatched as GitHub Actions run `28400884728` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84151501310`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 84151501310`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
553. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, or store-safe screenshot proof without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
554. DONE 2026-06-29: Tightened Today Command health routing so first-screen health prompts go to the real Health Watch/Bile Watch workflows instead of the Records vault. `deriveTodayCommand` now treats both vomit and symptom logs as health signals, routes vomit/bile watch commands to `/health?tab=bile`, routes non-vomit symptoms to `/health?tab=health`, and keeps the language non-diagnostic. Red/green verification first failed on the old `/records` route and on symptoms being buried behind meal prompts, then passed `todayCommand.test.ts` 9/9 after implementation. Fresh local verification passed mobile readiness 94/94, the 463-test mobile/domain/API/PWA behavior suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, and route smoke for `/`, `/health?tab=health`, `/health?tab=bile`, and `/log?type=symptom&detail=1&intent=smoke`.
555. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another first-screen exact-route/Health/Log mockup-accuracy pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
556. PARTIAL 2026-06-29: Remote verification for Today Command health-routing commit `648d685` was manually dispatched as GitHub Actions run `28402608398` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84157380035`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased." `gh run view --log-failed` returned `log not found: 84157380035`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
557. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another first-screen exact-route/Health/Log mockup-accuracy pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
558. DONE 2026-06-29: Upgraded Adventure Mode from an abstract gradient utility page to a mockup-aligned pixel care-RPG stage. The Adventure hero now uses the existing Option B pixel room asset, hard-pixel rendering, live `walk-loop` SpriteSheetPlayer care twin, speech bubble tied to the available quest, and a bottom HUD for level, XP, and memories while preserving the real local quest/log/memory actions and provider-gated privacy copy. Red/green verification first failed on missing `ImageBackground`, `SpriteSheetPlayer`, `walk-loop` asset wiring, and pixel rendering, then passed mobile readiness 94/94 after implementation. Fresh local verification passed mobile TypeScript, the 463-test mobile/domain/API/PWA behavior suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, live preview route smoke for `/adventure`, and `git diff --check` with expected Windows CRLF warnings only.
559. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visual mockup accuracy on Home/Log/Health/Plans/Records/More, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
560. PARTIAL 2026-06-29: Remote verification for Adventure Mode pixel-stage commit `d4e0479` was manually dispatched as GitHub Actions run `28404391031` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84163369485`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings." `gh run view --log-failed` returned `log not found: 84163369485`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
561. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visual mockup accuracy on Home/Log/Health/Plans/Records/More, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
562. DONE 2026-06-29: Upgraded Health Watch with a living pixel care-twin stage instead of leaving the screen as only a form-like clinical board. The Health hero now uses the dogless `healthWatch` room asset, hard-pixel rendering, live `health-watch` SpriteSheetPlayer care twin, calm speech bubble, status chip, and score/bile HUD while preserving the non-diagnostic Health Watch/Bile Watch formulas, review packet actions, exact Log routes, and Records handoff. Red/green verification first failed on missing `ImageBackground`, `CARE_TWIN_ROOM_VARIANT_ASSETS.healthWatch`, `SpriteSheetPlayer`, `health-watch` asset wiring, and pixel rendering, then passed mobile readiness 94/94 after implementation. Fresh local verification passed mobile TypeScript, the 463-test mobile/domain/API/PWA behavior suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files including the health-watch room and strip, live preview route smoke for `/health?tab=health` and `/health?tab=bile`, and `git diff --check` with expected Windows CRLF warnings only.
563. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visual mockup accuracy on Home/Log/Plans/Records/More, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
564. PARTIAL 2026-06-29: Remote verification for Health Watch pixel-stage commit `83f4a32` was manually dispatched as GitHub Actions run `28405759745` on branch `automation/premium-revenue-product-builder`, but the job did not start. GitHub reported job `84167756433`, `Install, Test, Typecheck, Build`, with the annotation: "The job was not started because recent account payments have failed or your spending limit needs to be increased. Please check the 'Billing & plans' section in your settings." `gh run view --log-failed` returned `log not found: 84167756433`. This matches the standing GitHub billing/spending-limit blocker and is not evidence of a local app regression.
565. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visual mockup accuracy on Home/Log/Plans/Records/More, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
566. DONE 2026-06-29: Upgraded Plans from a plain schedule list into a mockup-aligned pixel command deck while preserving the real routine board, reminder routing, owner assignment, and routine quick-log recovery. The Plans route now renders the Option B day room through `ImageBackground`, hard-pixel rendering, a live `idle-breathe` SpriteSheetPlayer care twin, next-care speech, Done/Open counters, and signal bars tied to the schedule state. Red/green verification first failed on the missing pixel-stage contract, then passed mobile readiness 95/95 after implementation. Fresh local verification passed mobile TypeScript, the 464-test API/mobile/PWA/care-domain focused suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, live preview route smoke for `/calendar`, and `git diff --check` with expected Windows CRLF warnings only.
567. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visual mockup accuracy on Home/Log/Records/More, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
568. PARTIAL 2026-06-29: Remote verification for Plans command-deck commit `bb4a2e0` was manually dispatched as GitHub Actions run `28407281363` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84172491881`, `Install, Test, Typecheck, Build`, with `steps: []`, `runner_id: 0`, and `gh run view --log-failed` returned `log not found: 84172491881`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
569. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start again. Continue visual mockup accuracy on Home/Log/Records/More, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
570. DONE 2026-06-29: Upgraded Records from a dense vault-first route into a mockup-aligned pixel credential stage without weakening the serious records/reporting workflow. Records now renders the Option B day room through `ImageBackground`, hard-pixel rendering, a live `tail-wag` `SpriteSheetPlayer` care twin, Dog ID speech, a status chip, and a compact Saved/Ready/Alerts HUD derived from actual record vault coverage, credential readiness, missing critical records, and record reminders. The existing Dog ID sharing, printable credential source, Care Pass previews/history, record reminders, medication history, health/potty/training/alone-time/grooming/incident summaries, and add/delete record flows remain intact. Red/green verification first failed on the missing Records pixel-stage contract, then passed mobile readiness 96/96 after implementation. Fresh local verification passed mobile TypeScript, the 465-test API/mobile/PWA/care-domain focused suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, live preview route smoke for `/records`, and `git diff --check` with expected Windows CRLF warnings only.
571. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/records` as the trustworthy Records proof until GitHub runners can start again. Continue visual mockup accuracy on More/Log/Home refinements, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
572. PARTIAL 2026-06-29: Remote verification for Records credential-stage commit `d56cce7` was manually dispatched as GitHub Actions run `28408619053` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84176575777`, `Install, Test, Typecheck, Build`, with `steps: []`, `runner_id: 0`, and `gh run view --log-failed` returned `log not found: 84176575777`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
573. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/records` as the trustworthy Records proof until GitHub runners can start again. Continue visual mockup accuracy on More/Log/Home refinements, actual native iOS/Android long-press/haptics/crop/gait QA, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
574. DONE 2026-06-29: Upgraded More from a generic profile/tools landing page into a mockup-aligned pixel launch command hub while preserving the real launch, household, roster, provider, QA, and sync workflows underneath. More now opens with the dogless night room through `ImageBackground`, hard-pixel rendering, a live `idle-breathe` `SpriteSheetPlayer` care twin, `Launch Command Hub` speech, a readiness chip, and a compact Launch/QA/Sync/Roster HUD derived from actual release score, saved native-QA proof counts, sync dashboard state, and CareTwin roster counts. The hub action routes to the native QA cockpit when beta is QA-first, or shares the beta handoff packet when ready. Red/green verification first failed on the missing More pixel-stage contract, then passed mobile readiness 97/97 after implementation. Fresh local verification passed mobile TypeScript, the 466-test API/mobile/PWA/care-domain focused suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, live preview route smoke for `/more`, and `git diff --check` with expected Windows CRLF warnings only.
575. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/more` as the trustworthy More proof until GitHub runners can start again. Continue Log/Home refinements, native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
576. PARTIAL 2026-06-29: Remote verification for More launch command-hub commit `64a76e8` was manually dispatched as GitHub Actions run `28410138254` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84181160392`, `Install, Test, Typecheck, Build`, with `steps: []`, `runner_id: 0`, and `gh run view --log-failed` returned `log not found: 84181160392`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
577. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/more` as the trustworthy More proof until GitHub runners can start again. Continue Log/Home refinements, native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
578. DONE 2026-06-29: Upgraded Quick Log into a mockup-aligned pixel care console while preserving the real tap/hold/detail/edit logging workflow. Quick Log now opens with the Option B day room through `ImageBackground`, hard-pixel rendering, a live `ear-perk` `SpriteSheetPlayer` care twin, `Quick Care Console` speech, selected-action readiness, and a Today/Care IQ/Open/Sync HUD derived from actual log count, care intelligence, pending meal/open walk/open alone loops, and the durable sync outbox. The stage primary action quick-logs the selected launcher item or opens the detail-first sheet for safety-critical logs, so it remains connected to the existing meal lifecycle, potty parent/outcome model, trust review, sticky notes, and audit history. Red/green verification first failed on the missing Quick Log pixel-stage contract, then passed mobile readiness 98/98 after implementation. Fresh local verification passed mobile TypeScript, the 467-test API/mobile/PWA/care-domain focused suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 219 assets / 223 files, live preview route smoke for `/log`, and `git diff --check` with expected Windows CRLF warnings only.
579. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/log` as the trustworthy Quick Log proof until GitHub runners can start again. Continue Home/Log visual refinements, actual native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
580. PARTIAL 2026-06-29: Remote verification for Quick Log pixel-console commit `f882f31` was manually dispatched as GitHub Actions run `28411487836` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84185172515`, `Install, Test, Typecheck, Build`, with `steps: []`, and `gh run view --log-failed` returned `log not found: 84185172515`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
581. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/log` as the trustworthy Quick Log proof until GitHub runners can start again. Continue Home/Log visual refinements, actual native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
582. DONE 2026-06-30: Upgraded WoofGuide from a static chat intro into a mockup-aligned pixel guidance console while preserving the owner-reviewed assistant workflow and health boundary. WoofGuide now opens with the dogless night room through `ImageBackground`, hard-pixel rendering, a live `idle-breathe` `SpriteSheetPlayer` care twin, `WoofGuide Console` speech sourced from the current suggested action, an `Owner review` chip, a source-backed Actions/Review/Watch/Boundary HUD, and a `Not veterinary advice` footer action that asks the first quick question without pretending provider-backed AI is live. Red/green verification first failed on the missing WoofGuide pixel-stage contract, then passed mobile readiness 99/99 after implementation. Fresh local verification passed mobile TypeScript, the 468-test API/mobile/PWA/care-domain focused suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files, live preview route smoke for `/woofguide`, and `git diff --check` with expected Windows CRLF warnings only.
583. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/woofguide` as the trustworthy WoofGuide proof until GitHub runners can start again. Continue Home/Log visual refinements, actual native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
584. PARTIAL 2026-06-30: Remote verification for WoofGuide pixel-console commit `a4c05ea` was manually dispatched as GitHub Actions run `28412519162` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84188359018`, `Install, Test, Typecheck, Build`, with `steps: []`, and `gh run view --log-failed` returned `log not found: 84188359018`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
585. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/woofguide` as the trustworthy WoofGuide proof until GitHub runners can start again. Continue Home/Log visual refinements, actual native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
586. DONE 2026-06-30: Upgraded Premium from a separate gradient sales hero into a mockup-aligned pixel value console while preserving the truthful checkout gate. Premium now opens with the dogless Option B day room through `ImageBackground`, hard-pixel rendering, a live `celebrate-hop` `SpriteSheetPlayer` care twin, `Plus Value Console` speech tied to the recommended plan and locked entitlement, a `Checkout gated` chip, a Plan/Price/Signals/Gate HUD, and a launch-checklist CTA. Existing premium value signals, plan cards, launch entitlements, disabled-payment notice, and current Free-plan copy remain intact; no payment provider or checkout action was enabled. Red/green verification first failed on the missing Premium pixel-stage contract, then passed mobile readiness 100/100 after implementation. Fresh local verification passed mobile TypeScript, the 469-test API/mobile/PWA/care-domain focused suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files, live preview route smoke for `/premium`, and `git diff --check` with expected Windows CRLF warnings only.
587. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/premium` as the trustworthy Premium proof until GitHub runners can start again. Continue Home/Log visual refinements, actual native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
588. PARTIAL 2026-06-30: Remote verification for Premium pixel value-stage commit `2e53abb` was manually dispatched as GitHub Actions run `28413556876` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84191529946`, `Install, Test, Typecheck, Build`, with `steps: []`, and `gh run view --log-failed` returned `log not found: 84191529946`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
589. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/premium` as the trustworthy Premium proof until GitHub runners can start again. Continue Home/Log visual refinements, actual native iOS/Android QA proof capture, first visible Needs tune fix, production-scale accessory/emote overlays, provider setup truth, store-safe screenshot proof, or another exact-route/open-loop pass without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
590. DONE 2026-06-30: Added a focused launch-readiness next gate so the app now tells Apollo/builders the exact next launch move instead of only listing raw blockers. `deriveLaunchReadiness` now returns `nextGate` with owner-readable kind/action/label/detail/CTA for native QA, Needs tune fix briefs, local Expo/EAS or PixelLab foundations, provider setup, owner approval, and final store packet sharing. More renders this as a pressable `Next launch gate` card directly under Launch Readiness; the card routes to the QA cockpit, Shares the Needs tune fix brief, opens provider setup, privacy, Premium, WoofGuide, Avatar Studio, beta handoff, launch packet, or store packet depending on the truthful gate. Red/green verification first failed on missing `nextGate` and More UI hooks, then passed after implementation. Fresh local verification passed launch readiness tests 6/6, mobile readiness 100/100, mobile TypeScript, the 470-test API/mobile/PWA/care-domain suite, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files, and `git diff --check` with expected Windows CRLF warnings only.
591. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/more` as the trustworthy Launch Readiness proof until GitHub runners can start again. Use More's `Next launch gate` to drive the next action: native iOS/Android QA proof capture, first Needs tune fix, provider setup truth, beta handoff, or store packet prep. Continue Home/Log visual refinements, production-scale accessory/emote overlays, store-safe screenshot proof, and native phone-size sprite/crop/gait review without claiming native beta, provider upload, generated PDF, AI/payment/push readiness, store approval, CI completion, or native device QA completion.
592. PARTIAL 2026-06-30: Remote verification for Launch Readiness next-gate commit `d50abea` was manually dispatched as GitHub Actions run `28415068044` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84196124798`, `Install, Test, Typecheck, Build`, with `steps: []`, and `gh run view --log-failed` returned `log not found: 84196124798`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
593. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/more` as the trustworthy Launch Readiness proof until GitHub runners can start again. Use More's `Next launch gate` to drive the next action, and prioritize actual native iOS/Android QA proof capture or first visible Needs tune repair next.
594. DONE 2026-06-30: Added a source-backed Native QA `primaryMission` so the app no longer guesses from `nextTargets[0]` when deciding what Apollo or a device tester should do next. `buildMobileLaunchQaCapturePlan` now prioritizes the first Needs tune target, then Pass-pending-proof targets, then the Owner Preview Core Loop, then store screenshot proof, then the normal capture queue, and the shareable QA capture plan now includes the primary mission, action, missing proof, and done condition. More's Care Twin QA link, Native QA tile, Next launch gate action, command-hub CTA, Native QA Next Captures panel, and beta packet CTA all route to the same primary mission. `/care-twin-qa` now starts the 48-hour beta run from the same primary mission, labels it as the `Primary device mission`, and shows the mission done condition in the card. Red/green verification first failed on missing `primaryMission`, then passed after implementation.
595. DONE 2026-06-30: Fresh local verification for the Native QA primary-mission slice passed `mobileLaunchQaEvidence.test.ts` 19/19, mobile readiness 100/100, mobile TypeScript, the 473-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files, live preview route smoke `200` for `/` and `/care-twin-qa` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
596. PARTIAL 2026-06-30: `scripts/mobile-beta-doctor.mjs --json` remained blocked in this shell because Corepack is not on PATH and the available bundled pnpm is `11.7.0` while the repo is pinned to `pnpm@10.24.0`. The doctor still passed Node 24, package-manager pin, Windows-friendly preinstall guard, mobile package/export command wiring, Expo iOS/Android/web platform config, EAS iOS/Android profiles, PixelLab verifier presence, beta handoff proof sections, owner-preview proof wiring, `/care-twin-qa` proof flow, Needs tune fix brief wiring, provider-aware Care Pass storage, and truth boundaries. This is an environment dependency blocker, not evidence of an app regression.
597. NEXT: Use More's `Next launch gate` or `Native QA Next Captures > Primary mission` to run the Owner Preview Core Loop on actual iOS and Android: open Home, Log, Plans, Health, More, Adventure, Records, Avatar Studio, and Care Pass without dead ends; attach iOS Quick Log/Log proof and Android Launch Readiness proof in `/care-twin-qa`; save the required Mission note; confirm `Pass pending proof` clears in both `/care-twin-qa` and More; then continue store screenshot proof, 12-state care-twin matrix review, or the first visible Needs tune repair. Before claiming beta export readiness, run `corepack prepare pnpm@10.24.0 --activate` or use an environment with pnpm 10.24.0, then rerun `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, and the mobile web export/preview commands.
598. PARTIAL 2026-06-30: Remote verification for Native QA primary-mission commit `2d0594d` was manually dispatched as GitHub Actions run `28416512265` on branch `automation/premium-revenue-product-builder`, but the job failed before execution. GitHub reported job `84200466442`, `Install, Test, Typecheck, Build`, with `steps: []`, and `gh run view --log-failed` returned `log not found: 84200466442`. This matches the standing GitHub billing/spending-limit pre-job blocker and is not evidence of a local app regression.
599. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy proof for the Native QA primary-mission branch state until GitHub runners can start again. Continue actual native iOS/Android primary-mission proof capture, pnpm 10.24.0 beta export doctor proof, store screenshot proof, or first visible Needs tune repair without claiming CI, native QA, provider setup, or store approval is complete.
600. DONE 2026-06-30: Tightened the Owner Preview Core Loop so Adventure Mode is a required part of the real beta owner journey instead of an implied secondary surface. `MOBILE_RELEASE_QA_SURFACES` now tells device testers to open Adventure Mode from More or Home, confirm private care quests, proof rows, and the memory shelf are reachable, and includes `/adventure` in the ordered owner route checklist before Records, Avatar Studio, and Care Pass. The capture-plan primary mission, share script, beta handoff fixture, and two-day beta ship plan now carry the same Adventure-aware route loop without claiming public maps, cloud sharing, native proof, or provider-backed storage.
601. DONE 2026-06-30: Fresh focused verification for the Adventure-aware owner-preview QA contract passed `mobileReleaseQa.test.ts` 10/10, `mobileLaunchQaEvidence.test.ts` 19/19, and `betaHandoffPacket.test.ts` 2/2 after the expected red test first caught the old route checklist omission.
602. DONE 2026-06-30: Fresh local closeout verification passed mobile readiness 100/100, launch readiness 6/6, release packet 5/5, store submission packet 3/3, mobile TypeScript, root TypeScript, the 473-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa`, `/adventure`, and `/log` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
603. PARTIAL 2026-06-30: `scripts/mobile-beta-doctor.mjs --json` remains truthfully `BLOCKED` because Corepack is not on PATH and the available bundled pnpm is `11.7.0` while the repo pins `pnpm@10.24.0`. The same doctor passed Node 24, package-manager pin, Windows-friendly preinstall, mobile package/export wiring, Expo iOS/Android/web platform config, EAS iOS/Android profiles, PixelLab verifier presence, beta handoff proof sections, owner-preview proof wiring, `/care-twin-qa` proof flow, Needs tune fix brief wiring, provider-aware Care Pass storage, Owner Preview storage proof, and truth boundaries. This is dependency-environment proof, not an app regression.
604. PARTIAL 2026-06-30: Remote verification for Owner Preview Adventure QA contract commit `f0810be` was manually dispatched as GitHub Actions run `28418527482`, but job `84206610368` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84206610368`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
605. NEXT: Keep the external gate unchanged: run the Owner Preview Core Loop on actual iOS and Android through `/care-twin-qa`, including Home, Log, Plans, Health, More, Adventure, Records, Avatar Studio, and Care Pass, attach iOS Quick Log/Log proof plus Android Launch Readiness proof, save the required Mission note, and confirm `Pass pending proof` clears only after required proof is saved. Before claiming beta export readiness, use Corepack/Replit/WSL or another environment with exact `pnpm@10.24.0`, then rerun `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, and mobile export/preview proof commands.
606. DONE 2026-06-30: Made the Owner Preview Core Loop rows inside `/care-twin-qa` actionable instead of passive checklist text. Each route-loop row is now a 48px pressable target with haptic selection on native, an owner-readable accessibility label, an open-route affordance, and `buildQaReturnRoute` params that return testers to the same QA mission after opening Home, Log, Plans, Health, More, Adventure, Records, Avatar Studio, or Care Pass. This keeps the required iOS/Android owner-preview proof loop navigable from one cockpit instead of forcing testers to hunt routes manually.
607. DONE 2026-06-30: Fresh local verification for the actionable route-loop slice passed the expected red/green readiness check, mobile readiness 100/100, mobile TypeScript, root TypeScript, the 473-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa`, and `/adventure` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
608. PARTIAL 2026-06-30: The in-app browser automation bridge timed out twice while attaching to the Codex webview, so the app was not browser-navigated by automation in this run. The exported preview server itself is live and route-smoke verified at `http://127.0.0.1:4194/`; reload or open that URL manually if the in-app browser tab still shows an old state.
609. NEXT: Keep the external launch gate unchanged, but use the now-actionable `/care-twin-qa` route loop to run the Owner Preview Core Loop faster on real iOS and Android. Tap each route row from the QA card, attach iOS Quick Log/Log proof plus Android Launch Readiness proof, save the required Mission note, confirm `Pass pending proof` clears in both `/care-twin-qa` and More, then continue store screenshot proof, first Needs tune repair, or exact `pnpm@10.24.0` beta doctor proof without claiming native QA, CI, provider setup, app-store approval, generated PDF, payment, push, or live AI readiness.
610. PARTIAL 2026-06-30: Remote verification for route-loop actionability commit `d2e4d80` was manually dispatched as GitHub Actions run `28419827821`, but job `84210404098` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84210404098`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
611. NEXT: Keep using local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start. The next product move remains real iOS/Android Owner Preview Core Loop proof, exact pnpm 10.24 beta doctor proof, store screenshot proof, or the first visible Needs tune repair.
612. DONE 2026-06-30: Made Store Screenshot QA in `/care-twin-qa` route-backed and actionable instead of only listing static screenshot requirements. `buildStoreSubmissionScreenshotQaSurfaces` now adds a store route-check row to every screenshot surface with the exact route, expected store-safe framing, proof requirement, and blocked-gate wording. `/care-twin-qa` renders those rows as 48px pressable targets with haptics, accessibility labels, open-route affordances, and QA return params, so testers can open Avatar Studio, Privacy/Launch Gates, Health Watch, Home, Log, Plans, Records, or the fallback QA route directly from the store packet card and return to attach iOS/Android proof.
613. DONE 2026-06-30: Fresh local verification for the Store Screenshot QA route-check slice passed the expected red/green tests, `mobileReleaseQa.test.ts` 11/11, mobile readiness 100/100, mobile TypeScript, root TypeScript, the 474-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa`, `/privacy`, `/portrait`, and `/health` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
614. NEXT: Keep the launch gate truthful: use `/care-twin-qa` Store Screenshot QA route rows to capture real iOS and Android screenshots, attach them as evidence, and write store notes without claiming provider setup, live AI, payment, push, app-store approval, or native screenshot proof until those artifacts exist. The next highest-impact moves remain real iOS/Android Owner Preview Core Loop proof, exact `pnpm@10.24.0` beta doctor proof from a dependency-complete environment, store screenshot proof capture, or the first visible Needs tune repair.
615. PARTIAL 2026-06-30: Remote verification for Store Screenshot QA route-check commit `53637e4` was manually dispatched as GitHub Actions run `28421032998`, but job `84214027680` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84214027680`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
616. DONE 2026-06-30: Added scenario-level native-proof gating to the Care Twin QA matrix so a care-twin state can no longer look launch-proven just because it is marked Pass. `summarizeCareTwinQaReviews` now separates total passes, native-proof passes, and `pass pending proof`; `careTwinQaReviewStatusLabel` renders `Pass pending proof` until at least one iOS or Android screenshot is attached for that state; the share report lists the same proof gap; and `/care-twin-qa` shows a `Pass pending native proof` gate directly on each scenario card. This preserves the 12-state pixel avatar/game-feel review path while keeping the native screenshot boundary honest.
617. DONE 2026-06-30: Fresh local verification for the scenario-level Care Twin QA proof gate passed the focused `careTwinQaReport.test.ts` 4/4, mobile readiness 100/100, mobile TypeScript, root TypeScript, the 475-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa`, `/health`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
618. NEXT: Keep the launch gate truthful: run the 12-state Care Twin QA matrix and Owner Preview Core Loop on actual iOS and Android, attach native screenshots to clear each `Pass pending proof` state, then share the QA report. The other highest-impact gates remain exact `pnpm@10.24.0` beta doctor proof from a dependency-complete environment, store screenshot proof capture, first visible Needs tune repair, and provider setup truth without claiming CI, native QA, app-store approval, generated PDF, payment, push, live AI, or provider-backed storage until those artifacts exist.
619. PARTIAL 2026-06-30: Remote verification for scenario-level Care Twin QA proof-gate commit `a7f5933` was manually dispatched as GitHub Actions run `28422240814`, but job `84217558788` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84217558788`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
620. DONE 2026-06-30: Extended proof-gated pass labels from the 12-state care-twin matrix to every Mobile Release QA and Store Screenshot QA surface. `mobileReleaseQa.ts` now separates total passes, proof-backed passes, and `pass pending proof`; `mobileReleaseQaReviewStatusLabel` renders `Pass pending proof` until each surface has its required iOS screenshot, Android screenshot, flexible screenshot, and required QA note evidence; the share report lists missing proof under each affected surface; and `/care-twin-qa` shows an amber `Pass pending release proof` gate on release/store cards instead of letting missing-proof passes look green.
621. DONE 2026-06-30: Fresh local verification for the release-surface proof gate passed `mobileReleaseQa.test.ts` 12/12, mobile readiness 100/100, mobile TypeScript, root TypeScript, the 476-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa`, `/health`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
622. NEXT: Keep the external launch gate unchanged but harder to overclaim: run the Owner Preview Core Loop, Store Screenshot QA, and 12-state Care Twin QA matrix on actual iOS and Android, attach the required screenshots and QA notes in `/care-twin-qa`, and only treat a surface as proof-backed when `Pass pending proof` clears. Continue exact `pnpm@10.24.0` beta doctor proof, first visible Needs tune repair, provider setup truth, store screenshot capture, and native phone-size sprite/crop/gait review without claiming CI, native QA, app-store approval, generated PDF, payment, push, live AI, or provider-backed storage until those artifacts exist.
623. PARTIAL 2026-06-30: Remote verification for release-surface proof-gate commit `9a507b1` was manually dispatched as GitHub Actions run `28423666264`, but job `84221815527` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84221815527`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
624. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy proof until GitHub runners can start. The next product move remains real iOS/Android release/store/care-twin screenshot proof, exact pnpm 10.24 beta doctor proof, first visible Needs tune repair, or provider setup truth without claiming CI, native QA, provider-backed storage, payment, push, live AI, or store approval.
625. DONE 2026-06-30: Hardened the mobile beta doctor so the release/store proof gate cannot regress silently during native-helper or Replit/Fable handoff. `scripts/mobile-beta-doctor.mjs --json` now emits `release QA proof gate is source-backed` only when `mobileReleaseQa.ts` still separates proof-backed passes from `pass pending proof`, keeps `mobileReleaseQaMissingEvidenceForSurface`, `mobileReleaseQaReviewStatusLabel`, `Pass pending proof`, and `Missing proof:` share-report language, and `/care-twin-qa` still renders the amber `Pass pending release proof` gate from the actual missing-evidence helper. The first focused readiness run failed on the missing doctor check, then passed after the doctor was patched.
626. DONE 2026-06-30: Fresh local verification for the beta-doctor release-proof guard passed mobile readiness 100/100, direct `scripts/mobile-beta-doctor.mjs --json` with the new source-backed check PASS while still truthfully `BLOCKED` only on pnpm `11.7.0` vs required `10.24.0`, mobile TypeScript, root TypeScript, the 476-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa`, `/health`, `/log`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
627. NEXT: Keep the proof gate honest and move to external evidence: run actual iOS/Android Owner Preview Core Loop, Store Screenshot QA, and the 12-state Care Twin QA matrix through `/care-twin-qa`, attach the required screenshots and QA notes, verify all `Pass pending proof` labels clear only after evidence exists, then use an environment with exact `pnpm@10.24.0` for `pnpm run doctor:mobile-beta`, `pnpm run doctor:mobile-beta:json`, install/export proof, and preview smoke. Do not claim CI, native QA, provider-backed storage, payment, push, live AI, app-store approval, generated PDF, or public launch readiness until those artifacts exist.
628. PARTIAL 2026-06-30: Remote verification for beta-doctor release-proof guard commit `178d91f` was manually dispatched as GitHub Actions run `28424879485`, but job `84225618354` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84225618354`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
629. NEXT: Keep using local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy proof until GitHub runners can start. The next product move remains real iOS/Android release/store/care-twin screenshot proof, exact pnpm 10.24 beta doctor proof, first visible Needs tune repair, or provider setup truth without claiming CI, native QA, provider-backed storage, payment, push, live AI, generated PDF, store approval, or public launch.
630. DONE 2026-06-30: Added a deterministic local QA proof manifest for `/care-twin-qa` so native-helper, Replit, Fable, and Apollo handoff can identify exactly which saved QA evidence set they are reviewing without pretending local metadata is native proof. `mobileQaSession.ts` now builds a stable `wwqa-*` proof ID from saved care-twin/release/store QA evidence, counts care-twin versus release reviews, platform evidence files, notes, passed/needs-tune/unreviewed states, and emits a share-text boundary that says the manifest does not prove App Store or Play Store approval, provider-backed storage, live AI, payments, push notifications, generated PDF output, or public launch readiness. `/care-twin-qa` now surfaces the proof ID, evidence count, platform summary, and prepends the manifest summary to the share report.
631. DONE 2026-06-30: Fresh local verification for the QA proof-manifest handoff passed `mobileQaSession.test.ts` 4/4, mobile readiness 100/100, mobile TypeScript, root TypeScript, the 477-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, and live preview route smoke `200` for `/`, `/care-twin-qa`, `/health`, `/log`, and `/portrait` at `http://127.0.0.1:4194/`.
632. PARTIAL 2026-06-30: `scripts/mobile-beta-doctor.mjs --json` still reports `BLOCKED` only because Corepack is not on PATH and the bundled pnpm is `11.7.0` while the repo is pinned to `pnpm@10.24.0`. The same doctor passed the source-backed proof checks for Node 24, package-manager pin, mobile export wiring, Expo iOS/Android/web config, EAS iOS/Android profiles, PixelLab verifier presence, beta handoff proof sections, owner-preview proof wiring, `/care-twin-qa` proof flow, release QA proof gate, Needs tune fix brief, provider-aware Care Pass storage, Owner Preview Care Pass storage proof, and truth boundaries.
633. NEXT: Keep the external launch gates unchanged: run actual iOS and Android Owner Preview Core Loop, Store Screenshot QA, and 12-state Care Twin QA matrix through `/care-twin-qa`; attach the required screenshots and QA notes; use the new proof manifest ID in handoff reports; confirm `Pass pending proof` clears only after evidence exists; then run exact `pnpm@10.24.0` beta doctor/export proof from Replit, WSL/Git Bash, Corepack, or CI after billing is fixed. Do not claim CI, native QA, provider-backed storage, generated PDF, payments, push, live AI, app-store approval, store launch, or public launch readiness until those artifacts exist.
634. PARTIAL 2026-06-30: Remote verification for QA proof-manifest handoff commit `34f9eca` was manually dispatched as GitHub Actions run `28426606118`, but job `84231097976` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84231097976`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
635. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start. Continue actual native iOS/Android proof capture, exact pnpm 10.24 beta doctor proof, first visible Needs tune repair, provider setup truth, or store screenshot proof without claiming CI, native QA, provider-backed storage, generated PDF, payments, push, live AI, app-store approval, store launch, or public launch readiness.
636. DONE 2026-06-30: Surfaced the saved QA proof manifest in More's Launch Readiness and Beta Handoff flow so the deterministic `wwqa-*` proof ID is no longer trapped inside `/care-twin-qa`. `buildBetaHandoffPacketShareText` now accepts an optional `MobileQaSessionProofManifest` and appends the manifest share text; More rebuilds the proof manifest from the saved local QA session, passes it into Share Beta Handoff, and shows a `Proof manifest` row in Native QA Next Captures with the proof ID, evidence file count, platform evidence label, and local-metadata-only boundary.
637. DONE 2026-06-30: Fresh local verification for the More/Beta Handoff proof-manifest surfacing passed `betaHandoffPacket.test.ts` 3/3, mobile readiness 100/100, mobile TypeScript, root TypeScript, the 478-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/more`, `/care-twin-qa`, `/health`, `/log`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
638. NEXT: Keep the beta handoff honest: Share Beta Handoff can now carry the saved proof manifest, but it still proves only local QA evidence metadata. Continue actual native iOS/Android proof capture through `/care-twin-qa`, exact pnpm 10.24 beta doctor proof, first visible Needs tune repair, provider setup truth, or store screenshot proof without claiming CI, native QA, provider-backed storage, generated PDF, payments, push, live AI, app-store approval, store launch, or public launch readiness.
639. PARTIAL 2026-06-30: Remote verification for More/Beta Handoff proof-manifest surfacing commit `a2701e6` was manually dispatched as GitHub Actions run `28428161588`, but job `84236133163` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84236133163`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
640. DONE 2026-06-30: Removed the passive `Proof manifest` dead-end in More. When a saved QA proof manifest exists, the Native QA Next Captures row is now a real accessible button that shares the beta handoff packet containing the manifest, with a share icon and owner-readable label tied to the current `wwqa-*` proof ID.
641. DONE 2026-06-30: Fresh local verification for the proof-manifest row action passed mobile readiness 100/100, mobile TypeScript, root TypeScript, the 478-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/more`, `/care-twin-qa`, `/health`, `/log`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
642. NEXT: Keep removing passive launch-cockpit rows while preserving truth boundaries. Highest-impact remaining gates are still actual native iOS/Android proof capture, exact pnpm 10.24 beta doctor proof, first visible Needs tune repair, provider setup truth, store screenshot proof, and native phone-size care-twin sprite/crop/gait review.
643. PARTIAL 2026-06-30: Remote verification for proof-manifest row action commit `8e7a611` was manually dispatched as GitHub Actions run `28429093154`, but job `84239132933` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84239132933`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
644. DONE 2026-06-30: Added the source-backed Avatar Sprite Production Review path for the native phone-size game-feel pass. `avatarSpriteProductionQa.ts` now derives a QA summary from the actual Avatar Studio templates and PixelLab sprite registry: 12/12 launch templates, 24 registered live template sprite slots, required checks for hard-pixel crispness, one visible dog, bottom-center anchor stability, idle/tail-wag motion, walk-loop gait, phone crop, and accessory overlay fit, plus a local-metadata-only boundary. `mobileReleaseQa.ts` now exposes that summary as a launch-critical `Avatar Sprite Production Review` card with route-backed proof rows to Avatar Studio and Care Twin State Lab, and the JSON beta doctor now checks `avatar sprite production review is source-backed`.
645. DONE 2026-06-30: Fresh local verification for the Avatar Sprite Production Review slice passed the intended red/green focused helper and release QA tests, focused doctor/readiness tests 114/114, the full 480-test API/mobile/PWA/care-domain focused suite, mobile TypeScript, root TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/care-twin-qa?qaSurface=avatar-sprite-production-review`, `/portrait`, `/more`, and `/health` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only. The JSON mobile beta doctor also reports the new avatar sprite review check as PASS while remaining truthfully BLOCKED on pnpm `11.7.0` versus the pinned `10.24.0`.
646. NEXT: Use the new `Avatar Sprite Production Review` focused card in `/care-twin-qa` to run the real native game-feel pass: open Avatar Studio, switch through Shepherd/Phoenix plus every live template, capture iOS and Android screenshots, write the gait/crop/overlay note, and mark the first weak gait, crop, duplicate-avatar, or accessory-fit issue as Needs tune. Continue exact pnpm 10.24 beta doctor proof, actual Owner Preview/Core Loop proof, store screenshot proof, provider setup truth, or first Needs tune repair without claiming CI, native QA, provider-backed storage, generated PDF, payments, push, live AI, app-store approval, or public launch readiness until those artifacts exist.
647. PARTIAL 2026-06-30: Remote verification for Avatar Sprite Production Review commit `67f5c08` was manually dispatched as GitHub Actions run `28464997946`, but job `84362376140` failed before execution with GitHub's billing/spending-limit annotation. `gh run view --log-failed` returned `log not found: 84362376140`. Treat this as the standing GitHub account/pre-job blocker, not as product verification evidence or a local app regression.
648. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy evidence until GitHub runners can start. The next product move remains actual iOS/Android Avatar Sprite Production Review proof, Owner Preview/Core Loop proof, exact pnpm 10.24 beta doctor proof, store screenshot proof, first visible Needs tune repair, or provider setup truth without claiming CI, native QA, provider-backed storage, generated PDF, payments, push, live AI, app-store approval, or public launch readiness.
649. DONE 2026-06-30: Polished the focused `/care-twin-qa?qaSurface=...` mission card so Avatar Sprite Production Review and other focused targets show a richer phone-ready checklist. Focused targets now expose up to four setup items, a six-item `Game-feel checklist`, and four pass criteria, instead of hiding most of the device review behind two-line previews.
650. DONE 2026-06-30: Fresh local verification for the focused QA checklist polish passed `mobileReadiness.test.ts` 100/100, mobile TypeScript, route smoke `200` for `/care-twin-qa?qaSurface=avatar-sprite-production-review`, and `git diff --check` with expected Windows CRLF warnings only.
651. PARTIAL 2026-06-30: Remote verification for focused QA checklist polish commit `de71616` was manually dispatched as GitHub Actions run `28466077400`, but job `84366081750` failed before execution with the standing GitHub billing/spending-limit annotation. `gh run view --log-failed` returned `log not found: 84366081750`. Treat this as an external account/pre-job blocker, not product verification evidence or a local regression.
652. DONE 2026-06-30: Surfaced the Avatar Sprite Production Review directly inside Avatar Studio so the PixelLab/game-feel review is no longer hidden behind release docs or the QA cockpit. `avatarSpriteProductionQa.ts` now builds a selected-template production review from the actual sprite registry, and `/portrait` renders a `Sprite production review` card with live-template counts, sprite-slot counts, body class, idle/walk frame and fps details, four game-feel checks, the local-metadata-only native proof boundary, and an accessible `Open sprite QA cockpit` route object to `/care-twin-qa` with `qaSurface=avatar-sprite-production-review`.
653. DONE 2026-06-30: Fresh local verification for the Avatar Studio sprite-review surface passed `avatarSpriteProductionQa.test.ts` 2/2, `mobileReadiness.test.ts` 100/100, mobile TypeScript, root TypeScript, the 481-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node and pnpm to PATH, live preview route smoke `200` for `/`, `/portrait`, and `/care-twin-qa?qaSurface=avatar-sprite-production-review` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only. The JSON mobile beta doctor remains truthfully BLOCKED only on pnpm `11.7.0` versus the pinned `10.24.0`; its source-backed release, proof, and avatar sprite checks still pass.
654. NEXT: Use Avatar Studio's new `Sprite production review` card on `/portrait` while running the real native game-feel pass. Switch through each live template, inspect the selected template's idle and walk loop, then open the focused QA cockpit from the card to attach iOS and Android proof. Continue exact pnpm 10.24 beta doctor proof, Owner Preview/Core Loop proof, store screenshot proof, first visible Needs tune repair, provider setup truth, and remaining PixelLab overlay/emote polish without claiming CI, native QA, provider-backed storage, generated PDF, payments, push, live AI, app-store approval, or public launch readiness until those artifacts exist.
655. PARTIAL 2026-06-30: Remote verification for Avatar Studio sprite-review surface commit `3c2778b` was manually dispatched as GitHub Actions run `28468154370`, but job `84373284710` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84373284710`. Treat this as the standing GitHub billing/spending-limit pre-job blocker, not product verification evidence or a local app regression.
656. DONE 2026-06-30: Polished the production auth entry into a truthful CareTwin gateway instead of a generic sign-in form. `AuthShell` now shows the WoofWatcher logo, PixelLab day room, hard-pixel Phoenix avatar, `Real care. Pixel heart.` account speech, provider/local-preview status, and three trust tiles for Provider account, Local-first care, and CareTwin readiness. Sign-in and sign-up copy now avoids fake cross-device sync claims by saying the account layer is ready while care data remains local-first until production sync providers are configured. Primary and Google auth actions now expose button roles and labels.
657. DONE 2026-06-30: Fresh local verification for the auth gateway polish passed `mobileReadiness.test.ts` 101/101, mobile TypeScript, root TypeScript, the 482-test API/mobile/PWA/care-domain focused suite, PixelLab assets `ok=149 missing=0 invalid=0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node and pnpm to PATH, live preview route smoke `200` for `/`, `/sign-in`, `/setup`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only. The JSON mobile beta doctor remains truthfully `BLOCKED` only on pnpm `11.7.0` versus pinned `10.24.0`; all source-backed launch, proof, avatar sprite, Care Pass storage, and truth-boundary checks still pass.
658. NEXT: Continue the launch-facing polish path without overclaiming: run real iOS/Android auth/setup route QA once provider keys exist, execute native Owner Preview/Core Loop proof, exact pnpm 10.24 beta doctor proof, store screenshot proof, first visible Needs tune repair, and provider setup truth. Do not claim CI, native QA, provider-backed account sync/storage, generated PDF, payments, push, live AI, app-store approval, or public launch readiness until those artifacts exist.
659. PARTIAL 2026-06-30: Remote verification for auth gateway polish commit `6a0319b` was manually dispatched as GitHub Actions run `28470515203`, but job `84381589343` failed before execution with `steps: []`. `gh run view --log-failed` returned `log not found: 84381589343`. Treat this as the standing GitHub billing/spending-limit pre-job blocker, not product verification evidence or a local app regression.
660. DONE 2026-06-30: Centralized return-aware launch QA target routes so Apollo, Fable/Replit, and native testers no longer receive raw route-only handoff scripts. `mobileLaunchQaEvidence.ts` now exposes `buildMobileLaunchQaReturnRoute`, adds `qaReturnRoute` to every `MobileLaunchQaCaptureTarget`, includes `Open with QA return` in focused target, capture plan, store screenshot, and Needs tune fix-brief share text, and `/care-twin-qa` now uses that shared helper for its open-target buttons instead of duplicating query-string logic. This keeps each target route carrying `qaReturn=care-twin-qa`, `qaSurface`, and `qaTitle` so testers can capture proof and return to the exact mission card.
661. DONE 2026-06-30: Fresh local verification for return-aware QA target routes passed the intended red/green `mobileLaunchQaEvidence.test.ts` 20/20, `mobileReadiness.test.ts` 101/101, the 483-test API/mobile/PWA/care-domain focused suite, root TypeScript, mobile TypeScript, PixelLab assets `ok=149 missing=0 invalid=0`, JSON mobile beta doctor source-backed checks while still truthfully `BLOCKED` only on local pnpm `11.7.0` versus pinned `10.24.0`, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node and pnpm to PATH, route smoke `200` for `/`, `/care-twin-qa?qaSurface=store-health-watch`, `/health?qaReturn=care-twin-qa&qaSurface=store-health-watch&qaTitle=Store%3A%20Health%20Watch`, and `/portrait` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
662. NEXT: Use the return-aware routes when handing off native QA: open the generated `Open with QA return` target, capture iOS/Android proof, tap `Return to QA Cockpit`, attach evidence, save notes, and mark Pass or Needs tune. Continue exact pnpm 10.24 beta doctor proof, real native Owner Preview/Core Loop proof, Store Screenshot QA proof, first Needs tune repair, provider setup truth, and remaining PixelLab sprite/gait/crop review without claiming CI, native QA, provider-backed storage/sync/AI/payments/push, generated PDF, app-store approval, store launch, or Apollo launch sign-off until those artifacts exist.
663. PARTIAL 2026-06-30: Remote verification for return-aware QA target routes commit `4a39d99` was manually dispatched as GitHub Actions run `28473097266`, but job `84390646083` failed before executing any steps with `steps: []`. `gh run view --log-failed` returned `log not found: 84390646083`. Treat this as the standing GitHub billing/runner pre-job blocker, not as product verification evidence or a local app regression.
664. NEXT: Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy proof until GitHub runners can start. The next unblocked product moves remain real iOS/Android proof capture through the return-aware `/care-twin-qa` routes, exact pnpm 10.24 beta doctor proof, Store Screenshot QA proof, first Needs tune repair, provider setup truth, and PixelLab sprite/gait/crop review.
665. DONE 2026-06-30: Started the mobile design-system recovery after Apollo rejected the current UI as crowded, overlapping, and confusing. The newest dark RPG board and Paw Friends simplicity reference are mirrored in `docs/design/reference/`, `docs/design/MOBILE_DESIGN_SYSTEM_RECOVERY_PLAN_2026-06-30.md` locks the new screen recipe, and Health Watch now uses one pixel stage, one compact care-status panel, scoped Health/Bile tab content, a row-based Health Snapshot, and a trimmed Review Packet instead of duplicate metric rails and cramped status grids.
666. DONE 2026-06-30: Fresh local verification for the Health Watch design-system recovery passed focused Avatar Studio/readiness tests 109/109, mobile TypeScript, and the 484-test API/mobile/PWA/care-domain focused suite. This is a design-system and local behavior proof only; it does not clear native iOS/Android visual QA, App Store/Play proof, provider-backed storage/sync/AI/payments/push, CI completion, generated PDF output, or Apollo launch sign-off.
667. PARTIAL 2026-06-30: Remote verification for Health Watch design-system recovery commit `f400c6c` was manually dispatched as GitHub Actions run `28477776271`, but the run failed before useful execution in 6 seconds. `gh run view --log-failed` returned `log not found: 84406620473`. Treat this as the standing GitHub runner/account pre-job blocker, not as product verification evidence or a local app regression.
668. NEXT: Continue design recovery route-by-route, starting with Quick Log tap/long-press hierarchy and detail-sheet polish, then Plans mission/responsibility layout, More grouped navigation, Records vault/credential scanability, and final Home dark-RPG alignment. Keep local verification and `http://127.0.0.1:4194/` preview as the working proof until GitHub runners and native devices are available.
669. DONE 2026-06-30: Completed the Quick Log design-system recovery slice without changing the care-event engine. The screen now has a tighter pixel command stage, a `Quick Log Flow` action console, action grid before the teaching rail, support metrics between launcher and detail composer, and the full rich composer preserved as a secondary detail dock for meal lifecycle, potty parent/outcomes, medication/vomit safety, sticky notes, trust review, and audit edits.
670. DONE 2026-06-30: Fresh local verification for the Quick Log design-system recovery passed mobile readiness 102/102, mobile TypeScript, the 485-test API/mobile/PWA/care-domain focused suite, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/health`, and `/log` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
671. NEXT: Continue design recovery route-by-route with Plans mission/responsibility layout, More grouped navigation, Records vault/credential scanability, then final Home dark-RPG alignment. Keep local verification and the live exported preview at `http://127.0.0.1:4194/` as the trustworthy proof until GitHub runners and native iOS/Android devices are available; do not claim CI, native QA, provider-backed storage/sync/AI/payments/push, generated PDF, app-store approval, store launch, or Apollo launch sign-off until those artifacts exist.
672. DONE 2026-06-30: Completed the Plans design-system recovery slice around a mission-first hierarchy. `/calendar` now connects the pixel Plans Command Deck to a new `Today's Missions` board that shows the next schedule item, household responsibility state, and the lead reminder before the detailed schedule. The schedule card now has a `Mission Schedule` header, open-count pill, and clearer tab/list anatomy while preserving routine edit, routine done logging, Reminder Center routing, Daily Routine owner loads, and household responsibility metrics.
673. DONE 2026-06-30: Fresh local verification for the Plans mission hierarchy passed mobile readiness 103/103, mobile TypeScript, the 486-test API/mobile/PWA/care-domain focused suite, package-local Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, route smoke `200` for `/`, `/calendar`, `/health`, and `/log` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only. The in-app browser was opened to `http://127.0.0.1:4194/calendar`, and DOM verification found the command deck, missions board, schedule, and reminders; screenshot capture still timed out in local browser tooling.
674. NEXT: Continue the route-by-route design recovery with More grouped navigation, Records vault/credential scanability, then final Home dark-RPG alignment. Keep native iOS/Android screenshot proof, exact pnpm 10.24 beta doctor proof, CI completion, provider-backed sync/storage/AI/payments/push, generated PDF output, app-store approval, store launch, and Apollo launch sign-off blocked until real artifacts exist.
675. DONE 2026-07-01: Completed the More grouped navigation recovery slice. `/more` now keeps the pixel Launch Command Hub but adds a first-screen `Command Directory` with four real workflow exits: Care Today from Care Intelligence, Household from responsibility state, Records & passes, and Launch QA. The dense roster, launch readiness, provider setup, access, tools, and diet sections remain available below, but the page now has a clearer top-level map before the deep operational panels.
676. DONE 2026-07-01: Fresh local verification for the More command directory passed mobile readiness 104/104, mobile TypeScript, the 487-test API/mobile/PWA/care-domain focused suite, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/more`, `/health`, `/calendar`, and `/` at `http://127.0.0.1:4194/`, and `git diff --check` with expected Windows CRLF warnings only.
677. NEXT: Continue design recovery with Records vault/credential scanability, then final Home dark-RPG alignment and route-to-route visual consistency. Keep native iOS/Android screenshot proof, exact pnpm 10.24 beta doctor proof, CI completion, provider-backed sync/storage/AI/payments/push, generated PDF output, app-store approval, store launch, and Apollo launch sign-off blocked until real artifacts exist.
678. DONE 2026-07-01: Completed the Records vault/credential scanability recovery slice. `/records` keeps the live pixel credential stage and existing dog ID, vault, trends, medication, Care Pass, and report systems, but now adds a first-screen `Vault Command` layer with real actions for Dog ID sharing, Record Vault filing, Vet Care Pass building, and report sharing. This gives owners a clean path before the dense evidence panels.
679. DONE 2026-07-01: Fresh local verification for the Records vault command passed mobile readiness 105/105, mobile TypeScript, the 488-test API/mobile/PWA/care-domain focused suite, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/records`, `/more`, `/health`, and `/` at `http://127.0.0.1:4194/`, and in-app browser DOM verification found Vault Command, Dog ID, Record Vault, Care Pass, and Reports on `/records`.
680. NEXT: Continue design recovery with final Home dark-RPG alignment and route-to-route visual consistency, then run a full mobile visual pass across Home, Log, Plans, Health, Records, and More. Keep native iOS/Android screenshot proof, exact pnpm 10.24 beta doctor proof, CI completion, provider-backed sync/storage/AI/payments/push, generated PDF output, app-store approval, store launch, and Apollo launch sign-off blocked until real artifacts exist.
681. DONE 2026-07-01: Completed the Home first-screen care-status recovery slice. `/` keeps the living Phoenix room, presence panel, Today Command, Next Up, Quick Log, and Care RPG missions, but the four live status tiles are now grouped inside a `Care Status` board with a real presence pill. This removes another loose-widget seam from the first screen while preserving the existing status routes.
682. DONE 2026-07-01: Fresh local verification for the Home care-status board passed mobile readiness 106/106, mobile TypeScript, the 489-test API/mobile/PWA/care-domain focused suite, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/log`, `/calendar`, `/health`, `/records`, and `/more` at `http://127.0.0.1:4194/`, and in-app browser DOM verification found Phoenix Room, Care Status, Today Command, and Today's Missions on Home.
683. NEXT: Run a final route-to-route visual consistency pass across Home, Log, Plans, Health, Records, and More, then capture real native iOS/Android screenshot proof. Keep exact pnpm 10.24 beta doctor proof, CI completion, provider-backed sync/storage/AI/payments/push, generated PDF output, app-store approval, store launch, and Apollo launch sign-off blocked until real artifacts exist.
684. DONE 2026-07-01: Completed the route-to-route visual consistency system pass. `mobileReleaseQa.ts` now includes a launch-critical `Route Visual Consistency` QA surface with a six-route checklist for Home, Log, Plans, Health, Records, and More. `/more` now exposes this as a first-screen `Design QA` command hub inside Command Directory, and shared board primitives now use crisper framed cards, compact source-readable header pills, one-line section headers, lighter shadows, and border rhythm closer to the Option B pixel boards.
685. DONE 2026-07-01: Fresh local verification for the route visual consistency pass passed `mobileReleaseQa.test.ts` 14/14, `mobileReadiness.test.ts` 106/106, mobile TypeScript, the 490-test API/mobile/PWA/care-domain focused suite, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/log`, `/calendar`, `/health`, `/records`, `/more`, and `/care-twin-qa?qaSurface=route-visual-consistency` at `http://127.0.0.1:4194/`, and in-app browser DOM verification found Route Visual Consistency with Home, Log, Plans, Health, Records, and More checklist steps plus the More Command Directory `Design QA` row.
686. NEXT: Capture real iOS and Android screenshots for the new `Route Visual Consistency` QA surface, then mark Pass or Needs tune for the first visible route with overlap, clipped copy, duplicate-avatar behavior, hidden primary action, bottom-nav collision, or mockup drift. Keep exact pnpm 10.24 beta doctor proof, CI completion, provider-backed sync/storage/AI/payments/push, generated PDF output, app-store approval, store launch, and Apollo launch sign-off blocked until real artifacts exist.
687. DONE 2026-07-01: Repaired the compact web-preview shell after visual QA showed the phone frame was shifted right and cutting off route content. The web root now clamps `html`, `body`, and `#root` to `100vw`, uses `visualViewport` for frame sizing, left-anchors compact preview captures under 520px, keeps desktop previews centered, and preserves the 390px iPhone-style frame. Tab routes also now bypass extra horizontal route padding in web preview mode so Home, Log, Health, Records, More, Plans, Premium, and Care Twin QA start visible without first-paint animation blanks or hidden wide canvases.
688. DONE 2026-07-01: Fresh local verification for the compact preview shell passed `mobileReadiness.test.ts` 110/110, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, and headless Chrome screenshots for Home, Log, Health, and More at 390x844. Visual proof paths: `tmp/route-home-anchorfix.png`, `tmp/route-log-anchorfix.png`, `tmp/route-health-anchorfix.png`, and `tmp/route-more-anchorfix.png`.
689. NEXT: Continue route polish from the now-correct phone frame instead of stale/cropped screenshots. Highest-impact visible refinements are Home stage density, More launch-stage hierarchy, Records/Plans first-screen screenshots, and native iOS/Android proof through `Route Visual Consistency`. Do not claim the app is launch-complete until native screenshots, exact pnpm 10.24 beta doctor proof, provider-backed services, generated PDF output, CI, store approval, and Apollo sign-off exist.
690. DONE 2026-07-01: Recovered Health Watch hierarchy for compact phones. `/health` now puts `Health Snapshot` first after the Health/Bile tabs, adds a compact care-status summary with score/progress/status copy, keeps the 7-day rhythm and tappable health rows in that first card, and reduces the pixel health room/sprite into supporting delight instead of the dominant first-screen object.
691. DONE 2026-07-01: Fresh local verification for the Health compact hierarchy passed `mobileReadiness.test.ts` 111/111, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, and route smoke `200` for `/`, `/log`, `/calendar`, `/health`, `/records`, and `/more` at `http://127.0.0.1:4194/`. Local Chrome stopped emitting new PNG files during this pass, so screenshot proof remains blocked to native/iOS/Android or a repaired capture tool.
692. NEXT: Continue visual recovery with Home stage density and first-screen nav clarity, then use `Route Visual Consistency` to capture native iOS/Android proof. Keep exact pnpm 10.24 beta doctor proof, provider-backed services, generated PDF output, CI, store approval, and Apollo sign-off blocked until real artifacts exist.
693. DONE 2026-07-01: Completed the Home stage-density and compact care-twin hero recovery. `/` now uses a tighter first-screen layout with a smaller Phoenix room, compact Care Status tiles, Today Command peeking above the paw nav, and a launch-safe compact room mode that hides duplicate in-room HUD chrome, bypasses the experimental Avatar Studio accessory runtime, anchors the approved pixel Phoenix hero on the rug, tightens the speech bubble, and removes the floating next-chip that covered the dog.
694. DONE 2026-07-01: Fresh local verification for the Home compact hero recovery passed `homeFirstScreenLayout.test.ts` 3/3, `mobileReadiness.test.ts` 112/112 within the focused run, combined focused readiness/layout tests 115/115, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, route smoke `200` for `/`, `/log`, `/calendar`, `/health`, `/records`, and `/more` at `http://127.0.0.1:4194/`, and Chrome screenshot proof at `tmp/route-home-compact-hero-final.png`.
695. NEXT: Continue route visual polish with native-sized proof: inspect Home, Log, Plans, Health, Records, and More through `Route Visual Consistency`, capture actual iOS and Android screenshots, and mark the first overlap, clipped copy, hidden primary action, duplicate-avatar state, or mockup drift as Needs tune. Keep exact pnpm 10.24 beta doctor proof, provider-backed sync/storage/AI/payments/push, generated PDF output, CI, store approval, public launch, and Apollo sign-off blocked until real artifacts exist.
696. DONE 2026-07-01: Completed the More compact command polish slice from the route visual consistency pass. `/more` now uses a padded centered route header, shorter pet-specific subtitle, a compact launch command stage with pinned HUD/footer, a smaller live pixel sprite, and denser Command Directory rows so the first-screen navigation reads like a real app hub instead of a tall launch poster.
697. DONE 2026-07-01: Fresh local verification for the More compact command polish passed `mobileReadiness.test.ts` 112/112, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/log`, `/calendar`, `/health`, `/records`, and `/more` at `http://127.0.0.1:4194/`, and Chrome screenshot proof at `tmp/route-more-compact-command-polish-final.png` with `overflowX=0` and `Command Directory` visible in the first viewport.
698. NEXT: Continue route visual polish with Records/Log stage cleanup using native-sized proof, then capture actual iOS and Android screenshots through `Route Visual Consistency`. Keep exact pnpm 10.24 beta doctor proof, provider-backed sync/storage/AI/payments/push, generated PDF output, CI, store approval, public launch, and Apollo sign-off blocked until real artifacts exist.
699. DONE 2026-07-01: Completed the Log/Records stage-dock polish from the route visual consistency pass. `/log` now keeps the live Quick Care Console as a compact game scene, moves the care-IQ HUD and selected quick action into a separated command dock, shortens the tap/hold instruction into a one-line control bar, and keeps the first quick-action row visible above the floating paw nav. `/records` now keeps the live Records Command Vault scene but moves the Dog ID plate and saved/ready/alerts HUD into a dock below the pixel room so the ID card, sprite, and room no longer overlap.
700. DONE 2026-07-01: Fresh local verification for the Log/Records stage-dock polish passed `mobileReadiness.test.ts` 112/112, mobile TypeScript, the 456-test mobile/care-domain focused suite, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, live preview route smoke `200` for `/`, `/log`, `/calendar`, `/health`, `/records`, and `/more` at `http://127.0.0.1:4194/`, `git diff --check` with expected Windows CRLF warnings only, and Chrome screenshot proof at `tmp/route-log-compact-stage-dock-final.png` and `tmp/route-records-credential-dock-final.png` with `overflowX=0`.
701. NEXT: Continue route visual polish with actual native iOS and Android screenshots through `Route Visual Consistency`, then mark the first remaining route with overlap, clipped copy, hidden primary action, duplicate-avatar behavior, bottom-nav collision, or mockup drift as Needs tune. Keep exact pnpm 10.24 beta doctor proof, provider-backed sync/storage/AI/payments/push, generated PDF output, CI, store approval, public launch, and Apollo sign-off blocked until real artifacts exist.
702. DONE 2026-07-01: Completed the Plans compact schedule-first recovery slice. `/calendar` keeps the live Plans Command Deck but cuts the stage height, sprite footprint, speech bubble, HUD, mission rows, and row action chips, then moves `Mission Schedule` above `Today's Missions` so the actual routine list appears before secondary mission context. Routine edit, routine done logging, Reminder Center routing, household responsibility, and WoofGuide discovery remain intact.
703. DONE 2026-07-01: Fresh local verification for the Plans compact schedule-first slice passed `mobileReadiness.test.ts` 112/112, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, and static preview route smoke `200` for `/calendar`, `/`, `/log`, and `/health` at `http://127.0.0.1:4196/`. The first export attempt failed because the smoke script could not find bare `node` on Windows PATH; rerunning with the bundled Node directory prepended passed.
704. NEXT: Continue native-sized visual proof for Home, Log, Plans, Health, Records, More, and Avatar Studio through `Route Visual Consistency`; if browser screenshot tooling is available, capture an updated Plans proof first. Keep exact pnpm 10.24 beta doctor proof, real iOS/Android screenshots, provider-backed sync/storage/AI/payments/push, generated PDF output, CI, store approval, public launch, and Apollo sign-off blocked until real artifacts exist.
705. DONE 2026-07-01: Completed the Quick Log and Health artwork/chrome polish slice after the final native-sized visual review. `/log` now stretches the approved pixel-room artwork instead of cropping into a blank wall, uses a larger live Phoenix sprite, a lighter stage shade, and a wider speech bubble so the command scene reads closer to the Option B boards. `/health` now treats the pixel room as compact support: stretch-framed room art, smaller Phoenix sprite, tighter stage, lighter shade, shorter bubble, and a lower-profile health HUD so the useful Health Snapshot stays dominant.
706. DONE 2026-07-01: Fresh local verification for the Quick Log and Health polish passed `mobileReadiness.test.ts` 112/112, mobile TypeScript, the 456-test mobile/care-domain focused suite, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, and static preview route smoke `200` for `/`, `/log`, and `/health` at `http://127.0.0.1:4197/`. Local Chrome screenshot commands exited `0` but produced no PNG files in this pass, so fresh screenshot proof remains blocked until capture tooling or native device proof is repaired.
707. NEXT: Continue from the live preview at `http://127.0.0.1:4197/` with a native/device visual pass across Home, Log, Plans, Health, Records, More, and Avatar Studio. Mark the first remaining overlap, clipped copy, hidden primary action, duplicate-avatar behavior, bottom-nav collision, or mockup drift as Needs tune. Keep exact pnpm 10.24 beta doctor proof, real iOS/Android screenshots, provider-backed sync/storage/AI/payments/push, generated PDF output, CI completion, store approval, public launch, and Apollo sign-off blocked until real artifacts exist.
708. DONE 2026-07-01: Repaired the current branch CI failure after run `28527740311` reached the real test step and failed one brittle doctor assertion. The machine-readable mobile beta doctor readiness test now requires the Corepack status check to exist, but only expects `Corepack available for pnpm bootstrap` in `warnings` when that check is not `PASS`. This keeps local blocked shells and CI-ready shells both truthful.
709. DONE 2026-07-01: Fresh local verification for the CI assertion repair passed `mobileReadiness.test.ts` 112/112 and the CI-equivalent focused suite 496/496 with the bundled Node runtime. The next step is to push this fix and rerun `WoofWatcher Verify` on `automation/premium-revenue-product-builder`.
710. NEXT: Rerun branch CI for the pushed assertion repair. If CI passes, continue device visual QA. If CI fails, pull the exact failed log before doing more design work.
711. DONE 2026-07-01: Repaired the second current branch CI failure after run `28528291630` reached `pnpm run build:ci` and failed API server typecheck with `TS5097` on `.ts` source imports in `src/lib/household-sharing-cleanup.ts`. The API server `tsconfig.json` now explicitly enables `allowImportingTsExtensions` for its no-emit typecheck path, preserving the Node strip-types focused tests that import TypeScript source directly.
712. DONE 2026-07-01: Fresh local verification for the API server CI repair passed direct API typecheck with `tsc -p artifacts/api-server/tsconfig.json --noEmit` and the CI-equivalent focused suite 496/496 with the bundled Node runtime. Direct local API build is still Windows-install-blocked by missing esbuild optional package `@esbuild/win32-x64`, so branch CI remains the build proof source.
713. DONE 2026-07-01: Branch CI passed after the API server typecheck repair. `WoofWatcher Verify` run `28529223713` completed `success` on `automation/premium-revenue-product-builder` after the earlier current-run blockers `28527740311` and `28528291630` were fixed.
714. NEXT: Resume the design/device QA path at the live preview. Continue native/device visual review for Home, Log, Plans, Health, Records, More, and Avatar Studio, then tune the first remaining overlap, clipped copy, hidden primary action, duplicate-avatar behavior, bottom-nav collision, or mockup drift.
715. DONE 2026-07-01: Completed the compact bottom-nav chrome recovery slice after the native-sized design review showed the floating paw nav visually eating lower command cards. Shared mobile chrome metrics now use a shorter tab bar, tighter web bottom offset, smaller center paw FAB, slightly wider horizontal inset, and lower route bottom padding so Home, Log, Health, and other command-first screens keep useful content visible above the nav.
716. DONE 2026-07-01: Fresh local verification for the compact bottom-nav chrome slice passed the intended red/green `mobileLayout.test.ts` 8/8, `mobileReadiness.test.ts` 112/112, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, static preview route smoke `200` for `http://127.0.0.1:4194/` and `http://127.0.0.1:4197/`, and the broad focused API/mobile/PWA/care-domain suite 497/497 with the bundled Node runtime.
717. DONE 2026-07-01: Branch CI passed for the compact bottom-nav chrome recovery. `WoofWatcher Verify` run `28529687466` completed `success` on `automation/premium-revenue-product-builder`.
718. NEXT: Re-run route visual consistency screenshots on the refreshed preview and actual iOS/Android frames. In-app browser screenshot capture timed out during this pass, so fresh visual proof remains blocked to repaired capture tooling or native device proof. Continue tuning the first remaining overlap, clipped copy, hidden primary action, duplicate-avatar behavior, bottom-nav collision, or mockup drift.
719. DONE 2026-07-01: Completed the Avatar Studio hero declutter slice from the route visual consistency pass. `/portrait` now follows the `one dog, one room, one bottom truth label` rule by removing the competing in-room console bar, top-right ID card, and separate live-chip overlay. The scan workflow cards were tightened so the first viewport keeps the live pixel dog, tabs, PixelLab truth copy, and first owner action visible without stacking extra HUD chrome over the room.
720. DONE 2026-07-01: Fresh local verification for the Avatar Studio hero declutter passed the intended red/green `mobileReadiness.test.ts` 112/112, `avatarStudio.test.ts` 8/8, mobile TypeScript, Expo web export to `.expo-smoke` with 218 assets / 222 files after prepending bundled Node to PATH, `git diff --check` with expected Windows CRLF warnings only, and the broad focused API/mobile/PWA/care-domain suite 497/497 with the bundled Node runtime.
721. DONE 2026-07-01: Branch CI passed for the Avatar Studio hero declutter. `WoofWatcher Verify` run `28534699641` completed `success` on `automation/premium-revenue-product-builder` at commit `d06ecfb`.
722. DONE 2026-07-01: Completed the Health flagship-room redesign slice after Apollo flagged `/health` as confusing and ugly. `/health` now opens with one larger pixel Health Watch room, one status panel, one rhythm block, and one signal list instead of a separate compact snapshot card plus an extra room HUD. The room sprite is larger, the stage HUD strip was removed, and the review packet remains below the flagship room so the page reads as `status -> room -> signals -> review` rather than stacked widgets.
723. DONE 2026-07-01: Fresh local verification for the Health flagship-room redesign passed the intended red/green `mobileReadiness.test.ts` 112/112, mobile TypeScript with absolute workspace paths, the broad focused API/mobile/PWA/care-domain suite 497/497, Expo web export to `.expo-smoke` with 218 assets / 222 files, and `git diff --check` with expected Windows CRLF warnings only. The first TypeScript rerun failed only because the sandbox remapped the relative `node_modules` path; rerunning with absolute paths passed.
724. DONE 2026-07-01: Branch CI passed for the Health flagship-room redesign. `WoofWatcher Verify` run `28536210206` completed `success` on `automation/premium-revenue-product-builder` at commit `557e544`.
725. NEXT: Capture refreshed `/health` and `/portrait` screenshots/native route proof, then continue route visual consistency for Home, Log, Plans, Records, More, and Avatar Studio. Keep provider-backed sync/storage/AI/payments/push, generated PDF output, public launch, store approval, and Apollo sign-off blocked until real artifacts exist.
726. DONE 2026-07-01: Completed a native-sized route visual polish checkpoint after fresh 390x844 proof showed the biggest remaining drift in the shared board rhythm, Plans command stage, and Records credential stage. Shared board primitives now use a tighter route-header/card rhythm, `/calendar` has a shorter Plans Command Deck with the dog, due badge, and HUD separated, and `/records` now uses the wide PixelLab 400x300 room so the credential vault reads as a real room instead of a cropped blank wall. Existing care logic, local-first storage, editable logs, routines, Records, and Health Watch behavior were preserved.
727. DONE 2026-07-01: Fresh local verification for the route visual polish checkpoint passed `mobileReadiness.test.ts` plus `mobileLayout.test.ts` 120/120, mobile TypeScript, Expo web export to `.expo-smoke` with 219 assets / 223 files, and `git diff --check` with expected Windows CRLF warnings only. Headless Chrome proof captured `overflowX=0` for Plans, Records, Health, and Avatar Studio at `output/playwright/woofwatcher-visual-audit-2026-07-01-final/01-plans.png`, `02-records.png`, `03-health.png`, and `04-avatar-studio.png`.
728. NEXT: Continue route visual consistency with Home and Log first-viewport density, then run actual iOS and Android screenshots through the `Route Visual Consistency` QA surface. Keep provider-backed sync/storage/AI/payments/push, generated PDF output, public launch, store approval, and Apollo sign-off blocked until real artifacts exist.
729. DONE 2026-07-01: Completed the Home and Log first-viewport density slice from the route visual consistency pass. Home now limits first-screen Quick Log to the four immediate actions and keeps the full matrix on `/log`. Quick Log now uses a shorter command stage, smaller live pixel sprite, compact one-row HUD/action dock, and no duplicate selected-action mission panel so the action grid is visually primary. Shared bottom-tab chrome now has readable label height and internal padding instead of clipping tab labels in phone proof.
730. DONE 2026-07-01: Fresh local verification for the Home/Log density slice passed `mobileReadiness.test.ts` plus `mobileLayout.test.ts` 120/120, mobile TypeScript, Expo web export to `.expo-smoke` with 219 assets / 223 files, and headless Chrome proof with `overflowX=0` for Home and Log at `output/playwright/woofwatcher-home-log-density-2026-07-01-readable-nav/home.png` and `log.png`.
731. NEXT: Run actual iOS and Android screenshots through the `Route Visual Consistency` QA surface, starting with Home and Log, then mark the first remaining route with overlap, clipped copy, hidden primary action, duplicate-avatar behavior, bottom-nav collision, or mockup drift as Needs tune. Keep provider-backed sync/storage/AI/payments/push, generated PDF output, public launch, store approval, and Apollo sign-off blocked until real artifacts exist.
732. DONE 2026-07-01: Completed the Health design-system declutter slice after Apollo flagged the current route as visually ugly, overlapped, and too complicated. `/health` now removes the duplicated hero signal rail, separate medical-boundary mini block, and secondary health command deck, keeping one flagship room, one status/rhythm panel, one primary action row, one signal list, and one review packet. The dog sprite and room stage were tightened so the first viewport reads closer to the neo-retro mobile boards without crowding the actual health workflow.
733. DONE 2026-07-01: Fresh local verification for the Health declutter slice passed the intended red/green `mobileReadiness.test.ts` 112/112, mobile TypeScript, `git diff --check` with expected Windows CRLF warnings only, and Expo web export to `.expo-smoke` with 219 assets / 223 files. Chrome could load the preview DOM, but local headless screenshot capture did not emit a PNG in this pass, so fresh visual screenshot proof remains blocked on capture tooling or native device proof.
734. NEXT: Continue route visual consistency with actual device-sized review across Home, Log, Plans, Health, More, Records, Avatar Studio, Care Pass, and WoofGuide. The design standard is now one primary pixel scene, one readable status/action layer, no duplicate HUD rails, no hidden primary action, no clipped bottom-nav labels, and no route chrome that fights the real task.

## Cadence

Recommended automation cadence from Apollo's shared thread: every 3 hours, using a dedicated Git worktree where the automation system supports it.

Operational manifest: `docs/operations/PREMIUM_REVENUE_PRODUCT_BUILDER.md`.

## Stop Conditions

Stop only for missing secrets, destructive data risk, app-store approval, legal/veterinary safety boundaries, production approval, or source-of-truth contradictions.
