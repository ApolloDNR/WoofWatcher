{
  "name": "woofwatcher",
  "version": "0.1.0",
  "private": true,
  "description": "A mobile-first dog care tracker for meals, walks, training, health events, and shared caregiver schedules.",
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 4173",
    "test": "node --test",
    "check": "node --check src/app.js && node --test",
    "dev": "python3 -m http.server 4173"
  },
  "dependencies": {},
  "devDependencies": {}
}
